import { normalizeRoleCode } from './role-code';
import { ROLE_NAMES_RU } from '../texts/roles.ru';

import type { UserSession } from '../../entities/session/model';

export interface RoleBlueprint {
  role: string;
  displayName: string;
  topJobs: string[];
  primaryNav: string[];
  /**
   * Меню роли — РОВНО этот список, без дополнения и без второго этажа «Ещё».
   *
   * Обычное правило (`IA-014`) дополняет короткое меню до семи пунктов любыми разделами, куда
   * пускают права. Для большинства ролей это удобно, но для тех, чей кабинет описан ТЗ поимённо,
   * это вредно: администратор платформы получал в меню «Курсы» — работу внутри центра, которой
   * он не занимается (ТЗ 8.1, журнал 527), а слушатель — двенадцать пунктов вместо пяти (6.1).
   */
  exactNav?: boolean;
}

/*
 * Экспортируется ради сторожевого теста: состав короткого меню — продуктовое
 * решение ТЗ редизайна §4.4, а не деталь реализации, и разъехаться с ТЗ он не должен.
 *
 * ПОРЯДОК ЗАПИСЕЙ ЗНАЧИМ. getSessionRoleBlueprints фильтрует этот массив и
 * сохраняет порядок объявления, а getNavigationView берёт меню у ПЕРВОЙ роли.
 * Имя роли (`displayName`) — ссылкой на словарь Р1 (`texts/roles.ru.ts`), а не строкой:
 * у роли одно имя на всё приложение (ТЗ 4.1). Раньше чертёж звал `manager` «Менеджером»,
 * а `tenant_admin` — «Администратором», при том что список пользователей печатал
 * «Manager» и «Tenant admin». Массив остаётся литералом `export const … = [ … ];` —
 * его читают сторожа бэкенда (`navigation-model.test-util.ts`).
 *
 * Поэтому список идёт от самой полной роли к самой узкой: администратор, которому
 * дополнительно выдали роль менеджера, должен увидеть меню администратора, а не
 * менеджера. Тот же принцип, что в таблице домашних маршрутов role-home.ts.
 */
export const roleBlueprints: RoleBlueprint[] = [
  {
    role: 'platform_admin',
    displayName: ROLE_NAMES_RU.platform_admin,
    topJobs: [
      'Проверить здоровье арендаторов',
      'Завести или приостановить центр',
      'Разобрать очередь и сбои',
      'Проверить лицензии и оплату',
      'Поднять журнал действий'
    ],
    primaryNav: [
      '/workspace',
      '/platform/tenants',
      '/admin/licenses',
      '/audit',
      '/admin/operations',
      /*
       * ТЗ 8.1: «тарифы, счета» — экран потребления и лимитов; «Уведомления» нужны и ему:
       * туда приходят отказы фоновых задач и окончание лицензий (матрица 11.1).
       */
      '/admin/usage',
      '/notifications'
    ],
    /*
     * ТЗ 8.1: «Убрать всё, что относится к работе внутри одного центра». Внутрь центра
     * администратор платформы попадает только через «Войти от имени» — механизм есть и сделан
     * правильно, с записью в аудит.
     */
    exactNav: true
  },
  {
    role: 'tenant_admin',
    displayName: ROLE_NAMES_RU.tenant_admin,
    // ТЗ §3.1: формулировки — результат для человека, а не обязанность роли.
    topJobs: [
      'Увидеть, что горит сегодня',
      'Зачислить слушателя в группу',
      'Закрыть группу и выдать документы',
      'Выгрузить реестр в надзор',
      'Найти слушателя и ответить по нему'
    ],
    primaryNav: [
      '/workspace',
      '/learners',
      '/groups',
      '/assessment',
      '/documents',
      '/reports',
      '/settings'
    ]
  },
  {
    role: 'manager',
    displayName: ROLE_NAMES_RU.manager,
    /*
     * ТЗ 8.3: четыре вопроса, вокруг которых собран кабинет — «как идёт обучение по моим
     * компаниям», «кто не успевает», «что горит по срокам», «сколько выдано документов».
     *
     * Здесь стояло «Выдать документы группе» — задача, которой у роли НЕТ. По живой
     * `iam.role_permissions` у руководителя одно право на документы, `documents.read`:
     * выдачу (`documents.generate`) миграция 0085 дала методисту и администрации, а ему —
     * нет. Чертёж обещал работу, которую роль физически не может сделать (журнал 530).
     */
    topJobs: [
      'Увидеть, как идёт обучение по компаниям',
      'Найти тех, кто не успевает',
      'Собрать группу под заказчика',
      'Зачислить слушателя в группу',
      'Выгрузить отчёт'
    ],
    /*
     * ТЗ 8.3: состав кабинета — Панель · Компании · Группы · Слушатели · Отчёты, «всё остальное
     * убрать». Раньше в меню стояли «Шаблоны документов» — страница, где у руководителя нет
     * ни одного действия (`documents.write` ему по живой базе не выдан), — а правило добивки
     * до семи пунктов дотягивало ещё «Курсы» и «Оценивание» и складывало под «Ещё» двадцать
     * три раздела, включая госвыгрузки и сводку методиста (журнал 529).
     */
    primaryNav: ['/manager', '/admin/clients', '/groups', '/learners', '/reports'],
    exactNav: true
  },
  {
    role: 'methodist',
    displayName: ROLE_NAMES_RU.methodist,
    topJobs: [
      'Собрать программу курса',
      'Обновить материалы и версии',
      'Собрать тест и задания',
      'Передать курс на публикацию',
      'Найти пробелы в программах'
    ],
    // ТЗ §4.4 ставило сюда «Группы» и «Отчёты», но `groups.read` и `learners.read` у роли
    // нет (живая `iam.role_permissions`): пункты молча не показывались, а меню добивалось
    // кабинетом слушателя (журнал 344). Вместо них — банки вопросов и библиотека курсов:
    // разделы работы методиста, открытые его правами. Сторож: `role-menu-reachable`.
    primaryNav: [
      '/methodist',
      '/courses',
      '/materials',
      '/assessment',
      '/admin/question-banks',
      '/library'
    ]
  },
  {
    role: 'teacher',
    displayName: ROLE_NAMES_RU.teacher,
    topJobs: [
      'Проверить работы в очереди',
      'Посмотреть прогресс группы',
      'Ответить слушателям',
      'Спланировать занятия',
      'Открыть материалы курса'
    ],
    primaryNav: ['/groups', '/teacher/review', '/learning/calendar', '/courses', '/notifications']
  },
  {
    role: 'learner',
    displayName: ROLE_NAMES_RU.learner,
    topJobs: [
      'Продолжить обучение с последнего места',
      'Сдать тест или задание',
      'Проверить сроки',
      'Забрать документы об обучении',
      /*
       * Было «Написать преподавателю» через чат — снято решением Р3 (ТЗ 5.12.6) вместе с
       * самим чатом. На замену — задача, которая у слушателя есть и работает СЕГОДНЯ: центр
       * пишет ему о сроках, проверке работ и выданных документах. Обратная сторона связи
       * («Написать в учебный центр» обращением) вернётся в фазе 11 (журнал 482).
       */
      'Прочитать сообщения от учебного центра'
    ],
    /*
     * ТЗ 6.1 (С1): ПЯТЬ пунктов, все на виду. Было двенадцать — семь сверху и пять под «Ещё»
     * (журнал 490). Целевая структура ТЗ: Обучение · Тесты · Документы · Сообщения · Профиль.
     *
     * Имена из ТЗ взяты не дословно, и это осознанно (журнал 492): «Тесты» уже носит раздел
     * `/admin/tests`, а «Уведомления» закреплены решением задачи 3.4 «один раздел — одно имя».
     * Два адреса с одним названием запрещены её сторожем, поэтому здесь структура из ТЗ, а
     * имена — прежние.
     *
     * «Мои курсы» ушли из меню: главная кабинета и так показывает курсы с прогрессом — это
     * был второй вход в то же место. «Задания», «Вебинары», «Календарь» открываются с
     * главной, «Подтверждение личности» и «Оплаты» — из профиля.
     */
    primaryNav: [
      '/learner',
      '/learner/tests',
      '/learner/documents',
      '/notifications',
      '/learner/profile'
    ],
    /* ТЗ 6.1: ровно пять пунктов, все на виду. */
    exactNav: true
  }
];

/* Синонимы кодов — одной таблицей на всех, в листе `role-code.ts` (ТЗ 4.1). */
const normalizeRole = normalizeRoleCode;

export const getSessionRoleBlueprints = (session: UserSession | null): RoleBlueprint[] => {
  if (!session) return [];
  const set = new Set(session.roles.map((role) => normalizeRole(role.toLowerCase())));
  return roleBlueprints.filter((blueprint) => set.has(blueprint.role));
};

export const getPrimaryRoleBlueprint = (session: UserSession | null): RoleBlueprint | null =>
  getSessionRoleBlueprints(session)[0] ?? null;
