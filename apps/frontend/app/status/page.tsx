import { StatusScreen } from '../../src/features/status/status-screen';

/**
 * Состояние системы (ТЗ «Стабилизация, UX и развитие», 18.3).
 *
 * Публичная страница вне оболочки: смотреть сюда приходят именно тогда, когда войти не
 * получается. Страница состояния, требующая входа, бесполезна ровно в тот момент, ради которого
 * заведена (журнал 586).
 */
export default function StatusPage() {
  return <StatusScreen />;
}
