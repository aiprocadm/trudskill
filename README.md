# LMS / СДО Project README

## 1. Project Overview

trudskill (прежнее имя — CDOProf) — монорепозиторий LMS/СДО платформы для обучающихся, преподавателей/кураторов и администраторов.

### Основные роли

- Learner (обучающийся)
- Teacher/Curator (преподаватель/куратор)
- Admin (администратор)

### Основные модули

- Аутентификация, сессии, роли/права (`apps/backend/src/modules/iam`, `apps/frontend/src/features/auth`)
- Курсы/обучение и смежные MVP домены (`apps/backend/src/modules/mvp`, `apps/frontend/app/courses*`, `apps/frontend/app/learner*`)
- Документы и e-sign (`apps/backend/src/modules/documents`, `apps/backend/src/modules/esign`)
- Коммуникации (чат, уведомления, вебинары) (`apps/backend/src/modules/communication`, `apps/frontend/app/chat`, `apps/frontend/app/notifications`, `apps/frontend/app/admin/webinars`, `apps/frontend/app/learner/webinars`)
- Интеграции (`apps/backend/src/modules/integrations`, `apps/frontend/app/integrations`)
- Аналитика/аудит/метрики (`apps/backend/src/modules/audit`, `apps/backend/src/common/metrics`)

### Текущий стек

- Monorepo: pnpm workspace + Turborepo
- Frontend: Next.js + TypeScript
- Backend: NestJS + TypeScript
- Realtime service: Node.js/TypeScript
- Worker service: Node.js/TypeScript
- DB migrations: SQL в `apps/backend/migrations`
- Контракты/типы: `packages/api-contracts`, `packages/shared-types`

### Важные ограничения

- Работать итеративно и минимальными безопасными изменениями.
- **`README.md` — канон по точке входа в репозиторий:** актуальный **AI Agent State**, команды и ссылки на E2E/CI (см. [docs/DOCUMENTATION_MAP.md](docs/DOCUMENTATION_MAP.md) — кто главный для ТЗ, трассировки и handoff; **несколько агентов** — [протокол передачи](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)).
- Перед следующими крупными изменениями нужно валидировать критические сценарии IAM/доступов и multitenancy.

### Ссылки на обязательные документы

- Агенты (несколько сессий, «продолжай по ТЗ»):
  - [`AGENTS.md`](AGENTS.md)
  - [`.cursor/rules/lms-multi-agent-handoff.mdc`](.cursor/rules/lms-multi-agent-handoff.mdc) (правило Cursor, `alwaysApply`)
  - [`docs/DOCUMENTATION_MAP.md` — протокол передачи](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)
- Требования и пилот (продуктовый канон и приёмка):
  - [`TZ_TRUDSKILL_BASE.md`](TZ_TRUDSKILL_BASE.md)
  - [`TZ_TRUDSKILL_ARENDNAYA_SDO.md`](TZ_TRUDSKILL_ARENDNAYA_SDO.md) — **действующее дельта-ТЗ «Арендная СДО»** по **поведению, функциям и правам** (эпики A–I, фазы 0–6; в рамках его эпиков приоритет деталей у него)
  - [`docs/TZ_ARENDNAYA_SDO_STATUS.md`](docs/TZ_ARENDNAYA_SDO_STATUS.md) — статус выполнения дельта-ТЗ (что сделано / частично / переделать)
  - [`docs/TZ_UI_REDESIGN_TRUDSKILL.md`](docs/TZ_UI_REDESIGN_TRUDSKILL.md) — **действующее дельта-ТЗ по интерфейсу** (ИА, визуальный язык, компоненты, тексты, ребрендинг UI; фазы 0–8; по вопросам представления приоритет у него)
  - [`docs/TZ_UI_REDESIGN_STATUS.md`](docs/TZ_UI_REDESIGN_STATUS.md) — статус выполнения ТЗ редизайна + **журнал расхождений «код ↔ ТЗ»**
  - [`docs/TZ_MVP_TRACEABILITY.md`](docs/TZ_MVP_TRACEABILITY.md)
  - [`docs/DOCUMENTATION_MAP.md`](docs/DOCUMENTATION_MAP.md)
- Полное техническое задание / архитектурный контекст:
  - `docs/architecture-overview.md`
  - `docs/mvp-domain-database.md`
  - `apps/backend/docs/mvp-domain-database.md`
- Текущее состояние и план работ:
  - `docs/ENTERPRISE_OPERATIONAL_PLAN.md`
  - `docs/ENTERPRISE_OPERATIONAL_NEXT_STEPS.md`
  - `docs/ENTERPRISE_OPERATIONAL_COMPLETION_REPORT.md`
  - `docs/ENTERPRISE_OPERATIONAL_REMAINING_GAPS.md`
- Тестирование и регламенты проверок:
  - `docs/testing/test-strategy-stage-13.md`
  - `docs/run-tests.md`
- Безопасность/надежность/операции:
  - `docs/security-remediation-roadmap.md`
  - `docs/operations-runbook.md`
  - `docs/backup-and-restore.md`
  - `docs/observability.md`

### Канонический E2E для приёмки §39 ТЗ

- Backend (бизнес-потоки без браузера): [`apps/backend/src/modules/mvp/business-flows.e2e.test.ts`](apps/backend/src/modules/mvp/business-flows.e2e.test.ts), сертификат по завершению: [`apps/backend/src/modules/documents/enrollment-certificate-flow.service.test.ts`](apps/backend/src/modules/documents/enrollment-certificate-flow.service.test.ts).
- Backend HTTP integration (envelope, `TenantGuard`, permission boundaries, доменные инварианты assessment; дополняют BL-010 в CI): [`mvp.http.integration.test.ts`](apps/backend/src/modules/mvp/mvp.http.integration.test.ts), [`mvp.domains.http.integration.test.ts`](apps/backend/src/modules/mvp/mvp.domains.http.integration.test.ts), [`mvp-internal-worker.http.integration.test.ts`](apps/backend/src/modules/mvp/mvp-internal-worker.http.integration.test.ts) (worker callback `…/internal/worker/mvp/bulk-enrollments`, BL-003), [`documents.http.integration.test.ts`](apps/backend/src/modules/documents/documents.http.integration.test.ts) (**GET** **`documents.read`**; **POST** → **`documents.write`** или **`documents.generate`** (**…/documents/generate**); **PATCH** / **PUT** / **DELETE** → **`documents.write`**, регресс **templates/:id** в harness), [`esign.http.integration.test.ts`](apps/backend/src/modules/esign/esign.http.integration.test.ts) (**POST:** submit, reuse-check (**read**), **applications/(approve|reject)**, start-review; **PATCH** **applications/:id** (**write**), **participants/:id** (**processes.write**); **processes** POST/start/**cancel**; **participants** POST (create)/invite/sign/skip/mark-viewed/**reject** (контуры **`processes.write`** / **`sign`**); application-files POST/verify/reject/**DELETE**; **GET:** **applications/:id**, application-files и **application-files/:id**, **legal-log** и **legal-log/:id**, **processes**, **processes/:id**, **processes/:id/status**, **participants**, **events**, **events/:id**), [`workspace.http.integration.test.ts`](apps/backend/src/modules/workspace/workspace.http.integration.test.ts), [`integrations.http.integration.test.ts`](apps/backend/src/modules/integrations/integrations.http.integration.test.ts) (**GET** → **`integrations.read`**; **POST**/**PATCH**/**PUT**/**DELETE** → **`integrations.write`**, стаб **…/integrations/providers/:id**); liveness (**без auth**): [`health.http.integration.test.ts`](apps/backend/src/modules/health/health.http.integration.test.ts); IAM: [`auth.http-regression.e2e.test.ts`](apps/backend/src/modules/iam/auth.http-regression.e2e.test.ts).
- Frontend (маршруты / роли): [`apps/frontend/src/e2e/lms-role-flows.e2e.test.ts`](apps/frontend/src/e2e/lms-role-flows.e2e.test.ts), при необходимости — [`apps/frontend/src/e2e/canonical-e2e-readiness.e2e.test.ts`](apps/frontend/src/e2e/canonical-e2e-readiness.e2e.test.ts).
- Регресс качества: `pnpm -s ci:check`. Отдельный Playwright/Cypress при появлении требования — не заменяет указанные Vitest-наборы до явного решения команды.

## 2. AI Agent State

Состояние ниже должно совпадать по смыслу с [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) (факты сессии, файлы, тесты). Подробный лог — там §5–§20; здесь — краткий ориентир для следующего агента.

> 📋 **Сводный статус всех 17 планов** (что слито / что осталось, карта PR→план) — [docs/superpowers/plans/PLANS_STATUS.md](docs/superpowers/plans/PLANS_STATUS.md) (сверено по git + коду, 2026-05-31).
>
> 🚀 **Чек-лист до запуска пилота** (что осталось по всему проекту, разбито по исполнителю dev/ops/юрист/владелец) — [docs/LAUNCH_CHECKLIST.md](docs/LAUNCH_CHECKLIST.md) (2026-06-23, после §5.140).
>
> 🎯 **Действующее дельта-ТЗ «Арендная СДО»** (5 несущих блоков: рендер документов / видео / идентификация / аренда-мультитенантность / эксплуатация; эпики A–I, фазы 0–6) — [TZ_TRUDSKILL_ARENDNAYA_SDO.md](TZ_TRUDSKILL_ARENDNAYA_SDO.md). **Статус выполнения по каждому ФТ** (что сделано / частично / не начато / переделать) — [docs/TZ_ARENDNAYA_SDO_STATUS.md](docs/TZ_ARENDNAYA_SDO_STATUS.md) (2026-07-24, §5.166).
>
> 🎨 **Действующее дельта-ТЗ по интерфейсу** (редизайн + удаление бренда CDOprof; приоритетная роль — администратор учебного центра; фазы 0–8) — [docs/TZ_UI_REDESIGN_TRUDSKILL.md](docs/TZ_UI_REDESIGN_TRUDSKILL.md). **Статус и журнал расхождений «код ↔ ТЗ»** — [docs/TZ_UI_REDESIGN_STATUS.md](docs/TZ_UI_REDESIGN_STATUS.md) (2026-08-18, §5.302). **Кодовая часть фаз 0–8 закрыта** (PR #491…#537). Остаток — не код: ⏳ **выкатка N+1** (уборка прежних имён ключей хранения) **через 60 дней после выкатки в прод**, план — [docs/REBRANDING_KEYS_ROLLOUT.md](docs/REBRANDING_KEYS_ROLLOUT.md); `BR-031`/C-2 (`CDOPROF_*` в скриптах бэкапа) — после повторных учений восстановления; вопросы владельцу — реальный почтовый домен (`BR-032`) и уведомление о новом имени TOTP (`BR-010`).
>
> ⚠️ **По фразе «продолжай по ТЗ» сначала определить, о каком из двух ТЗ речь** — правило маршрутизации в [DOCUMENTATION_MAP.md#tz-routing](docs/DOCUMENTATION_MAP.md#tz-routing). Затем: статус-трекер выбранного ТЗ → фаза по дорожной карте → план + апрув перед кодом.

### Current Stage

**2026-08-23 (текущее, §5.326, ветка `worktree-cmp-020-wave1`):** **Срез 33 по ТЗ редизайна — волна 1 `CMP-020`.** Одиннадцать экранов с одним первичным действием переведены на контракт `primaryAction`; остаток — 15 экранов, и его держит сторож-храповик `page-header-actions-ratchet` (список сверяется на равенство: новый экран в него не попадёт, переведённый обязан уйти). Контракт действия дополнен формой `href` (переход остаётся ссылкой) и `busy` (занятость крутилкой). Три находки: **ссылка в одежде кнопки теряла высоту, отступы и тач-зону 44px на телефоне** (базовое правило перечисляло только прежнее написание класса); **комментарий в пакете обещал сторожа, которого не существовало**; кнопка «Проверить сроки» переименовывалась по ходу работы вопреки `TXT-003`. `pnpm ci:check` зелёный.

**2026-08-23 (§5.325, ветка `worktree-cmp-021-cleanup`):** **Срез 32а по ТЗ редизайна — `CMP-021` закрыт.** Удалены три компонента в обход дизайн-системы (`SimplePagination`, `SearchStatusFilter`, `patterns/registry.tsx`) и два файла, которых ТЗ не называет: мёртвая витрина `components/foundation.tsx` (единственный потребитель `registry.tsx` — ТЗ ошибочно считало его неиспользуемым) и третий самодельный листатель `PaginationControls`. Экран «Центр уведомлений» вынесен в `features/communication/notifications-screen.tsx` по чек-листу §15.1 и собран из компонентов пакета: статус — чип с русской подписью вместо `unread`/`read` латиницей, дата — вместо машинной строки, отметка о прочтении — действие строки вместо второго списка под таблицей. Сторож `pagination-discipline`. `pnpm ci:check` зелёный.

**2026-08-22 (§5.324, ветка `worktree-cmp-015-page-shell`):** **Срез 32 по ТЗ редизайна — `CMP-015` + ядро `CMP-020`.** Каркас страницы переехал в пакет (`composition/page-shell.tsx`); `PageHeader` получил контракт ТЗ: `primaryAction` — **не массив** (бюджет UI-007 стал ограничением типа), `secondaryActions` — в меню «Ещё» нейтральными. Переходный `actions` (28 экранов) юнионом исключает первичную кнопку. Прежний файл — прослойка: реэкспорт + разбор ошибок (`describeError` в пакет нельзя, прецедент CMP-022). Убран дубль `PageContainer` из примитивов. Два сторожа отреагировали на переезд и актуализированы. Тесты: `ci:check` — код выхода 0.

**2026-08-22 (§5.323, ветка `worktree-ui-016-spacious`):** **Срез 31 по ТЗ редизайна — `UI-016` закрыт целиком.** Контекст «просторно» включён модификатором `.ui-page--spacious` на трёх обзорных экранах (`PageContainer spacious`): оперативная панель, кабинет слушателя, кабинет методиста. **Мёртвых токенов в дизайн-системе не осталось** — последний (`--ui-space-xxl`) применён, сторож `token-roles` расширен на весь `baseVars` без исключений. Мутация ловится двумя сторожами сразу. Тесты: `ci:check` — код выхода 0.

**2026-08-21 (§5.322, ветка `worktree-ui-016-density`):** **Срез 30 по ТЗ редизайна — `UI-016`: плотность задаётся контекстом.** Прежние отступы 20/14/18/11 не были ступенями шкалы вовсе. Применены контексты «реестр» (строка таблицы ровно 44px — height как минимум, та же цифра, что тач-зона; ячейка sm/md) и «карточка» (внутренний lg, между колонками разворота xl — `--ui-space-xl` наконец применён). Контекст «просторно» намеренно ждёт шаблонного класса дашборда (`TPL-005`) — остаток записан, статус 🟡. Сторож `density-contexts` проверен мутацией; его первая редакция проверяла отступ в правиле про фон. Тесты: `ci:check` — код выхода 0.

**2026-08-21 (§5.321, ветка `worktree-typography-tokens`):** **Срез 29 по ТЗ редизайна — типографика применена, запись 163 закрыта.** Потолок контента (`UI-017`) встал на каркас страницы — до этого он существовал на бумаге, и на широком мониторе контент растягивался без предела. **45 голых весов шрифта** (включая 650 и 800 — ступени вне шкалы) заменены тремя токенами, **18 интерлиньяжей** из зоопарка 1.1…1.7 сведены к двум ступеням; `line-height: 1` и px-значения оставлены осознанно (служебные элементы и геометрия). Сторож `type-rhythm` проверен мутацией. Остаток — `--ui-space-xl`/`xxl` из `UI-016`. Тесты: `ci:check` — код выхода 0.

**2026-08-21 (§5.320, ветка `worktree-ui-002-003-tokens`):** **Срез 28 по ТЗ редизайна — `UI-002` и `UI-003`.** Четыре из шести нейтралей не применялись НИГДЕ и удалены; у оставшихся роли записаны в токенах (`-500` — нейтральный статус, `-900` — подложка медиа). Разведка сначала соврала: поиск без границы слова находил «-50» как префикс «-500». По `UI-003` нарушение было одно — декоративная полоса карточки курса с кораллом (десять карточек = десять акцентных пятен на экран), переведена на индиго. Сторож `token-roles`: каждый токен темы применяется + коралл только у первичной кнопки и CTA героя, проверен мутацией. Попутный дрейф: пять токенов типографики объявлены и не применены (запись 163). Тесты: `ci:check` — код выхода 0.

**2026-08-21 (§5.319, ветка `worktree-ui-014-hierarchy`):** **Срез 27 по ТЗ редизайна — `UI-014`: иерархию держат фон и расстояние, а не рамки.** Главное в срезе — найденное противоречие: выполнить требование дословно было нельзя. Фон страницы и фон карточки отличались на доли процента (контраст 1.04), а после среза 26 рамка оставалась последним, что выделяло карточку, — убрав её, мы сделали бы карточки невидимыми. Правило выполнено вместе с правкой палитры (фон страницы `#eef2f7`), аудит контрастов 55/55. Рамка убрана у шести карточных классов; сторож `card-hierarchy` проверяет обе половины — и отсутствие рамки, и различимость фонов. **⚠️ Вид меняется на всех экранах сразу**, откат — одно значение токена. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.318, ветка `worktree-txt-007-tone`):** **Срез 26 по ТЗ редизайна — `UI-019` (тени) и `TXT-007` (тон).** Тень стояла почти везде: у **всех карточек секций**, героя, плиток, карточки прогресса; сильная — даже у карточки курса и палитры команд, хотя ТЗ оставляет её только модалке и выдвижной панели. Когда тень есть у всего, она перестаёт значить «слой всплыл над страницей». Оставлена только всплывающим слоям; сторож `shadow-discipline`. `TXT-007` оказался почти выполнен (один восклицательный знак), но **разведка grep'ом соврала** — ноль вместо восьми совпадений; правило закреплено сторожем `tone-of-voice` на 17 форм по падежам, обе проверки испытаны мутацией. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.317, ветка `worktree-i3-staging-second-tenant`):** **ФТ-I3 — на стенде появился второй арендатор; ФТ-C3 закрыт сверкой.** Изоляция арендаторов закрыта тестами, но **на живом стенде проверить её было нечем**: в базе жил ровно один арендатор `tenant_demo` из миграции `0010`. Заведён скрипт `pnpm seed:staging` — арендатор «Бета» со своими ролями, пользователями, **правами** (без них арендатор рождается с ролями без единого права), курсом, группой и слушателем; скрипт отказывается работать при `NODE_ENV=production`, пароль — из `STAGING_SEED_PASSWORD`. Сторож проверяет, что сид не ссылается на чужого арендатора и идемпотентен. Попутно сверкой закрыт `C3` (настраиваемый срок хранения снимков на деле сделан) — десятая отставшая строка трекера. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.316, ветка `worktree-ui-024-icon-scale`):** **Срез 25 по ТЗ редизайна — `UI-024` «иконки только через `Icon`, размеры 16/20/24».** Находка не про единообразие: оглавление курса показывало состояние уроков символами «🔒 ✓ ⏳ ☐» под `aria-hidden`, поэтому **слушатель со скринридером слышал только название урока** и не знал, пройден он, идёт или закрыт. Та же схема в результате теста, онбординге и стрелке тренда (там терялось «рост/снижение»). Значки заменены иконками набора, смысловым дана подпись для читалки. Шкала приведена к ТЗ: ступень 18 убрана, по умолчанию 16. Сторож `icon-discipline` дважды чинился по ходу (пропускал «⏳»; ловил символы в комментариях) и проверен мутацией. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.315, ветка `worktree-c1-pep-signature`):** **ФТ-C1 закрыт — уровень ПЭП подключён.** Контур простой электронной подписи был построен ЦЕЛИКОМ и не работал: таблицы `0067` (соглашение и принятия с IP, user-agent, хэшем текста), сервис `getStatus`/`accept`/`signAction`, право и экран настройки — всё на месте, но **ни `requiresSimpleSignature`, ни `signAction` не вызывались ниоткуда**. Центр мог включить уровень 1 и считать, что собирает подписи под ответами на тесты, — не собиралось ничего. Подключено двумя местами: гейт в `startAttempt` (уровень ≥1 без соглашения → 412 с объяснением, что делать) и подпись сданного теста через `signAction` → запись в журнале юридически значимых действий. Миграция не понадобилась. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.314, ветка `worktree-ui-024-icons`):** **Срез 24 по ТЗ редизайна — `TXT-004` «ошибка объясняет, что произошло и что делать».** Приложение показывало ответ сервера слово в слово («Unexpected API error»), а администратору центра это не говорит ни что случилось, ни что делать. Починка вышла дешёвой, потому что все **57 мест** берут `error.message`: словарь `lib/errors/error-text.ts` подключён в `ApiClientError` — текст стал человеческим везде разом. Русские сообщения сервера сохраняются (в них конкретика), «что делать» дописывается. Технический код, ответ сервера и номер запроса — в спойлере «Подробности»; 10 экранов переведены на `SectionError error={…}`. **Найдено по пути:** общий помощник `readApiMessage` возвращал сырое сообщение и обходил словарь на 12 экранах; фильтр статусов показывал `active`/`blocked` латиницей; два пустых состояния были подписаны «Pending attempts отсутствуют». Сторож связки проверен мутацией. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.313, ветка `worktree-g2-exam-code-throttle`):** **ФТ-G2 закрыт; сверка остатка ТЗ «Арендная СДО» по коду.** Первые три кандидата на работу (`A1` рендер, `C2` «личное дело», `G3` 2FA) оказались уже сделанными, поэтому проверен весь остаток: **трекер отстал по восьми строкам** (`A1`, `A2`, `A3`, `B2`, `B3`, `B4`, `C2`, `G3`) — все приведены в соответствие коду с указанием файла и функции. **Реальный остаток нашёлся один, про безопасность:** ввод кода допуска к экзамену (`verify-pre-exam-token`) не был ограничен по частоте — ни предела, ни счётчика попыток; рядом `request-pre-exam-token` слал сколько угодно писем. Пределы вынесены в `pre-exam-rate-limit.ts` (10/мин и 5/мин), сторож проверяет числа, наличие `ThrottlerGuard` и живой 429. Найден дрейф (в срез не взят): `requiresSimpleSignature` — ПЭП-уровень политики идентификации написан, но не вызывается ниоткуда (единственный незакрытый кусок `C1`). Исправлена своя ошибка §5.311 — дубли номеров в журнале расхождений. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.312, ветка `worktree-c41-snils-input`):** **ФТ-C4 по ТЗ «Арендная СДО» закрыт — СНИЛС проверяется ПРИ ВВОДЕ.** Строка трекера была неверна дважды. Pre-export проверка со списком «у кого чего не хватает» на деле есть у всех пяти реестров и все пятеро **блокируют сборку файла** при пробелах — критерий приёмки выполнен ещё раньше. А настоящий пробел был в другой половине требования: проверка контрольной суммы стояла в массовом импорте, допуске к экзамену и preflight, но **карточка слушателя — единственная форма ручного ввода — сохраняла номер как есть**. Опечатка жила в базе месяцами и всплывала в день отчётности. Добавлен `assertSnilsChecksum` в оба пути записи (до первой записи полей) + мгновенная подсказка в форме; правило вынесено в общее `lib/snils`, серверный дубль `normalizeSnils` убран. Попутно: образец в поле показывал номер, который сам не проходит проверку. Тесты: `ci:check` — код выхода 0.

**2026-08-20 (§5.311, ветка `worktree-ui-023-status-color`):** **Срез 23 по ТЗ редизайна — `UI-023` «статус читается без цвета».** Сверка по коду нашла обратную поломку: подпись у чипа статуса была всегда, а **цвет не работал на 11 экранах** — в проп ключа палитры уходила готовая русская подпись (`STATUS_LABEL[...]`, `formatEntityStatus(...)`), поиск по карте промахивался, и все статусы красились одинаковым серым. Дефект односторонний и невидимый на глаз: текст оставался верным, различие «Действует / Истекла / Отозвана» пропадало. Задеты реестры слушателей, клиентов, лицензий, очередь переаттестации и шесть экранов проверки знаний. Плюс **6 из 13 ключей палитры не имели русской подписи** (`queued`, `running`, `published`, `blocked`, `suspended`, `cancelled`) — чип показал бы латиницу. Добавлены подписи и тип `SemanticStatus`, два сторожа (`status-label.test.ts`, `status-color-not-alone`); первая редакция второго сторожа врала — `\b` не срабатывает после подчёркивания, проверено мутацией. Тесты: фронт 1148/1148, `ui` 248/248, typecheck 13/13, lint 9/9.

**2026-08-20 (§5.310, ветка `docs/2026-08-20-session-5310`):** **Обслуживание: синхронизация, слияние среза 22, приборка. 🔴 СТЕНД ПАДАЛ И ПОТЕРЯЛ SSH-ДОСТУП.** Несколько часов `lms.ptsfera.online` был недоступен: машина **отвечала на ping** (0% потерь), но **все TCP-порты молчали по таймауту** — 22, 80, 443, 14252, при исправном интернете и расстоянии в один хоп. «ICMP жив, TCP мёртв» — не выключенный сервер, а исчерпание ресурса: на эхо отвечает ядро, а принять соединение некому. К 11:37 стенд поднялся сам (nginx отдаёт `401`, basic-auth жив), **но `ssh` теперь отвечает `Permission denied` — ключ пропал из `authorized_keys`**. Похоже, машину пересоздали или восстановили из копии. **Вернуть доступ можно только через консоль VNC/KVM в панели хостинга** — [docs/RUNBOOK_TEST_STAND.md](docs/RUNBOOK_TEST_STAND.md); пока доступа нет, причину падения узнать нельзя. Попутно: локальная копия отставала на 139 коммитов (перемотана, переустановка под pnpm 11); конфликты PR #516 оказались squash-дублём среза 21 и сняты — PR принят как `e6a2b894`; удалены 3 брошенных каталога (2,3 ГБ), 4 мёртвые ветки и 2 стэша. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-20 (§5.309, ветка `feat/2026-08-20-ui-typography`):** **Типографическая шкала и ширина строки (`UI-011`, `UI-012`, `UI-013`, `UI-015`, `UI-017`).** Размеры букв брались «на глаз»: **58 мест** задавали их сами, вразнобой и в разных единицах (`0.95rem` против `0.97rem` — глазом не отличить, а смысл разный). Заголовок героя доходил до 33px при потолке шкалы 22px, то есть самый крупный текст продукта жил вне шкалы. Стало: ступени `2xl`/`3xl`, все 58 размеров на шкале (36 без изменения вида, 22 округлены на 1–2px), заголовки через `clamp()` на ступенях, ширина строки 68 знаков там, где текст читают предложениями. Сторож проверен двумя нарушителями; исключение для `em` (размер относительно родителя) сделано с объяснением.

**2026-08-19 (§5.308, ветка `feat/2026-08-19-ui-motion-a11y`):** **Движение теперь уважает настройку доступности (`UI-028`, `UI-029`, `UI-030`).** Найдено: правило «поменьше движения» отключало **ровно один переход из шестнадцати** — шеврон бокового меню; кнопки, поля, панели, полоса прогресса и бесконечное мерцание скелетона продолжали двигаться. Для людей с вестибулярными нарушениями, склонностью к укачиванию и мигренью это физическая помеха. Стало: одно правило на все элементы, токены длительности вместо 29 чисел вразнобой, у скелетона мерцание убрано (анимация данных запрещена требованием), у кнопки в работе текст прячется только там, где движение разрешено — иначе была бы пустая кнопка без слов. Сторож проверен двумя нарушителями. Попутно: `UI-023` («статус читается без цвета») оказался уже сделан — трекер отстал и здесь.

**2026-08-19 (§5.307, ветка `fix/2026-08-19-live-run-defects`):** **`ФТ-G1` закрыт — скачивание документов пишется в журнал действий.** Требование называет четыре чувствительных действия; сверка по коду показала, что три уже пишутся, а скачивание — нет. Скачанный документ это удостоверение с ФИО, СНИЛСом и датой рождения: без записи центр не ответит, кто и когда его выгрузил (152-ФЗ). Запись добавлена на обоих каналах — обычная выдача и портал заказчика — со слушателем и номером документа, строго ПОСЛЕ проверок доступа. **⚠️ Попутно: четыре дефекта живого прогона, которые трекер держал открытыми, оказались починены все четыре** — второй случай за два среза, когда трекер отстал от кода (первый — цифра §12.1). Отсюда правило: **прежде чем брать задачу из трекера, проверить её по коду**.

**2026-08-19 (§5.306, ветка `feat/2026-08-19-perf-12-1-guards`):** **§12.1 — сторожа производительности и найденный дефект планировщиков.** Первым делом сверил цифры: трекер называл 2,32 с, канон замеров — **0,378 с** при норме 0,3 с. Разрыв 26%, а не «в семь раз»; цифра в трекере исправлена, владельцу названа верная. Замер объяснил природу остатка: расшифровка одного СНИЛС ~64 мкс — **цена среды, а не кода** (чистый Node даёт столько же), на 500 слушателях 27–32 мс на запрос; оптимизировать её бессмысленно. **⚠️ Найден дефект: удаление видеозаписей экзаменов могло не выполняться никогда** — оно и закрытие просроченных попыток держали один ключ блокировки и молча блокировали друг друга (152-ФЗ: видео с ПДн обязаны удаляться по сроку). Закрыта третья задача плана ленивого чтения, добавлены два сторожа, оба проверены подсадными нарушителями. Проверено, что несколько экземпляров бэкенда запускать безопасно (все планировщики под замками, миграции под локом) — остаток §12.1 это ops-шаг за владельцем.

**2026-08-19 (§5.305, ветка `feat/2026-08-19-ft-d1-permission-surface`):** **`ФТ-D1` закрыт — gap A2 (права на всех ручках), и разбор нашёл ТРИ дефекта.** (1) **Слушатель мог изменить реквизиты учебного центра**: правка настроек и реквизитов не требовала никакого права, а экран открыт по `tenant.read`, которое есть у всех ролей; из реквизитов берутся юр. название, ИНН и картинки подписи с печатью для **выдаваемых удостоверений**. (2) Обе ручки к тому же **падали ошибкой сервера** при сохранении — экран «Реквизиты» не работал вообще. (3) **Переобработка неудачных вебхуков была недостижима** — её перехватывал маршрут внешнего вебхука, объявленный выше. Миграция `0083`, порядок маршрутов исправлен, два новых сторожа (реестр прав + перехват маршрутов) проверены подсадными нарушителями. ⚠️ Сторожа соврали дважды, пока писались (поймали маршрут в комментарии; засчитали право соседней ручки) — записи 131–134. Живая проверка обеих сторон: слушатель 403, администратор 200/201. `ci:check` exit 0.

**2026-08-19 (§5.304, ветка `feat/2026-08-19-ft-d1-platform-catalog`):** **`ФТ-D1` — закрыты два остатка изоляции из трёх.** Проверка показала, что арендатор в сообщениях очереди (gap A1) фактически уже есть, а вот сплошной проверки ручек не было — и она сразу нашла дефект: **администратор одного учебного центра мог выключить провайдера интеграции, общего для всех** (право `integrations.write` есть у `tenant_admin` — сверено по живой базе, а не по названию роли; выключил «ФИС ФРДО» — выгрузки в надзор встали у всех арендаторов). Миграция `0082` вводит платформенное право; чтение каталога у арендатора осталось, интерфейс его только читает — ни один сценарий не сломан. Живая проверка: центр 403, платформа 201, миграция идемпотентна. Добавлен сторож уровня ОБРАБОТЧИКА (соседний проверял только контроллер): каждая из 490 ручек либо использует арендатора, либо внесена в реестр исключений с обоснованием. Три подсадных нарушителя убиты. Остаток `ФТ-D1`: gap A2 — 66 ручек без явных прав, нужен такой же реестр.

**2026-08-19 (§5.303, ветка `feat/2026-08-19-ft-g7-security-headers`):** **`ФТ-G7` закрыт — заголовки безопасности, session-политики, защита от CSRF.** Было: во всём бэкенде ОДИН заголовок безопасности, в настройках фронта — ни одного, при том что требование стоит в эпике безопасности с высшим приоритетом. Стало: три набора политик чистыми функциями — на каждый ответ API (включая ответы об ошибке), отдельный на чужой код учебного курса, третий на страницу с одноразовым числом. **CSRF дописывать не понадобилось:** авторизация Bearer-заголовком, который браузер сам не шлёт; cookie-авторизуется ровно `/auth/refresh` и уже защищён — вместо кода поставлен сторож поверхности, проверенный тремя подсадными нарушителями. **Живой прогон нашёл то, чего не видели тесты:** клиент ходит на сервер уведомлений двумя схемами — с одной оповещения молча не приходили бы. ⚠️ Вне среза: курс SCORM исполняется на том же источнике и видит родительское окно (запись 127) — лечится переводом моста плеера на обмен сообщениями, нужно решение владельца. `ci:check` exit 0. Подробности — `docs/security-headers.md`.

**2026-08-18 (§5.302, ветка `worktree-ui-faza8-srez1`, PR #537):** **Фаза 8, выкатка N — окно совместимости ключей хранения.** Пять ключей (cookie сессии и защиты от подделки, cookie кода центра, снимок сессии, выбор оформления) переименованы на trudskill: читаем новое → при отсутствии прежнее, пишем оба одним значением, при выходе гасим оба. ⚠️ Отступление от буквы `BR-020` п.1 обосновано (запись 115): иначе откат дал бы либо ложную кражу токена с отзывом всех сессий, либо разлогин всех. Состязательное ревью нашло до слияния три дефекта (записи 116–118), включая «Выйти» не выходило при прямом заходе на страницу выхода. **⏳ Выкатка N+1 — через 60 дней после выкатки в прод**, план в `docs/REBRANDING_KEYS_ROLLOUT.md`. Остаток ребрендинга: `BR-031`/C-2 (`CDOPROF_*` в скриптах бэкапа) — после повторных учений восстановления.

**2026-08-18 (§5.301, ветка `worktree-ui-faza7-srez2`, PR #536):** **ФАЗА 7 РЕДИЗАЙНА ЗАВЕРШЕНА** (срезы 1–2, PR #535/#536). Срез 2: `SDOPROF_TZ_FINAL.md` → **`TZ_TRUDSKILL_BASE.md`** (`BR-040`/`BR-041` — 20 файлов, ~60 ссылок одним коммитом, якоря сохранены, старого имени ноль); живые заголовки (`MET-004`, AGENTS, README/CLAUDE). Вопросы владельцу: реальный почтовый домен (BR-032), уведомление о новом имени TOTP. Дальше — фаза 8: ключи хранения двойным чтением + две выкатки с интервалом 60 дней, инфраструктура (`BR-031`/C-2 — последними).

**2026-08-18 (§5.300, ветка `worktree-ui-faza7-srez1`, PR #535):** **Фаза 7 редизайна начата — категория A ребрендинга:** письма входа, TOTP issuer (`BR-010`), дефолты `SMTP_FROM`/`VAPID_SUBJECT` (`BR-032`), OpenAPI title — на trudskill; контракты перегенерированы, тесты кат. E в том же PR. **`BR-011`: подписанты НЕ меняются** (запись 114 — надзорные выгрузки). Вопросы владельцу: реальный почтовый домен, уведомление о новом имени TOTP. Дальше: срез 2 — категория D (документация, `BR-040`/`BR-041`).

**2026-08-18 (§5.299, ветка `worktree-ui-faza6-srez8`, PR #534):** **КОДОВАЯ ЧАСТЬ ФАЗЫ 6 РЕДИЗАЙНА ЗАВЕРШЕНА** (срезы 1–8, PR #527–#534). Срез 8: ⚠️ редиректы по §4.9 с предупреждением в PR — `/student/dashboard` → `/learner` (дубль главной кабинета), `/teacher/grading-center` → `/teacher/review` (виджеты на несуществующей роли `teacher`); пункты меню убраны, адреса живы. Хвосты 107: один коралл на `/documents`, крошки с многоточием. Итог фазы: все экраны кабинетов, `UI-010`/`UI-021` ✅, шесть очередей сторожей закрыты. Дальше по ТЗ редизайна — фазы 7–8 (ребрендинг).

**2026-08-18 (§5.298, ветка `worktree-ui-faza6-srez7`, PR #533):** **Фаза 6, срез 7 — очередь `unified-states` закрыта: пять самодельных панелей (4 дровера + модалка приказа) на общем `DetailDrawer`.** ШЕСТЬ очередей сторожей фазы 6 закрыты. Попутно: «ID модуля» текстом (запись 113 — слово «ID» в label, мимо сторожа) → выбор по названию; термины «Учеников»/«Email»/«ревью» приведены к канону. Открыто по фазе 6: судьба `/student/dashboard` и `/teacher/grading-center` (запись 109 — нужно слово владельца), мелкие хвосты записи 107; после них — фазы 7–8 (ребрендинг).

**2026-08-18 (§5.297, ветка `worktree-ui-faza6-srez6`, PR #532):** **Фаза 6, срез 6 — очередь `id-input-ban` закрыта: ни одного поля «вставьте идентификатор».** Закрытие группы: шаблоны по имени, сдавшие — фамилиями с флажками вместо textarea с id; добавление в группу и материалы — выбор по названию; слепая зона сторожа (образец `mat_...` без слова «ID», запись 112) закрыта с расширением регулярки. **Пять очередей сторожей пусты.** Открыто по фазе 6: судьба `/student/dashboard` и `/teacher/grading-center` (запись 109 — нужно слово владельца), очередь `unified-states` (8 файлов), мелкие хвосты записи 107.

**2026-08-17 (§5.296, ветка `worktree-ui-faza6-srez5`, PR #531):** **Фаза 6, срез 5 — компонента выбора файла; браузерных окон в интерфейсе не осталось.** `FilePicker` в пакете перевёл девять английских «Choose File» в восьми файлах на русскую кнопку с именем файла; два последних `alert` заменены выключенными кнопками с пояснением — списки исключений `browser-dialogs-ban` и остальных трёх очередей пусты. Попутно: четвёртая копия словаря видов документов сведена в общий слой. Открыто по фазе 6: судьба `/student/dashboard` и `/teacher/grading-center` (запись 109 — нужно слово владельца), очереди `id-input-ban` (3 места) и `unified-states` (8 файлов).

**2026-08-17 (§5.295, ветка `worktree-ui-faza6-srez4`, PR #530):** **Фаза 6, срез 4 — методист, календарь (`UI-021` ✅) и портал заказчика; закрыты сторожа `styled-jsx-ban` и `empty-states-explain`.** Календарь — последний styled-jsx — переехал в пакет; в ячейках были обрезанные идентификаторы слушателей → фамилии (справочник гейтится правом — живая проверка под слушателем поймала «Permission denied» до слияния); дрейф двух словарей статусов документов сведён к канону (запись 111). Открыто по фазе 6: судьба `/student/dashboard` и `/teacher/grading-center` (запись 109 — нужно слово владельца), очереди `id-input-ban`/`browser-dialogs-ban`/`unified-states`, компонента выбора файла.

**2026-08-17 (§5.294, ветка `worktree-ui-faza6-srez3`, PR #529):** **Фаза 6, срез 3 — прохождение теста и просмотр курса; очередь самодельных полос ЗАКРЫТА.** Последние две (`test-attempt`, `course-viewer`) переведены на общую `ProgressBar` — все восемь мест из сверки среза 22 закрыты; фолбэк «Курс course\_…» убран (закрыты все фолбэки записи 107); пустые состояния «Моих тестов» и «Результата теста» объясняют себя. Открыто по фазе 6: `/methodist` (последний в очереди empty-states), `/counterparty-portal`, календарь `UI-021`, судьба `/student/dashboard` и `/teacher/grading-center` (запись 109), очереди `id-input-ban`/`browser-dialogs-ban`/`unified-states`, компонента выбора файла.

**2026-08-17 (§5.293, ветка `worktree-ui-faza6-srez2`, PR #528):** **Фаза 6, срез 2 — главная слушателя и hero по `UI-010`.** Hero «Следующий шаг» раздет от декора: плоская подложка в тон `--ui-surface-accent`, бренд-акцент на заголовке, печать и eyebrow удалены, hover CTA — `--ui-accent-700`; фон героя впервые измерим — +3 пары в контраст-аудите (×2 темы). «Мои курсы» на главной: статусы словами, `ProgressBar`, подсказки пустых состояний; **третья копия словаря видов документов (уже разъехавшаяся)** сведена в общий слой; сняты 3 позиции очередей сторожей. Открыто по фазе 6: тест-плеер и просмотр курса, `/methodist`, `/counterparty-portal`, календарь `UI-021`, судьба `/student/dashboard` и `/teacher/grading-center` (запись 109), компонента выбора файла.

**2026-08-17 (§5.292, ветка `worktree-ui-faza6-srez1`, PR #527):** **Фаза 6 редизайна начата; план разбиения монолита §8.3 ЗАВЕРШЁН** — `features/mvp/screens.tsx` (3 019 строк на старте) удалён полностью: курсы слушателя → `features/learner-courses/`, дашборды ролей → `features/role-dashboards/`. Редизайн «Моих курсов»: документы подписаны названием курса вместо «Назначение enrollment_x», статусы и виды документов словами, ProgressBar вместо голого progress, пустые состояния с пояснениями; сняты две позиции очередей сторожей. Открыто (запись 109): судьба `/student/dashboard` («Главная учащегося») и `/teacher/grading-center` (роль `teacher` не существует), остальные экраны кабинетов, hero `UI-010`, календарь `UI-021`, компонента выбора файла.

**2026-08-17 (§5.291, ветка `worktree-ui-faza5-srez3-dark-pass`, PR #526):** **Фаза 5 редизайна ЗАКРЫТА ЦЕЛИКОМ** — срез 3, визуальный прогон всех маршрутов волн 1–2 в тёмной теме на живом стенде. Прогон вскрыл: **вечный цикл запросов в шиме useQuery** (шесть ручек оболочки опрашивались ~2 раза в секунду, `/workspace` вечно в скелетоне — починено ref-паттерном + сторож), **`/learners` так и не был переключён на эталонный реестр Фазы 2** (флагман PR #493 был мёртвым кодом — маршрут переключён), ссылки без класса браузерным синим (~1.1:1 в тёмной теме), дорожка прогресса выглядела заполненной, `[object Object]` в ячейках, график аналитики на несуществующих `var(--color-*)`, сырые id в подзаголовках и крошках. Журнал расхождений 99–107; очередь — общая компонента выбора файла (9 английских «Choose File»). Дальше по ТЗ редизайна — **Фаза 6: кабинеты слушателя и преподавателя** + накопленные очереди сторожей.

**2026-08-16 (§5.290, ветка `worktree-ui-redesign-faza5-srez2`):** **Фаза 5, срез 2 — переключатель темы в шапке и вторая половина правила про идентификаторы.** Переключатель оформления жил только в настройках: человеку, которому прямо сейчас слишком ярко, приходилось вспоминать про их существование. Переехал в шапку. **Попутно вскрылось, что проверялась только половина правила про идентификаторы:** сторож ловит, когда идентификатор просят ВВЕСТИ, а когда его ПОКАЗЫВАЮТ значением — не проверял никто. Нашлось четыре места, из них грубое: **список зачисленных в группу состоял из идентификаторов вместо фамилий** — понять, кто учится, было нельзя. Ещё: идентификатор арендатора висел в шапке на каждом экране под словом «Тенант». Всё исправлено, поставлен сторож. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-16 (§5.289, ветка `worktree-ui-redesign-faza5-srez1`):** **Фаза 5 «Тёмная тема», срез 1 — контрасты измерены, брендирование починено.** ТЗ требует мерить контрасты инструментом, а не переносить числа из комментариев: заведена своя функция расчёта по WCAG 2.2 и прогон из **49 измерений** (23 пары «что на чём» × 2 темы). **Измерение сразу нашло два дефекта.** Первый: **граница поля ввода даёт 1.23:1** при норме 3:1 — у поля граница это единственный признак, что сюда можно писать, и человек со слабым зрением его не различал. Второй: **центр с тёмным фирменным цветом не мог прочитать собственную первичную кнопку** — цвет текста на ней не пересчитывался. Оба починены. Попутно: есть цвета, на которых не читается никакой текст (фиолетовый `#8b5cf6` — 4.23:1) — форма брендирования предупреждает, но не запрещает. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-16 (§5.288, ветка `claude/focused-bohr-001278`):** **Сторож `UI-022` перестал падать на пустом месте.** `styled-jsx-ban.e2e.test.ts` считал относительный путь обрезанием префикса, без перевода разделителей: на Windows выходило `app\learning\calendar\page.tsx`, а список исключений записан через `/`. Совпадений нет — **список исключений не работал целиком**, и прогон был красным при любом состоянии кода. На Linux сторож зелёный, но **CI на GitHub выключен владельцем**, значит вердикт даёт только локальный прогон — то есть проверка не давала вердикта вообще. Путь приведён к форме соседних сторожей; добавлена проверка, что запись в списке исключений кого-то описывает — иначе промах снова остался бы немым. Остальные сторожа с обходом файлов проверены — включая двух новых из среза 22, — разделители у них нормализованы. После слияния со срезами 22–25: `src/e2e` 41 файл / 281 тест, фронт 168 / 1099, `@trudskill/ui` 154 — зелено на Windows.

**2026-08-16 (§5.287, ветка `worktree-ui-redesign-faza4-srez25`):** **Фаза 4 ТЗ по редизайну, срез 25 — внутренние названия и англицизмы в заголовках. Закрыт.** Сторож латиницы из среза 19 проверял подписи в данных и **не смотрел на заголовки в разметке**. Из-за этого администратору учебного центра показывались **наши рабочие обозначения этапов разработки** — «Нормативные параметры программы (Pillar A)», «Действия проверки — Plan C», — а также «CRM · Сделки», «Панель LMS», «KPI обучения», «Push-уведомления», «Бренд центра (white-label)». Всё переписано по-русски, сторож расширен и теперь ловит латинское слово даже внутри русской фразы. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-16 (§5.286, ветка `worktree-ui-redesign-faza4-srez24`):** **Фаза 4 ТЗ по редизайну, срез 24 — конструктор отчётов и платформа. ВОЛНА 4 ЗАКРЫТА, все админские экраны пройдены.** Конструктор отчётов говорил по-программистски: «Сущность», «Поля», «Превью», «Скачать XLSX» — переведён на человеческий язык. **Главная находка: одна запись выводилась дважды сразу на четырёх экранах** (арендаторы, счета, библиотека программ, сделки) — таблица, а под ней второй список тех же записей с кнопками. **В сделках кнопки не называли сделку вообще** — только стадию: понять, какая строка чья, можно было лишь по порядку, одна оплошность и переведена чужая сделка. Всё сведено к действиям строки, поставлен сторож. **Не выдумал колонку «Тариф»** у арендаторов — сервер этих данных не присылает, записано открытым расхождением. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-16 (§5.285, ветка `worktree-ui-redesign-faza4-srez23`):** **Фаза 4 ТЗ по редизайну, срез 23 — очереди проверки. Закрыт.** Подтверждение личности и видеозаписи экзаменов переведены на шаблон реестра. **Дефект: заявку можно было отклонить молча** — причина не требовалась, слушатель видел «Отклонена» без единого слова о том, что переснять. Теперь без причины кнопка недоступна. **Дрейф терминологии:** один раздел назывался «Подтверждение личности» у слушателя и «Идентификация» у администратора; «Записи прокторинга» — жаргон. Сведено к одному имени, адреса не менялись. С карточки записи убран идентификатор попытки с кодом состояния. **Слепая зона своего же сторожа:** он видел только один способ задать пустое состояние из двух — расширен, попутно `DataTable` научилась пояснению. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-16 (§5.284, ветка `worktree-ui-redesign-faza4-srez22`):** **Фаза 4 ТЗ по редизайну, срез 22 — лицензии и использование. Закрыт.** В реестре лицензий форма из шести полей висела развёрнутой под таблицей — экран отвечал «заполни меня» вместо «вот твои лицензии». Форма уехала в панель, «Добавить лицензию» стало первичным действием в шапке, даты выводятся по-русски. С экрана использования убран код тарифа как значение. **Главная находка: полоса заполнения нарисована в коде восемь раз** — шесть голых `<progress>` и две собраны вручную с инлайновыми цветами, которые вдобавок не видит сторож дисциплины токенов. Заведён общий компонент, где **тон отделён от значения** (90% прогресса по курсу — хорошо, 90% расхода лимита — тревога), сведены 4 места, 4 экрана кабинета слушателя — в очередь волны 6. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-16 (§5.283, ветка `worktree-ui-redesign-faza4-srez21`):** **Фаза 4 ТЗ по редизайну, срез 21 — пустой экран объясняет себя. Закрыт.** Сплошная проверка: **59 пустых состояний из 102 не объясняли ничего** — «Записей нет», «Задачи не найдены». Человек видит пустоту и без подписи; ТЗ требует сказать, что это за раздел и что сделать первым. Пояснение добавлено 46 состояниям в 30 файлах, заведён сторож с очередью на кабинеты слушателя и методиста (волна 6). **Попутно вскрыта слепая зона всех сторожей:** семь из них считали пути от текущего каталога, поэтому при запуске из корня репозитория либо падали, либо сканировали весь монорепозиторий — проверка зависела от способа запуска. Заведён общий модуль, считающий корень от файла теста. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-15 (§5.282, ветка `worktree-ui-redesign-faza4-srez20`):** **Фаза 4 ТЗ по редизайну, срез 20 — одно слово для одного понятия. Закрыт.** Сверка показала, что состояние сущности подписано двумя словами сразу: 39 мест «Статус», 32 — «Состояние». **Разнобой завёл я сам** в прошлых срезах, не сверившись с макетом ТЗ, где стоит «Статус». Свёл к слову из ТЗ в 19 файлах и поставил сторожа. Попутно: колонка «Задача» на экране эксплуатации показывала «certificate · 3f7a-…» — стала «Что выпускалось» с видом документа словом. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-15 (§5.281, ветка `worktree-ui-redesign-faza4-srez19`):** **Фаза 4 ТЗ по редизайну, срез 19 — обмен данными и английские заголовки. Закрыт.** Экран интеграций был самым англоязычным: «Name», «Status», «Secret», «Creds», «Last sync», пустое состояние «Нет credentials», раздел «Подключения тенанта». **Сплошная проверка показала, что беда шире одного экрана:** 13 заголовков виджетов на стартовых экранах были английскими при русских пояснениях под ними, плюс «Session ID», «Email», «Actor», «HTTP» и пять колонок «ID» в разных местах. Все переведены, заведён сторож — заголовок обязан содержать русские буквы (имена форматов и название продукта разрешены отдельно). **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-15 (§5.280, ветка `worktree-ui-redesign-faza4-srez18`):** **Фаза 4 ТЗ по редизайну, срез 18 — журнал действий. Закрыт.** **Нашлись ссылки-обманки:** под таблицей висели пять ссылок «Детали события…», которые вели на рабочий стол. Экран говорил по-английски — колонки «Actor», «Action», «Entity ID», значения кодами вида `learning.learner_created`. Теперь «Когда / Кто / Что произошло / Над чем», а код разбирается в человеческую фразу с согласованием по роду («Попытка теста начата», а не «начат»). Журнал грузится при открытии, а не по кнопке «Обновить». **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-14 (§5.279, ветка `worktree-ui-redesign-faza4-srez17`):** **Фаза 4 ТЗ по редизайну, срез 17 — пользователи и направления. Закрыт.** Последние экраны по плану разбиения вынесены из большого файла: он прошёл путь **с 3019 до 314 строк**, остались только экраны слушателя и преподавателя. **Реестр пользователей выводил каждого дважды** — строкой таблицы и ниже ссылкой «Открыть карточку»; двадцать сотрудников занимали сорок строк. Теперь одна таблица, имя — ссылка. Пометка «Только просмотр» повторялась у каждой строки — стала одной строкой в шапке. Направления были списком из одних названий — стали таблицей с объяснением, что это такое. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-14 (§5.278, ветка `worktree-ui-redesign-faza4-srez16`):** **Фаза 4 ТЗ по редизайну, срез 16 — заказы и переаттестация. ВОЛНА 3 ЗАКРЫТА.** На экране заказов **кнопка переименовывалась по ходу сценария** («+ Новый заказ» ↔ «Скрыть форму») — теперь называется одинаково, а форма открывается панелью. **Три поля просили вставить идентификаторы** — заменены выбором по имени; покупатель в списке показывался как «Слушатель: 3f7a…», теперь это имя человека или название компании. Очередь переаттестации переведена на общий каркас. Очередь сторожа «вставьте идентификатор» сократилась с 19 мест до 4. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-14 (§5.277, ветка `worktree-ui-redesign-faza4-srez15`):** **Фаза 4 ТЗ по редизайну, срез 15 — дубль сущности сведён по решению владельца.** «Заказчики обучения» и «Компании» были двумя экранами одного и того же: одна ручка, два пункта меню. Владелец выбрал **«Компании»** — там ИНН, контакты, карточка и сводка обучения. Прежние адреса стали перенаправлениями (идентификатор тот же, поэтому старая ссылка открывает ту же организацию), экран-дубль удалён, короткое меню роли «менеджер» переключено. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.276, ветка `worktree-ui-redesign-faza4-srez14`):** **Фаза 4 ТЗ по редизайну, срез 14 — карточка курса и реестр компаний. Закрыт.** На карточке курса **печаталось имя поля базы**: материалы выводились как «1. Инструктаж [text] min_view_seconds=60». Теперь вид материала словом, время просмотра по-человечески, а условие публикации перечисляет ровно то, чего не хватает этому курсу (была одна общая фраза). Реестр компаний переведён на общий каркас. **⚠️ Найден дубль: `/counterparties` и `/admin/clients` — два экрана одной сущности** на одной ручке, оба в меню; вынесено владельцу решением, молча не сливал. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.275, ветка `worktree-ui-redesign-faza4-srez13`):** **Фаза 4 ТЗ по редизайну, срез 13 — дубли в меню. Закрыт.** Сверка вскрыла недочёт прошлых срезов: **шесть пунктов меню вели на перенаправления** — адреса-дубли убрали, а пункты меню оставили, и человек по-прежнему видел два входа в один раздел. Пункты убраны (адреса сохранены), заведён сторож, который сам находит такие страницы в коде. Отдельно: `/registry` оказался вторым, более бедным входом в журнал действий и висел в меню у ролей, которым всегда отвечал ошибкой, — стал перенаправлением на «Аудит». Пункт «SCORM» переименован в «Учебные пакеты (SCORM)». **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.274, ветка `worktree-ui-redesign-faza4-srez12`):** **Фаза 4 ТЗ по редизайну, срез 12 — выгрузки в государственные реестры. Закрыт.** Экран на **759 строк лежал прямо в странице** — вынесен, страница стала обёрткой на 10 строк. Было **двенадцать блоков подряд** (мастер, история, журнал и пять ведомств со своими формами) — теперь ведомство выбирается списком, и виден только его блок. **Нашлось поле, куда просили вписать JSON** — заменено выбором группы по названию; ещё четыре поля «ID группы»/«ID клиента» тоже стали выбором. В истории и журнале коды `frdo`, `learners`, `queued` заменены словами. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.273, ветка `worktree-ui-redesign-faza4-srez11`):** **Фаза 4 ТЗ по редизайну, срез 11 — заказчики и комиссии. Закрыт.** Оба раздела вынесены из большого файла: он похудел с 1092 до **597 строк**. Реестр заказчиков был вертикальной стопкой ссылок — стал таблицей с состоянием и датой. Карточка называлась «Карточка контрагента» одинаково для всех и показывала только код — теперь в заголовке название организации. У комиссий форма создания стояла блоком под таблицей, а пустой экран отправлял «создайте комиссию ниже» — стало кнопкой в шапке и панелью, а пустой экран объясняет, что без комиссии не выпускаются протоколы и удостоверения. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.272, ветка `worktree-ui-redesign-faza4-srez10`):** **Фаза 4 ТЗ по редизайну, срез 10 — курсы, начало волны 3. Закрыт.** Экраны курсов вынесены из большого файла: он похудел с 2153 до **1095 строк**. **Попутно найден мёртвый экран создания курса на 212 строк** — на него не ссылался никто, потому что `/courses/new` давно открывает другой, настоящий мастер; удалён, а не перенесён. Реестр курсов был списком ссылок — стал таблицей с кодом, датой изменения и состоянием; отбор по состоянию показывал коды (`draft`, `published`) — стал словами; пустой экран объясняет, что такое курс. Надпись «Недостаточно прав для создания курса» убрана — без права кнопка просто не показывается. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.271, ветка `worktree-ui-redesign-faza4-srez9`):** **Фаза 4 ТЗ по редизайну, срез 9 — волна 2 закрыта.** Три реестра оценивания (тесты, задания, банки вопросов) переведены на общий каркас; во всех трёх **колонка «Курс» показывала идентификатор вместо названия**. Форма создания теста стояла блоком над списком и просила «ID курса» текстом — стала кнопкой в шапке и панелью с выбором курса по названию. В отчётах и аналитике поля `course_id`, `group_id`, `client_id` заменены выбором по названию, с графика убрана запрещённая надпись «Нет данных для графика». **Сплошная проверка вскрыла системную беду: 19 полей в 8 файлах просят у человека идентификатор** — места волн 1–2 исправлены, остальные внесены в новый сторож очередью с указанием волны. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.270, ветка `worktree-ui-redesign-faza4-srez8`):** **Фаза 4 ТЗ по редизайну, срез 8 — экран оценивания, начало волны 2. Закрыт.** Экран вынесен из монолита (тот похудел с 2596 до 2153 строк) и переделан. Было **девять блоков подряд** при бюджете «не больше трёх до сгиба», причём три из них просто повторяли содержимое своих же экранов — убраны, вместо них ссылка. **На экране не было ни одной фамилии:** слушатель, тест, задание и зачисление показывались идентификаторами, а результат попытки печатался как `score=8/10, passed=да`. Теперь везде имена и названия, итог — «Зачёт: 8 из 10 баллов». Пояснения, написанные для разработчика («субмиты с learnerId… IAM: learners.act_as»), заменены человеческими. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.269, ветка `worktree-ui-redesign-faza4-srez7`):** **Фаза 4 ТЗ по редизайну, срез 7 — экран документов. ВОЛНА 1 ЗАКРЫТА.** Крупнейший файл приложения (763 строки, фактически три экрана в одном) сначала разобран переносом «как есть», затем переделан. Порядок работы был вывернут: блок настройки посреди страницы писал «Сначала выберите шаблон в блоке генерации», а сам выбор стоял ещё ниже. Теперь шаблон выбирается кликом по строке, настройка и выпуск открываются панелями. **Найден дефект: вид выпускаемого документа брался из формы создания шаблона**, а не из самого шаблона — выпуск по удостоверению мог уйти с видом «протокол». Объект выпуска теперь выбирается по названию, а не вводится идентификатором. Подробности задачи показывались как JSON — стали панелью с полями по-русски. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.268, ветка `worktree-ui-redesign-faza4-srez6`):** **Фаза 4 ТЗ по редизайну, срез 6 «хвост волны 1». Закрыт частично — два экрана из трёх.** Карточка слушателя (по ТЗ это «эталон карточки») печатала коды вместо значений: заголовок «Карточка слушателя» у всех одинаковый, строка `ID: 3f7a…`, «Связанный IAM user», а в зачислениях — «Курс (id)» и «Группа» кодами. Теперь заголовок — имя человека, справа сводка, названия курса и группы берутся из справочников, а вместо идентификатора учётки написано, открыт ли человеку вход в кабинет. Книга выдачи переехала на каркас реестра: видимых фильтров три, восемь галочек видов документов ушли под «Ещё фильтры», **коды состояний `generated`/`final`/`archived` заменены словами**, действия строки — в своей колонке. Форма закрытия группы больше не висит развёрнутой посреди реестра (путь не удалён). **⚠️ Волна 1 не закрыта: остался `/documents`** — 763 строки и фактически три экрана в одном файле, это срез 7. **⚠️ CI на GitHub выключен владельцем** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.267, ветка `worktree-ui-redesign-faza4-srez5`):** **Фаза 4 ТЗ по редизайну, срез 5 «мастера волны 1» (`TPL-004`). Закрыт.** Зачисление списком стало мастером из трёх шагов вместо четырёх блоков, открытых разом. **Главное: отчёт перестал терять людей** — итог считался от ответа сервера, а на сервер уходили только валидные строки, поэтому отсеянные проверкой пропадали из отчёта молча. Теперь итог считается от файла, отказы показываются **поимённо с причиной**, «уже был зачислен» отнесён к успеху. Форма создания группы перестала называться мастером и получила подсказки под полями. В пакет добавлены шаги мастера и общий блок итога операции. Попутно вскрыто: цвет статусов брался из переменных, которых в палитре нет. **⚠️ Волна 1 не закрыта:** карточка слушателя, `/documents` и журнал выдачи ждут своих шаблонов — это срез 6. **⚠️ CI на GitHub выключен владельцем до восстановления лимитов** — вердикт даёт локальный `pnpm ci:check`.

**2026-08-13 (§5.266, ветка `worktree-ui-redesign-faza4-srez4`):** **Фаза 4 ТЗ по редизайну, срез 4 «сборка настроек» (`IA-018`). Закрыт.** Раздел «Настройки и система» насчитывал **14 пунктов меню** — нужную настройку искали перебором. Теперь `/settings` — единый вход: оглавление плитками, у каждой подсказка «что здесь настраивают». Три коротких блока (платёжный провайдер, адреса для копий писем, площадка вебинаров) встроены секциями, их прежние адреса **стали перенаправлениями на разделы** (⚠️ меняет поведение, §4.9, владелец предупреждён заранее; ссылки не ломаются). Крупные разделы остались своими экранами — ТЗ прямо запрещает сливать их в один файл. **Права разделов берутся из карты навигации**, а не выписаны заново: два списка прав на одни маршруты разошлись бы при первой правке. **Дальше:** срез 5 — волны 2–4 экранов и массовое зачисление.

**2026-08-13 (§5.265, ветка `worktree-ui-redesign-faza4-srez3`):** **Фаза 4 ТЗ по редизайну, срез 3 «редизайн экранов волны 1». Закрыт.** Главное — карточка группы: **«Закрыть группу» стало единственным первичным действием** и открывается панелью прямо в карточке. Раньше первичным было «Сгенерировать приказ», а закрытие жило на другом экране, где идентификатор группы **вписывали руками**, скопировав из адресной строки; теперь это два клика от оперативной панели. Механика закрытия переиспользована, а не продублирована. Реестр групп: вместо списка ссылок — таблица со статусом и пустой экран, который объясняет, что такое группа и предлагает создать первую. Отзыв документа переехал с самодельной модалки на общую боковую панель (восьмая такая реализация сведена к одной). **Записано владельцу:** ТЗ просит массовое «Закрыть группы», но массовой операции в API нет — чекбоксы без операции ставить не стал. **Дальше:** срез 4 — сборка настроек в один экран.

**2026-08-13 (§5.264, ветка `worktree-ui-redesign-faza4-srez2`):** **Фаза 4 ТЗ по редизайну, срез 2 «разбиение монолитов волны 1». Закрыт.** Правило `SCR-001`: перенос «как есть», диф читается как перемещение, редизайн — отдельным срезом. **`/documents` — крупнейший файл приложения (766 строк) — переехал** в `features/documents/documents-screen.tsx`; `page.tsx` стал обёрткой на 14 строк, и экран впервые попал под расширенный сторож единых состояний. Из монолита `mvp/screens.tsx` вынесены карточка слушателя и три экрана групп (§8.3, порядки 1–2), разделяемые помощники — в общий слой `mvp/screen-helpers.tsx`. **Монолит: 3019 → 2606 строк.** Поведение не менялось, все 1020 тестов прежние и зелёные. **Дальше:** срез 3 — редизайн вынесенных экранов (`/groups` → ListPage с «Закрыть группы», карточка → DetailLayout, журнал выдачи → DetailDrawer), затем `IA-018`.

**2026-08-12 (§5.263, ветка `worktree-ui-redesign-faza4-srez1`):** **Фаза 4 ТЗ по редизайну, срез 1 «сквозные правила». Закрыт.** Все **9 браузерных окон подтверждения** заменены диалогами приложения. Главное: **два из них были написаны без префикса `window`**, и поиск, которым пользовался аудит, их не находил — ровно то, о чём предупреждал трекер. Механика вынесена в общий хук, иначе одно и то же состояние пришлось бы написать на семи экранах; в хук добавлено поле ввода — оно заменило второе окно браузера в очереди переаттестации, где на одно действие открывалось ДВА окна подряд. Опасная кнопка теперь отличается цветом от обычной. **Новый сторож ловит обе формы вызова** и попутно нашёл два `alert`, которых в аудите не было вовсе (экраны слушателя, Фаза 6). Убран дубль `/admin/learners` — он показывал тот же реестр, что `/learners`; адрес сохранён редиректом. **Дальше:** срез 2 — волна 1 экранов и разбиение `/documents` (766 строк) до редизайна.

**2026-08-12 (§5.262, ветка `worktree-ui-redesign-faza3-workspace`):** **Фаза 3 ТЗ по редизайну — «Стартовый экран администратора». Закрыта.** `/workspace` перестроен с пяти блоков до трёх зон: сводка тремя числами (каждое ведёт туда, где с ним работают), одна очередь **«Разобрать»** вместо трёх таблиц и всё остальное ниже сгиба. Из трёх таблиц убрана колонка «Маршрут», где пользователю показывали **сырой адрес страницы**; теперь ссылкой служит название. «Обновить» перестало быть первичным действием — перезагрузка списка это не работа пользователя. **⚠️ Меняет поведение (§4.9, владелец предупреждён):** `/admin/cockpit` теперь перенаправляет на `/workspace` — оба экрана отвечали на один вопрос «что сейчас происходит»; виджеты кокпита переехали в нижнюю зону панели. Вычищены англицизмы: «Задачи inbox» → «Задачи», «Все severity» → «Любая критичность». Сортировка очереди по срочности вынесена в чистые функции (13 новых тестов). **Дальше:** Фаза 4 «Админские экраны волнами».

**2026-08-12 (§5.261, ветка `worktree-ui-redesign-faza2-learners`):** **Фаза 2 ТЗ по редизайну — «Эталонный реестр и карточка». Закрыта.** `/learners` стал образцом для остальных 30 экранов: строки выделяются, массовое архивирование возвращает отказы **поимённо с причиной**, колонки настраиваются, карточка слушателя открывается **панелью рядом со списком** вместо модалки поверх экрана, пустой экран объясняет что делать первым. Шесть компонентов добавлены в `@trudskill/ui` — все дополнения обратно совместимы, иначе разом сломались бы 30 экранов. Ловушка фокуса вынесена из `Modal` в общий модуль и переиспользована: две копии одной механики неизбежно разъезжаются — именно так завелись пять независимых дроверов. **Сторож единых состояний расширен с ~46 файлов до 150+** (`IA-001`) и вскрыл 9 нарушителей: один починен, восемь внесены в явную очередь с указанием фазы. Замер `MET-003`: p95 списка 15,8 мс. **Найдено и требует решения владельца:** колонка «Подразделение» показывает сырой идентификатор `ou_…`, справочника названий на фронте нет — без новой ручки API это не чинится. **Дальше:** Фаза 3 «Стартовый экран администратора» (`/workspace`: пять блоков → три).

**2026-08-12 (§5.259, ветка `claude/trusting-heyrovsky-2845b3`):** **Фаза 1 редизайна закрыта — меню администратора с 70 пунктов до 7.** После входа в сайдбаре семь пунктов своей роли вместо семидесяти в десяти раскрывающихся блоках; всё остальное под «Ещё» по тем же 10 блокам ИА и в палитре `Ctrl+K`; при первом входе однократная подсказка. **Разбиение маршрутов не тронуто** — сторож `ia-architecture` зелёный без правок, изменился только слой видимости. Главное: работа была сделана до этой фазы и просто не подключена — `getNavigationView` написана и покрыта тестом, 54 пункта размечены, не хватало одной строки. 364 строки CSS каркаса и палитры уехали из `<style jsx>` в `packages/ui/src/styles/shell.ts`, где их видят сторожа (`app-shell.tsx` 572 → 284 строки); введён токен `--ui-overlay`. **Сторож каркаса переписан с 13-строчного смоук-импорта на 8 инвариантов** — ТЗ считало, что он охраняет структуру сайдбара, а он не охранял ничего. **Найдено попыткой написать тест:** порядок ролей в `roleBlueprints` молча решал, чьё меню увидит человек с двумя ролями — администратор с ролью преподавателя получал меню преподавателя. **Вне фазы, отдельный [PR #490](https://github.com/aiprocadm/trudskill/pull/490): `pnpm lint` не работал ни в одном из 9 пакетов, а значит и весь `ci:check`** — корневой конфиг импортировал незаявленную зависимость, видимую только за счёт подъёма пакетов до pnpm 11; Actions выключены, сообщить было некому. **Дальше:** Фаза 2 «Эталонный реестр и карточка» с обязательным замером p95 (`MET-003`).

**2026-08-11 (§5.258, ветка `claude/trusting-heyrovsky-2845b3`):** **ТЗ по редизайну интерфейса внесено в репозиторий — Фаза 0 закрыта. Кода нет,** сессия документационная. Дельта-ТЗ по представлению легло в `docs/TZ_UI_REDESIGN_TRUDSKILL.md`, заведён живой трекер `docs/TZ_UI_REDESIGN_STATUS.md`. **Главное следствие: у репозитория теперь ДВА действующих дельта-ТЗ** — «Арендная СДО» (поведение, функции, права) и редизайн (представление); они разделены **предметом**, а не приоритетом. Поэтому фраза «продолжай по ТЗ» стала двусмысленной и получила правило маршрутизации ([DOCUMENTATION_MAP.md#tz-routing](docs/DOCUMENTATION_MAP.md#tz-routing)): по ключевым словам → по предмету задачи → **если задачи нет, спросить владельца, а не выбирать молча**. В `CLAUDE.md` записан **протокол сверки логики кода с ТЗ**: четыре класса расхождений с разным действием по каждому и явные границы (не править исторические миграции, контракты, RBAC, URL; не ослаблять сторожей; не расширять задачу молча). Журнал расхождений заполнен **11 записями** из аудита ТЗ, три переподтверждены по коду: маршрутов 102 и `navSlot:'more'` 54 — сходится; **`getNavigationView` написана, покрыта тестом и просто не подключена** (`app-shell.tsx:31` берёт старую функцию) — то есть сокращение меню с 70 пунктов до 7 это одна строка, а не новая работа; вызовов подтверждения **9, а не 7** — два написаны голым `confirm(`, и сторож по строке `window.confirm` их не поймает. **Шесть развилок владельца открыты, но работу не блокируют** (действует решение по умолчанию); отдельно нужен реальный почтовый домен взамен `no-reply@cdoprof.local`. **Дальше:** Фаза 1 «Навигация и оболочка» — план + апрув перед кодом.

**2026-08-08 (§5.253, ветка `feat/2026-08-08-faza6-task3-4-backups`):** **Фаза 6 Task 3+4 — диск, логи, бэкапы. Закрыты. УЧЕНИЯ ВОССТАНОВЛЕНИЯ ПРОВЕДЕНЫ** — это ключевой результат фазы по ТЗ. **Первый прогон учений провалился и вскрыл настоящую беду: копия не восстанавливалась на чистом сервере** (в дампе записаны владельцы объектов, на новой машине таких ролей нет — psql обрывается на первой строке). Исправлено `--no-owner --no-privileges`; после правки учения пройдены на живой копии базы стенда: 6 схем, 150 таблиц, 80 миграций, **RTO 8 секунд**. Боевая база не затрагивается (одноразовый контейнер), поэтому блокер «нужно согласие на учения», висевший с Фазы 4, снят кодом. Переписан `infra/backup.sh`: топология не зашита (`CDOPROF_PG_EXEC` — на стенде общий контейнер Postgres), запись через временный файл (раньше сбой оставлял обрезанный архив, похожий на копию), `gzip -t`, контрольная сумма, порог свободного места, отметка `last-success`. Новые `restore-drill.sh` и `backup-watchdog.sh` (проверен вживую: пустой каталог и испорченный файл → тревога и код 1). Дамп перед выкаткой в `deploy.yml`. Ротация журналов — всем **24** сервисам обоих compose (раньше не было ни у одного: диск общий с базой и MinIO). Сторож `backup-scripts.test.ts` — 17 проверок. **Осталось владельцу: поставить 3 строки в crontab** (готовы в `docs/BACKUP_ROLLBACK.md`) и решить по копии вне сервера. **Дальше:** Task 5 (метрики, readiness, heartbeat воркера).

**2026-08-08 (§5.252, ветка `feat/2026-08-08-faza6-task1-live-defects`):** **Фаза 6 Task 1 — аварийные дефекты кабинета. Закрыт. Попутно закрыта УТЕЧКА ПДн.** Главное: любой вошедший **слушатель** мог выгрузить ФИО и СНИЛС всех слушателей центра — раздел отчётов (включая конструктор с выгрузкой до 50 000 строк в XLSX) был закрыт правом `enrollments.read`, которое есть у роли `learner`. Закрыто правом `learners.read` (отчёты) и `groups.read` (сводки по группам) — по живой базе они есть ровно у manager/tenant_admin/platform_admin, и никто доступ не теряет. Четыре дефекта §5.245 починены: шторм realtime (соединение на пару «комната+токен», колбэк в ref, возобновление по курсору вместо повтора за минуту), F5-разлогин (`/auth/csrf` в bootstrap-списке), `?next=` (строгий разбор), пустой кабинет (`GET /me/enrollments` с `courseId` + отбор зачислений переведён в **fail-closed**: раньше пользователь без привязки к карточке видел все зачисления центра). **Адверсарная рецензия нашла три блокера в моей же работе:** защита от открытого редиректа обходилась через `/..//evil.com`, 38 «злых» тестов были зелёными на дырявом коде, а настоящая форма входа новый модуль вообще не использовала. Исправлены и приёмочные тесты, проверявшие слушателя на выдуманном наборе прав. Кодовая часть Task 2: таймауты джоб CI, contracts-проверки, снят четырёхкратный перепрогон (**Actions в репозитории всё ещё выключены — шаг владельца**). Тесты: бэкенд **3000**, фронт **959**, ci:check зелёный, миграций нет. **Дальше:** Task 3 (диск и логи) и Task 4 (бэкапы и учения восстановления).

**2026-08-08 (§5.251, ветка `docs/2026-08-08-faza6-plan`):** **План Фазы 6 «Эксплуатация» написан — ждёт апрува владельца.** Кода нет по правилу ТЗ §13/§15. Файл `docs/superpowers/plans/2026-08-08-tz-faza6-ekspluataciya.md`, 11 задач. Разведка (8 параллельных агентов + адверсарная перепроверка + критик полноты) вскрыла три вещи, которых не было ни в одной строке трекера: **бэкапов не существует** (в cron только автообновление стендов; `infra/backup.sh` написан под прод-compose, которого на стенде нет → фактический RPO = ∞); **GitHub Actions выключен в репозитории** с 27.05.2026, 2,5 месяца PR проверял только локальный `ci:check`; **`/metrics` завёрнут в конверт API** — Prometheus его не прочитает, да и собирать некому. Трекер при этом ЗАНИЖАЛ по очередям: backoff, DLQ и poison-сообщения в воркере уже написаны — но карантин никто не читает, «Повторить» у задачи документа не публикует сообщение, а экран здоровья читает вечно пустые таблицы. Все 4 дефекта §5.245 живы и хуже записанного; сверх них найден **fail-open отбора зачислений** (пользователь без привязки к карточке слушателя видит все зачисления центра). **Ключевое решение плана: «restore-drill проведён» закрывается кодом** — восстановлением дампа в одноразовую БД, что снимает блокер с Фазы 4. Вопросы владельцу: №A Actions, №B канал алертов, №C offsite, №D RPO/RTO, №E ЕСИА, №F Sentry (предлагаю не брать). **Дальше:** апрув → Task 1 (аварийные дефекты кабинета).

**2026-08-08 (§5.250, ветка `feat/2026-08-08-faza5-task9-webinar-attendance`):** **Фаза 5 «UX и порталы» ЗАКРЫТА ЦЕЛИКОМ — Task 9 (вебинары, ФТ-F4) + завершение фазы.** Отметка посещения `POST /webinars/:id/join` под `webinars.attend`: сначала отметка, потом открытие комнаты (закрытая вкладка не съедает посещение); идемпотентно, не участнику 404; точный `durationSeconds` от вебхука не затирается. **Посещённые вебинары — в журнале часов группы** (колонка на экране и в CSV; в формуле складываются с материалами, в отличие от видео-максимума); неотмеченный приглашённый часов не даёт — ровно acceptance. **Закрытие фазы:** ci:check 9/9 (первый прогон поймал 2 lint-ошибки — исправлены), security 24 / migrations 51 / isolation 31 зелёные; критерий ЭПИКа H выполнен (10 блоков сторожем, роли на своих дашбордах, кабинет 360px вживую); трекер добит (H5, F4 → ✅, фаза → закрыта); миграций в фазе не было (`0079`). Вне фазы: дефекты §5.245 (шторм realtime, пустой кабинет, F5-разлогин, ?next), дашборд заказчика (№B), Telegram-бот → Фаза 6 (№A). **Дальше:** план Фазы 6 «Эксплуатация» + судьба внеочередных дефектов.

**2026-08-07 (§5.249, ветка `feat/2026-08-07-faza5-task8-ia-audit`):** **Фаза 5, Task 8 — сверка информационной архитектуры (ФТ-H1). Закрыт.** Маршрутов **100**, блоков **10** (согласованы владельцем в Фазе 2; «8–9» из ТЗ — уточнено). Сверка закреплена сторожевым тестом `ia-architecture.e2e.test.ts` (7 инвариантов), а не протухающим документом. Найдено и исправлено: **2 недостижимые страницы** (`/forms`, `/module-empty` вне карты доступа — класс бага `/learner` из Task 1), 3 публичные страницы вне карты (`/verify`, `/exam-auth`, `/tenant-not-found`), блок «Моё обучение» ссылался на `/learner` без пункта меню (добавлен «Мой кабинет» — это же объясняет странный клик по заголовку блока из Task 4). Заглушки `/mailings`/`/crm/deals`/`/forms` скрыты из меню осознанно (прошлое решение уважено) — легализованы явным списком `HIDDEN_STUB_ROUTES` вместо молчаливого сиротства. **URL не менялись.** Тесты: фронт **896** (+7). **Дальше:** Task 9 — вебинары, фиксация посещения (ФТ-F4) — финал фазы.

**2026-08-07 (§5.248, ветка `feat/2026-08-07-faza5-task7-close-group-chain`):** **Фаза 5, Task 7 — экзамен → протокол → документы → реестр одной цепочкой (ФТ-E3). Закрыт.** `POST /groups/:id/close-chain` (`documents.generate` + `regulatory.export.write` ОБА сразу) → `CloseGroupChainService` — оркестровка поверх готовых шагов без их переписывания: готовность (ФТ-E3.2) → отбор сдавших (клиент состав НЕ передаёт) → `closeGroup` (протокол+удостоверения) → выгрузка реестра. **Частичный успех:** отсев поимённо с причиной (не сдал/нет результата/не завершил/СНИЛС); «всё или ничего» — только у проблем уровня группы (комиссия/программа), протокол один на всех. **Идемпотентность двухслойная:** ключ цепочки (повтор → прежний отчёт `cached:true`, без второй выгрузки; коллекция `closeGroupChainIdempotency` зарегистрирована в `mvp-collections`) + детерминированные ключи `closeGroup`. Отдельный контроллер (не трогаем сборку `MvpController` в доменном тесте). Кнопка и отчёт на экране закрытия группы. Известный шов: строки реестра ждут номер протокола зачисления, а файлы рождаются асинхронно — до готовности честная предполётная ошибка, как в ручной выгрузке. Тесты: +10 бэкенд (отбор+склейка), +2 HTTP-границы (114), +1 контракт фронта (889), полный бэкенд зелёный. **Дальше:** Task 8 — сверка информационной архитектуры (ФТ-H1).

**2026-08-07 (§5.247, ветка `feat/2026-08-07-faza5-task6-portal-document-download`):** **Фаза 5, Task 6 — представитель заказчика скачивает документ своего сотрудника (ФТ-E5). Закрыт.** Разрыв: список документов был под `portal.read`, скачивание — только под `documents.read` («весь центр», представителю не выдаётся). Новая ручка `GET /portal/documents/:id/download` под `portal.read` + `getPortalDocumentDownload` с проверкой владения: документ должен быть выпущен по зачислению в группу контрагента актора (тот же вывод «чей сотрудник», что и в списке). **Чужой/несуществующий/не-зачисленческий документ неразличимы — единый 404** (403 подтвердил бы существование); свой без файла — 404 `document_file_missing`; персонал без привязки не ограничен. Оговорка: `/files/:id/download` — общесистемная заглушка для всех ролей до подключения отдачи PDF (отдельный известный остаток). Тесты: +6 анти-IDOR в изоляционном файле (19), +2 HTTP-границы (112), полный бэкенд зелёный, миграций и новых прав нет. **Дальше:** Task 7 — экзамен → протокол → реестр одной цепочкой (ФТ-E3).

**2026-08-07 (§5.246, ветка `feat/2026-08-07-faza5-task5-unified-states`):** **Фаза 5, Task 5 — единые состояния (ФТ-H3). Закрыт.** Чинить оказалось нечего: `learners-list-screen.tsx` из трекера УЖЕ переведён в Фазе 4 (PR #303, `ListPage`→`AsyncSection`) — замер разведки не считал `ListPage` за обёртку; витрина `ui-kit` — исключение по смыслу. Свежий скан: 46 файлов-экранов, нарушителей 0. Поставлен сторожевой тест `unified-states.e2e.test.ts`: экран с данными без общих обёрток валит сборку с именем файла (проверено подложным нарушителем); витрина — явное исключение списком; сторож самого сканера — экранов должно быть >40. Тесты: фронт **888** (+2). **Дальше:** Task 6 — представитель заказчика скачивает документ сотрудника (ФТ-E5); дефекты §5.245 (шторм realtime, пустой кабинет) ждут внеочередных задач.

**2026-08-06 (§5.245, ветка `feat/2026-08-06-faza5-task4-cabinet-360px`):** **Фаза 5, Task 4 — кабинет слушателя на 360px (ФТ-H4). Закрыт.** Телефонный брейкпоинт `@media (max-width: 480px)` (раньше самый узкий — 768px): контролы 40→44px (решение владельца №C), `DataTable` превращается в карточки (подписи ячеек — `data-label` из заголовка колонки), пункты оглавления курса и бокового меню — 44px, у шапки каркаса снята фиксированная высота 64px (наезжала на контент), крошки переносятся (уезжали за 381px). Правило «новый экран проверяется на 360px» закреплено в `CLAUDE.md`; сторожевые тесты в пакете `ui`. **Приёмка вживую** на прод-сборке через прокси к бэкенду стенда: все 4 экрана кабинета (дом, плеер, тест-раннер, документы) — `scrollWidth 360`, тач-зоны ≥44px. **Живой прогон вскрыл 4 дефекта НЕ вёрстки** (см. §5.245): шторм переподписок realtime с каждой открытой вкладки (бьёт по стенду прямо сейчас), кабинет слушателя пуст у привязанных слушателей (фильтр по IAM id вместо реестрового + у зачисления нет `courseId` — лечить через `/me/enrollments`), F5 разлогинивает (`GET /auth/csrf` заперт `TenantGuard`), вход игнорирует `?next=`. Тесты: ui **78** (+8), фронт **886**, typecheck 13/13, `next build` зелёный. **Дальше:** Task 5 — единые состояния (ФТ-H3); дефекты 1–2 достойны внеочередных задач.

**2026-08-05 (§5.244, ветка `feat/2026-08-05-tz-faza5-task3-test-runner`):** **Фаза 5, Task 3 — тест-раннер перестаёт молча терять ответы (ФТ-H5). Закрыт.** Постоянный индикатор сохранности, предупреждение браузера при уходе со страницы с несохранённым ответом, тач-зона варианта ответа доведена до 44px. **Дефект, найденный по ходу и более важный самой задачи: автосохранение НИКОГДА не снимало пометку «изменено»** — `dirtyRef` пополнялся при каждом ответе, а очищался только в `flushDraft`, поэтому счётчик несохранённых рос до числа отвеченных вопросов и любой индикатор поверх него врал бы. Теперь пометка снимается по успеху и возвращается по неудаче; изменение во время запроса пометку сохраняет (сравнение черновика по ссылке). Состояние связи — **чистая функция** (`connection.ts`) от четырёх признаков: компонентных тестов React в проекте нет, логика внутри JSX осталась бы непроверенной. Порядок проверок = порядок опасности: отсутствие сети важнее неудачи сохранения, иначе человек решит, что дело в системе, а не в связи; «нет сети, но всё сохранено» — предупреждение, а не тревога. Индикатор виден **всегда**, включая спокойное состояние: плашка, появляющаяся только при беде, сама читается как беда. `beforeunload` вешается только при наличии несохранённого — постоянный мешает обычному выходу. Тесты: фронт **886** (+9), пакет ui +2 сторожевых на тач-зоны. **Дальше:** Task 4 — кабинет слушателя на 360px.

**2026-08-05 (§5.242, ветка `feat/2026-08-05-tz-faza5-task2-methodist-dashboard`):** **Фаза 5, Task 2 — дашборд методиста (ФТ-H2). Закрыт.** `GET /dashboards/methodist` (право `groups.read`, **новых прав и миграций нет**) + экран `/methodist` + пункт в блоке «Обзор»; домашний маршрут методиста переведён на него. Четыре раздела по убыванию срочности: просрочено → ближайшие сроки (14 дней) → ждут проверки → программы без опубликованного итогового экзамена. **Главное ограничение, вскрытое разведкой: понятия «МОЯ группа» в системе нет** — у `learning.groups` ни куратора, ни ответственного; заводить поле значит миграцию и продуктовые решения, а фаза безмиграционная, поэтому дашборд показывает «где горит по обучению» в масштабе центра (ровно то, что просит ТЗ и что методист и так видит по `groups.read`). Ничего не предпосчитывается — счётчики разъехались бы с действительностью при первом пропущенном событии; очередь проверки берётся из того же `aggregateReviewerQueue`, что и экран проверки, чтобы два числа не разошлись; просрочки не дублируются в «ближайших»; зачисление без плановой даты дедлайном не становится; тест модуля не считается итоговым экзаменом. **Главную ошибку поймал живой прогон, а не тесты: у методиста НЕТ прав `groups.read` и `enrollments.read`** — первая версия закрыла ручку правом `groups.read`, то есть от того, для кого экран написан (методист получал 403). Это расхождение ТЗ с ролевой моделью: ТЗ говорит «методист — группы и дедлайны», а в системе группами и сроками ведает `manager`, методист же отвечает за содержание. **Вторая ошибка того же прогона, опаснее первой:** промежуточный гейт по `enrollments.read` пустил бы к срокам всех групп **слушателя** — это право про СВОИ зачисления, а не про чужие. Итог: ручка под `courses.read`, разделы гейтятся по правам актора — сроки по `groups.read` (менеджер, администрация), пробелы в программах по `courses.write` (методист, администрация), очередь по `assessment.reviews.review`; без права на группы пробелы считаются по курсам **без названий групп**; если не положен ни один раздел — **403, а не пустая сводка**. Давать методисту `groups.read` не стал: расширение полномочий роли — решение владельца. **Вывод: наборы прав брать из `iam.role_permissions` живой базы, а не из названия роли и не из ТЗ** — обе ошибки от додуманных прав. **Прочие грабли:** сторожевой тест навигации поймал пункт-сироту вне 10 блоков; доменный HTTP-тест третий раз подряд потребовал регистрации нового сервиса контроллера. Тесты: бэкенд **2951** (+22), фронт **877**, ci:check exit 0. **Дальше:** Task 3 — тест-раннер перестаёт молча терять ответы.

**2026-08-05 (§5.241, ветка `feat/2026-08-05-tz-faza5-task1-role-home`):** **План Фазы 5 апрувнут владельцем. Task 1 — каждая роль приземляется на свой экран (ФТ-H2). Закрыт.** Домашний маршрут роли вынесен в **данные** (`role-home.ts`: таблица «роль → адрес», порядок = приоритет), резолвер перед перенаправлением сверяется с картой доступа тем же `evaluateRouteAccess`, что и навигация — если дом роли закрыт правами, берётся следующий, а не отказ. Раньше перенаправлялся только слушатель; `manager` и `counterparty_rep` видели пустую витрину «Роль не определена». **Главная находка — работающий баг: кабинет слушателя не открывался вовсе.** Маршрута `/learner` не было в `routeMeta`, неизвестный маршрут считается `not-found`, и `ProtectedRoute` делал `replace('/not-found')` — слушатель после входа выбрасывался из своего кабинета. Запись добавлена (право `enrollments.read`) и поставлена ПОСЛЕ всех `/learner/*`: совпадение ищется по префиксу и берётся первое, иначе она перехватила бы все внутренние страницы кабинета — закреплено тестом. **Замечено и НЕ тронуто:** записи с параметрами (`/learner/tests/[testId]/attempt/[attemptId]`) в карте доступа мёртвые — сопоставление по литералам, реальный адрес ловит более короткий префикс; сегодня безвредно, при разделении прав выстрелит, заслуживает своей задачи. Тесты: фронт **876** (+14), ci:check exit 0. **Дальше:** Task 2 — дашборд методиста.

**2026-08-05 (§5.240, ветка `feat/2026-08-05-tz-faza5-plan`):** **Фаза 4 закрыта по коду целиком (12/12). Написан план Фазы 5 «UX и порталы» — ждёт апрува владельца.** Кода в сессии нет: по протоколу ТЗ §13/§15 фаза стартует после плана и апрува. 9 задач по ЭПИКу H + остатки E, **миграций не требует**. **Главный вывод разведки: трекер расходился с кодом в пяти строках** — ФТ-F2 (СМС-шов) и ФТ-E2 (рандомизация/автосейв/серверный таймер) стояли невыполненными, а сделаны; ФТ-H3 закрыт на 56 файлов из 58; дашборды слушателя и tenant_admin — настоящие, у методиста и заказчика их нет. Строки трекера исправлены. **Главный дефект (задача №1): корневая `/` — витрина из 9 плиток-ссылок без единого запроса к серверу**, то есть точка входа уводит от уже написанных дашбордов; лечится перенаправлением на домашний маршрут роли, заданный данными. Прочее вскрытое: мобильных брейкпоинтов нет (самый узкий `@media` — 768px, ширины 360px из ТЗ не касались); тест-раннер молча теряет ответы без сети (`navigator.onLine`/`beforeunload` отсутствуют); представитель заказчика видит документ, но скачать не может; маршрутов стало 99 против «94» в ТЗ. **Вопросы владельцу: №A Telegram-бот (предлагаю в Фазу 6), №B дашборд заказчика под неиспользуемую роль, №C критерий «проверено на 360px».** ФТ-H1 (сверка архитектуры) поставлен последней задачей намеренно. **Дальше:** апрув → задача 1.

**2026-08-05 (§5.239, ветка `feat/2026-08-04-tz-faza4-152fz`):** **Фаза 4, Task 12 — контур 152-ФЗ: права субъекта персональных данных (ФТ-G6). Код закрыт;** restore-drill — за владельцем. Выгрузка `GET /learners/:id/personal-data` (машиночитаемый JSON: карточка, зачисления, экзамены, документы, идентификация, прокторинг) и обезличивание `POST .../personal-data/erasure` под ОТДЕЛЬНЫМ правом `learners.pii.manage` (`0079`, только администрации — вести карточки может методист, стирать ПДн по заявлению нет). **Обезличивание, а не удаление, и это требование закона:** документы об обучении обрабатываются по обязанности (152-ФЗ ст. 6 ч. 1 п. 2), отзыв согласия их не отменяет, а стёртое ФИО в удостоверении лишило бы самого слушателя подтверждения обучения. Ответ перечисляет и стёртое, и сохранённое с основанием. Биометрия (селфи, паспорт, чанки прокторинга) удаляется сразу, не дожидаясь 90-дневного срока; в выгрузку снимки НЕ вкладываются — только метаданные. **Дыра, вскрытая по пути:** `redaction.util.ts` редактировал секреты и контакты, а ФИО, СНИЛС, паспорт и дату рождения писал в журнал открытым текстом — исправлено. Панель «Персональные данные» в карточке слушателя скрыта без права, обезличивание требует набрать слово-подтверждение. **Грабли:** у `Enrollment` нет `courseId` (зачисление в группу, курсы на ней); `snilsHash` в состоянии не живёт — считается при записи в БД и срезается при чтении. **Дальше:** критерий приёмки эпика D — завершение Фазы 4.

**2026-08-04 (§5.238, ветка `feat/2026-08-04-tz-faza4-health`):** **Фаза 4, Task 11 — здоровье арендаторов (ФТ-D7). Закрыт.** `GET /platform/health/tenants` + секция в платформенной админке: задачи документов, задания обмена, карантин, отказы выгрузок, последняя активность; общая очередь платформы отдельно (у `outbox_events` нет привязки к арендатору). **Единственное место с намеренным чтением сквозь тенанты — и потому отдаёт ТОЛЬКО агрегаты** (два теста стерегут форму ответа и текст SQL). Очередь сама по себе не считается поломкой — тревога по фактическим отказам. Весь экран — два запроса, не N+1. **Грабля (мой недосмотр в PR #447):** `eslint-plugin-import` не поддерживает ESLint 10 и падает при ИСПРАВЛЕНИИ порядка импортов — переведено на форк `eslint-plugin-import-x`. Тесты: бэкенд **2911** (+8), фронт **862** (+8), ci:check exit 0. **Дальше:** Task 12 — ФТ-G6 контур 152-ФЗ.

**2026-08-04 (§5.237, ветка `feat/2026-08-04-tz-faza4-library`):** **Фаза 4, Task 10 — библиотека курсов платформы (ФТ-D6). Закрыт.** Каталог хранит СНИМОК курса (`0078`), а не ссылку на курс арендатора. **Главное: ссылки на файлы, видео и SCORM между центрами НЕ переносятся** — это была бы утечка чужого хранилища; такие материалы приезжают заготовками (тип → текст, «требуется загрузить материал»), число заготовок видно в каталоге ДО копирования. Занятый код курса получает суффикс — чужой курс не перезаписывается. Ручки: центру `courses.read`/`courses.write`, платформе `library.publish`. Грабли: `as never` скрывал отсутствие `sortOrder` в DTO; переиспользованный `Response` в тесте (тело читается один раз); сторожевой тест навигации поймал пункт-сироту. Живой прогон: снимок без чужого `fileId` и без идентификаторов источника, копия `LIB-TEST-2` со своим модулем и метой. Тесты: бэкенд **2903** (+17), фронт **854** (+5), ci:check exit 0. **Дальше:** Task 11 — ФТ-D7 здоровье тенантов.

**2026-08-04 (§5.236, ветка `feat/2026-08-04-tz-faza4-recert`):** **Фаза 4, Task 9 — переобучение (ФТ-E4). Закрыт.** Окна напоминаний приведены к ТЗ 60/30/7 (было 90/30/7 с Фазы 5B), горизонт скана связан с самым дальним окном тестом. **Находка:** те же окна использовал сканер ЛИЦЕНЗИИ центра — правка общей константы молча сузила бы предупреждение о самом дорогом сбое (продление лицензии занимает месяцы); заведена отдельная `LICENSE_EXPIRY_MILESTONES = [7,30,90]`, разделение закреплено тестом. Периодичность переобучения per программа теперь задаётся из мастера курса пресетами «3 года / 1 год / бессрочно» (в данных поле было и определяло validUntil, но в интерфейсе отсутствовало; в `ProgramMetaPatch` его тоже не было — значение терялось). Дашборд «истекающие удостоверения» (`GET /recertification/expiring`) рядом с очередью: просроченные идут ПЕРВЫМИ наравне с истекающими, отозванные и бессрочные исключены. Живой прогон: горизонт 60, скан проходит, слушателю 403. **Дальше:** Task 10 — ФТ-D6 библиотека курсов платформы.

**2026-08-04 (§5.235, ветка `feat/2026-08-04-tz-faza4-host-resolve`):** **Фаза 4, Task 8 — арендатор по адресу сайта (ФТ-D3.2). Код закрыт;** DNS и wildcard-сертификат — за владельцем (вопрос №5). Чистый резолвер `resolveTenantHost` (порт/регистр, `www`, локалка и IP, многоуровневые поддомены, защита от `lms.example.ru.evil.com`), `middleware.ts` кладёт код арендатора в cookie и переписывает чужой домен на `/tenant-not-found` (rewrite, а не redirect — адрес остаётся виден). Недостающее звено: публичная ручка `GET /public/tenants/by-code/:code` (rate-limit 30/мин) — из адреса приходит КОД, а вход ждёт ИДЕНТИФИКАТОР; **архивный не отдаётся вовсе**, приостановленный — со статусом. Клиент: `resolveCurrentTenantId()` с откатом к `NEXT_PUBLIC_DEFAULT_TENANT_ID`, вход идёт в центр по адресу. Пока `NEXT_PUBLIC_TENANT_BASE_DOMAIN` пуст — прежнее поведение, выкладка не ждёт DNS. Гейт изоляции отверг новый публичный контроллер до мерджа (внесён в белый список с обоснованием). Живой прогон: резолв без авторизации, 404 на несуществующий и на внедрение, 429 после 27 запросов, архивация → 404. **Дальше:** Task 9 — ФТ-E4 переобучение.

**2026-08-04 (§5.234, ветка `feat/2026-08-04-tz-faza4-onboarding`):** **Фаза 4, Task 7 — мастер онбординга центра (ФТ-D2.3). Закрыт.** Шесть шагов (реквизиты, лицензия, бренд, комиссия, шаблон, курс); **прогресс вычисляется из реальных данных, а не хранится флагами** — поэтому «продолжить незавершённый мастер» работает по построению, в том числе после смены администратора и браузера. `GET /tenant/onboarding` (право `tenant.read`) + экран `/onboarding` со ссылками на настоящие экраны (мастер не дублирует формы). **Грабля:** курсы и комиссии живут в снимке состояния MVP, а не в `learning.courses`/`learning.commissions` — первая версия считала бы ноль всегда; переписано на `MvpTenantRunner`. Сервис живёт в `MvpModule` (в `TenantModule` был бы цикл, прецедент — `TenantUsageService`). Тесты: бэкенд **2871** (+10), фронт **823** (+5), ci:check exit 0; живой прогон: после настройки бренда обычным экраном шаг закрылся сам. **Дальше:** Task 8 — ФТ-D3.2 резолв тенанта по Host.

**2026-08-04 (§5.233, ветка `feat/2026-08-04-tz-faza4-billing`):** **Фаза 4, Task 6 — биллинг аренды (ФТ-D5.1). Закрыт.** `core.rental_invoices` (0077, копейки целыми, номер уникален) + `grace_working_days` в ТАРИФЕ (настройки тенанта правит сам арендатор — там grace хранить нельзя). Шов `RentalBillingProvider`: `manual` печатает счёт HTML→Gotenberg→PDF (бланк владельца не нужен), `noop` спит, место под `yookassa`. Grace в РАБОЧИХ днях; ежедневный sweep `15 3 * * *` с advisory-локом переводит неплательщиков в `suspended` (только active/trial), каждая приостановка в аудите. Ручки счетов + секция «Счета аренды» в платформенной админке; свои счета арендатору под `tenant.usage.read`. **Грабли:** кириллица в HTTP-заголовке валит ответ (`ERR_INVALID_CHAR`) — имя файла ушло в тело + RFC 5987; `toLocaleString` в денежном документе зависит от сборки ICU — группировка вручную. Тесты: бэкенд **2861** (+31), фронт **818** (+5), ci:check exit 0, живой прогон с настоящим PDF пройден. **Дальше:** Task 7 — ФТ-D2.3 мастер онбординга.

**2026-08-04 (§5.232, ветка `feat/2026-08-04-tz-faza4-tariffs`):** **Фаза 4, Task 5 — тарифы и лимиты (ФТ-D4). Закрыт.** `core.plans` + `core.tenant_subscriptions` (0076, одна активная подписка на тенанта), счётчики — запросами по типизированным таблицам (UsageCounter-таблицы нет намеренно). Ручки: `/platform/plans` + назначение (под platform.tenants.\*), `GET /tenant/usage` (право tenant.usage.read). Экран `/admin/usage`: прогресс-бары, предупреждения 80/95%, текст мягкой деградации. Гейт `assertCanAddLearners` на POST /learners и bulk-import: при исчерпании 409, идущие группы не трогаются. Лимит хранилища тарифа главнее ручного ключа ФТ-B1.3. Грабли: pg bigint приходит строкой (нормализовано); домен-тест с настоящим MvpController требует заглушку новой зависимости. Тесты: бэкенд **2830** (+19), фронт **813** (+13), ci:check exit 0, живой прогон пройден. **Дальше:** Task 6 — ФТ-D5.1 биллинг «счёт+акт».

**2026-08-03 (§5.231, ветка `feat/2026-08-03-tz-faza4-whitelabel`):** **Фаза 4, Task 4 — white-label (ФТ-D3.1). Закрыт** (D3.2 поддомены — отдельный Task 8). Бренд (`displayName`/`logoUrl`/`brandColor`/`accentColor`) в `tenant_settings.payload.branding`: чтение терпимое (мусор → дефолт, вёрстка не ломается), запись строгая (`PUT /tenant/branding`, право `tenant.branding.configure`, миграция `0075`). Тема поверх токенов ui через обёртку с CSS-переменными (пакет ui бренд-нейтрален), ховер — затемнением; шапка с логотипом и именем; секция «Бренд центра» в `/settings`. Письма: подпись `{{tenantName}}` (диспетчер всегда подставляет: бренд → тенант → «учебный центр»). Публичная проверка: `issuerName/LogoUrl/BrandColor` (issuerName — не ПДн, публичен по ТЗ; изъятый документ центра не называет). Грабля: `tenant_settings.id` — PK без default, INSERT обязан писать id. Тесты: бэкенд **2811** (+20), фронт **808** (+8), ci:check exit 0, живой прогон PUT/GET/403 пройден. **Дальше:** Task 5 — ФТ-D4 тарифы и лимиты.

**2026-08-03 (§5.230, ветка `feat/2026-08-03-tz-faza4-platform-admin-ui`):** **Фаза 4, Task 3, срез 3 — экраны платформенной админки (ФТ-D2.2). Task 3 закрыт целиком.** Экран `/platform/tenants` под `platform.tenants.read` (пункт «Арендаторы платформы» в «Настройки и система», у админов центров скрыт): список/создание/переводы статусов + «Войти от имени». Impersonate-ручка теперь ставит refresh+csrf cookie как `/auth/login` и не отдаёт `refreshToken` в теле — иначе сессия «от имени» из UI жила бы 15 минут. Переключение сессии: hydrate цели с ЯВНЫМ `x-tenant-id` (дефолтный заголовок → `tenant_header_mismatch`), `adoptSession`, редирект на `/`; возврат — обычный выход/вход. Живой прогон пройден (cookie стоят, токен цели работает в её кабинете). Тесты: бэкенд **2791** (+2), фронт **800** (+8), `ci:check` exit 0. **Дальше:** Task 4 — ФТ-D3.1 white-label.

**2026-07-31 (§5.229, ветка `feat/2026-07-31-tz-faza4-impersonation`):** **Фаза 4, Task 3, срез 2 — вход «от имени» с аудитом до выдачи сессии (ФТ-D2.2).** `POST /platform/tenants/:id/impersonate` под ОТДЕЛЬНЫМ правом `platform.impersonate` (`0074`) — `platform.tenants.write` его не включает. Ключевой инвариант: аудит `writeCritical` пишется в журнал целевого тенанта ДО выдачи сессии — сбой журнала отменяет вход (лучше след без входа, чем вход без следа; закреплено тестом порядка). `AuthService.issueImpersonatedSession`: обычная сессия цели, TOTP цели сознательно не проходится (актор уже прошёл свой 2FA), блокировка не обходится. Цель по умолчанию — активный tenant_admin; архивный тенант — отказ. Тесты: +13. **Остаток Task 3:** срез 3 — экраны платформенной админки. **Дальше:** срез 3.

**2026-07-31 (§5.228, ветка `feat/2026-07-31-tz-faza4-platform-admin`):** **Фаза 4, Task 3, срез 1 — платформенная админка тенантов: API (ФТ-D2.2).** Новый модуль `platform`: кросс-тенантный список, создание (по умолчанию `trial`), смена статуса — под правами `platform.tenants.read|write` (миграция `0073`), выданными ТОЛЬКО `platform_admin`: раздать их tenant_admin значило бы дать каждому арендатору админку всех остальных (граница закреплена HTTP-тестом: полный набор «своих» прав арендатора → 403). Создание тенанта — один атомарный SQL: тенант + клон ролей тенанта платформы (БЕЗ `platform_admin`) + их права; сбой между запросами не оставит тенант без ролей. Аудит `writeCritical` на создание и смену статуса (old/new). Тесты: +16 (сервис 6, DTO 5, миграция 3, HTTP-граница 2). **Остаток Task 3:** срез 2 — вход «от имени» с обязательным аудитом; срез 3 — экраны. **Дальше:** срез 2 (impersonation).

**2026-07-31 (§5.227, ветка `feat/2026-07-31-tz-faza4-tenants-db-only`):** **Фаза 4, Task 2 — единственный источник тенантов — БД (ФТ-D2.1).** In-memory фоллбек `tenant_demo` из `TenantService` убран целиком (включая демо-состав комиссии): без БД — честная 503 `tenant_store_unavailable`, а не подмена арендатора демо-данными; в живом приложении `DatabaseService` есть всегда, фоллбек работал только в собранных руками юнит-тестах. Статусы расширены до `trial|active|suspended|archived` (миграция `0072`: CHECK + нормализация мусора в `suspended` — fail-closed, ДО добавления ограничения). `listActiveTenantIds` включает `trial` (пробный центр учится и получает напоминания). Тесты: `tenant.service.test.ts` 1→9, `migrations.0072.test.ts` 4, `test:migrations` 51. **Дальше:** Task 3 — ФТ-D2.2, платформенная админка тенантов.

**2026-07-31 (§5.226, ветка `feat/2026-07-31-tz-faza4-portal-screen`):** **Фаза 4, Task 1, срез 3 — экран портала заказчика под правом `portal.read` + документы представителя (ФТ-E5).** Три ручки `/portal/{learners,groups,documents}` под отдельным правом `portal.read` — старые ручки требуют прав «по всему центру» и представителю не подходят; персонал без привязки видит всё (менеджер разбирает обращение клиента). `MvpService.listPortalDocuments`: документы = зачисления в группы контрагента; проекция без `variablesSnapshot` (ПДн), отзыв не скрывается. Грабля: хранилище документов пагинирует по умолчанию 20 строками — скоуп фильтрует ДО пагинации (тест: 25 чужих документов не вытесняют один свой). Экран переписан: «Мои сотрудники / Группы обучения / Документы», маршрут и меню — `portal.read`. Тесты: isolation 13 (+4), HTTP integration 99 (+2), фронт +3. **Остаток Task 1 (сознательно):** проверка владения при скачивании — вместе с реальной отдачей файла. **Дальше:** Task 2 — ФТ-D2.1, тенанты только из БД.

**2026-07-28 (§5.204, ветка `feat/2026-07-28-tz-faza3-task3-simple-signature`):** **Фаза 3, Task 3 — ПЭП: соглашение об электронном взаимодействии (ФТ-C1.1).** **НАХОДКА, критичная для Task 9:** таблица `esign.legal_log_entries` существует с `0004` и защищена триггером append-only, но `EsignService` пишет **только в память** — журнал, на котором держится юридическая значимость, не переживает перезапуск. ПЭП-события теперь пишутся напрямую в эту таблицу (второго журнала не заводим); приведение остальных событий esign к durable-записи — отдельная задача, НЕ сделана. Доказательство = хэш текста + кто/когда + IP/user-agent; хэш, а не ссылка на «соглашение тенанта», потому что текст редактируется. Новая версия создаётся только при реальном изменении текста (нормализация CRLF из Word), иначе все слушатели принимали бы заново при неизменном содержании. Повторное принятие идемпотентно и не сдвигает момент подписи. На живой базе проверено, что UPDATE записи журнала отвергается триггером. **Не сделано:** экран принятия при первом входе — клиент API готов, вклинивание в поток входа переезжает в Task 4. Тесты: +28 бэкенд (**2403**), +4 фронт (**778**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 4 — UX модерации, экран принятия и отложенный `require_photo_before_exam`.

**2026-07-28 (текущее, §5.203, ветка `feat/2026-07-28-tz-faza3-task2-policy-gates`):** **Фаза 3, Task 2 — гейты читают политику идентификации (ФТ-C1).** Требование подтверждения личности теперь включается уровнем политики, а не только галочкой на связке группа-курс: уровень 2 → нужен документ, уровень 3 → плюс одноразовый код. Центр задаёт уровень один раз, забытая группа больше не дыра. **Включается «либо политикой, либо флагом» — не замена:** флаги остались на идущих группах, и выключение политики не открывает то, что явно ужесточено. Политика разрешается в контроллере и передаётся внутрь (`startAttempt` синхронный, политика в таблице `0066`); без неё работают прежние флаги — обратная совместимость. Промежуточные тесты модулей не гейтятся даже на уровне 3. **Честно не сделано:** `require_photo_before_exam` пока не применяется — «фото перед экзаменом» требует захвата снимка на попытку, такого потока нет; переезжает в Task 4 вместе с UX подачи фото. Попутно подтвердилась грабля из CLAUDE.md: HTTP-интеграционный тест поднимает контроллер своим модулем и упал на новой зависимости. Тесты: +7 бэкенд (**2375**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 3 — ПЭП: соглашение и подписанные действия.

**2026-07-28 (текущее, §5.202, ветка `feat/2026-07-28-tz-faza3-task1-identity-policy`):** **Фаза 3 «Идентификация» стартовала — Task 1: политика идентификации (ФТ-C1).** План фазы апрувнут (§5.201, PR #346). Было: три гейта перед итоговым тестом уже стоят, но включаются флагами на связке группа-курс — центр не может задать «у нас везде уровень 2», и любая забытая группа это дыра. Стало: политика как ДАННЫЕ — уровень 0–3 с областью действия (тенант / направление / курс), миграция `0066`, чистое вычисление, эндпоинты под правом `identity.configure` (только администрация центра, не методист). **Побеждает самая узкая запись, это не максимум уровней:** ослабление на конкретном курсе применяется, иначе настройка курса не имела бы смысла. **Мусорный уровень опускается до 0, а не поднимается до 3** — поднять значило бы молча запретить экзамен всей группе из-за опечатки. Тонкость, зафиксированная в коде: уникальность через `coalesce(scope_id, '')`, потому что в SQL `NULL <> NULL` и обычный UNIQUE пропустил бы две тенантские политики. Ограничения проверены на живой базе. Тесты: +31 бэкенд (**2368**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 2 — гейты читают политику вместо флагов; флаги остаются ужесточением, чтобы не ослабить идущие группы.

**2026-07-28 (текущее, §5.201, ветка `feat/2026-07-28-tz-faza3-plan`):** **Фаза 2 «Видео и часы» закрыта по коду (все 12 задач, §5.188–§5.200). Написан план Фазы 3 «Идентификация» — ждёт апрува владельца.** 10 задач по ЭПИКу C + ФТ-E3. **Вопрос №4 (СМС на экзамен) расшит: блокирует только Task 5** — одноразовый токен и гейт уже работают и доставляются по email, СМС это второй транспорт того же токена; рекомендация — шов `SmsProvider` со спящим `noop`, оператора подключить в Фазе 4 вместе с биллингом. **Главный вывод разведки: кирпичи есть, политики нет** — три гейта уже стоят перед итоговым тестом, но включаются флагами группы-курса, а не уровнем политики тенанта; ФТ-C1 собирает их в политику 0–3, а не пишет заново. Ещё вскрыто: согласие сейчас ОДНО на всё (ФТ-C3.2 требует разделить ПДн и фото), выгрузка в реестр собирается без проверки СНИЛС. **Дальше:** апрув → Task 1 (политика, миграция `0066`). Не закрыто параллельно: приёмка Фазы 2 на живом аккаунте видеосервиса; хвост Фазы 1 — **A3.6** (бланки от владельца).

**2026-07-28 (текущее, §5.200, ветка `feat/2026-07-28-tz-faza2-task11b-course-wizard`):** **Фаза 2, Task 11b — единый мастер создания курса (ФТ-E1). ВСЕ 12 ЗАДАЧ ФАЗЫ 2 ВЫПОЛНЕНЫ ПО КОДУ.** Разведка вскрыла, что «мастер» существовал только на картинке: степпер рисовался, но все поля лежали на одной форме и создавалась ТОЛЬКО карточка курса. Стало: пять реальных шагов (карточка → программа и часы → модули и материалы → правила → проверка), черновик в `localStorage` переживает закрытую вкладку и чистится только после успешного создания. Вся логика — чистые функции, React-обёртка тонкая (RTL в проекте нет, иначе мастер был бы непроверяемым). Проверки только по текущему шагу; назад вернуться можно всегда, вперёд перепрыгнуть — нет. Отдельная проверка ловит строгий порядок модулей без обязательных материалов — такой курс не открылся бы дальше первого модуля. Порядок вызовов API (курс → версия → метаданные → модули → материалы) вынесен в план и покрыт тестом. Тесты: +29 фронт (**774**), бэкенд **2337**; `ci:check` exit 0, isolation 12. **Осталась приёмка эпика на живом аккаунте видеосервиса. Дальше по дорожной карте — Фаза 3 «Идентификация»; её блокер — открытый вопрос №4 (СМС-код на экзамен и провайдер).**

**2026-07-28 (текущее, §5.199, ветка `feat/2026-07-28-tz-faza2-task10-video-provider`):** **Фаза 2, Task 10 — адаптер готового видеосервиса (ФТ-B1.2). Открытый вопрос №1 ЗАКРЫТ решением владельца: «берём готовый видеосервис».** Реализован адаптер Kinescope (первым назван в ТЗ §4, российский хостинг — важно для 152-ФЗ) + публичный вебхук `media.update.status`. Ветка self-hosted не писалась и остаётся возможной без переделок — ради этого шов и заводился. Секреты только из env, в БД по-прежнему несекретная конфигурация; нет токена → адаптер спит. **Без секрета вебхука события не принимаются вообще** — иначе кто угодно объявит чужое видео готовым. Тенант берётся ИЗ НАЙДЕННОГО ассета по `provider_asset_id`, а не из тела вебхука. Гейт изоляции сработал как задумано: поймал новый публичный контроллер, внесён в белый список с обоснованием. **Для внедрения:** чтение ответов API намеренно терпимо к именам полей (`PLAYBACK_URL_FIELDS` / `DURATION_FIELDS`) — их надо подтвердить на живом аккаунте, учётной записи для прогона не было. Тесты: +25 бэкенд (**2337**), фронт **745**; `ci:check` exit 0, isolation 12, security 24. **Состояние Фазы 2: сделаны Tasks 1–10, 11 (частично), 12. Осталось: Task 11b — мастер создания курса; приёмка эпика — на живом аккаунте провайдера.**

**2026-07-28 (текущее, §5.198, ветка `feat/2026-07-28-tz-faza2-task12-testing`):** **Фаза 2, Task 12 — тестирование: рандомизация, таймер, автосейв (ФТ-E2).** План велел сначала ПРОВЕРИТЬ — и снова оправдалось. **Рандомизация вопросов была фиктивной:** в двух местах стоял `sort(() => Math.random() - 0.5)`; такой компаратор несогласован, перестановка неравновероятна, в V8 вопросы массово остаются вблизи исходных мест — центры включали рандомизацию, а слушатели получали похожий порядок. Заменено Фишером–Йетсом, равномерность доказана статистически (12 000 прогонов, ±20% по каждой позиции). **Серверный таймер срабатывал лениво** — только когда клиент трогал попытку; закрыл вкладку → попытка навсегда `in_progress`, занимает лимит и висит в отчётах. Добавлен сканер + планировщик раз в 5 минут с advisory-локом. Автосейв уже работал — не трогали. Починены два предупреждения `react-hooks` — не косметика: «правильные» зависимости сбрасывали бы таймер автосохранения на каждом ререндере. Тесты: +15 бэкенд (**2313**), фронт **745**; `ci:check` exit 0, isolation 12, security 24. **Состояние Фазы 2: сделаны Tasks 1–9, 11 (частично), 12. Осталось: Task 10 — ЖДЁТ РЕШЕНИЯ ВЛАДЕЛЬЦА по вопросу №1; Task 11b — мастер создания курса.**

**2026-07-28 (текущее, §5.197, ветка `feat/2026-07-28-tz-faza2-task11-course-rules`):** **Фаза 2, Task 11 ЧАСТИЧНО — правила прохождения курса (ФТ-E1) сделаны, единый мастер курса НЕ сделан.** Правило: флаг `sequentialModules` на версии курса — материал модуля недоступен, пока не закрыты ОБЯЗАТЕЛЬНЫЕ материалы предыдущих; выключен по умолчанию, чтобы включение задним числом не заперло идущие группы. Проверка живёт в `VideoAccessService`, через который уже проходят и видео (Task 4), и документы (Task 9) — одна проверка на оба типа материала. Необязательные материалы не запирают: справочную методичку добавляют не для блокировки курса. Замок в интерфейсе — подсказка, настоящий запрет серверный, тест бьёт запросом мимо UI. **Остаток Task 11 (мастер создания курса с черновиком между шагами) вынесен как Task 11b** — самостоятельная UI-работа объёмом с под-PR. Тесты: +6 бэкенд (**2298**), +4 фронт (**745**); `ci:check` exit 0, isolation 12. **Дальше:** Task 12 (ФТ-E2) и Task 11b — обе без блокеров; **Task 10 ждёт решения владельца по вопросу №1** (провайдер vs self-hosted).

**2026-07-28 (текущее, §5.196, ветка `feat/2026-07-28-tz-faza2-task9-pdf-scorm`):** **Фаза 2, Task 9 — PDF и SCORM (ФТ-B4.1/B4.2). ЭПИК B закрыт, кроме Task 10 (транскодирование).** План требовал сначала ПРОВЕРИТЬ SCORM, а не писать код, — оправдалось дважды. **SCORM:** мост подтверждён (статус в трекере был занижен), но проверка вскрыла, что тестами покрыта была только половина цепочки — «завершение → `materialProgress`» проверялось, а дальше до прогресса МОДУЛЯ и КУРСА никогда; написаны три теста сквозного пути. Второго моста не писали. **PDF:** оказался больше, чем «фиксировать открытие» — `PdfViewer` всегда получал `pdfUrl={null}`, файл не отдавался вообще (как видео до Task 4). Теперь один запрос отдаёт ссылку по зачислению и записывает открытие; два запроса дали бы возможность получить ссылку, не отметившись. Правило зачёта: нет `minViewSeconds` → открытие и есть ознакомление; время задано явно → фиксируем, но не закрываем, иначе молча отменили бы настройку методиста. Тесты: +9 бэкенд (**2292**), +2 фронт (**741**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 10 — **единственная задача фазы, которой нужен ответ на вопрос №1** (провайдер vs self-hosted); Tasks 11–12 (ФТ-E1/E2) от него не зависят.

**2026-07-28 (текущее, §5.195, ветка `feat/2026-07-28-tz-faza2-task8-hours-journal`):** **Фаза 2, Task 8 — журнал учебной активности и часов (ФТ-B3.4). ФТ-B3 закрыт целиком.** Доказательная база на проверке ГИТ/Минтруда: инспектор спрашивает не «стоит ли галочка “пройдено”», а «докажите, что 40-часовая программа освоена». Журнал даёт фактическое время против плановых часов по каждому слушателю, недобравшие — первыми, плюс выгрузка CSV (на проверке просят файл, а не экран). **Академический час считается как 45 минут, а не 60** — иначе отчёт занижал бы выполнение программы примерно на четверть против самого центра. **Видео-время не складывается с временем материалов** (видео-урок и есть материал) — берём максимум, иначе двойной счёт, та же ловушка, что с лимитом хранилища в Task 3. Время тестов — из попыток, битые даты дают ноль. CSV в формате книги выдачи: BOM + `;`, `;` в ФИО экранируется. Тесты: +19 бэкенд (**2283**), +4 фронт (**739**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 9 — PDF и SCORM (ФТ-B4); по плану сначала ПРОВЕРИТЬ SCORM — мост в общий прогресс уже есть, статус в трекере занижен.

**2026-07-28 (текущее, §5.194, ветка `feat/2026-07-28-tz-faza2-task7-no-seek`):** **Фаза 2, Task 7 — антиперемотка per курс (ФТ-B3.2).** Флаг `noSeekOnFirstView` на версии курса, выключен по умолчанию. **Правило серверное, и это принципиально:** запрет в интерфейсе снимается через DevTools за минуту, поэтому зачёт даёт сервер, а клиентская блокировка — лишь удобство; отдельный тест бьёт запросом мимо интерфейса. Отсечение опирается на устройство `video.played`: непрерывный просмотр продолжает существующий отрезок, а прыжок создаёт новый далеко впереди — его и выбрасываем (допуск 60 с переживает несколько потерянных heartbeat'ов). Перемотка назад разрешена всегда, после зачёта — свободна и вперёд. **Найден и исправлен собственный баг Task 6:** максимум досмотренного считался по заявленной клиентом позиции и первый же heartbeat её обнулял; теперь берётся от правого края зачтённых отрезков — вскрыл тест на перемотку назад. Попутно закрыт пробел Task 6: порог зачёта `videoCompletionPercent` не имел ни DTO, ни интерфейса — теперь оба поля правятся в карточке курса. Тесты: +12 бэкенд (**2264**), +1 фронт (**735**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 8 — журнал учебной активности и часов (ФТ-B3.4).

**2026-07-28 (текущее, §5.193, ветка `feat/2026-07-28-tz-faza2-task6-video-progress`):** **Фаза 2, Task 6 — прогресс по реальному воспроизведению и возобновление (ФТ-B3.1/B3.3). Закрыта содержательная дыра фазы.** Было: прогресс считал время на ОТКРЫТОЙ ВКЛАДКЕ — урок засчитывался тому, кто открыл его и ушёл пить чай, то есть весь учёт нормативного обучения был фиктивным. Стало: клиент раз в 12 с шлёт позицию и `video.played` (что браузер сам отметил проигранным), сервер объединяет отрезки и считает покрытие ролика; **перемотанное, но не просмотренное, не засчитывается** (минута + прыжок в конец десятиминутного ролика = 20%, а не «пройдено»). Порог 90% по умолчанию, настраивается на версии курса. Зачёт отдаётся существующему `upsertMaterialProgress` — второй источник правды развалил бы расчёт модулей. Без известной длительности зачёта нет. Повторный и запоздавший heartbeat не откатывают покрытие; `max_position_seconds` растёт через `greatest(...)` — на нём будет держаться антиперемотка. Возобновление: `playback` отдаёт `lastPositionSeconds`, плеер стартует оттуда. Миграция `0065` проверена на живой базе. Тесты: +37 бэкенд (**2252**), +4 фронт (**734**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 7 — антиперемотка per курс (ФТ-B3.2).

**2026-07-28 (текущее, §5.192, ветка `feat/2026-07-28-tz-faza2-task5-watermark`):** **Фаза 2, Task 5 — динамический водяной знак (ФТ-B2.2). ФТ-B2 закрыт целиком.** В Task 4 зафиксировано, что запретить скачивание нельзя; работающая мера против перепродажи — сделать запись **именной**. Стало: полупрозрачная надпись «ФИО · e-mail» поверх видео, меняющая положение раз в 25 секунд. Знак обязан двигаться — статичный заклеивают одним прямоугольником при перезаписи экрана, поэтому соседние точки маршрута разнесены не меньше чем на 30 процентных пунктов (проверяется тестом). Позиции не залезают в нижние 20% (панель плеера) и не выходят за правый край. Расчётная часть отделена от React-обёртки ради тестируемости — RTL в проекте нет. Тесты: +10 фронт (**730**); `ci:check` exit 0, isolation 12. **Дальше:** Task 6 — прогресс по реальному воспроизведению и возобновление (ФТ-B3.1/B3.3), миграция берёт номер **`0065`**.

**2026-07-28 (текущее, §5.191, ветка `feat/2026-07-28-tz-faza2-task4-playback`):** **Фаза 2, Task 4 — защищённое воспроизведение (ФТ-B2.1).** До неё плеер получал `videoUrl={null}` всегда: видео не играло, проверки прав не существовало. Стало: ссылку выдаёт `VideoPlaybackService` (request-scoped, как `ScormService`) только владельцу зачисления — цепочка материал → модуль → версия курса → зачисление → связь группы с курсом → совпадение actor'а, та же, что у запуска SCORM. Ссылка живёт 10 минут, плеер обновляет её на 80% срока, иначе просмотр оборвался бы посреди урока. Две ветки источника: провайдер — HLS, self-hosted — оригинал (`kind` говорит плееру, что делать); `hls.js` грузится динамически. **Честно в коде:** `controlsList="nodownload"` и запрет контекстного меню — барьер удобства, а не защита; браузер по определению может сохранить показанное, реальная мера — водяной знак (Task 5) плюс короткий TTL. Попутно: `completeUpload` в self-hosted теперь ставит `ready`, а не `processing` — транскодера ещё нет, и иначе залитое видео было бы невозможно посмотреть. Тесты: +12 бэкенд (**2215**), +4 фронт (**720**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 5 — динамический водяной знак (ФТ-B2.2).

**2026-07-28 (текущее, §5.190, ветка `feat/2026-07-28-tz-faza2-task3-storage-quota`):** **Фаза 2, Task 3 — лимит хранилища per tenant (ФТ-B1.3), фундамент тарифов аренды (ФТ-D4).** Главная ловушка счётчика — двойной учёт: self-hosted видео имеет строку И в `learning.video_assets`, И в `storage.files` (файл регистрируется ради AV-гейта), наивная сумма завысила бы место вдвое по всем видео — из суммы файлов вычитаются те, на которые ссылаются ассеты. Проверено на живой базе с посевом (два тенанта, видео у провайдера, битый ассет, удалённый файл): 3 005 000 000 вместо 4 012 000 000 при наивной сумме. Лимит — в `payload` настроек тенанта (`storageLimitBytes`, миграция не нужна); нет ключа = безлимит, существующие тенанты не затронуты. Гейт срабатывает ДО создания ассета с текстом «занято X из Y»; удаление ассета удаляет и файл, иначе место остаётся занятым. **Ограничение:** настройки правит сам `tenant_admin`, поэтому до платформенной админки и тарифов (ФТ-D2/D4) это защита от случайного переполнения, а не коммерческий контроль. Тесты: +14 бэкенд (**2203**), +2 фронт (**716**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 4 — защищённое воспроизведение (ФТ-B2.1).

**2026-07-28 (текущее, §5.189, ветка `feat/2026-07-28-tz-faza2-task2-video-upload`):** **Фаза 2, Task 2 — загрузка видео методистом (ФТ-B1.1).** Было: ассет из Task 1 существовал, а залить в него файл было нечем — интент files-модуля подписывает ОДИН PUT, а ТЗ требует 2–4 ГБ. Стало: загрузка по частям (32 МБ на часть; S3 держит до 10 000 частей → потолок 312 ГБ), файл регистрируется в `storage.files` обычным путём, поэтому **антивирусный гейт достаётся даром**; две ветки за одним API — провайдер или self-hosted, видно по `uploadKind`; `DELETE` отменяет незавершённую загрузку, иначе брошенные части молча занимают место; повторный `complete` при обрыве ответа безопасен. Миграция `0064` (`file_id`, `multipart_upload_id`, обе nullable) проверена на живом Postgres, включая идемпотентность. Интерфейс — секция «Видео урока» на `/materials` (редактора урока ещё нет, он в Task 11). **Требование к ops:** в CORS хранилища должен быть разрешён заголовок `ETag`, иначе браузеру нечем собрать список частей. Тесты: +23 бэкенд (**2189**), +6 фронт (**714**); `ci:check` exit 0, isolation 12, security 24. **Дальше:** Task 3 — лимит хранилища per tenant (ФТ-B1.3).

**2026-07-28 (текущее, §5.188, ветка `feat/2026-07-28-tz-faza2-task1-video-seam`):** **Фаза 2 «Видео и часы» стартовала — Task 1: шов `VideoProvider` и сущность видео-ассета (ФТ-B1.1).** План фазы апрувнут (§5.187). Сделано: интерфейс `VideoProvider` (`createUploadTarget`/`getPlayback`/`parseWebhook`) + `noop`/`fake`, выбор провайдера **пер тенант** через `VideoProviderResolver` с прод-предохранителем (сохранённый `fake` в production опускается до `noop`), миграция `0063` — `learning.video_assets` (жизненный цикл `uploading → processing → ready | failed`, индексы «тенант+материал» и «идентификатор провайдера» под вебхук) + `learning.video_provider_settings` (только несекретная конфигурация) + права `video.read/write/configure`. Видео — отдельная сущность, а не колонки в `materials`: свой жизненный цикл, размер, длительность и идентификатор у провайдера. **Грабля для следующих агентов: `test:migrations` не применяет SQL к базе** — это статический разбор текста; `0063` проверена отдельным прогоном всех миграций во временную базу на живом Postgres + повторным прогоном на идемпотентность. Тесты: 21 новый, бэкенд 2166; `ci:check` зелёный (exit 0), isolation 12. **Дальше:** Task 2 — загрузка видео методистом (multipart в S3, статусы, AV-гейт). Ответ на открытый вопрос №1 нужен только к Task 10.

**2026-07-28 (§5.187, PR #331):** **Фаза 1 «Документы» закрыта (PR #330). Написан и апрувнут план Фазы 2 «Видео и часы».** По протоколу ТЗ §13/§15 фаза стартует только после плана и апрува, поэтому кода в этой сессии нет. План: 12 задач по ЭПИКу B + ФТ-E1/E2 вокруг шва `VideoProvider` (форма скопирована с обкатанного `WebinarProvider`, миграция `0055`). **Главное: открытый вопрос №1 (провайдер vs self-hosted) больше не блокирует старт фазы** — он касается ровно одной задачи из двенадцати (Task 10), остальные пишутся против шва и не переделываются ни при каком ответе; рекомендация по существу — старт с провайдера, self-hosted к фазе аренды. Разведка кода вскрыла: прогресс видео сейчас считает время на открытой вкладке, а не воспроизведение (содержательная дыра ФТ-B3); SCORM-трекинг в общий прогресс уже реализован, статус ФТ-B4.2 в трекере был занижен; multipart-загрузки для файлов 2–4 ГБ в files-модуле нет. **Дальше:** апрув → Task 1 (шов `VideoProvider` + `learning.video_assets`, миграция `0063`). Хвост Фазы 1: **A3.6** — нужны готовые бланки от владельца.

**2026-07-27 (текущее, §5.186, ветка `worktree-tz-faza1-task9-images`):** **Фаза 1, Task 9 — изображения и подписи в бланке (ФТ-A7.1). Все задачи плана Фазы 1 закрыты.** Было: подпись руководителя и печать УЦ приходилось вшивать в сам файл бланка — смена подписанта означала перезаливку всех бланков. Решение по модулю картинок (вопрос №2 в части ФТ-A7) принято в пользу **своего кода** в `@trudskill/docx-render`: официальный image-модуль платный, бесплатный community-модуль (2019 г.) несовместим с docxtemplater 3.69, наложение поверх PDF привязало бы подпись к координатам листа. Тег `{%tenant.stamp_image}` рендерится в маркер, затем в готовом пакете заменяется на `<w:drawing>` + медиа-файл + связь + тип содержимого; ширина задаётся в мм, высота — по пропорциям; байт-детерминизм ФТ-A1.4 сохранён. Картинки лежат в реквизитах тенанта (`payload.documentImages`, миграция не нужна); подписи членов комиссии переиспользуют существующие поля. Недоступный файл не срывает выдачу — документ печатается без факсимиле. Админ-UX: раздел «Подпись и печать» на `/documents`, теги-картинки в разборе бланка, предупреждение при вставке картинки обычным тегом. Юридически это факсимиле, о чём сказано в интерфейсе. Живой прогон через Gotenberg: PDF содержит оба изображения. Тесты: docx-render 27, documents 334, worker 30, фронт 708; `ci:check` зелёный (exit 0), isolation 12, security 24, migrations 51. **Дальше:** A3.6 — нужны готовые бланки от владельца; приёмка ЭПИКа A на реальной группе 20+ — за владельцем; затем Фаза 2 (блокер — вопрос №1: видео провайдер vs self-hosted).

**2026-07-27 (текущее, §5.185, ветка `feat/2026-07-27-tz-faza1-task10-protocol-validator`):** **Фаза 1, Task 10 — мягкий комплаенс-валидатор протокола (ФТ-A8). В ЭПИКе A остался только Task 9 (картинки), и он ждёт решения владельца.** Было: протокол обязан нести реквизиты п. 92 ПП 2464, но платформа молчала — пропуск всплывал у инспектора. Стало: `validateProtocolTemplate` сверяет бланк с семью реквизитами (организация, номер, дата, программа, ФИО/должности, результат, комиссия), каждый закрывается любым из равнозначных плейсхолдеров, каждый несёт ссылку на норму. Проверка мягкая по букве ТЗ: приходит полем `compliance` в `parse-variables`, загрузку не блокирует — бланк вправе нести реквизит текстом. Фронт показывает список пропусков с подсказками. Ограничение: переменной «результат по слушателю» в каталоге нет, требование засчитывается формой аттестации. Тесты: documents 328, фронт 703. **Дальше:** Task 9 (ФТ-A7) — нужен выбор модуля картинок; A3.6 — нужны готовые бланки от владельца.

**2026-07-27 (§5.184, ветка `feat/2026-07-27-tz-faza1-task8-partial-name`):** **Фаза 1, Task 8 — частичное ФИО на публичной проверке (ФТ-A6.1). ЭПИК A: A6 закрыт.** Было: страница проверки по QR не показывала слушателя вовсе — поле в контракте есть, заполнять некому; регулятор видел номер и статус, но не мог сверить, чьё удостоверение. Наивный путь «взять из снапшота на чтении» закрыт намеренно: публичный путь вырезает `variablesSnapshot` с полными ПДн, и это защита. Стало: маска считается один раз при выпуске (`learnerNamePublic`), наружу уходят только инициалы. `maskFullName` держит русскую конвенцию — двойная фамилия не рвётся, отчество опционально, хвост длиннее трёх частей отбрасывается; архивный документ не раскрывает даже инициалы. Фронт не тронут — страница `features/verify/` уже рендерила это поле. Тесты: documents 320, security 24, isolation 12. **Дальше:** Task 10 (мягкий валидатор протокола, ФТ-A8) — свободен; Task 9 (изображения/подписи, ФТ-A7) ждёт решения владельца по модулю картинок.

**2026-07-27 (§5.183, ветка `feat/2026-07-27-tz-faza1-task7b-zip-ui`):** **Фаза 1, Task 7b — ZIP-комплект группы и кнопка «Закрыть группу» (ФТ-A5.2). ЭПИК A: ФТ-A5 закрыт целиком.** Task 7a дал серверный сценарий, но пользоваться им было нечем. Стало: `GroupPackageService` собирает архив из готовых PDF группы (только завершённые задачи; документ без `pdfFileId` пропускается — класть DOCX под именем .pdf нельзя; имена по номеру документа, чтобы пачка читалась без базы), эндпоинт отдаёт поток. Секция «Закрыть группу» в `/admin/issuance-journal`: форма, кнопка, выгрузка ZIP и прогресс, который обновляется сам каждые 5 с и замолкает по готовности; при упавших подсказка говорит нажать ещё раз. Тесты: documents 307, фронт 703. **Дальше:** Task 8 — частичное ФИО «Иванов И. И.» на публичной проверке (задел больше, чем считал план: `public-verify.util.ts` и страница `features/verify/` уже есть).

**2026-07-27 (§5.182, ветка `feat/2026-07-27-tz-faza1-task7a-close-group`):** **Фаза 1, Task 7a — «закрыть группу», серверная часть (ФТ-A5.1/A5.3).** Было: закрытие группы собиралось вручную — протокол отдельно, удостоверения по одному (для группы 25 человек 26 нажатий), сбой посреди процесса не оставлял понимания, что уже выпущено. Стало: `closeGroup` одним вызовом ставит на рендер протокол по группе и удостоверение каждому сдавшему — задачами через очередь, а не записями с пустым `fileId` (как `issueGroupOrder`). Повтор безопасен: идемпотентные ключи, упавшие возвращаются в очередь, готовые не трогаются, новые слушатели дозаказываются. `getGroupClosureStatus` даёт прогресс; считается по новому полю `groupId` в задаче, а не по идемпотентному кэшу — тот живёт сутки и вычищается. Тесты: documents 300, isolation 12. **Дальше:** Task 7b — ZIP всех PDF группы + кнопка «Закрыть группу» в `/admin/issuance-journal`.

**2026-07-27 (§5.181, ветка `feat/2026-07-27-tz-faza1-task6-numbering`):** **Фаза 1, Task 6 — нумераторы (ФТ-A4.1/A4.2): номер больше не сгорает, а маску можно настроить из интерфейса.** Было: упавшая задача помечала резервацию `failed` — в сквозном реестре оставалась дыра (для проверяющего это утраченный документ); нумератор заводился только запросом к API; «стартового значения» из ФТ-A4.1 в контрактах не существовало. Стало: `failTask` ставит `released` + пишет аудит `documents.number.released`, `reserveNumber` подбирает освобождённый номер (границу периода не пересекает — в резервации появилось `periodKey`); `startCounter` в create/update с запретом отката назад (иначе дубли); секция «Нумераторы документов» на `/documents` — маска, период, стартовое значение, предпросмотр номера той же формулой, что на сервере. Миграция не нужна (резервации персистятся JSONB-снапшотом). Тесты: documents 290, фронт 696. **Дальше:** Task 7 — «закрыть группу» (ФТ-A5).

**2026-07-26 (§5.180, ветка `feat/2026-07-26-tz-faza1-task5b-template-ui`):** **Фаза 1, Task 5b — интерфейс загрузки бланков (ФТ-A3): админ может загрузить свой бланк без разработчика.** Было: поле «`fileId` из backend файлов» руками. Стало: выбор `.docx` → интент → PUT в хранилище → версия → активация → разбор, одной кнопкой; таблица распознанных плейсхолдеров с описаниями + **явный список неизвестных** (опечатка видна до выдачи документа); кнопка «Пример PDF». Бэкенд: `createTemplateVersion` проверяет файл (тенантный, AV, читается как DOCX) — раньше принимал `fileId` на честном слове. Тесты: фронт 688, documents 280. **Дальше:** Task 6 — нумераторы.

**2026-07-26 (§5.179, ветка `feat/2026-07-26-tz-faza1-task5a-template-upload`):** **Фаза 1, Task 5a — серверная часть админ-UX шаблонов (ФТ-A3.1/A3.2/A3.3).** Движок рендера вынесен из воркера в общий пакет **`@trudskill/docx-render`** — предпросмотр обязан использовать тот же код, что боевая выдача (worker-специфика убрана: `DocumentConversionError{retryable}` вместо класса из очередей). Эндпоинты: `templates/upload-url` (только DOCX + AV), `parse-variables` (**была заглушка, не читавшая DOCX** — теперь реальный разбор «найдено/соответствует/неизвестно»), `preview` (PDF на демо-данных). Живой прогон: опечатка `learner.favourit_color` поймана, предпросмотр — PDF 17 КБ с «УДОСТОВЕРЕНИЕ № ОБРАЗЕЦ-0001». Тесты: пакет 16, worker 27, documents 280, isolation 12. **Дальше:** Task 5b — интерфейс (input файла, таблица плейсхолдеров, кнопка предпросмотра).

**2026-07-26 (§5.178, ветка `feat/2026-07-26-tz-faza1-task4-variables`):** **Фаза 1, Task 4 — каталог переменных подключён (ФТ-A2.3): бланк заполняется целиком.** Разведка вскрыла, что 5 существующих резолверов никогда не вызывались из рабочего кода — поэтому сделано «дописать 5 + подключить все 10»: `entity-variables.ts` (tenant/learner/counterparty/group/course; лицензия и аккредитация — из `org.training_licenses`), `variable-catalog.ts` (**70 переменных как SSOT в коде** + тест-страж «каталог не врёт» + `classifyPlaceholders` под Task 5), `DocumentVariablesBuilder` (цепочка запись→слушатель/группа/курс/комиссия через `MvpTenantRunner` без цикла модулей), дата прописью без зависимости от локали среды. **Живой прогон** дал полный протокол (шапка с лицензией, нумерованная таблица со СНИЛС, председатель) и **вскрыл баг**: строки таблицы были в camelCase без номера строки — исправлено. Тесты: documents 274 (+32), documents+mvp 1311, isolation 12. **Дальше:** Task 5 — админ-UX загрузки шаблонов.

**2026-07-26 (§5.177, ветка `feat/2026-07-26-tz-faza1-task3-pdf-snapshot`):** **Фаза 1, Task 3 — PDF, оба формата, снапшот (ФТ-A1.3/A1.4/A1.5).** DOCX→PDF через Gotenberg (`gotenberg-convert.ts`; контракт снят с живого сервиса), документ хранится в обоих форматах (`pdfFileId`), снапшот подстановки (`variablesSnapshot`, миграция `0062`) шифруется на границе БД и **вырезается на публичном QR-пути**; ошибки классифицированы: 4xx → задача `failed`, сеть/5xx → ретрай/DLQ. **Живой прогон:** PDF 16 КБ, текст извлечён `pdftotext` и совпал с бланком дословно, кириллица читаема; повтор из снапшота — байт-в-байт. Тесты: worker 43, documents 241, crypto 14, isolation 12, migrations 51. **Дальше:** Task 4 — наполнить каталог переменных (сейчас в бланк идут только номер и дата).

**2026-07-26 (§5.176, ветка `feat/2026-07-26-tz-faza1-task2-transport`):** **Фаза 1, Task 2 — транспорт рендера (ФТ-A1.1): цепочка «generate → RabbitMQ → worker → DOCX в S3» собрана.** Enqueue из 3 точек (generate/batch/авто-выдача — после сохранения состояния; best-effort), `case 'document'` в worker → `document-job.ts` (claim → presigned GET шаблона → renderDocx → presigned PUT → complete; ошибки шаблона → fail+ack, транспортные → retry/DLQ, гонка состояния → 404-retry), internal-эндпоинты documents под `WorkerCallbackGuard` через `DocumentsTenantRunner`, результат — стандартным files-циклом (AV-гейт). Тесты: documents 236, worker 32, isolation 12 — зелёные. **Дальше:** Task 3 — PDF/Gotenberg + снапшот (0062, pii-crypto) + живой smoke цепочки.

**2026-07-26 (§5.175, ветка `feat/2026-07-26-tz-faza1-task1-docx-render`):** **Фаза 1 «Документы» стартовала** (план PR #314 апрувнут; вопрос №2 = docxtemplater free). **Task 1 — движок рендера DOCX готов:** `worker/src/render/docx-render.ts` — docxtemplater с кастомным parser'ом (плоские ключи `pillar-a-variables` + вложенные пути), циклы/условия/split-runs, `nullGetter`→'', байт-детерминизм (фиксация zip-дат), `extractPlaceholders` для админ-UX, `TemplateRenderError`. 10 юнитов на фикстурах-фабрике (DOCX собирается кодом). **Дальше:** Task 2 — транспорт (enqueue → worker → internal-эндпоинты → presigned файлы).

**2026-07-26 (§5.174, ветка `feat/2026-07-26-tz-faza0-task7-pii-encryption`):** **ФАЗА 0 «ФУНДАМЕНТ» ЗАВЕРШЕНА И ПРИНЯТА.** Все 7 задач — PR #307–#313: test:isolation-гейт, контейнеры ClamAV+Gotenberg (+фикс протокола clamd по живому EICAR), rate limiting `/verify/{qr}`, email-события по-настоящему, 2FA TOTP для админ-ролей, шифрование ПДн (СНИЛС at-rest). Приёмка: полный `ci:check` (2033 бэк + 682 фронт + сборки) + isolation 12 + security 24 + migrations 51 (0000–0061) — зелёные; вопрос №7 закрыт. Осознанные хвосты — в плане фазы. **Дальше:** план Фазы 1 «Документы» (ЭПИК A — движок рендера) + апрув; блокер — вопрос №2 (docxtemplater?) — нужно решение владельца.

**2026-07-26 (§5.173, ветка `feat/2026-07-26-tz-faza0-task7-pii-encryption`):** **Фаза 0, Task 7 — шифрование ПДн (ФТ-C3.3).** СНИЛС слушателей хранится в БД только шифртекстом: learners лежат JSONB-документами, поэтому шифрование — на границе персистенса (`infrastructure/crypto/pii-crypto.ts` + хуки в `PostgresMvpPersistenceBackend`): запись → AES-256-GCM (keyring `INTEGRATION_CRYPTO_KEYS`) + слепой keyed-HMAC `snilsHash` по нормализованным цифрам; чтение → расшифровка. **Рантайм не менялся**: реестры/ЕСИА/поиски/PDF видят открытое значение в памяти; legacy plaintext перешифровывается при первом сохранении (lazy). Паспортных текстовых полей в коде нет (только файлы-сканы) — объём сужен по факту. Миграция `0061` — functional-индексы по хэшу. Тесты: pii 6 + persistence 4 + migrations 51 + смежные 70 — зелёные. **Все 7 задач Фазы 0 сделаны** (PR #312 и этот — на мердже) → дальше приёмка фазы (`ci:check` + финал статусов).

**2026-07-26 (§5.172, ветка `feat/2026-07-26-tz-faza0-task5-2fa-totp`):** **Фаза 0, Task 5 — 2FA (TOTP) для админ-ролей (ФТ-G3).** Миграция `0060` (три totp-колонки на `iam.users`, секрет в AES-256-GCM), самописный RFC 6238 на `node:crypto` (`totp.util.ts`, эталонные вектора), **гейт в `issueSessionForUser`** — 2FA закрывает пароль, magic-link и ЕСИА; login/redeem отдают `{totpRequired, challengeToken}` (TTL 5 мин), второй шаг `POST auth/2fa/verify` (bootstrap-роут TenantGuard, throttle 10/мин, anti-replay по `totp_last_used_step`); самообслуживание `setup`(QR data-URI, роль-гейт)/`confirm`/`disable`(нужен код)/`status`. Фронт: шаг кода в логине и на magic-link странице, карточка «Безопасность» в `/settings`. Тесты: totp 25, IAM 150, security 24, isolation 12, фронт 682 — зелёные. **Дальше по Фазе 0:** Task 7 (шифрование ПДн) → приёмка фазы.

**2026-07-26 (§5.171, ветка `feat/2026-07-26-tz-faza0-task4-email`):** **Фаза 0, Task 4 — email-события по-настоящему (ФТ-F1).** Два log-only стаба переведены на события → новый `ExamIdentityEmailListener` → `NotificationDispatcher` (журнал + send-once dedup): **код на экзамен** (`PRE_EXAM_AUTH_REQUESTED_EVENT`, шаблон `pre_exam_auth` с `{{verifyUrl}}`; сырой URL в prod-логах теперь редактируется — раньше живой токен печатался в stdout) и **отклонение identity-проверки** (`IDENTITY_VERIFICATION_REJECTED_EVENT`, шаблон с `{{reason}}`; dedup на `reviewedAt` — reject после resubmit шлёт новое письмо). В `enrollment_invite` — рабочая ссылка входа `{{loginUrl}}`; enrollment-письма получили dedup-ключи. Env: задокументированы SMTP + `RECERTIFICATION_SCAN_ENABLED` (в прод-примере `true`). Конструктор `MvpService` не тронут. 150+ таргет-тестов, isolation 12, typecheck, eslint — зелёные. **Дальше по Фазе 0:** Task 5 (2FA TOTP) → Task 7 (шифрование ПДн).

**2026-07-26 (§5.170, ветка `feat/2026-07-26-clamav-container`):** **Фаза 0, добивка Task 3 — живой EICAR-smoke + фикс протокола INSTREAM (ФТ-G5).** Живой прогон на тестовом сервере (clamd `clamav/clamav:1.4` из compose, дошёл до `healthy`) вскрыл протокольный баг сканера: z-команды clamd NUL-терминированы, а код слал `'zINSTREAM '` с пробелом, и NUL-терминированные ответы clamd не парсились — с `ANTIVIRUS_ENABLED=true` prod отбивал бы **все** загрузки (fail-closed). Починено в `clamav-antivirus.scanner.ts`, юнит-симулятор clamd приведён к реальному поведению. Smoke: чистый файл → `clean`, EICAR → `infected: Eicar-Test-Signature`; юнит 5/5, eslint/typecheck зелёные. Ops-оговорка §5.168 «живой EICAR-прогон на деплое» закрыта. **Дальше по Фазе 0:** Task 2 (rate limiting `/verify/{qr}` — PR #309) → Task 4 (email-события) → Task 5 (2FA TOTP) → Task 7 (шифрование ПДн).

**2026-07-24 (§5.169, ветка `feat/2026-07-24-verify-rate-limit`):** **Фаза 0, Task 2 — rate limiting на публичной проверке `/verify/{qr}`.** На endpoint уже стоял `@Throttle(30/мин)`, но он «спал» без `@UseGuards(ThrottlerGuard)` (глобального throttler-guard в app.module нет). Guard добавлен — лимит реально применяется (защита от перебора QR, ФТ-G2/A6.2); тест на 429. Контейнеры ClamAV+Gotenberg — §5.168 (PR #308 влит); живой EICAR-smoke + фикс протокола clamd — §5.170 (PR #310). **Дальше по Фазе 0:** Task 4 (email-события) → Task 5 (2FA TOTP) → Task 7 (шифрование ПДн).

**2026-07-24 (§5.168, ветка `feat/2026-07-24-phase0-infra-containers`):** **Фаза 0, Task 3 + Task 6 — инфраструктура ClamAV и Gotenberg.** В `docker-compose` (dev+prod) добавлены сервисы `clamav` (антивирус, ФТ-G5 — в проде включён, backend ждёт healthcheck) и `gotenberg` (DOCX→PDF, ФТ-A1.3 — под будущий движок рендера Фазы 1); добавлена env-переменная `GOTENBERG_URL`. Только инфраструктура, кода приложения нет. `docker compose config` и backend typecheck зелёные. **Дальше по Фазе 0:** Task 2 (rate limiting `/verify/{qr}`) → Task 4 (email-события) → Task 5 (2FA TOTP) → Task 7 (шифрование ПДн).

**2026-07-24 (§5.167, ветка `feat/2026-07-23-tenant-isolation-suite`):** **Фаза 0 «Фундамент» дельта-ТЗ «Арендная СДО» — старт.** План Фазы 0 создан и апрувнут владельцем ([docs/superpowers/plans/2026-07-23-tz-faza0-fundament.md](docs/superpowers/plans/2026-07-23-tz-faza0-fundament.md), PR #306); открытый вопрос №7 решён — **шифрование ПДн делаем в Фазе 0**. Сделан **Task 1 — гейт изоляции тенантов `pnpm test:isolation`** (ФТ-D1.3): контракт `TenantGuard` (эффективный тенант только из токена, `x-tenant-id` не подменяет) + data-layer `enforceTenantScope` + структурный «сторож» контроллеров (любой новый контроллер без `@UseGuards(TenantGuard)` роняет суиту) + шаг в CI. 12 тестов зелёные, typecheck зелёный. **Дальше по Фазе 0:** Task 3 (ClamAV-контейнер) → Task 6 (Gotenberg) → Task 2 (rate limiting `/verify/{qr}`) → Task 4 (email-события) → Task 5 (2FA TOTP) → Task 7 (шифрование ПДн). Каждая — отдельный под-PR.

**2026-07-24 (§5.166, ветка `claude/tz-project-integration-27b955`):** **Принято в работу дельта-ТЗ «Арендная СДО»** — [TZ_TRUDSKILL_ARENDNAYA_SDO.md](TZ_TRUDSKILL_ARENDNAYA_SDO.md) добавлено в корень (гэп-анализ к `TZ_TRUDSKILL_BASE.md` по аудиту коммита `ac2af17`: 9 эпиков A–I, требования ФТ-\*, дорожная карта фаз 0–6, приоритеты P0–P2). Создан живой статус-трекер [docs/TZ_ARENDNAYA_SDO_STATUS.md](docs/TZ_ARENDNAYA_SDO_STATUS.md) (статус каждого ФТ: сделано / частично / не начато / переделать; начальные статусы из аудита ТЗ + факта кода, вкл. досрочный прогресс ЭПИКа H по UI redesign §5.162–§5.165). Протокол «продолжай по ТЗ» обновлён в CLAUDE.md / docs/DOCUMENTATION_MAP.md / README (порядок чтения теперь включает дельта-ТЗ §13 + статус-трекер). Docs-only, код не тронут. **Дальше:** решения владельца по открытым вопросам ТЗ §14 (минимум №2 docxtemplater и №7 шифрование ПДн — блокируют фазы 0–1) → план Фазы 0 «Фундамент» в `docs/superpowers/plans/` → апрув → код.

**2026-07-13 (предыдущее, §5.164, ветка `feat/2026-07-12-ui-phase-3-reference-screens`):** **UI redesign Фаза 3 — эталонные шаблоны экранов (14/14 задач плана) завершена.** 6 переиспользуемых каркасов в `@trudskill/ui` поверх существующего CSS (`AsyncSection`, `StatGrid`, `DetailLayout` двухколоночная, `ListPage`, `Form/FormSection/FormActions`, `SelectField`; новый CSS только `.ui-detail`/`.ui-section-head` на токенах); слоты `actions`/`subtitle` у `SectionCard`; живая витрина `/admin/ui-kit` (под `auth.manage_sessions`, вне меню); 4 пилотных экрана мигрированы **без изменения поведения/данных/прав/URL** (список `learners`→`ListPage`, карточка `clients`→`DetailLayout`+`KeyValueList`, форма `academy/requisites`→`Form`+`FormField`, dashboard `analytics`→`StatGrid`+`AsyncSection`). Полный цикл: brainstorming (визуальный компаньон) → спека → план (14 задач TDD) → subagent-driven исполнение (4 слоя) → adversarial-ревью (2 находки, обе опровергнуты 2/2 скептиками). Гейты: ui **21 файл/68 тестов**, frontend **111 файлов/679 тестов**, typecheck 8/8, ESLint чисто; без миграций/новых прав/бэкенда. **Сужено по YAGNI:** `Drawer`/`Hero`/`DashboardTile` + дедуп каталогов плиток → Фаза 4; a11y accname общего `.ui-field` (наследие `FormField`) → Фаза 6. **Дальше:** Фаза 4 — миграция страниц пачками (список+карточка ≈ 65% экранов; монолит `mvp/screens.tsx` — highest-leverage). Детали — §5.164.

**2026-07-02 (§5.163, ветка `feat/2026-07-02-ui-phase-2-navigation`):** **UI redesign Фаза 2 — навигация и оболочка (7/7 задач плана) завершена.** Сгруппированный сайдбар (10 сворачиваемых смысловых блоков вместо плоского «основные»+`<details>`), командная палитра Ctrl/⌘+K (поиск+переход по всем доступным по правам страницам), хлебные крошки «Блок → Страница → Деталь»; заведены сироты `/admin/licenses`/`/admin/issuance-journal`/`/admin/webinars/settings`, русифицированы 3 латинские метки, ESLint-страж против прямого `lucide-react` вне `nav-icons.ts`. Гейты: frontend **109 файлов/675 тестов**, typecheck и full ESLint чистые. Детали — §5.163. **Дальше:** Фаза 3 UI redesign (следующий блок по дорожной карте редизайна — уточнить у владельца scope).

**2026-07-02 (предыдущее, §5.162, ветка `feat/2026-07-02-ui-phase-1-design-system`, PR #299):** **UI redesign Фаза 1 — дизайн-система `packages/ui` (12/12 задач плана).** По согласованному аудиту Фазы 0 (91 страница → 10 блоков ИА). Токены: `baseVars` (CSS-мост `--ui-space/radius/font-*`) в `UiThemeProvider` (`buildThemeVars` + стражи); все радиусы 7 CSS-слоёв на `var(--ui-radius-*)` + тест-страж дисциплины; вне-шкальные нормализованы (единственное видимое изменение). Иконки: `lucide-react@^1.23.0` (ui+frontend) + компонент `Icon` (шкала 16/18/20/24, a11y-контракт). Новые компоненты-обёртки над существующим CSS: `Button` (variant/loading/icon), `Skeleton` (план скорректирован: контейнер+линии вместо невидимого kind=block), `StatCard`, `KeyValueList`, `Callout` (+override role для статичных баннеров). Фиксы: `DateRangeField` в контракте ui-field + aria; русские дефолты состояний. Маршруты/экраны/RBAC не тронуты; ветка без миграций/новых прав (последняя в main — 0059). Каждая задача через spec+quality ревью (2 Important-фикса применены); финальное холистическое ревью «Ready to merge». Гейты: ui 16 файлов/53 теста, frontend typecheck + 106 файлов/646 тестов — всё зелёное. Запись изначально §5.155, перенумерована в §5.162 при merge (аудит-ветки заняли §5.154–§5.161; их фикс #5 уже закрыл битый `/learner/documents` из аудита Фазы 0). **Дальше:** Фаза 2 — сгруппированный сайдбар (10 блоков) + палитра Ctrl/⌘+K + крошки; остальные nav/RBAC-баги — отдельным PR. Детали — §5.162.

**2026-06-30 (предыдущее, §5.161, ветка `fix/2026-06-29-audit-7-bug-fixes`, HEAD `ae58977`):** **Новый аудит-проход — 6 латентных багов (3 HIGH + 1 MEDIUM + 2 LOW) + esign-хардненинг + cleanup, все по TDD; диф проверен 4-линзовым adversarial-workflow.** **HIGH:** (1) очередь bulk-enrollment (worker callback) гоняла request-scoped `MvpService` поверх ПУСТОГО `MVP_STATE` → зачисление молча терялось → проводка через `MvpEnrollmentService` (гидратация+сохранение); (2) `finalizeDocument` воскрешал аннулированный-но-`generated` документ (revoke оставляет `isFinal=false`) → guard `revoked` (достижимо через esign `tryCompleteProcess`); (3) дашборд слушателя показывал «Пройден»/«Не пройден» до ручной проверки эссе (сырой `attempt.passed`) → `submitted` пока pending-review (радиус только дисплей, registries читают `ExamResult`). **MEDIUM:** (4) `rejectParticipant` без `assertProcessMutable` → ложная пост-отменная запись в legal-log; (5) `tryCompleteProcess` валидировал документ ПОСЛЕ мутации в `signed` → validate-before-mutate (revoked/archived → `failProcess`). **LOW:** (6) `inviteParticipant`/`markViewed` без того же guard; (7) module/course `completedAt` дрейфовал вперёд → монотонный latch. **Cleanup:** удалён мёртвый `finalizeAttempt`. **⚠️** параллельная сессия откатила `mvp.service.ts` во время ревью — ревью поймал, перенакатил+закоммитил. **Verified:** typecheck **8/8**, ESLint чисто, затронутые backend-сьюты **258** зелёные; без миграций (0059)/новых прав. **Отложено:** CloudPayments `providerPaymentId` mismatch (сверка с песочницей эквайера).

**2026-06-30 (§5.160, ветка `fix/2026-06-29-audit-7-bug-fixes`):** **Закрыты все follow-up'ы §5.154 (§5.155–§5.159) + новый аудит-проход §5.160 (7 багов: 3 HIGH + 3 MEDIUM + 1 LOW).** §5.155 group-order дубль-серт · §5.156 провизорный `ExamResult` · §5.157 cannot-unset clear-vs-keep (**PR #294** — параллельная сессия) · §5.158 `resolvePermissions` dead conditional · §5.159 `GroupCourse` version pinning · **§5.160 кросс-доменный аудит:** эссе-bypass ручной проверки (HIGH), IDOR enrollment/progress (HIGH), permission-map дрейф → backend-гидрация прав (HIGH), `mark-paid` идемпотентность (**migration 0059**), немонотонный прогресс материала, webhook verify-order, + (глубокий 2-й проход) `completeAttemptReview` completeness. **Отложено:** CloudPayments `providerPaymentId` mismatch (сверка с песочницей эквайера). **Verified:** typecheck **8/8**, backend key-сьюты **279** + frontend full **651** зелёные; migration 0059, без новых прав. _История ветки: origin §5.155–§5.157 (PR #294) + локальные §5.158–§5.160; redundant локальный §5.157 (edit-payloads) отброшен в пользу origin payloads.ts при rebase._

**2026-06-29 (§5.154, ветка `fix/2026-06-29-audit-7-bug-fixes`):** **Новый аудит-проход «максимум багов» — 8 подтверждённых (2 CRITICAL + 3 HIGH + 3 MEDIUM), кросс-доменный, все по TDD.** 6 параллельных subagent-разведчиков (iam/auth · mvp bulk · documents/esign · assessment · communication/org · frontend); каждая находка перепроверена чтением кода. **CRITICAL:** (1) esign `tryCompleteProcess` финализировал регулируемый документ при НУЛЕ подписей (все `skipped`) + воскрешал `cancelled`→`signed` через поздний skip → guard'ы `assertProcessMutable`/`in_signing`/«≥1 signed»; (2) `BulkImportIdempotencyRecord` без `id` ронял Postgres-снапшот (NOT NULL PK) после каждого bulk-import → добавлен `id`. **HIGH:** (3) magic-link TOCTOU (две сессии по одному single-use токену) → `markConsumed` возвращает affected-rows; (4) `TenantGuard` решал по `request.url` с query-string → `?redirect=/auth/esia/` обходил гард → решение по PATH (`split('?')`); (5) nav-ссылка `/learner/documents` без `routeMeta` → 404 у слушателя → добавлена запись + инвариант-тест; (6) `finishAttempt` запирал эссе-попытку из ручной проверки (замораживал на 0, выбивал из reviewer-queue) → остаётся `submitted` при `autoGraded===false`; (7) гейт итогового экзамена собирал модули со ВСЕХ версий (draft/archived запирали учащихся published) → scope PINNED>PUBLISHED. **MEDIUM/latent:** (8) deadline-`dedupKey` без `plannedEndAt` (как §5.150 license-fix). Затронутые наборы зелёные (esign 79, magic-link 20, guards 8, module-gating 7, learners-bulk-import 39, mvp.service 101, business-flows.e2e 4, navigation 15, postgres-persistence 1), **typecheck 8/8** (backend+frontend), ESLint чисто. **Без миграций (0057), без новых прав.** Отложено (follow-up-задачи): frontend cannot-unset, payment retry dead-end, esign group-order дубль-серт, провизорный ExamResult до проверки эссе, in-memory resolvePermissions (non-prod), pinning `GroupCourse.courseVersionId` не подключён.

**2026-06-29 (предыдущее, §5.153, HEAD `0be206e` / PR #289):** **Пред-пилотная стабилизация завершена.** Закрыт отложенный follow-up §5.152: bulk-import reuse-детекция теперь покрывает тенантов >10k слушателей (целевой `MvpService.findLearnersByEmailOrSnils` вместо `listLearners(page_size: 10_000)` → совпадение за границей больше не теряется и не плодит молчаливый дубль). Перед этим §5.152 — латентный аудит, 7 фиксов (2 HIGH + MED-HIGH + 3 MED + frontend data-loss). Полный `pnpm test:backend`/frontend зелёный, typecheck 8/8, ESLint чисто; без миграций (последняя 0057) / без новых прав. **Предыдущее (§5.151):** **Новый аудит-проход по непрочёсанным доменам — 8 подтверждённых багов (1 CRITICAL + 2 HIGH + 5 MEDIUM) + 2 LOW-hardening** (ветка `fix/2026-06-28-audit-worker-retry-exam-timeout-ot-counter`). LOW (follow-up #7/#8): webhook-верификатор переведён на constant-time сравнение (sha256+`timingSafeEqual`); удалён мёртвый дублёр-грейдер `calculateAttemptScore`. Спорный follow-up #6 (bulk-идемпотентность без request-hash) оставлен намеренно — стандартный контракт идемпотентности, не баг. Фикс #8 (bulk-import, MEDIUM, follow-up #5): две строки, резолвящиеся в одного существующего слушателя по разным полям (email vs СНИЛС), затирали друг друга в `learnerIdToRowNumber` (двойной счёт `reused`, потеря enrollment у первой); фикс — вторая помечается `duplicate_in_file`/failed (learners-bulk-import 38). Фикс #4 (esign-«кирпич», HIGH, follow-up #1): `startProcess` мутировал `status='prepared'` до guard'а участников → процесс без участников застревал навсегда; фикс — «validate-first, mutate-last». Фикс #5 (`finalizeDocument` идемпотентность, MEDIUM, follow-up #2): два процесса подписи на один документ дважды накладывали подпись + дублировали critical-audit; фикс — `if (doc.isFinal) return` (convergence-point, бонусом не «разотзывает» отозванный документ). Фикс #6 (esign roster-guard, MEDIUM, follow-up #3): `assertProcessMutable` пускал изменение состава подписантов на `in_signing`/`failed`; фикс — разделил guard'ы (`assertProcessRosterMutable` только `draft`/`prepared`, подпись по-прежнему пускает `in_signing`). documents+esign **300/300**. Фикс #7 (assessment exam-result, MEDIUM, follow-up #4): два финализатора писали разные поля (`finalScore` vs `bestScore`) в одну запись `ExamResult` → consumer читал `undefined` → NaN; фикс — оба пишут оба синонимичных поля + `passingScore` (test-player 14). «продолжай улучшать код» после закрытия аудит-хвоста (§5.150). 4 параллельных subagent-разведчика по доменам, не покрытым прежними аудитами (esign / assessment / bulk+worker / IAM+integrations); каждая находка перепроверена чтением кода; все фиксы по TDD. (1) **CRITICAL** — воркер (`apps/worker/src/main.ts`) звал `markProcessed` **до** `processJob` → любая bulk-enrollment, упавшая на транзиентной ошибке, **терялась навсегда** (ретрай пропускался как дубликат); фикс — вынесен тестируемый шов `message-consumer.ts`, порядок «проверить → обработать → пометить после успеха» (at-least-once; бэкенд идемпотентен по `idempotencyKey`). (2) **HIGH** — `submitAttempt` перезатирал `expired→submitted` → просроченная попытка принималась как `passed` (обход таймаута); фикс — финализация как `expired` + early-return (зеркало `finishAttempt`). (3) **MEDIUM** — ОТ-экспортёр считал объекты ошибок вместо сущностей (`failed`/`total` завышались на комплексных курсах); фикс — distinct-счёт по сиблинг-паттерну rostechnadzor/eisot. typecheck **8/8**, целевые сьюты зелёные (worker 15, test-player 13, mvp.service 100, ot-registry 34, business-flows.e2e 4), ESLint чисто. **Без миграций (0057), без новых прав.** 8 подтверждённых follow-up'ов (esign brick HIGH и др.) задокументированы в §5.151.

**2026-06-27 (предыдущая сессия, §5.148):** **Аудит латентных багов — 5 подтверждённых (3 CRITICAL + 2 HIGH)** (ветка `fix/2026-06-27-audit-latent-bugs`). Кодовый roadmap (Phase 0–11) слит → владелец выбрал предпилотный аудит. 5 параллельных subagent-разведчиков, **каждая находка проверена чтением кода** (ложные отсеяны). Найдено и починено по TDD: (1) CRITICAL — DB CHECK `payments.payments.provider` допускал лишь yookassa из 4 эквайеров → tinkoff/cloudpayments/robokassa падали на реальном Postgres (**migration 0058**, проверено на throwaway-БД); (2) CRITICAL — публичная QR-проверка читала пустой request-scoped state → любой реальный QR давал `not_found` (кросс-tenant durable-поиск по `qrToken`); (3) CRITICAL — `getAttemptResult` (GET) сбрасывал `passed→false` для `submitted`-попыток из-за расхождения фильтров двух финализаторов; (4) HIGH — лицензионный гейт публикации игнорировал `validUntil` (просроченная active-лицензия проходила §5.10 вечно); (5) HIGH — частичное исполнение платежа метило `enrolled` несозданные места и флипало заказ в `fulfilled`. typecheck 8/8, затронутые сьюты зелёные, ESLint clean. **Migration 0058**, без новых прав.

**2026-06-27 (эта сессия, §5.147):** **Documents — фикс латентной коллизии нумерации при сбросе периода** (ветка `claude/hungry-margulis-071cfe`, PR #280). `DocumentsService.reserveNumber` при `resetPeriod:'year'|'month'` после смены периода сбрасывал счётчик, но форматировал номер **без токена периода** → `#000001` нового года коллидировал с прошлогодним → `ConflictException`, проглатываемый fire-and-forget `EnrollmentDocumentIssuanceListener` → регулируемый документ **молча не выпускался**. Фикс (выбор владельца «год в номере»): токен `{period}` → `CERT-2026-000001` (глобально уникален + счётчик реально сбрасывается) + **validate-before-commit** (проваленная резервация больше не прожигает счётчик). TDD +3 теста (`vi.setSystemTime` как clock-seam); documents.service **57** + смежные **43** зелёные, typecheck 8/8, ESLint clean. **Без миграции (0057)/новых прав.** _Merge-заметка:_ ветка несёт вперёд §5.146 (refactor `029ccdc`), не попавший в squash PR #279.

**2026-06-26 (эта сессия, §5.146):** **Phase 5C-2 — ревью-проход по качеству** (та же ветка, без изменения поведения). Многоугловое ревью диффа ветки: отклонены ложные срабатывания (NestJS сам резолвит промис из обработчика → «missing await» в контроллерах — не баг; `useEffect`-seed в notification-settings безопасен из-за `refetchOnWindowFocus:false`). Применены 3 низкорисковых улучшения: `buildStaffRecipients` вынесена из dispatch-циклов в `course-deadline`/`recertification`-сканерах (приведено к паттерну `license-expiry-scanner`), убран избыточный голый провайдер `PostgresLicensesRepository` в `org.module`. Тесты затронутых модулей зелёные (20+11), typecheck чист. Коммит `029ccdc`.

**2026-06-26 (ранее в этот день, §5.145):** **Phase 5C-2 (продолжение) — durable-персистенция лицензий + `license_expiring` cron** (та же ветка/PR). Закрыт последний отложенный хвост уведомлений. **Находка:** `org`-модуль имел **нулевую персистенцию** — лицензии жили в `Scope.REQUEST` in-memory и терялись между запросами, а таблица `org.training_licenses` (миграция 0035, с partial-индексом под expiry-job) **не использовалась** → лицензионный гейт публикации курса (Pillar A §5.10) был фактически инертен в проде. **Сделано:** durable-репозиторий поверх 0035 (токен `LICENSES_REPOSITORY` + in-memory/postgres impl, прецедент `recertification-drafts`); `LicensesService` → async на репозитории; `publishCourseVersion` стал async; `OrgModule` снят с request-scope. **Поведенческая импликация (фикс задуманного, не регресс):** публикация курс-версии теперь требует **сохранённую** активную лицензию — владельцу нужно один раз внести лицензию центра перед публикацией пилотных курсов. **`license_expiring`:** новый staff-facing шаблон + `LicenseExpiryScanner` (90/30/7 по `valid_until`, аудитория = настроенный staff-ящик из §5.144, opt-in) встроен в `RemindersSchedulerService`. TDD; гейт (изолированно): org **41**, mvp.service **95**, reminders+DI **42**, business-flows+communication **104**, typecheck чисто, ESLint clean. **Без новой миграции (0035 уже была), без новых прав.**

**2026-06-26 (ранее в этот день, §5.144):** **Phase 5 — хвосты уведомлений (5C-2): approve-в-UI + staff-получатели** (ветка `feat/2026-06-26-phase-5c-2-notification-tails`). Кодовый roadmap (фазы 0–11) уже слит — взяты два неначатых под-хвоста Phase 5 по выбору владельца. (1) **Approve в очереди переаттестации** (ранее намеренно скрыт, «5C-2 follow-up»): кнопка «Перезачислить» + модалка выбора целевой группы (`useGroupsList`, зеркало `issue-order-modal`) проводят уже протестированный backend-эндпоинт `POST /recertification-drafts/:id/approve`; новый contract-тест. (2) **Curator/admin получатели**: настраиваемый per-tenant список email сотрудников (opt-in), хранится в **MVP-снимке** (новая коллекция `notificationStaffRecipients`, зарегистрирована в `mvp-collections.ts`, **без миграции/нового права** — переиспользует `notifications.read/write`), читается ночным cron через `MvpTenantRunner` без coupling к IAM; helper `buildStaffRecipients` → `kind:'admin'` добавлен в **тот же** `dispatch` (send-once-дедуп покрывает всех) в 3 событиях — переаттестация / дедлайн / отзыв документа (НЕ «завершён курс», по решению владельца); guard сменён на комбинированный (staff уведомляются даже без email слушателя); `MvpService.get/setNotificationStaffRecipients` (нормализация+dedup+аудит), DTO, контроллер `NotificationRecipientsController` под MVP-интерсептором, экран `/admin/notification-settings` + навигация. TDD на каждый юнит (RED→GREEN). **Гейт (изолированные прогоны):** backend reminders/recert/communication **155**, сервис **5**, HTTP-integration **97** (+4 границы прав), DI-guard+wiring **2**, frontend **232** (вкл. e2e-роутинг), typecheck 8/8, ESLint clean. **Без миграции (0057), без новых прав.** _Замечание:_ рабочее дерево на старте уже содержало незакоммиченную заготовку этой задачи (untracked) — перезаписана TDD-реализацией. Отложено: `license_expiring` (нужна postgres-персистенция лицензий из in-memory).

**2026-06-24 (предыдущая сессия, §5.143):** **Завершение редизайна UI + переименование внутренних пакетов `@cdoprof/*` → `@trudskill/*`** (ветка `chore/2026-06-23-phase-11-stabilization-security`, не закоммичено). (1) **Этап 0 — аудит единообразия:** ~93 маршрута, ~86% уже на `@trudskill/ui`; легаси-долг локализован (4 параллельных агента-разведчика). (2) **Этап 1:** дизайн-система уже укомплектована для LMS (карточка курса `.course-card`, дашборд `.stat-card`/`.ui-hero`, плеер/SCORM `.course-viewer-*`, тест `.ui-option`/`.test-result__banner`, модалка `Modal`/`Dialog`) — строить нечего, новых компонентов не вводил. (3) **Этап 2 — миграция 14 экранов** на существующие `.ui-*`-классы: `verify` (публичная, была полностью вне ДС с хардкод-цветами), мегафайл `mvp/screens.tsx`, 2 самописные модалки → `.ui-modal*`, `licenses`/`issuance-journal`/`report-builder`/`identity-verification`/`proctoring` (`#c00`→`.ui-callout--danger`), `theme-appearance-settings` и др.; финальный греп — **0 хардкод-цветов** во фронтенде; логика/контракты/бэкенд не тронуты. (4) **Этап 3:** UI-текст уже без старого бренда (проверено грепом); по выбору владельца выполнено **переименование scope 8 пакетов** (`ui/frontend/backend/api-contracts/shared-types/worker/realtime/test-utils`) + 962 ссылки в 125 build-файлах (импорты, `tsconfig`/`vitest`/`next` алиасы, Dockerfile, CI, root-скрипты) + `pnpm install` (lockfile регенерирован); история в `docs/superpowers/**` + handoff намеренно сохранена с `@cdoprof`. Гейты: typecheck **8/8**, lint **8/8** (поправлен 1 `import/order` из-за смены алфавитной позиции scope), contracts **7/7**, frontend **643/643**, backend integration-smoke зелёный. **Без миграций** (0057)/новых прав. Остаток Этапа 3 — переименование GitHub-репозитория/remote (`cdoprof`) делает владелец.

**2026-06-24 (ранее в этот день, §5.142):** **UI-редизайн + ребрендинг `CDOProf` → `trudskill`** (frontend-only, ветка `chore/2026-06-23-phase-11-stabilization-security`, не закоммичено). Дизайн-токены перекрашены под бренд-индиго `#3B4FE4` + акцент-коралл `#FF7A45` (все цвета проверены на WCAG AA как текст; коралловые CTA — тёмный текст через `--ui-on-accent`), шрифт Golos/PT Serif → **Inter**, видимое кольцо фокуса. Редизайн экранов учащегося (домашний, каталог, курс, тесты+результат), входа, профиля + **админки (4 области: учащиеся/группы, учебный контент, документы, аналитика)** — включая высокорычажные правки общего слоя таблиц (`tables.ts`) и фикс ~19 нерабочих классов кнопок/сеток (`ui-button-primary` и т.п. без определения) одной правкой, дашборд-метрики `.stat-card`, переиспользуемый `.kv-list`. Ребрендинг только пользовательских текстов (вордмарк, PWA-манифест, push); внутренний scope пакетов тогда намеренно не трогался (переименован в §5.143). Проверка: typecheck 8/8, ESLint clean, UI smoke-тест, доставка CSS в браузере; без миграций/прав/бэкенда. **Предыдущее:** **2026-06-23 (эта сессия, §5.141):** **LAUNCH_CHECKLIST — закрыты все достижимые из кода defense-in-depth follow-ups** (та же ветка). A1 #1 provider-scoped order lookup (коллизия `providerPaymentId`); A1 #2 amount cross-check (`WebhookEvent.amount` во всех 4 адаптерах + отказ в fulfillment при mismatch); A4 presigned-upload `ContentLength` (S3 enforce-ит размер), webinar webhook ACK через `@Res()` (+`webhookAck?` в `WebinarProvider`), SCORM zip-budget по фактическим инфлейт-байтам. CI-блокер **диагностирован**: GitHub Actions отключены на уровне репо (`actions/permissions.enabled=false`) → включение за владельцем. Тест-статус: 209/28 затронутых кластеров зелёные, typecheck 8/8, lint clean, миграции 0057. Заблокированные пункты (A2 Jitsi-стенд, A3 КриптоПро SDK, B офиц. XSD регуляторов, ops-мониторинг/стенд, юр/бизнес) помечены в [docs/LAUNCH_CHECKLIST.md](docs/LAUNCH_CHECKLIST.md). Подробности — §5.141.

**2026-06-23 (предыдущая сессия):** **Phase 11 — стабилизация `main` + security-аудит + k6-харнесс** (ветка `chore/2026-06-23-phase-11-stabilization-security`, §5.140, коммит `136fd87`). Кодовый roadmap (Phase 1–10 + Pillar A + Wave 1/2 + V1.1) уже слит — единственная неначатая кодовая фаза — Phase 11. **Стабилизация:** снят полный локальный quality-gate, всё зелёное (typecheck 8/8, lint 8/8, contracts 7/7, frontend 639 + 4 env-flaky, backend canonical e2e + IAM + DI-стражи + payments 52 + HTTP-integration 94); фикс флака `testTimeout` 30→60s. **Security-аудит (OWASP, 4 агента):** подтверждены sound подписи вебхуков/ЕСИА/SCORM/AV-гейт/SQL/crypto; **исправлены 3 реально достижимые authz-дыры** — (1) CRITICAL: `migration/backfill` контроллер был полностью без гвардов (неаутентифицированный кросс-тенантный backfill) → shared-secret `WorkerCallbackGuard` (fail-closed); (2) HIGH: `integrations`/`exports`/`sync-logs` были `TenantGuard`-only (стаб-тест маскировал) → `PermissionGuard` + `@RequirePermissions` на каждый handler + право засеяно миграцией **0057**; (3) MEDIUM: magic-link токен логировался в проде → редактируется в `NODE_ENV=production`. Плюс constant-time `WorkerCallbackGuard` (CWE-208). **Phase 11 артефакт:** read-only k6-харнесс `infra/load/` (пороги под NFR). Миграции до **0057**. **Остаток Phase 11:** нагрузочный прогон + числовые NFR (заказчик/ops), мониторинг/backup (ops); defense-in-depth follow-ups (LOW) перечислены в §5.140.

**2026-06-23 (предыдущая сессия):** **Phase 7 — активация онлайн-платежей (multi-provider, per-tenant) поверх дормант-шва** реализована на ветке `feat/2026-06-22-phase-7-payments-multi-provider` (§5.139). Зеркало платёжного шва (§5.133, PR #262) на паттерн Phase 8: одиночный токен `PAYMENT_PROVIDER` заменён **реестром `PAYMENT_PROVIDER_REGISTRY` (Map) + `PaymentProviderResolver.forTenant`** (prod-guard `fake` переехал в резолвер; `provider.id`→`provider.code`; единый источник `PAYMENT_PROVIDER_CODES`). **Миграция 0056** (`payments.payment_provider_settings` — per-tenant НЕсекретный выбор провайдера + право `payments.configure`); per-tenant settings repo (in-memory + postgres) + service. Provider-specific webhook `POST /payments/webhook/:providerCode` + опц. `webhookAck` per provider. **Четыре реальных адаптера** (`infrastructure/payments/`), credential-gated в реестре (пустые креды → адаптер опущен, boot не падает): **ЮKassa** (REST createPayment Basic+Idempotence-Key; webhook = ре-фетч статуса из API + IPv4-allowlist), **Т-Касса/Tinkoff** (/v2/Init копейки, token SHA-256; webhook timing-safe; ACK 'OK'), **CloudPayments** (/orders/create мажорные единицы; webhook HMAC-SHA256 Content-HMAC; ACK {code:0}), **Robokassa** (подписанный redirect md5, без HTTP; ResultURL md5 требует PASSWORD_2; ACK OK{InvId}). env: убран `PAYMENTS_PROVIDER` (выбор per-tenant), добавлены опц. credential-gated cred-vars; `main.ts` `rawBody:true`; `infra/.env.production.example` обновлён. Frontend `/admin/payments/settings` (per-tenant select + enabled) под `payments.configure`, nav перед `/admin/orders`. **Тест-статус (финальный, всё зелёное):** backend payments-кластер **89** (17 файлов), typecheck **8/8**, ESLint clean (backend+frontend payments), frontend payments **12** (2 файла). Миграции до **0056**. **Финальное холистическое ревью** поймало и исправило CRITICAL-баг: `ResponseEnvelopeInterceptor` оборачивал ACK вебхука в `{data,meta}` — эквайеры не видели ожидаемое буквальное тело и уходили в retry-loop; фикс — `@Res()`-direct-send + `if (res.headersSent) return data;`-guard в интерцепторе. Также: CloudPayments `application/x-www-form-urlencoded` парсинг (IMPORTANT), cross-check `payment.provider === providerCode` (IMPORTANT), стейлый комментарий (MINOR). **Go-live остаток:** договор эквайера + боевые креды в env + `PAYMENTS_ENABLED=true` + выбор провайдера тенантом в `/admin/payments/settings`. **Follow-ups (отложены):** (1) `findOrderByProviderPaymentId` без фильтра по `provider` (теор. коллизия короткого Robokassa InvId) → добавить provider-фильтр; (2) webhook не сверяет сумму уведомления со суммой заказа (подпись покрывает сумму серверным секретом — форджа исключена; остаток — экзотический partial); (3) 4 адаптера кодируют контракты эквайеров по тек. знанию — сверить с актуальными доками/песочницей перед go-live (юнит-тесты пинят поведение → правка локальна).

**2026-06-22 (предыдущая сессия):** **Полный зелёный прогон всех сьют + починка DI-guard вскрыла 2 реальных deadlock-бага** (§5.138, ветка `fix/2026-06-21-payments-module-iam-boot`, коммит `19b5a6f`). Закрыта краснота, оставшаяся после §5.137 (§13 Issue 5): статический `di-explicit-injection.test.ts` переписан — теперь сканирует конструктор **каждого** класса с `@Injectable()`/`@Controller()` (а не «первый `constructor(` в файле»). Это убрало 4 ложных срабатывания (Error-класс `InvalidOrderTransitionError` + фабричные ЕСИА-провайдеры) **и одновременно вскрыло 2 настоящих латентных бага**: в [integrations.controller.ts](apps/backend/src/modules/integrations/integrations.controller.ts) `ExportsController` и `SyncLogsController` инъектировали `IntegrationOrchestratorService` по типу **без `@Inject`** → orphaned-promise deadlock в `NestFactory.create()` под `tsx`/esbuild (в проде с `tsc`-метаданными грузилось) → добавлен `@Inject`. Плюс перф-фикс: `payments.http.integration.test.ts` `hookTimeout` 30→120с (импорт реального IAM-графа как DI-токенов даёт ~38с boot на Cyrillic-пути). **Тест-статус (всё зелёное, прогон батчами):** typecheck **8/8**, ESLint clean, frontend **634**, contracts **7**, backend по всем модулям зелёный; единственное исключение — `mvp.domains.http.integration.test.ts` локально `ERR_IPC_CHANNEL_CLOSED` (tinypool+Cyrillic, задокументированный env-краш, в CI зелёный). Без новых миграций/прав (0055).

**2026-06-21 (предыдущая сессия):** **Локальный подъём dev-стека (`pnpm dev:stack`) вскрыл boot-блокер деплой-готовности** (продолжение §5.136). Backend не стартовал: NestFactory падал на DI — `PaymentsModule` (Phase 7, §5.133) объявляет контроллеры с `@UseGuards(PermissionGuard)`, но не импортировал `IamModule` (экспортирует `IamService`/`AuthService`, которые нужны guard'у); CI пропустил, т.к. `payments.http.integration.test.ts` инстанцирует stub-контроллер, а не реальный модуль. **Фикс** — `IamModule` в `imports` [payments.module.ts](apps/backend/src/modules/payments/payments.module.ts) (паттерн `documents`/`org`/`esign`) + постоянный статический страж [permission-guard-module-wiring.test.ts](apps/backend/src/common/permission-guard-module-wiring.test.ts) (любой модуль с `PermissionGuard`-контроллером обязан импортировать `IamModule`/предоставлять `IamService`). После фикса стек поднялся: backend `Nest application successfully started`, `GET /health/ready` зелёный (миграции 56/0, redis/RabbitMQ/MinIO/outbox/secrets healthy), frontend `:3000` HTTP 200. Без новых миграций/прав (последняя 0055). **Пред-существующее (не от правки):** `di-explicit-injection.test.ts` красный (ложные срабатывания на дормант-ЕСИА + `payments.state-machine`) — см. §13 Issue 5. Детали — §5.137.

**2026-06-20 (предыдущая сессия):** **Phase 8 — provider-agnostic webinar seam (dormant, multi-provider + per-tenant)** реализован на ветке `feat/2026-06-20-phase-8-webinar-provider-seam` (§5.134), открывает Phase 8 «Вебинары» на уровне _шва_ поверх ранней CRUD-заглушки в `communication` (таблицы с миграции 0007). Полный цикл brainstorming → spec → план (18 задач) → subagent-driven + backend-ревью + финальное холистическое ревью. **Поправка владельца:** Pruffme НЕ основной → **мульти-провайдер с выбором per-tenant** («своё» = самохостинг Jitsi). Ключевое отличие от платёжного эталона: одиночный DI-токен заменён **реестром стратегий + per-tenant резолвером** (`WebinarProviderResolver.forTenant`); env `WEBINARS_ENABLED` лишь включает подсистему, провайдер берётся из настроек тенанта, **prod-guard `fake` переехал в резолвер**. Seam `WebinarProvider`+Noop(default)/Fake(staging)/Jitsi(каркас); **миграция 0055** (права `webinars.read/write/attend/configure` + таблица `webinar_provider_settings` несекретного конфига + индекс на `provider_session_id`, без изменений таблиц 0007); fail-soft `create`-проводка; unguarded webhook (тенант из строки вебинара) → durable посещаемость (НЕ влияет на завершение — отложено); хардбенинг контроллера (права+DTO). Frontend: `/admin/webinars` + `/admin/webinars/settings` + `/learner/webinars`; снесён legacy `/webinars`. **Ревью поймало:** порядок routeMeta (`/admin/webinars/settings` ПЕРЕД `/admin/webinars`, иначе settings наследует `webinars.read`) → фикс+регресс-тест; мёртвые learner-плитки на `/webinars` → переадресованы. Backend вебинары **37 тестов** (12 файлов) / frontend **16** / typecheck **8/8** / ESLint clean / миграции до **0055**. **Остаток Phase 8 = активация:** реальные адаптеры за реестром (самохостинг Jitsi createRoom+JWT+HMAC, опц. Pruffme/Zoom/BBB) + `rawBody` в `main.ts` + `WEBINARS_ENABLED=true`; отложено: посещаемость→вебинар-часы, per-tenant секреты, записи/.ics/напоминания.

**2026-06-20 (предыдущая сессия):** **Phase 7 — provider-agnostic payment seam (dormant foundation)** реализован на ветке `feat/2026-06-20-phase-7-payment-provider-seam` (§5.133, **слит PR #262**), открывает Phase 7 «Оплаты» на уровне _шва_ по паттерну последних 4 интеграций (AV→esign→export-sign→ЕСИА). Полный цикл brainstorming → spec → план (15 задач) → subagent-driven (implementer + spec/quality-review per task) + финальное холистическое opus-ревью. Новый модуль `modules/payments/`: seam `PaymentProvider`+`Noop`(default)/`Fake`(staging, prod-guard)+токен `PAYMENT_PROVIDER`; env `PAYMENTS_*` (dormant); **миграция 0054** (схема `payments`, деньги integer-копейки, права `payments.read/write/self_purchase`); order/payment домен с line items (позиция = `(group, learner)`); **рабочая ручная отметка безнала** (`mark-paid`) + dormant онлайн-pay через провайдер; идемпотентный fail-soft fulfillment в `createBulkEnrollments`; unguarded webhook (тенант из строки платежа). Frontend: `/admin/orders` + `/learner/payments`. **Финальное холистическое ревью снова поймало CRITICAL** (fulfillment звал request-scoped `MvpService` без `MvpRequestPersistenceInterceptor` → пустой `MVP_STATE` → ноль зачислений при `fulfilled`) → фикс: новый singleton `MvpEnrollmentService` гоняет зачисление через `MvpTenantRunner.runWithTenantStateAndSave` (гидратация+сохранение, реентрантный per-tenant-лок); ключевой тест поднимает реальный runner. Девиация: `course_version_id`→`group_id` (реальный примитив зачисления). Backend payments **46 тестов** / frontend **16** / typecheck **8/8** / ESLint clean. **Остаток Phase 7 = активация:** реальный ЮKassa-адаптер за `PAYMENT_PROVIDER` + `rawBody` в `main.ts` + `PAYMENTS_ENABLED=true`; отложено отдельными spec'ами: чек 54-ФЗ, счёт/акт PDF, checkout-лендинг, рефанды.

**2026-06-17 (предыдущая сессия):** **Phase 6 — КЭП-подпись файлов выгрузок (provider-agnostic seam, dormant)** реализована на ветке `feat/2026-06-17-export-kep-signature-seam` (§5.132, **PR #260**), закрывает **вторую половину гибридной модели владельца** (НЭП на документы + КЭП на выгрузки) на уровне _шва_. Зеркало document-signature seam (§5.129/§5.131) + AV-паттерна, но семантика — **detached `.p7s`** (CMS/PKCS#7) над файлом выгрузки. Полный цикл brainstorming → spec → план (9 задач) → subagent-driven (2 волны по 4 параллельных субагента на интеграцию 5 экспортёров) + inline-эталон ФРДО. Seam: `ExportSignatureProvider`+`Noop`/`Fake`+токен `EXPORT_SIGNATURE_PROVIDER`; env `EXPORT_SIGN_*` (dormant) + **prod-guard** (`fake` запрещён в проде, разрешён в staging); общий **fail-soft** `signExportArtifact` (хранит `.p7s` соседним файлом, сбой не откатывает выгрузку); 3 поля подписи на 5 `*Batch` (**без миграции** — JSON-снимки); инъекция в 5 экспортёров + `GET …/exports/:id/signature` (под `regulatory.export.read`, **без нового права**); frontend — бейдж «Подписано КЭП» в 5 таблицах `gov-export`. **Находки:** вскрыт **пред-существующий красный тест на main** (`env.test.ts` «parses production env» — PR #258/ЕСИА добавил strict-проверку `ESIA_STATE_SECRET` без обновления фикстуры) → фикс слит **PR #261**. Объединённый backend-прогон затронутых сюит **31 файл / 114 тестов**, монорепо typecheck **8/8**, ESLint clean. **Остаток Phase 6 = только активация ЭП** (реальный КриптоПро-адаптер для обоих швов + юр-оформление + ops).

**2026-06-17 (эта сессия, ранее):** **Phase 6 — проброс `signatureStatus` end-to-end + prod-guarded fake staging-signer** реализован на ветке `feat/2026-06-17-document-signature-status-passthrough` (§5.131, **PR #259 слит**), активирует один из follow-up-остатков ЭП-шва (§5.129) на уровне кода. Закрывает «висящий провод»: поля подписи писались на `GeneratedDocumentEntity`, но **никем не читались** — теперь кабинет слушателя (`LearnerDocumentDto.signatureStatus` → бейдж «Подписана НЭП»/«Ошибка подписи», plain `<span>` не StatusChip) и публичная QR-проверка (`PublicVerifyResult.signatureStatus?: 'signed'`, сужено — `failed`/`unsigned` не утекают) показывают статус. Плюс `FakeDocumentSignatureProvider` (синтетический `signed`, self-маркировка `fake-sig://`) — owner-facing превью пайплайна без КриптоПро, **запрещён в `NODE_ENV=production`** env-refinement'ом (staging намеренно разрешён; решение задокументировано+протестировано). **Финальное холистическое ревью: SHIP (0 Critical/Important)** — подтвердило, что MVP-модуль делегирует в `DocumentsService` (тот же entity, что мутирует подпись) → бейдж реально загорится; поля переживают персистентность; прод-гард без лазеек. **Без миграции** (последняя 0053), **без новых прав**. Изолированные прогоны: backend 65/65, frontend 9/9, typecheck 8/8, ESLint clean.

**2026-06-16 (предыдущая сессия):** **ЕСИА (Госуслуги) — вход + идентификация через provider-agnostic OAuth seam** реализован на ветке `feat/2026-06-16-esia-oauth-login-identity` (§5.130), закрывает отложенную задачу Phase 4 «OAuth-интеграция с ЕСИА». Полный цикл brainstorming → spec → план (15 задач) → subagent-driven + финальное холистическое ревью + fix-цикл. **Спящий шов по AV/esign-паттерну** (`ESIA_ENABLED=false` → `NoopEsiaProvider`; `MockEsiaProvider` для dev; `EsiaOidcProvider` — каркас, реальная ГОСТ-подпись = follow-up): два потока — **вход** (резолв слушателя по СНИЛС, аккаунт НЕ создаётся для незнакомца — decision A; сессия через существующий `issueSessionForUser`) и **идентификация** (авто-зачёт 4-го гейта `assertIdentityVerificationGate` записью `IdentityVerification{method:'esia'}`). HMAC-подписанный `state`; **без миграции** (последняя **0053**), **без новых прав** (bootstrap-маршруты). **Финальное холистическое ревью снова поймало ДВА CRITICAL:** C1 — `TenantGuard` отклонял браузерные ЕСИА-маршруты 401 до контроллера; C2 — identity-callback зависел от `context.userId`, которого при редиректе нет → поток был мёртв. **Фикс-редизайн:** callback **полностью state-driven** (tenantId/learnerId внутри подписанного state), identity инициируется SPA через **POST с Bearer**, login — GET; `TenantGuard` exemption для `/auth/esia/*`; + hardening слабого `ESIA_STATE_SECRET` в проде + снят BOM. Повторное ревью: READY TO MERGE. Изолированные прогоны: ЕСИА+guard+env **31+**, IAM-регресс 10/10, frontend 3 + регресс, typecheck 8/8, ESLint clean. Ожидает PR.

**2026-06-15 (эта сессия, поздно):** **Phase 6 — provider-agnostic e-signature seam (НЭП)** реализован на ветке `feat/2026-06-15-phase-6-esign-provider-seam` (§5.129) — закрывает ЭП-остаток Phase 6 на уровне _шва_ (реальный КриптоПро — отдельный follow-up). Проработка ЭП (3 research-агента: код/право/провайдеры) → решение владельца: **гибрид НЭП(документы)+КЭП(выгрузки)** + **provider-agnostic по AV-паттерну**. Ключевая находка права: для ДПО форму документа УЦ утверждает сам (ст. 60 273-ФЗ) → КЭП НЕ обязательна, НЭП по соглашению достаточно; КЭП нужна отдельно — для подписи **файлов выгрузки** в ФИС ФРДО/ЕИСОТ. Реализация (зеркало `AntivirusScanner`): `DocumentSignatureProvider`+`NoopDocumentSignatureProvider`+токен; env `ESIGN_ENABLED`/`ESIGN_PROVIDER`/`ESIGN_SIGNER_NAME` (dormant); 6 полей подписи на `GeneratedDocumentEntity` (**без миграции** — jsonb-снимок); подпись при `finalizeDocument` (**fail-soft**) + ручной `signDocument` (гарды archived/revoked/!isFinal); фабрика в `DocumentsModule` (Noop даже при `cryptopro`); миграция **0053** право `documents.sign`; endpoint `POST /documents/:id/sign`; frontend `LearnerDocument.signatureStatus?`. 9 задач TDD subagent-driven + two-stage review; **финальное холистическое ревью поймало IMPORTANT** (подпись отозванного документа — `revoke` оставляет `isFinal=true`) → гард + тест. Изолированные прогоны: backend-кластер **209/209** (documents.service 52, http 15), frontend learner-documents 6/6, typecheck **8/8**, ESLint clean. Миграции до **0053**. Ожидает PR.

**2026-06-15 (эта сессия):** **Phase 6 — реестры Ростехнадзор + Минздрав-НМО** реализованы на ветке `feat/2026-06-14-phase-6-rostechnadzor-nmo-registries` (§5.126), закрывают реестровый остаток Phase 6 (оставался только ЭП/НЭП, отложен владельцем). Два новых durable-экспортёра как сиблинги трёх существующих (ФРДО/ОТ/ЕИСОТ): **без миграции, без нового права** (`regulatory.export.read/write`), **XLSX-only**, PROVISIONAL-`COLUMNS` со swap-точками. **Ростехнадзор** (`rostechnadzor-registry/`) — источник завершённые+сданные зачисления (ОТ-архетип), swap `attestationArea`. **Минздрав-НМО** (`nmo-registry/`) — источник выданные документы (ФРДО-архетип), swap `specialty`+`creditUnits`/ЗЕТ. Frontend: 2 секции в `app/gov-export/page.tsx`. Полный цикл brainstorming→spec→plan(20 TDD-задач)→subagent-driven (two-stage review на каждую пачку + холистическое финальное ревью; ревью-улучшение `failed`=distinct-сущности; +2 теста-паритета на сервис). Изолированные прогоны: backend 123, frontend 17, typecheck 8/8, ESLint clean. Миграций нет (последняя 0052). Ожидает PR.

**2026-06-14 (docs-only):** **Phase 0 — плейбук готовности к первому пилоту** (ветка `docs/2026-06-14-phase-0-readiness-spec`, §5.125). Некодовый owner-facing артефакт [docs/phase-0/](docs/phase-0/): индекс + 6 направлений (бренд/домен, запуск сервера, модель подписи, пилотный контент, пилотный клиент, лицензия+реестры) — критический путь к первому пилоту, выдающему юридически пригодные документы. После сверки с записью владельца 2026-06-08 (бренд/домен/лицензия/клиенты уже есть) фокус сужен до **НЭП + деплой + пилотный курс + прогон**; ЕСИА/платежи отложены в Phase 7. Backend/frontend нетронуты.

**2026-06-13:** **Phase 10 Track A — Excel-конструктор отчётов** реализован на ветке `feat/2026-06-13-phase-10a-excel-report-builder` (§5.121). Phase 9 полностью слита (Plan A #242 + Plan B #243). Read-model без миграции/нового права: реестр сущностей + чистый `buildReport` + exceljs-writer + 6 endpoints `reports/builder/*` + коллекция `reportTemplates` + страница `/admin/reports/builder`. v1 = ученики + назначения (`documents` отложена, D-A5); XLSX base64-в-конверте без S3 (D-A3). Реализовано последовательно in-process TDD (субагент-диспатч недоступен — `403`). Изолированные прогоны: backend 33 + http 85, frontend 14, contracts 7, typecheck 8/8, ESLint clean. **Слит в `main` (#245).** **Track B — WCAG-доступность** реализован (§5.122, ветка `feat/2026-06-13-phase-10b-wcag-accessibility`): статический гейт `eslint-plugin-jsx-a11y/recommended` в обоих путях линтования + a11y-фиксы 9 общих примитивов `@trudskill/ui` + AppShell live-region; холистическое ревью поймало IMPORTANT-баг (`fieldId` схлопывал кириллицу в один id → дубль-id) → фикс; ui+frontend+lint+typecheck зелёные → **PR #246**. **Track C — PWA + web-push** реализован (§5.123, ветка `feat/2026-06-13-phase-10c-pwa-push`): PWA-манифест + Serwist service worker + web-push (VAPID, всё **dormant** за `WEB_PUSH_ENABLED=false` → `NoopWebPushSender`) + канал push в `NotificationDispatcher` к 5 событиям Phase 5; MVP-коллекция `pushSubscriptions` **без миграции/нового права**. Изолированные прогоны: frontend 566/566, backend таргет 73, typecheck 8/8, ESLint clean → **PR #248**.

**2026-06-11 (предыдущая сессия, вечер):** **Phase 4 Plan B — прокторинг (запись видео итогового теста)** реализована на ветке `feat/2026-06-11-phase-4-plan-b-proctoring` (§5.118), **Phase 4 закрыта целиком** (кроме отложенных владельцем ЕСИА/live-оператора): migration **0051** (`learning.proctoring_recordings` + `requires_proctoring` + per-student `proctoring_override` + права `proctoring.submit/read`), браузерный MediaRecorder → 30-сек webm-чанки → presigned PUT (лимит 10MB files-слоя), **5-й гейт `assertProctoringGate`** в `startAttempt` (412 `proctoring_required`, non-collision с Wave 1/Plan A), согласие 152-ФЗ + превью камеры, `● REC` + stop-on-submit + **resume-баннер после F5**, админ-очередь `/admin/proctoring-recordings` с плеером-склейкой чанков (AV-гейт, presigned GET, предупреждения о разрывах), override-select на странице группы, dormant **365-дневный** retention-cron (`PROCTORING_VIDEO_RETENTION_ENABLED=false`, lock 528_493, write-режим runner + регресс-guard). Финальное холистическое ревью снова оправдалось: **CRITICAL C1 — ретрай чанка запрашивал новый intent → 409 → мёртвый ретрай + фантомный чанк ронял всю сборку видео** (фикс: intent-кэш per-sequence + per-chunk деградация плеера) + I1 (F5 убивал запись навсегда → resume-баннер) + I2 (override был API-only → UI). Миграции теперь до **0051**. PR не создан на момент записи — см. Current Task.

**Предыдущее (2026-06-11, утро):** **Phase 4 Plan A — идентификация личности (селфи + паспорт, ручная сверка админом)** реализована на ветке `feat/2026-06-10-phase-4-plan-a-identity-verification` → **PR #240** (§5.117): migration **0050** (toggle + `learning.identity_verifications` + права `identity.submit/read/review`), MVP-коллекция `identityVerifications`, lifecycle start→upload→submit(согласие 152-ФЗ)→review, **4-й гейт `assertIdentityVerificationGate`** в `startAttempt` (412 `identity_verification_required`; сообщение намеренно не пересекается с Wave 1-регексом), dormant 90-дневный retention-cron изображений (`IDENTITY_IMAGE_RETENTION_ENABLED=false`, advisory-lock 528_492), 7 эндпоинтов, `/learner/identity` + `/admin/identity-verifications` (+`[id]`) + интерстициал в test-player + e2e. Холистическое ревью поймало **CRITICAL: `MvpTenantRunner` read-only** (штампы cron терялись) → добавлен **`runWithTenantStateAndSave`** (write-режим, зеркало `DocumentsTenantRunner`). Прокторинг → отдельный Plan B (не запланирован). Миграции теперь до **0050**.

**Предыдущее (2026-06-10):** Обе ветки 2026-06-08 **слиты в `main`**: Phase 0 деплой-фундамент (**#235**) + Production auth readiness A+B (**#237**; плюс **#236** — boot-fix под tsx). Закрыт follow-up §3.3 плана auth-readiness: `infra/bootstrap-admin.md` приведён к новой реальности (email-доставка magic-link по `NOTIFICATIONS_EMAIL_ENABLED`, авто-`SeedCredentialHygiene` при прод-буте, ручные psql-шаги → подстраховка) → слито **#238**, §5.115. Дополнительно слит **#239** (`DB_MIGRATIONS_ENABLED=''` → `'false'` в 13 тест-харнессах после #236). **Кодовых блокеров пилота не осталось** — следующий шаг deploy-execution владельцем.

V1 roadmap (см. [docs/superpowers/plans/2026-05-21-cdoprof-v1-roadmap.md](docs/superpowers/plans/2026-05-21-cdoprof-v1-roadmap.md)) — **Phase 1 + Pillar A + Phase 2 (A+B+C) + Phase 3 (Plan A + Plan B + Plan C)** реализованы и слиты (Plan C — PR #215). **Wave 1** — **Plan 1 (модульный гейтинг + время)** слит (PR #218), **Plan 2 (аутентификация перед экзаменом №816)** слит (PR #219). **Wave 2 (регуляторные выгрузки)** — **под-цели A (ФРДО #225) + B (ОТ-реестр #222/#223) + C (ЕИСОТ «лица на тестирование» #226) слиты**. **V1.1 антивирус-гейт полностью закрыт** (#216-217, #224). **Phase 5 Plan 5A (notification foundation)** слита в `main` (PR #228): `MailerService` (NoopMailer + SmtpMailer), `email_templates`/`email_deliveries` (миграция 0047), `NotificationDispatcher`, `EnrollmentEmailListener`, admin endpoints, `documents.revoked` event. **Phase 5 Plan 5B (recertification foundation)** реализована на ветке `feat/2026-06-05-phase-5-plan-b-recertification`: per-program validity `recertificationPeriodMonths` + штамповка `valid_until` при выдаче документа + таблица `recertification_drafts` (migration **0048**) + `RecertificationService` (scan→draft→approve/reject, письмо `recertification_due` слушателю/заказчику) + admin endpoints (`recertification.read/write`); планировщик/cron → отдельный план **5B-2**, frontend-очередь «Нужна переаттестация» → план **5C**. **Phase 5 Plan 5B-2 (recertification scheduler)** реализована на ветке `feat/2026-06-06-phase-5-plan-b2-recertification-scheduler`: ночной `@nestjs/schedule` cron (UTC, dormant за `RECERTIFICATION_SCAN_ENABLED=false`) + `pg_try_advisory_xact_lock` + cross-tenant скан (`MvpTenantRunner`); каденс 90/30/7 (`recertification_due`) и 14/7/1 (`course_deadline`) с send-once dedup (`email_deliveries.dedup_key`, migration **0049**); письмо `document_revoked`; `RecertificationScanner` вынесен из request-scoped сервиса. **Phase 5 Plan 5C (frontend-очередь «Нужна переаттестация»)** реализована на ветке `feat/2026-06-07-phase-5c-recertification-queue`: обогащение `GET /recertification-drafts` (`learnerName`/`courseTitle`/`learnerSnils?` через mvp-state резолверы, без миграции) + feature-модуль `features/recertification/` + страница `/admin/recertification` + навигация; объём «только список» (reject + «Проверить сейчас»; approve намеренно не выведен). Стабильный quality gate `pnpm -s ci:check` (Cyrillic-path fallback на isolated backend runs — см. CLAUDE.md Gotchas); миграции до **0049**.

### Current Goal

**Phase 0 — деплой-фундамент пилота** завершён → **PR #235** (ветка `feat/2026-06-08-phase-0-pilot-launch-foundation`): right-sized под соло-владельца — один сервер + Docker Compose + Caddy (авто-HTTPS) + сборка на сервере; 8 артефактов (`apps/frontend/Dockerfile`, `infra/docker-compose.prod.yml`, `infra/Caddyfile`, `infra/.env.production.example`, `.github/workflows/deploy.yml`, `infra/backup.sh`, рунбуки) + harden `scripts/check-env.ts`. **Production auth readiness (A+B)** **слита в `main` (#237)**: A) `EmailMagicLinkEmailSender` — доставка magic-link по SMTP; B) `SeedCredentialHygiene` — прод-хук, обезвреживающий утёкший seed-пароль `Password123!`. Активация ops пилота: `NOTIFICATIONS_EMAIL_ENABLED=true` + реальный `SMTP_*`. Рунбук `infra/bootstrap-admin.md` синхронизирован с этим (§5.115).

### Last Completed Task

**Чтение состояния тенанта одним запросом** (2026-08-09, §5.252): загрузка шла циклом по
полусотне коллекций — полсотни обращений к базе на каждый показ списка. Теперь один запрос.
p95 при 20 сессиях — **0,152 с, требование §12.1 взято**; при 50 — 0,79 с, ещё нет.

Перед этим — **ФАЗА 6 «Эксплуатация» ЗАКРЫТА ПО КОДУ, 11 задач из 11** (2026-08-09, §5.241–§5.251).
Последняя — Task 11: ответы API больше не оседают в кэше браузера (на общем компьютере
класса следующий человек видел бы чужие ФИО и оценки), страница не перезагружается сама во
время экзамена, появилась понятная страница «нет интернета», а тестовый вход через Госуслуги
больше **не может** быть включён в бою — он пускал любого, кто назовёт СНИЛС.

Перед этим — **Task 10, нагрузка по §12** (§5.250). Замер показал, что обычное
**чтение** списка переписывало всё состояние центра: p95 = 30 секунд при требовании 300 мс.
Две точечные правки дали ×13 (30,25 с → 2,32 с), но требование **всё ещё не выполнено** —
и по цифрам принято решение о переделке чтения отдельной задачей. Числа:
[docs/LOAD_TEST_RESULTS.md](docs/LOAD_TEST_RESULTS.md).

Перед этим — **Task 9, чистка таблиц и безопасный старт** (§5.249). Четыре таблицы
росли без единой чистки, журнал аудита читался целиком в память (фильтры применялись уже
там же), а миграции накатывались без блокировки — два экземпляра могли делать это
одновременно. Всё три починено.

Перед этим — **Task 8, экран «Эксплуатация»** (§5.248). Администратор центра сам
видит упавшие задачи выпуска, застрявшие сообщения и журнал писем — и чинит их кнопкой,
без разработчика. Добавлена ручка повторной отправки письма: её не существовало вовсе.

Перед этим — **Task 7, карантин упавших задач** (§5.247). Очередь `jobs.dead-letter`
наполнялась с самого начала, но читать её было некому: неудавшийся выпуск документа пропадал
молча. Теперь такое сообщение видно и переотправляемо, «Повторить» действительно
перезапускает задачу (раньше только меняло статус), зависшие возвращаются в очередь сами, а
экран здоровья читает реальные источники вместо четырёх вечно пустых таблиц.

Метрики снова можно
собрать (конверт API их ломал), ручка закрыта токеном, вместо средних — перцентили,
готовность считает по реальным таблицам, воркер получил проверку живости (был единственным
сервисом без неё), пять планировщиков пишут отметку «отработал», `infra/ops-alerts.sh`
даёт шесть сигналов аварий — каждый проверен искусственной поломкой.

### Current Task

**Фаза 6 «Эксплуатация» закрыта по коду целиком (11 из 11).**
План — [`docs/superpowers/plans/2026-08-08-tz-faza6-ekspluataciya.md`](docs/superpowers/plans/2026-08-08-tz-faza6-ekspluataciya.md), 11 задач.
Ранее: **Фаза 5 закрыта целиком** (§5.240–§5.245, PR #462–#467). В Фазе 6 уже закрыты:
Task 1 — четыре живых дефекта и **утечка ПДн** (слушатель мог выгрузить ФИО/СНИЛС всего
центра через конструктор отчётов, PR #469); Task 3+4 — резервное копирование, учения по
восстановлению, сторож и ротация журналов (PR #470); Task 5+6 — наблюдаемость и тревоги.

### Next Task

**Постраничное чтение списков из базы.** Требование §12.1 «p95 списков < 300 мс» уже
**выполняется на нижней границе диапазона** (0,152 с при 20 сессиях), но не на верхней
(0,79 с при 50). Осталось перестать загружать состояние центра целиком: читать списки
постранично прямо из базы вместо среза массива в памяти. Числа —
[docs/LOAD_TEST_RESULTS.md](docs/LOAD_TEST_RESULTS.md).

Дальше по дорожной карте ТЗ — приёмка фаз владельцем (все шесть фаз закрыты по коду).

**Параллельно ждёт владельца** (сделать без него нельзя):

1. **Включить GitHub Actions** — они выключены на уровне репозитория, поэтому CI не
   запускался с 27 мая (вопрос №A плана Фазы 6).
2. **Поставить cron-строки** из [`docs/BACKUP_ROLLBACK.md`](docs/BACKUP_ROLLBACK.md):
   копии, сторож, учения и тревоги. **До этого копии на стенде не снимаются вообще.**
3. **Задать `METRICS_TOKEN`** в `.env.production` — без него прод-бэкенд не стартует.
4. Решить про хранение копии вне сервера (вопрос №C), DNS `*.<домен>` + wildcard-сертификат,
   приёмка эпика D. Открытый продуктовый вопрос: должен ли методист видеть отчёты.

### Do Not Touch

- Историю миграций в `apps/backend/migrations/*` (без отдельной задачи миграционного изменения).
- Сгенерированные контракты в `packages/api-contracts/src/generated/*` (менять только через генерацию).

### Important Decisions

- Репозиторий: единый **операционный** конспект между агентами — блок `README` + передача сессии в `LMS_AGENT_HANDOFF.md`. Продуктовый канон — `TZ_TRUDSKILL_BASE.md`; роли и порядок чтения при фразе «продолжай по ТЗ» — [docs/DOCUMENTATION_MAP.md](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol).
- Изменения вносятся малыми итерациями с обязательной фиксацией тестового статуса и рисков.
- Следующий высокий приоритет: безопасность и устойчивость IAM + контроль доступа по ролям.

### Known Risks

- Риск рассинхронизации документации и кода при отсутствии регулярного обновления README после каждой итерации.
- Возможные регрессии в auth/session/permission flows при несистемном изменении backend и frontend.
- Наличие широкого backlog по enterprise-operational задачам; требуется приоритизация по критичности (security/reliability first).
- ✅ **Полное приложение бутится; логин работает** (исправлено 2026-06-06, §5.108): DI-«deadlock» был из-за того, что `tsx`/esbuild не эмитит `emitDecoratorMetadata` → type-based DI без `@Inject` виснет. Все инъекции переведены на явный `@Inject`, `CommunicationModule` импортирует `IamModule`, добавлен regression-guard. `pnpm dev:web` → `Nest application successfully started`, логин end-to-end OK.
- ⚠️ **Остаток (Issue 4):** цепочка миграций `0003`/`0004`/… не накатывается на свежую БД из коробки (паттерн «FK на `(tenant_id,id)` до UNIQUE») — нужен consolidated baseline. `DB_MIGRATIONS_ENABLED` (`z.coerce.boolean`) — **исправлен**. Dev-БД развёрнута обходным путём (FK-safe IAM/audit-миграции + запись всех checksum'ов). Детали — [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) §13 Issue 3 (resolved) + Issue 4, §5.108.

### Last Updated By

Claude Code — §5.302: фаза 8 выкатка N — окно совместимости ключей хранения открыто.

### Last Updated At

2026-08-26 (§5.362 — ревизия кода, порция 14: очередь непроверяемого входа закрыта до нуля; итог порций 11–14 — 44 ручки, принимавшие что угодно, теперь проверяют тело, и это держится сторожем).

## 3. Current Project Status

### Что уже сделано

- Сформирована многосервисная архитектура (`apps/frontend`, `apps/backend`, `apps/worker`, `apps/realtime`).
- Добавлены ключевые backend-модули: IAM, документы, e-sign, коммуникации, интеграции, аудит, tenant/workspace.
- Добавлены frontend-страницы и feature-слои для auth, navigation, communication, integrations, MVP-сценариев.
- Есть инфраструктурные SQL-миграции, включая IAM/security и operational индексы.
- Подготовлены базовые runbook/observability/testing документы.

### Какие задачи выполнены (на уровне репозитория)

- Базовый foundation монорепозитория и окружения.
- Контрактный слой API и shared types packages.
- Набор unit/integration/e2e тестов по ключевым доменам (по структуре репозитория).

### Какие задачи в работе

- Поддержание синхронности README ↔ `LMS_AGENT_HANDOFF.md` после каждой инженерной сессии ([протокол](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)).

### Что делать следующим шагом

1. Открыть [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) §14 Recommended Next Steps и §20 Final Status.
2. Сверить с [docs/TZ_MVP_TRACEABILITY.md](docs/TZ_MVP_TRACEABILITY.md) и при необходимости с §41 [TZ_TRUDSKILL_BASE.md](TZ_TRUDSKILL_BASE.md).
3. Запустить `pnpm -s ci:check` перед и после значимых изменений; обновить этот README и handoff.

## 4. Iteration Log (текущая итерация)

### Измененные файлы

- `README.md`, `docs/DOCUMENTATION_MAP.md`, `LMS_AGENT_HANDOFF.md`, `TZ_TRUDSKILL_BASE.md`, `AGENTS.md`, `.cursor/rules/lms-multi-agent-handoff.mdc` (протокол многоагентной передачи, Cursor rule `alwaysApply`, устранение противоречий README vs handoff).

### Принятые решения в итерации

- Один смысл «где остановились»: детали в `LMS_AGENT_HANDOFF.md`, краткое резюме в `README` §2; фраза «продолжай по ТЗ» → порядок в [DOCUMENTATION_MAP.md](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol).

### Проверки в итерации

- Сверка содержимого README, handoff и карты документации на логическую согласованность (без обязательного полного `ci:check` только для правок markdown).

## 5. Backlog (приоритезированный)

Укрупнённый продуктовый backlog и BL — только в [TZ_TRUDSKILL_BASE.md](TZ_TRUDSKILL_BASE.md) §41 и матрице [docs/TZ_MVP_TRACEABILITY.md](docs/TZ_MVP_TRACEABILITY.md). Ниже — **репозиторные** приоритеты, если не задан иной порядок:

1. **P0 Security/Auth:** см. [docs/security-remediation-roadmap.md](docs/security-remediation-roadmap.md) и handoff §10.
2. **P0 Reliability:** health/readiness/metrics, [docs/operations-runbook.md](docs/operations-runbook.md).
3. **P1 UX Learning Flow:** состояния загрузки/ошибок в learning flow.
4. **P1 Reporting:** сценарии KPI/экспорта (см. BL-008 в трассировке).
5. **P2 Integrations:** idempotency/observability webhooks.

## 6. Test Status

### Что уже проходило (по доступным артефактам репозитория)

- В репозитории присутствуют unit/integration/e2e тесты для backend/frontend/packages.
- Общий запуск проверок описан в quick start и `docs/run-tests.md`.

### Статус текущей итерации

- Запущен consolidated regression для IAM/role-access + permission-boundary HTTP integration:
  - `pnpm exec vitest run apps/frontend/src/e2e/lms-role-flows.e2e.test.ts apps/frontend/src/e2e/role-access.e2e.test.ts apps/frontend/src/e2e/auth-routing.e2e.test.ts apps/backend/src/modules/iam/permission.guard.test.ts apps/backend/src/modules/iam/auth.security.test.ts apps/backend/src/modules/workspace/workspace.http.integration.test.ts apps/backend/src/modules/documents/documents.http.integration.test.ts`
  - Результат: success, 7 files / 23 tests passed.

## 7. Known Issues / Open Errors

- **V1.1 AV-гейт ставится с `ANTIVIRUS_ENABLED=false`** — `NoopAntivirusScanner` помечает файлы `clean` (dev/пилот). Реальная защита требует поднятого clamd + `ANTIVIRUS_ENABLED=true` (ops, spec §9). Файлы, загруженные до V1.1, остаются `pending` и сканируются лениво при первом скачивании. Задачи 7-13 V1.1 (проактивный скан при submit, `antivirusStatus` в чтении сабмишена + очереди ревью, HTTP-гейт boundary, гейтинг кнопки скачивания у ревьюера, статус у слушателя) — **выполнены** (handoff §5.102, ветка `feat/2026-06-02-v1.1-antivirus-tasks-7-13`). Единственный остаток — ops: поднять clamd и включить флаг.
- Для полной картины по тестам и командам см. таблицу в [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) §12; здесь держите только краткое резюме после крупных прогонов.
- Регулярно синхронизировать `AI Agent State` с handoff ([протокол](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)).

## 8. Quick Start

1. Скопировать env: `cp .env.example .env`.
2. Поднять сервисы: `docker compose -f infra/docker-compose.yml up -d --build`.
3. Запустить проверки: `pnpm test`.

Backend при старте применяет SQL миграции (`apps/backend/migrations`) и baseline seed IAM при `DB_MIGRATIONS_ENABLED=true`.

## 9. Operational Endpoints

- Backend: `/api/v1/health/live`, `/api/v1/health/ready`, `/api/v1/health/startup`, `/api/v1/metrics`.
- Realtime: `/health`, `/ready`.
