# LMS / СДО Project README

## 1. Project Overview

CDOProf — монорепозиторий LMS/СДО платформы для обучающихся, преподавателей/кураторов и администраторов.

### Основные роли

- Learner (обучающийся)
- Teacher/Curator (преподаватель/куратор)
- Admin (администратор)

### Основные модули

- Аутентификация, сессии, роли/права (`apps/backend/src/modules/iam`, `apps/frontend/src/features/auth`)
- Курсы/обучение и смежные MVP домены (`apps/backend/src/modules/mvp`, `apps/frontend/app/courses*`, `apps/frontend/app/learner*`)
- Документы и e-sign (`apps/backend/src/modules/documents`, `apps/backend/src/modules/esign`)
- Коммуникации (чат, уведомления, вебинары) (`apps/backend/src/modules/communication`, `apps/frontend/app/chat`, `apps/frontend/app/notifications`, `apps/frontend/app/admin/webinars`, `apps/frontend/app/learner/webinars`)
- Интеграции (`apps/backend/src/modules/integrations`, `apps/frontend/app/integrations`)
- Аналитика/аудит/метрики (`apps/backend/src/modules/audit`, `apps/backend/src/common/metrics`)

### Текущий стек

- Monorepo: pnpm workspace + Turborepo
- Frontend: Next.js + TypeScript
- Backend: NestJS + TypeScript
- Realtime service: Node.js/TypeScript
- Worker service: Node.js/TypeScript
- DB migrations: SQL в `apps/backend/migrations`
- Контракты/типы: `packages/api-contracts`, `packages/shared-types`

### Важные ограничения

- Работать итеративно и минимальными безопасными изменениями.
- **`README.md` — канон по точке входа в репозиторий:** актуальный **AI Agent State**, команды и ссылки на E2E/CI (см. [docs/DOCUMENTATION_MAP.md](docs/DOCUMENTATION_MAP.md) — кто главный для ТЗ, трассировки и handoff; **несколько агентов** — [протокол передачи](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)).
- Перед следующими крупными изменениями нужно валидировать критические сценарии IAM/доступов и multitenancy.

### Ссылки на обязательные документы

- Агенты (несколько сессий, «продолжай по ТЗ»):
  - [`AGENTS.md`](AGENTS.md)
  - [`.cursor/rules/lms-multi-agent-handoff.mdc`](.cursor/rules/lms-multi-agent-handoff.mdc) (правило Cursor, `alwaysApply`)
  - [`docs/DOCUMENTATION_MAP.md` — протокол передачи](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)
- Требования и пилот (продуктовый канон и приёмка):
  - [`SDOPROF_TZ_FINAL.md`](SDOPROF_TZ_FINAL.md)
  - [`docs/TZ_MVP_TRACEABILITY.md`](docs/TZ_MVP_TRACEABILITY.md)
  - [`docs/DOCUMENTATION_MAP.md`](docs/DOCUMENTATION_MAP.md)
- Полное техническое задание / архитектурный контекст:
  - `docs/architecture-overview.md`
  - `docs/mvp-domain-database.md`
  - `apps/backend/docs/mvp-domain-database.md`
- Текущее состояние и план работ:
  - `docs/ENTERPRISE_OPERATIONAL_PLAN.md`
  - `docs/ENTERPRISE_OPERATIONAL_NEXT_STEPS.md`
  - `docs/ENTERPRISE_OPERATIONAL_COMPLETION_REPORT.md`
  - `docs/ENTERPRISE_OPERATIONAL_REMAINING_GAPS.md`
- Тестирование и регламенты проверок:
  - `docs/testing/test-strategy-stage-13.md`
  - `docs/run-tests.md`
- Безопасность/надежность/операции:
  - `docs/security-remediation-roadmap.md`
  - `docs/operations-runbook.md`
  - `docs/backup-and-restore.md`
  - `docs/observability.md`

### Канонический E2E для приёмки §39 ТЗ

- Backend (бизнес-потоки без браузера): [`apps/backend/src/modules/mvp/business-flows.e2e.test.ts`](apps/backend/src/modules/mvp/business-flows.e2e.test.ts), сертификат по завершению: [`apps/backend/src/modules/documents/enrollment-certificate-flow.service.test.ts`](apps/backend/src/modules/documents/enrollment-certificate-flow.service.test.ts).
- Backend HTTP integration (envelope, `TenantGuard`, permission boundaries, доменные инварианты assessment; дополняют BL-010 в CI): [`mvp.http.integration.test.ts`](apps/backend/src/modules/mvp/mvp.http.integration.test.ts), [`mvp.domains.http.integration.test.ts`](apps/backend/src/modules/mvp/mvp.domains.http.integration.test.ts), [`mvp-internal-worker.http.integration.test.ts`](apps/backend/src/modules/mvp/mvp-internal-worker.http.integration.test.ts) (worker callback `…/internal/worker/mvp/bulk-enrollments`, BL-003), [`documents.http.integration.test.ts`](apps/backend/src/modules/documents/documents.http.integration.test.ts) (**GET** **`documents.read`**; **POST** → **`documents.write`** или **`documents.generate`** (**…/documents/generate**); **PATCH** / **PUT** / **DELETE** → **`documents.write`**, регресс **templates/:id** в harness), [`esign.http.integration.test.ts`](apps/backend/src/modules/esign/esign.http.integration.test.ts) (**POST:** submit, reuse-check (**read**), **applications/(approve|reject)**, start-review; **PATCH** **applications/:id** (**write**), **participants/:id** (**processes.write**); **processes** POST/start/**cancel**; **participants** POST (create)/invite/sign/skip/mark-viewed/**reject** (контуры **`processes.write`** / **`sign`**); application-files POST/verify/reject/**DELETE**; **GET:** **applications/:id**, application-files и **application-files/:id**, **legal-log** и **legal-log/:id**, **processes**, **processes/:id**, **processes/:id/status**, **participants**, **events**, **events/:id**), [`workspace.http.integration.test.ts`](apps/backend/src/modules/workspace/workspace.http.integration.test.ts), [`integrations.http.integration.test.ts`](apps/backend/src/modules/integrations/integrations.http.integration.test.ts) (**GET** → **`integrations.read`**; **POST**/**PATCH**/**PUT**/**DELETE** → **`integrations.write`**, стаб **…/integrations/providers/:id**); liveness (**без auth**): [`health.http.integration.test.ts`](apps/backend/src/modules/health/health.http.integration.test.ts); IAM: [`auth.http-regression.e2e.test.ts`](apps/backend/src/modules/iam/auth.http-regression.e2e.test.ts).
- Frontend (маршруты / роли): [`apps/frontend/src/e2e/lms-role-flows.e2e.test.ts`](apps/frontend/src/e2e/lms-role-flows.e2e.test.ts), при необходимости — [`apps/frontend/src/e2e/canonical-e2e-readiness.e2e.test.ts`](apps/frontend/src/e2e/canonical-e2e-readiness.e2e.test.ts).
- Регресс качества: `pnpm -s ci:check`. Отдельный Playwright/Cypress при появлении требования — не заменяет указанные Vitest-наборы до явного решения команды.

## 2. AI Agent State

Состояние ниже должно совпадать по смыслу с [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) (факты сессии, файлы, тесты). Подробный лог — там §5–§20; здесь — краткий ориентир для следующего агента.

> 📋 **Сводный статус всех 17 планов** (что слито / что осталось, карта PR→план) — [docs/superpowers/plans/PLANS_STATUS.md](docs/superpowers/plans/PLANS_STATUS.md) (сверено по git + коду, 2026-05-31).
>
> 🚀 **Чек-лист до запуска пилота** (что осталось по всему проекту, разбито по исполнителю dev/ops/юрист/владелец) — [docs/LAUNCH_CHECKLIST.md](docs/LAUNCH_CHECKLIST.md) (2026-06-23, после §5.140).

### Current Stage

**2026-07-02 (текущее, §5.162, ветка `feat/2026-07-02-ui-phase-1-design-system`, PR #299):** **UI redesign Фаза 1 — дизайн-система `packages/ui` (12/12 задач плана).** По согласованному аудиту Фазы 0 (91 страница → 10 блоков ИА). Токены: `baseVars` (CSS-мост `--ui-space/radius/font-*`) в `UiThemeProvider` (`buildThemeVars` + стражи); все радиусы 7 CSS-слоёв на `var(--ui-radius-*)` + тест-страж дисциплины; вне-шкальные нормализованы (единственное видимое изменение). Иконки: `lucide-react@^1.23.0` (ui+frontend) + компонент `Icon` (шкала 16/18/20/24, a11y-контракт). Новые компоненты-обёртки над существующим CSS: `Button` (variant/loading/icon), `Skeleton` (план скорректирован: контейнер+линии вместо невидимого kind=block), `StatCard`, `KeyValueList`, `Callout` (+override role для статичных баннеров). Фиксы: `DateRangeField` в контракте ui-field + aria; русские дефолты состояний. Маршруты/экраны/RBAC не тронуты; ветка без миграций/новых прав (последняя в main — 0059). Каждая задача через spec+quality ревью (2 Important-фикса применены); финальное холистическое ревью «Ready to merge». Гейты: ui 16 файлов/53 теста, frontend typecheck + 106 файлов/646 тестов — всё зелёное. Запись изначально §5.155, перенумерована в §5.162 при merge (аудит-ветки заняли §5.154–§5.161; их фикс #5 уже закрыл битый `/learner/documents` из аудита Фазы 0). **Дальше:** Фаза 2 — сгруппированный сайдбар (10 блоков) + палитра Ctrl/⌘+K + крошки; остальные nav/RBAC-баги — отдельным PR. Детали — §5.162.

**2026-06-30 (предыдущее, §5.161, ветка `fix/2026-06-29-audit-7-bug-fixes`, HEAD `ae58977`):** **Новый аудит-проход — 6 латентных багов (3 HIGH + 1 MEDIUM + 2 LOW) + esign-хардненинг + cleanup, все по TDD; диф проверен 4-линзовым adversarial-workflow.** **HIGH:** (1) очередь bulk-enrollment (worker callback) гоняла request-scoped `MvpService` поверх ПУСТОГО `MVP_STATE` → зачисление молча терялось → проводка через `MvpEnrollmentService` (гидратация+сохранение); (2) `finalizeDocument` воскрешал аннулированный-но-`generated` документ (revoke оставляет `isFinal=false`) → guard `revoked` (достижимо через esign `tryCompleteProcess`); (3) дашборд слушателя показывал «Пройден»/«Не пройден» до ручной проверки эссе (сырой `attempt.passed`) → `submitted` пока pending-review (радиус только дисплей, registries читают `ExamResult`). **MEDIUM:** (4) `rejectParticipant` без `assertProcessMutable` → ложная пост-отменная запись в legal-log; (5) `tryCompleteProcess` валидировал документ ПОСЛЕ мутации в `signed` → validate-before-mutate (revoked/archived → `failProcess`). **LOW:** (6) `inviteParticipant`/`markViewed` без того же guard; (7) module/course `completedAt` дрейфовал вперёд → монотонный latch. **Cleanup:** удалён мёртвый `finalizeAttempt`. **⚠️** параллельная сессия откатила `mvp.service.ts` во время ревью — ревью поймал, перенакатил+закоммитил. **Verified:** typecheck **8/8**, ESLint чисто, затронутые backend-сьюты **258** зелёные; без миграций (0059)/новых прав. **Отложено:** CloudPayments `providerPaymentId` mismatch (сверка с песочницей эквайера).

**2026-06-30 (§5.160, ветка `fix/2026-06-29-audit-7-bug-fixes`):** **Закрыты все follow-up'ы §5.154 (§5.155–§5.159) + новый аудит-проход §5.160 (7 багов: 3 HIGH + 3 MEDIUM + 1 LOW).** §5.155 group-order дубль-серт · §5.156 провизорный `ExamResult` · §5.157 cannot-unset clear-vs-keep (**PR #294** — параллельная сессия) · §5.158 `resolvePermissions` dead conditional · §5.159 `GroupCourse` version pinning · **§5.160 кросс-доменный аудит:** эссе-bypass ручной проверки (HIGH), IDOR enrollment/progress (HIGH), permission-map дрейф → backend-гидрация прав (HIGH), `mark-paid` идемпотентность (**migration 0059**), немонотонный прогресс материала, webhook verify-order, + (глубокий 2-й проход) `completeAttemptReview` completeness. **Отложено:** CloudPayments `providerPaymentId` mismatch (сверка с песочницей эквайера). **Verified:** typecheck **8/8**, backend key-сьюты **279** + frontend full **651** зелёные; migration 0059, без новых прав. _История ветки: origin §5.155–§5.157 (PR #294) + локальные §5.158–§5.160; redundant локальный §5.157 (edit-payloads) отброшен в пользу origin payloads.ts при rebase._

**2026-06-29 (§5.154, ветка `fix/2026-06-29-audit-7-bug-fixes`):** **Новый аудит-проход «максимум багов» — 8 подтверждённых (2 CRITICAL + 3 HIGH + 3 MEDIUM), кросс-доменный, все по TDD.** 6 параллельных subagent-разведчиков (iam/auth · mvp bulk · documents/esign · assessment · communication/org · frontend); каждая находка перепроверена чтением кода. **CRITICAL:** (1) esign `tryCompleteProcess` финализировал регулируемый документ при НУЛЕ подписей (все `skipped`) + воскрешал `cancelled`→`signed` через поздний skip → guard'ы `assertProcessMutable`/`in_signing`/«≥1 signed»; (2) `BulkImportIdempotencyRecord` без `id` ронял Postgres-снапшот (NOT NULL PK) после каждого bulk-import → добавлен `id`. **HIGH:** (3) magic-link TOCTOU (две сессии по одному single-use токену) → `markConsumed` возвращает affected-rows; (4) `TenantGuard` решал по `request.url` с query-string → `?redirect=/auth/esia/` обходил гард → решение по PATH (`split('?')`); (5) nav-ссылка `/learner/documents` без `routeMeta` → 404 у слушателя → добавлена запись + инвариант-тест; (6) `finishAttempt` запирал эссе-попытку из ручной проверки (замораживал на 0, выбивал из reviewer-queue) → остаётся `submitted` при `autoGraded===false`; (7) гейт итогового экзамена собирал модули со ВСЕХ версий (draft/archived запирали учащихся published) → scope PINNED>PUBLISHED. **MEDIUM/latent:** (8) deadline-`dedupKey` без `plannedEndAt` (как §5.150 license-fix). Затронутые наборы зелёные (esign 79, magic-link 20, guards 8, module-gating 7, learners-bulk-import 39, mvp.service 101, business-flows.e2e 4, navigation 15, postgres-persistence 1), **typecheck 8/8** (backend+frontend), ESLint чисто. **Без миграций (0057), без новых прав.** Отложено (follow-up-задачи): frontend cannot-unset, payment retry dead-end, esign group-order дубль-серт, провизорный ExamResult до проверки эссе, in-memory resolvePermissions (non-prod), pinning `GroupCourse.courseVersionId` не подключён.

**2026-06-29 (предыдущее, §5.153, HEAD `0be206e` / PR #289):** **Пред-пилотная стабилизация завершена.** Закрыт отложенный follow-up §5.152: bulk-import reuse-детекция теперь покрывает тенантов >10k слушателей (целевой `MvpService.findLearnersByEmailOrSnils` вместо `listLearners(page_size: 10_000)` → совпадение за границей больше не теряется и не плодит молчаливый дубль). Перед этим §5.152 — латентный аудит, 7 фиксов (2 HIGH + MED-HIGH + 3 MED + frontend data-loss). Полный `pnpm test:backend`/frontend зелёный, typecheck 8/8, ESLint чисто; без миграций (последняя 0057) / без новых прав. **Предыдущее (§5.151):** **Новый аудит-проход по непрочёсанным доменам — 8 подтверждённых багов (1 CRITICAL + 2 HIGH + 5 MEDIUM) + 2 LOW-hardening** (ветка `fix/2026-06-28-audit-worker-retry-exam-timeout-ot-counter`). LOW (follow-up #7/#8): webhook-верификатор переведён на constant-time сравнение (sha256+`timingSafeEqual`); удалён мёртвый дублёр-грейдер `calculateAttemptScore`. Спорный follow-up #6 (bulk-идемпотентность без request-hash) оставлен намеренно — стандартный контракт идемпотентности, не баг. Фикс #8 (bulk-import, MEDIUM, follow-up #5): две строки, резолвящиеся в одного существующего слушателя по разным полям (email vs СНИЛС), затирали друг друга в `learnerIdToRowNumber` (двойной счёт `reused`, потеря enrollment у первой); фикс — вторая помечается `duplicate_in_file`/failed (learners-bulk-import 38). Фикс #4 (esign-«кирпич», HIGH, follow-up #1): `startProcess` мутировал `status='prepared'` до guard'а участников → процесс без участников застревал навсегда; фикс — «validate-first, mutate-last». Фикс #5 (`finalizeDocument` идемпотентность, MEDIUM, follow-up #2): два процесса подписи на один документ дважды накладывали подпись + дублировали critical-audit; фикс — `if (doc.isFinal) return` (convergence-point, бонусом не «разотзывает» отозванный документ). Фикс #6 (esign roster-guard, MEDIUM, follow-up #3): `assertProcessMutable` пускал изменение состава подписантов на `in_signing`/`failed`; фикс — разделил guard'ы (`assertProcessRosterMutable` только `draft`/`prepared`, подпись по-прежнему пускает `in_signing`). documents+esign **300/300**. Фикс #7 (assessment exam-result, MEDIUM, follow-up #4): два финализатора писали разные поля (`finalScore` vs `bestScore`) в одну запись `ExamResult` → consumer читал `undefined` → NaN; фикс — оба пишут оба синонимичных поля + `passingScore` (test-player 14). «продолжай улучшать код» после закрытия аудит-хвоста (§5.150). 4 параллельных subagent-разведчика по доменам, не покрытым прежними аудитами (esign / assessment / bulk+worker / IAM+integrations); каждая находка перепроверена чтением кода; все фиксы по TDD. (1) **CRITICAL** — воркер (`apps/worker/src/main.ts`) звал `markProcessed` **до** `processJob` → любая bulk-enrollment, упавшая на транзиентной ошибке, **терялась навсегда** (ретрай пропускался как дубликат); фикс — вынесен тестируемый шов `message-consumer.ts`, порядок «проверить → обработать → пометить после успеха» (at-least-once; бэкенд идемпотентен по `idempotencyKey`). (2) **HIGH** — `submitAttempt` перезатирал `expired→submitted` → просроченная попытка принималась как `passed` (обход таймаута); фикс — финализация как `expired` + early-return (зеркало `finishAttempt`). (3) **MEDIUM** — ОТ-экспортёр считал объекты ошибок вместо сущностей (`failed`/`total` завышались на комплексных курсах); фикс — distinct-счёт по сиблинг-паттерну rostechnadzor/eisot. typecheck **8/8**, целевые сьюты зелёные (worker 15, test-player 13, mvp.service 100, ot-registry 34, business-flows.e2e 4), ESLint чисто. **Без миграций (0057), без новых прав.** 8 подтверждённых follow-up'ов (esign brick HIGH и др.) задокументированы в §5.151.

**2026-06-27 (предыдущая сессия, §5.148):** **Аудит латентных багов — 5 подтверждённых (3 CRITICAL + 2 HIGH)** (ветка `fix/2026-06-27-audit-latent-bugs`). Кодовый roadmap (Phase 0–11) слит → владелец выбрал предпилотный аудит. 5 параллельных subagent-разведчиков, **каждая находка проверена чтением кода** (ложные отсеяны). Найдено и починено по TDD: (1) CRITICAL — DB CHECK `payments.payments.provider` допускал лишь yookassa из 4 эквайеров → tinkoff/cloudpayments/robokassa падали на реальном Postgres (**migration 0058**, проверено на throwaway-БД); (2) CRITICAL — публичная QR-проверка читала пустой request-scoped state → любой реальный QR давал `not_found` (кросс-tenant durable-поиск по `qrToken`); (3) CRITICAL — `getAttemptResult` (GET) сбрасывал `passed→false` для `submitted`-попыток из-за расхождения фильтров двух финализаторов; (4) HIGH — лицензионный гейт публикации игнорировал `validUntil` (просроченная active-лицензия проходила §5.10 вечно); (5) HIGH — частичное исполнение платежа метило `enrolled` несозданные места и флипало заказ в `fulfilled`. typecheck 8/8, затронутые сьюты зелёные, ESLint clean. **Migration 0058**, без новых прав.

**2026-06-27 (эта сессия, §5.147):** **Documents — фикс латентной коллизии нумерации при сбросе периода** (ветка `claude/hungry-margulis-071cfe`, PR #280). `DocumentsService.reserveNumber` при `resetPeriod:'year'|'month'` после смены периода сбрасывал счётчик, но форматировал номер **без токена периода** → `#000001` нового года коллидировал с прошлогодним → `ConflictException`, проглатываемый fire-and-forget `EnrollmentDocumentIssuanceListener` → регулируемый документ **молча не выпускался**. Фикс (выбор владельца «год в номере»): токен `{period}` → `CERT-2026-000001` (глобально уникален + счётчик реально сбрасывается) + **validate-before-commit** (проваленная резервация больше не прожигает счётчик). TDD +3 теста (`vi.setSystemTime` как clock-seam); documents.service **57** + смежные **43** зелёные, typecheck 8/8, ESLint clean. **Без миграции (0057)/новых прав.** _Merge-заметка:_ ветка несёт вперёд §5.146 (refactor `029ccdc`), не попавший в squash PR #279.

**2026-06-26 (эта сессия, §5.146):** **Phase 5C-2 — ревью-проход по качеству** (та же ветка, без изменения поведения). Многоугловое ревью диффа ветки: отклонены ложные срабатывания (NestJS сам резолвит промис из обработчика → «missing await» в контроллерах — не баг; `useEffect`-seed в notification-settings безопасен из-за `refetchOnWindowFocus:false`). Применены 3 низкорисковых улучшения: `buildStaffRecipients` вынесена из dispatch-циклов в `course-deadline`/`recertification`-сканерах (приведено к паттерну `license-expiry-scanner`), убран избыточный голый провайдер `PostgresLicensesRepository` в `org.module`. Тесты затронутых модулей зелёные (20+11), typecheck чист. Коммит `029ccdc`.

**2026-06-26 (ранее в этот день, §5.145):** **Phase 5C-2 (продолжение) — durable-персистенция лицензий + `license_expiring` cron** (та же ветка/PR). Закрыт последний отложенный хвост уведомлений. **Находка:** `org`-модуль имел **нулевую персистенцию** — лицензии жили в `Scope.REQUEST` in-memory и терялись между запросами, а таблица `org.training_licenses` (миграция 0035, с partial-индексом под expiry-job) **не использовалась** → лицензионный гейт публикации курса (Pillar A §5.10) был фактически инертен в проде. **Сделано:** durable-репозиторий поверх 0035 (токен `LICENSES_REPOSITORY` + in-memory/postgres impl, прецедент `recertification-drafts`); `LicensesService` → async на репозитории; `publishCourseVersion` стал async; `OrgModule` снят с request-scope. **Поведенческая импликация (фикс задуманного, не регресс):** публикация курс-версии теперь требует **сохранённую** активную лицензию — владельцу нужно один раз внести лицензию центра перед публикацией пилотных курсов. **`license_expiring`:** новый staff-facing шаблон + `LicenseExpiryScanner` (90/30/7 по `valid_until`, аудитория = настроенный staff-ящик из §5.144, opt-in) встроен в `RemindersSchedulerService`. TDD; гейт (изолированно): org **41**, mvp.service **95**, reminders+DI **42**, business-flows+communication **104**, typecheck чисто, ESLint clean. **Без новой миграции (0035 уже была), без новых прав.**

**2026-06-26 (ранее в этот день, §5.144):** **Phase 5 — хвосты уведомлений (5C-2): approve-в-UI + staff-получатели** (ветка `feat/2026-06-26-phase-5c-2-notification-tails`). Кодовый roadmap (фазы 0–11) уже слит — взяты два неначатых под-хвоста Phase 5 по выбору владельца. (1) **Approve в очереди переаттестации** (ранее намеренно скрыт, «5C-2 follow-up»): кнопка «Перезачислить» + модалка выбора целевой группы (`useGroupsList`, зеркало `issue-order-modal`) проводят уже протестированный backend-эндпоинт `POST /recertification-drafts/:id/approve`; новый contract-тест. (2) **Curator/admin получатели**: настраиваемый per-tenant список email сотрудников (opt-in), хранится в **MVP-снимке** (новая коллекция `notificationStaffRecipients`, зарегистрирована в `mvp-collections.ts`, **без миграции/нового права** — переиспользует `notifications.read/write`), читается ночным cron через `MvpTenantRunner` без coupling к IAM; helper `buildStaffRecipients` → `kind:'admin'` добавлен в **тот же** `dispatch` (send-once-дедуп покрывает всех) в 3 событиях — переаттестация / дедлайн / отзыв документа (НЕ «завершён курс», по решению владельца); guard сменён на комбинированный (staff уведомляются даже без email слушателя); `MvpService.get/setNotificationStaffRecipients` (нормализация+dedup+аудит), DTO, контроллер `NotificationRecipientsController` под MVP-интерсептором, экран `/admin/notification-settings` + навигация. TDD на каждый юнит (RED→GREEN). **Гейт (изолированные прогоны):** backend reminders/recert/communication **155**, сервис **5**, HTTP-integration **97** (+4 границы прав), DI-guard+wiring **2**, frontend **232** (вкл. e2e-роутинг), typecheck 8/8, ESLint clean. **Без миграции (0057), без новых прав.** _Замечание:_ рабочее дерево на старте уже содержало незакоммиченную заготовку этой задачи (untracked) — перезаписана TDD-реализацией. Отложено: `license_expiring` (нужна postgres-персистенция лицензий из in-memory).

**2026-06-24 (предыдущая сессия, §5.143):** **Завершение редизайна UI + переименование внутренних пакетов `@cdoprof/*` → `@trudskill/*`** (ветка `chore/2026-06-23-phase-11-stabilization-security`, не закоммичено). (1) **Этап 0 — аудит единообразия:** ~93 маршрута, ~86% уже на `@trudskill/ui`; легаси-долг локализован (4 параллельных агента-разведчика). (2) **Этап 1:** дизайн-система уже укомплектована для LMS (карточка курса `.course-card`, дашборд `.stat-card`/`.ui-hero`, плеер/SCORM `.course-viewer-*`, тест `.ui-option`/`.test-result__banner`, модалка `Modal`/`Dialog`) — строить нечего, новых компонентов не вводил. (3) **Этап 2 — миграция 14 экранов** на существующие `.ui-*`-классы: `verify` (публичная, была полностью вне ДС с хардкод-цветами), мегафайл `mvp/screens.tsx`, 2 самописные модалки → `.ui-modal*`, `licenses`/`issuance-journal`/`report-builder`/`identity-verification`/`proctoring` (`#c00`→`.ui-callout--danger`), `theme-appearance-settings` и др.; финальный греп — **0 хардкод-цветов** во фронтенде; логика/контракты/бэкенд не тронуты. (4) **Этап 3:** UI-текст уже без старого бренда (проверено грепом); по выбору владельца выполнено **переименование scope 8 пакетов** (`ui/frontend/backend/api-contracts/shared-types/worker/realtime/test-utils`) + 962 ссылки в 125 build-файлах (импорты, `tsconfig`/`vitest`/`next` алиасы, Dockerfile, CI, root-скрипты) + `pnpm install` (lockfile регенерирован); история в `docs/superpowers/**` + handoff намеренно сохранена с `@cdoprof`. Гейты: typecheck **8/8**, lint **8/8** (поправлен 1 `import/order` из-за смены алфавитной позиции scope), contracts **7/7**, frontend **643/643**, backend integration-smoke зелёный. **Без миграций** (0057)/новых прав. Остаток Этапа 3 — переименование GitHub-репозитория/remote (`cdoprof`) делает владелец.

**2026-06-24 (ранее в этот день, §5.142):** **UI-редизайн + ребрендинг `CDOProf` → `trudskill`** (frontend-only, ветка `chore/2026-06-23-phase-11-stabilization-security`, не закоммичено). Дизайн-токены перекрашены под бренд-индиго `#3B4FE4` + акцент-коралл `#FF7A45` (все цвета проверены на WCAG AA как текст; коралловые CTA — тёмный текст через `--ui-on-accent`), шрифт Golos/PT Serif → **Inter**, видимое кольцо фокуса. Редизайн экранов учащегося (домашний, каталог, курс, тесты+результат), входа, профиля + **админки (4 области: учащиеся/группы, учебный контент, документы, аналитика)** — включая высокорычажные правки общего слоя таблиц (`tables.ts`) и фикс ~19 нерабочих классов кнопок/сеток (`ui-button-primary` и т.п. без определения) одной правкой, дашборд-метрики `.stat-card`, переиспользуемый `.kv-list`. Ребрендинг только пользовательских текстов (вордмарк, PWA-манифест, push); внутренний scope пакетов тогда намеренно не трогался (переименован в §5.143). Проверка: typecheck 8/8, ESLint clean, UI smoke-тест, доставка CSS в браузере; без миграций/прав/бэкенда. **Предыдущее:** **2026-06-23 (эта сессия, §5.141):** **LAUNCH_CHECKLIST — закрыты все достижимые из кода defense-in-depth follow-ups** (та же ветка). A1 #1 provider-scoped order lookup (коллизия `providerPaymentId`); A1 #2 amount cross-check (`WebhookEvent.amount` во всех 4 адаптерах + отказ в fulfillment при mismatch); A4 presigned-upload `ContentLength` (S3 enforce-ит размер), webinar webhook ACK через `@Res()` (+`webhookAck?` в `WebinarProvider`), SCORM zip-budget по фактическим инфлейт-байтам. CI-блокер **диагностирован**: GitHub Actions отключены на уровне репо (`actions/permissions.enabled=false`) → включение за владельцем. Тест-статус: 209/28 затронутых кластеров зелёные, typecheck 8/8, lint clean, миграции 0057. Заблокированные пункты (A2 Jitsi-стенд, A3 КриптоПро SDK, B офиц. XSD регуляторов, ops-мониторинг/стенд, юр/бизнес) помечены в [docs/LAUNCH_CHECKLIST.md](docs/LAUNCH_CHECKLIST.md). Подробности — §5.141.

**2026-06-23 (предыдущая сессия):** **Phase 11 — стабилизация `main` + security-аудит + k6-харнесс** (ветка `chore/2026-06-23-phase-11-stabilization-security`, §5.140, коммит `136fd87`). Кодовый roadmap (Phase 1–10 + Pillar A + Wave 1/2 + V1.1) уже слит — единственная неначатая кодовая фаза — Phase 11. **Стабилизация:** снят полный локальный quality-gate, всё зелёное (typecheck 8/8, lint 8/8, contracts 7/7, frontend 639 + 4 env-flaky, backend canonical e2e + IAM + DI-стражи + payments 52 + HTTP-integration 94); фикс флака `testTimeout` 30→60s. **Security-аудит (OWASP, 4 агента):** подтверждены sound подписи вебхуков/ЕСИА/SCORM/AV-гейт/SQL/crypto; **исправлены 3 реально достижимые authz-дыры** — (1) CRITICAL: `migration/backfill` контроллер был полностью без гвардов (неаутентифицированный кросс-тенантный backfill) → shared-secret `WorkerCallbackGuard` (fail-closed); (2) HIGH: `integrations`/`exports`/`sync-logs` были `TenantGuard`-only (стаб-тест маскировал) → `PermissionGuard` + `@RequirePermissions` на каждый handler + право засеяно миграцией **0057**; (3) MEDIUM: magic-link токен логировался в проде → редактируется в `NODE_ENV=production`. Плюс constant-time `WorkerCallbackGuard` (CWE-208). **Phase 11 артефакт:** read-only k6-харнесс `infra/load/` (пороги под NFR). Миграции до **0057**. **Остаток Phase 11:** нагрузочный прогон + числовые NFR (заказчик/ops), мониторинг/backup (ops); defense-in-depth follow-ups (LOW) перечислены в §5.140.

**2026-06-23 (предыдущая сессия):** **Phase 7 — активация онлайн-платежей (multi-provider, per-tenant) поверх дормант-шва** реализована на ветке `feat/2026-06-22-phase-7-payments-multi-provider` (§5.139). Зеркало платёжного шва (§5.133, PR #262) на паттерн Phase 8: одиночный токен `PAYMENT_PROVIDER` заменён **реестром `PAYMENT_PROVIDER_REGISTRY` (Map) + `PaymentProviderResolver.forTenant`** (prod-guard `fake` переехал в резолвер; `provider.id`→`provider.code`; единый источник `PAYMENT_PROVIDER_CODES`). **Миграция 0056** (`payments.payment_provider_settings` — per-tenant НЕсекретный выбор провайдера + право `payments.configure`); per-tenant settings repo (in-memory + postgres) + service. Provider-specific webhook `POST /payments/webhook/:providerCode` + опц. `webhookAck` per provider. **Четыре реальных адаптера** (`infrastructure/payments/`), credential-gated в реестре (пустые креды → адаптер опущен, boot не падает): **ЮKassa** (REST createPayment Basic+Idempotence-Key; webhook = ре-фетч статуса из API + IPv4-allowlist), **Т-Касса/Tinkoff** (/v2/Init копейки, token SHA-256; webhook timing-safe; ACK 'OK'), **CloudPayments** (/orders/create мажорные единицы; webhook HMAC-SHA256 Content-HMAC; ACK {code:0}), **Robokassa** (подписанный redirect md5, без HTTP; ResultURL md5 требует PASSWORD_2; ACK OK{InvId}). env: убран `PAYMENTS_PROVIDER` (выбор per-tenant), добавлены опц. credential-gated cred-vars; `main.ts` `rawBody:true`; `infra/.env.production.example` обновлён. Frontend `/admin/payments/settings` (per-tenant select + enabled) под `payments.configure`, nav перед `/admin/orders`. **Тест-статус (финальный, всё зелёное):** backend payments-кластер **89** (17 файлов), typecheck **8/8**, ESLint clean (backend+frontend payments), frontend payments **12** (2 файла). Миграции до **0056**. **Финальное холистическое ревью** поймало и исправило CRITICAL-баг: `ResponseEnvelopeInterceptor` оборачивал ACK вебхука в `{data,meta}` — эквайеры не видели ожидаемое буквальное тело и уходили в retry-loop; фикс — `@Res()`-direct-send + `if (res.headersSent) return data;`-guard в интерцепторе. Также: CloudPayments `application/x-www-form-urlencoded` парсинг (IMPORTANT), cross-check `payment.provider === providerCode` (IMPORTANT), стейлый комментарий (MINOR). **Go-live остаток:** договор эквайера + боевые креды в env + `PAYMENTS_ENABLED=true` + выбор провайдера тенантом в `/admin/payments/settings`. **Follow-ups (отложены):** (1) `findOrderByProviderPaymentId` без фильтра по `provider` (теор. коллизия короткого Robokassa InvId) → добавить provider-фильтр; (2) webhook не сверяет сумму уведомления со суммой заказа (подпись покрывает сумму серверным секретом — форджа исключена; остаток — экзотический partial); (3) 4 адаптера кодируют контракты эквайеров по тек. знанию — сверить с актуальными доками/песочницей перед go-live (юнит-тесты пинят поведение → правка локальна).

**2026-06-22 (предыдущая сессия):** **Полный зелёный прогон всех сьют + починка DI-guard вскрыла 2 реальных deadlock-бага** (§5.138, ветка `fix/2026-06-21-payments-module-iam-boot`, коммит `19b5a6f`). Закрыта краснота, оставшаяся после §5.137 (§13 Issue 5): статический `di-explicit-injection.test.ts` переписан — теперь сканирует конструктор **каждого** класса с `@Injectable()`/`@Controller()` (а не «первый `constructor(` в файле»). Это убрало 4 ложных срабатывания (Error-класс `InvalidOrderTransitionError` + фабричные ЕСИА-провайдеры) **и одновременно вскрыло 2 настоящих латентных бага**: в [integrations.controller.ts](apps/backend/src/modules/integrations/integrations.controller.ts) `ExportsController` и `SyncLogsController` инъектировали `IntegrationOrchestratorService` по типу **без `@Inject`** → orphaned-promise deadlock в `NestFactory.create()` под `tsx`/esbuild (в проде с `tsc`-метаданными грузилось) → добавлен `@Inject`. Плюс перф-фикс: `payments.http.integration.test.ts` `hookTimeout` 30→120с (импорт реального IAM-графа как DI-токенов даёт ~38с boot на Cyrillic-пути). **Тест-статус (всё зелёное, прогон батчами):** typecheck **8/8**, ESLint clean, frontend **634**, contracts **7**, backend по всем модулям зелёный; единственное исключение — `mvp.domains.http.integration.test.ts` локально `ERR_IPC_CHANNEL_CLOSED` (tinypool+Cyrillic, задокументированный env-краш, в CI зелёный). Без новых миграций/прав (0055).

**2026-06-21 (предыдущая сессия):** **Локальный подъём dev-стека (`pnpm dev:stack`) вскрыл boot-блокер деплой-готовности** (продолжение §5.136). Backend не стартовал: NestFactory падал на DI — `PaymentsModule` (Phase 7, §5.133) объявляет контроллеры с `@UseGuards(PermissionGuard)`, но не импортировал `IamModule` (экспортирует `IamService`/`AuthService`, которые нужны guard'у); CI пропустил, т.к. `payments.http.integration.test.ts` инстанцирует stub-контроллер, а не реальный модуль. **Фикс** — `IamModule` в `imports` [payments.module.ts](apps/backend/src/modules/payments/payments.module.ts) (паттерн `documents`/`org`/`esign`) + постоянный статический страж [permission-guard-module-wiring.test.ts](apps/backend/src/common/permission-guard-module-wiring.test.ts) (любой модуль с `PermissionGuard`-контроллером обязан импортировать `IamModule`/предоставлять `IamService`). После фикса стек поднялся: backend `Nest application successfully started`, `GET /health/ready` зелёный (миграции 56/0, redis/RabbitMQ/MinIO/outbox/secrets healthy), frontend `:3000` HTTP 200. Без новых миграций/прав (последняя 0055). **Пред-существующее (не от правки):** `di-explicit-injection.test.ts` красный (ложные срабатывания на дормант-ЕСИА + `payments.state-machine`) — см. §13 Issue 5. Детали — §5.137.

**2026-06-20 (предыдущая сессия):** **Phase 8 — provider-agnostic webinar seam (dormant, multi-provider + per-tenant)** реализован на ветке `feat/2026-06-20-phase-8-webinar-provider-seam` (§5.134), открывает Phase 8 «Вебинары» на уровне _шва_ поверх ранней CRUD-заглушки в `communication` (таблицы с миграции 0007). Полный цикл brainstorming → spec → план (18 задач) → subagent-driven + backend-ревью + финальное холистическое ревью. **Поправка владельца:** Pruffme НЕ основной → **мульти-провайдер с выбором per-tenant** («своё» = самохостинг Jitsi). Ключевое отличие от платёжного эталона: одиночный DI-токен заменён **реестром стратегий + per-tenant резолвером** (`WebinarProviderResolver.forTenant`); env `WEBINARS_ENABLED` лишь включает подсистему, провайдер берётся из настроек тенанта, **prod-guard `fake` переехал в резолвер**. Seam `WebinarProvider`+Noop(default)/Fake(staging)/Jitsi(каркас); **миграция 0055** (права `webinars.read/write/attend/configure` + таблица `webinar_provider_settings` несекретного конфига + индекс на `provider_session_id`, без изменений таблиц 0007); fail-soft `create`-проводка; unguarded webhook (тенант из строки вебинара) → durable посещаемость (НЕ влияет на завершение — отложено); хардбенинг контроллера (права+DTO). Frontend: `/admin/webinars` + `/admin/webinars/settings` + `/learner/webinars`; снесён legacy `/webinars`. **Ревью поймало:** порядок routeMeta (`/admin/webinars/settings` ПЕРЕД `/admin/webinars`, иначе settings наследует `webinars.read`) → фикс+регресс-тест; мёртвые learner-плитки на `/webinars` → переадресованы. Backend вебинары **37 тестов** (12 файлов) / frontend **16** / typecheck **8/8** / ESLint clean / миграции до **0055**. **Остаток Phase 8 = активация:** реальные адаптеры за реестром (самохостинг Jitsi createRoom+JWT+HMAC, опц. Pruffme/Zoom/BBB) + `rawBody` в `main.ts` + `WEBINARS_ENABLED=true`; отложено: посещаемость→вебинар-часы, per-tenant секреты, записи/.ics/напоминания.

**2026-06-20 (предыдущая сессия):** **Phase 7 — provider-agnostic payment seam (dormant foundation)** реализован на ветке `feat/2026-06-20-phase-7-payment-provider-seam` (§5.133, **слит PR #262**), открывает Phase 7 «Оплаты» на уровне _шва_ по паттерну последних 4 интеграций (AV→esign→export-sign→ЕСИА). Полный цикл brainstorming → spec → план (15 задач) → subagent-driven (implementer + spec/quality-review per task) + финальное холистическое opus-ревью. Новый модуль `modules/payments/`: seam `PaymentProvider`+`Noop`(default)/`Fake`(staging, prod-guard)+токен `PAYMENT_PROVIDER`; env `PAYMENTS_*` (dormant); **миграция 0054** (схема `payments`, деньги integer-копейки, права `payments.read/write/self_purchase`); order/payment домен с line items (позиция = `(group, learner)`); **рабочая ручная отметка безнала** (`mark-paid`) + dormant онлайн-pay через провайдер; идемпотентный fail-soft fulfillment в `createBulkEnrollments`; unguarded webhook (тенант из строки платежа). Frontend: `/admin/orders` + `/learner/payments`. **Финальное холистическое ревью снова поймало CRITICAL** (fulfillment звал request-scoped `MvpService` без `MvpRequestPersistenceInterceptor` → пустой `MVP_STATE` → ноль зачислений при `fulfilled`) → фикс: новый singleton `MvpEnrollmentService` гоняет зачисление через `MvpTenantRunner.runWithTenantStateAndSave` (гидратация+сохранение, реентрантный per-tenant-лок); ключевой тест поднимает реальный runner. Девиация: `course_version_id`→`group_id` (реальный примитив зачисления). Backend payments **46 тестов** / frontend **16** / typecheck **8/8** / ESLint clean. **Остаток Phase 7 = активация:** реальный ЮKassa-адаптер за `PAYMENT_PROVIDER` + `rawBody` в `main.ts` + `PAYMENTS_ENABLED=true`; отложено отдельными spec'ами: чек 54-ФЗ, счёт/акт PDF, checkout-лендинг, рефанды.

**2026-06-17 (предыдущая сессия):** **Phase 6 — КЭП-подпись файлов выгрузок (provider-agnostic seam, dormant)** реализована на ветке `feat/2026-06-17-export-kep-signature-seam` (§5.132, **PR #260**), закрывает **вторую половину гибридной модели владельца** (НЭП на документы + КЭП на выгрузки) на уровне _шва_. Зеркало document-signature seam (§5.129/§5.131) + AV-паттерна, но семантика — **detached `.p7s`** (CMS/PKCS#7) над файлом выгрузки. Полный цикл brainstorming → spec → план (9 задач) → subagent-driven (2 волны по 4 параллельных субагента на интеграцию 5 экспортёров) + inline-эталон ФРДО. Seam: `ExportSignatureProvider`+`Noop`/`Fake`+токен `EXPORT_SIGNATURE_PROVIDER`; env `EXPORT_SIGN_*` (dormant) + **prod-guard** (`fake` запрещён в проде, разрешён в staging); общий **fail-soft** `signExportArtifact` (хранит `.p7s` соседним файлом, сбой не откатывает выгрузку); 3 поля подписи на 5 `*Batch` (**без миграции** — JSON-снимки); инъекция в 5 экспортёров + `GET …/exports/:id/signature` (под `regulatory.export.read`, **без нового права**); frontend — бейдж «Подписано КЭП» в 5 таблицах `gov-export`. **Находки:** вскрыт **пред-существующий красный тест на main** (`env.test.ts` «parses production env» — PR #258/ЕСИА добавил strict-проверку `ESIA_STATE_SECRET` без обновления фикстуры) → фикс слит **PR #261**. Объединённый backend-прогон затронутых сюит **31 файл / 114 тестов**, монорепо typecheck **8/8**, ESLint clean. **Остаток Phase 6 = только активация ЭП** (реальный КриптоПро-адаптер для обоих швов + юр-оформление + ops).

**2026-06-17 (эта сессия, ранее):** **Phase 6 — проброс `signatureStatus` end-to-end + prod-guarded fake staging-signer** реализован на ветке `feat/2026-06-17-document-signature-status-passthrough` (§5.131, **PR #259 слит**), активирует один из follow-up-остатков ЭП-шва (§5.129) на уровне кода. Закрывает «висящий провод»: поля подписи писались на `GeneratedDocumentEntity`, но **никем не читались** — теперь кабинет слушателя (`LearnerDocumentDto.signatureStatus` → бейдж «Подписана НЭП»/«Ошибка подписи», plain `<span>` не StatusChip) и публичная QR-проверка (`PublicVerifyResult.signatureStatus?: 'signed'`, сужено — `failed`/`unsigned` не утекают) показывают статус. Плюс `FakeDocumentSignatureProvider` (синтетический `signed`, self-маркировка `fake-sig://`) — owner-facing превью пайплайна без КриптоПро, **запрещён в `NODE_ENV=production`** env-refinement'ом (staging намеренно разрешён; решение задокументировано+протестировано). **Финальное холистическое ревью: SHIP (0 Critical/Important)** — подтвердило, что MVP-модуль делегирует в `DocumentsService` (тот же entity, что мутирует подпись) → бейдж реально загорится; поля переживают персистентность; прод-гард без лазеек. **Без миграции** (последняя 0053), **без новых прав**. Изолированные прогоны: backend 65/65, frontend 9/9, typecheck 8/8, ESLint clean.

**2026-06-16 (предыдущая сессия):** **ЕСИА (Госуслуги) — вход + идентификация через provider-agnostic OAuth seam** реализован на ветке `feat/2026-06-16-esia-oauth-login-identity` (§5.130), закрывает отложенную задачу Phase 4 «OAuth-интеграция с ЕСИА». Полный цикл brainstorming → spec → план (15 задач) → subagent-driven + финальное холистическое ревью + fix-цикл. **Спящий шов по AV/esign-паттерну** (`ESIA_ENABLED=false` → `NoopEsiaProvider`; `MockEsiaProvider` для dev; `EsiaOidcProvider` — каркас, реальная ГОСТ-подпись = follow-up): два потока — **вход** (резолв слушателя по СНИЛС, аккаунт НЕ создаётся для незнакомца — decision A; сессия через существующий `issueSessionForUser`) и **идентификация** (авто-зачёт 4-го гейта `assertIdentityVerificationGate` записью `IdentityVerification{method:'esia'}`). HMAC-подписанный `state`; **без миграции** (последняя **0053**), **без новых прав** (bootstrap-маршруты). **Финальное холистическое ревью снова поймало ДВА CRITICAL:** C1 — `TenantGuard` отклонял браузерные ЕСИА-маршруты 401 до контроллера; C2 — identity-callback зависел от `context.userId`, которого при редиректе нет → поток был мёртв. **Фикс-редизайн:** callback **полностью state-driven** (tenantId/learnerId внутри подписанного state), identity инициируется SPA через **POST с Bearer**, login — GET; `TenantGuard` exemption для `/auth/esia/*`; + hardening слабого `ESIA_STATE_SECRET` в проде + снят BOM. Повторное ревью: READY TO MERGE. Изолированные прогоны: ЕСИА+guard+env **31+**, IAM-регресс 10/10, frontend 3 + регресс, typecheck 8/8, ESLint clean. Ожидает PR.

**2026-06-15 (эта сессия, поздно):** **Phase 6 — provider-agnostic e-signature seam (НЭП)** реализован на ветке `feat/2026-06-15-phase-6-esign-provider-seam` (§5.129) — закрывает ЭП-остаток Phase 6 на уровне _шва_ (реальный КриптоПро — отдельный follow-up). Проработка ЭП (3 research-агента: код/право/провайдеры) → решение владельца: **гибрид НЭП(документы)+КЭП(выгрузки)** + **provider-agnostic по AV-паттерну**. Ключевая находка права: для ДПО форму документа УЦ утверждает сам (ст. 60 273-ФЗ) → КЭП НЕ обязательна, НЭП по соглашению достаточно; КЭП нужна отдельно — для подписи **файлов выгрузки** в ФИС ФРДО/ЕИСОТ. Реализация (зеркало `AntivirusScanner`): `DocumentSignatureProvider`+`NoopDocumentSignatureProvider`+токен; env `ESIGN_ENABLED`/`ESIGN_PROVIDER`/`ESIGN_SIGNER_NAME` (dormant); 6 полей подписи на `GeneratedDocumentEntity` (**без миграции** — jsonb-снимок); подпись при `finalizeDocument` (**fail-soft**) + ручной `signDocument` (гарды archived/revoked/!isFinal); фабрика в `DocumentsModule` (Noop даже при `cryptopro`); миграция **0053** право `documents.sign`; endpoint `POST /documents/:id/sign`; frontend `LearnerDocument.signatureStatus?`. 9 задач TDD subagent-driven + two-stage review; **финальное холистическое ревью поймало IMPORTANT** (подпись отозванного документа — `revoke` оставляет `isFinal=true`) → гард + тест. Изолированные прогоны: backend-кластер **209/209** (documents.service 52, http 15), frontend learner-documents 6/6, typecheck **8/8**, ESLint clean. Миграции до **0053**. Ожидает PR.

**2026-06-15 (эта сессия):** **Phase 6 — реестры Ростехнадзор + Минздрав-НМО** реализованы на ветке `feat/2026-06-14-phase-6-rostechnadzor-nmo-registries` (§5.126), закрывают реестровый остаток Phase 6 (оставался только ЭП/НЭП, отложен владельцем). Два новых durable-экспортёра как сиблинги трёх существующих (ФРДО/ОТ/ЕИСОТ): **без миграции, без нового права** (`regulatory.export.read/write`), **XLSX-only**, PROVISIONAL-`COLUMNS` со swap-точками. **Ростехнадзор** (`rostechnadzor-registry/`) — источник завершённые+сданные зачисления (ОТ-архетип), swap `attestationArea`. **Минздрав-НМО** (`nmo-registry/`) — источник выданные документы (ФРДО-архетип), swap `specialty`+`creditUnits`/ЗЕТ. Frontend: 2 секции в `app/gov-export/page.tsx`. Полный цикл brainstorming→spec→plan(20 TDD-задач)→subagent-driven (two-stage review на каждую пачку + холистическое финальное ревью; ревью-улучшение `failed`=distinct-сущности; +2 теста-паритета на сервис). Изолированные прогоны: backend 123, frontend 17, typecheck 8/8, ESLint clean. Миграций нет (последняя 0052). Ожидает PR.

**2026-06-14 (docs-only):** **Phase 0 — плейбук готовности к первому пилоту** (ветка `docs/2026-06-14-phase-0-readiness-spec`, §5.125). Некодовый owner-facing артефакт [docs/phase-0/](docs/phase-0/): индекс + 6 направлений (бренд/домен, запуск сервера, модель подписи, пилотный контент, пилотный клиент, лицензия+реестры) — критический путь к первому пилоту, выдающему юридически пригодные документы. После сверки с записью владельца 2026-06-08 (бренд/домен/лицензия/клиенты уже есть) фокус сужен до **НЭП + деплой + пилотный курс + прогон**; ЕСИА/платежи отложены в Phase 7. Backend/frontend нетронуты.

**2026-06-13:** **Phase 10 Track A — Excel-конструктор отчётов** реализован на ветке `feat/2026-06-13-phase-10a-excel-report-builder` (§5.121). Phase 9 полностью слита (Plan A #242 + Plan B #243). Read-model без миграции/нового права: реестр сущностей + чистый `buildReport` + exceljs-writer + 6 endpoints `reports/builder/*` + коллекция `reportTemplates` + страница `/admin/reports/builder`. v1 = ученики + назначения (`documents` отложена, D-A5); XLSX base64-в-конверте без S3 (D-A3). Реализовано последовательно in-process TDD (субагент-диспатч недоступен — `403`). Изолированные прогоны: backend 33 + http 85, frontend 14, contracts 7, typecheck 8/8, ESLint clean. **Слит в `main` (#245).** **Track B — WCAG-доступность** реализован (§5.122, ветка `feat/2026-06-13-phase-10b-wcag-accessibility`): статический гейт `eslint-plugin-jsx-a11y/recommended` в обоих путях линтования + a11y-фиксы 9 общих примитивов `@trudskill/ui` + AppShell live-region; холистическое ревью поймало IMPORTANT-баг (`fieldId` схлопывал кириллицу в один id → дубль-id) → фикс; ui+frontend+lint+typecheck зелёные → **PR #246**. **Track C — PWA + web-push** реализован (§5.123, ветка `feat/2026-06-13-phase-10c-pwa-push`): PWA-манифест + Serwist service worker + web-push (VAPID, всё **dormant** за `WEB_PUSH_ENABLED=false` → `NoopWebPushSender`) + канал push в `NotificationDispatcher` к 5 событиям Phase 5; MVP-коллекция `pushSubscriptions` **без миграции/нового права**. Изолированные прогоны: frontend 566/566, backend таргет 73, typecheck 8/8, ESLint clean → **PR #248**.

**2026-06-11 (предыдущая сессия, вечер):** **Phase 4 Plan B — прокторинг (запись видео итогового теста)** реализована на ветке `feat/2026-06-11-phase-4-plan-b-proctoring` (§5.118), **Phase 4 закрыта целиком** (кроме отложенных владельцем ЕСИА/live-оператора): migration **0051** (`learning.proctoring_recordings` + `requires_proctoring` + per-student `proctoring_override` + права `proctoring.submit/read`), браузерный MediaRecorder → 30-сек webm-чанки → presigned PUT (лимит 10MB files-слоя), **5-й гейт `assertProctoringGate`** в `startAttempt` (412 `proctoring_required`, non-collision с Wave 1/Plan A), согласие 152-ФЗ + превью камеры, `● REC` + stop-on-submit + **resume-баннер после F5**, админ-очередь `/admin/proctoring-recordings` с плеером-склейкой чанков (AV-гейт, presigned GET, предупреждения о разрывах), override-select на странице группы, dormant **365-дневный** retention-cron (`PROCTORING_VIDEO_RETENTION_ENABLED=false`, lock 528_493, write-режим runner + регресс-guard). Финальное холистическое ревью снова оправдалось: **CRITICAL C1 — ретрай чанка запрашивал новый intent → 409 → мёртвый ретрай + фантомный чанк ронял всю сборку видео** (фикс: intent-кэш per-sequence + per-chunk деградация плеера) + I1 (F5 убивал запись навсегда → resume-баннер) + I2 (override был API-only → UI). Миграции теперь до **0051**. PR не создан на момент записи — см. Current Task.

**Предыдущее (2026-06-11, утро):** **Phase 4 Plan A — идентификация личности (селфи + паспорт, ручная сверка админом)** реализована на ветке `feat/2026-06-10-phase-4-plan-a-identity-verification` → **PR #240** (§5.117): migration **0050** (toggle + `learning.identity_verifications` + права `identity.submit/read/review`), MVP-коллекция `identityVerifications`, lifecycle start→upload→submit(согласие 152-ФЗ)→review, **4-й гейт `assertIdentityVerificationGate`** в `startAttempt` (412 `identity_verification_required`; сообщение намеренно не пересекается с Wave 1-регексом), dormant 90-дневный retention-cron изображений (`IDENTITY_IMAGE_RETENTION_ENABLED=false`, advisory-lock 528_492), 7 эндпоинтов, `/learner/identity` + `/admin/identity-verifications` (+`[id]`) + интерстициал в test-player + e2e. Холистическое ревью поймало **CRITICAL: `MvpTenantRunner` read-only** (штампы cron терялись) → добавлен **`runWithTenantStateAndSave`** (write-режим, зеркало `DocumentsTenantRunner`). Прокторинг → отдельный Plan B (не запланирован). Миграции теперь до **0050**.

**Предыдущее (2026-06-10):** Обе ветки 2026-06-08 **слиты в `main`**: Phase 0 деплой-фундамент (**#235**) + Production auth readiness A+B (**#237**; плюс **#236** — boot-fix под tsx). Закрыт follow-up §3.3 плана auth-readiness: `infra/bootstrap-admin.md` приведён к новой реальности (email-доставка magic-link по `NOTIFICATIONS_EMAIL_ENABLED`, авто-`SeedCredentialHygiene` при прод-буте, ручные psql-шаги → подстраховка) → слито **#238**, §5.115. Дополнительно слит **#239** (`DB_MIGRATIONS_ENABLED=''` → `'false'` в 13 тест-харнессах после #236). **Кодовых блокеров пилота не осталось** — следующий шаг deploy-execution владельцем.

V1 roadmap (см. [docs/superpowers/plans/2026-05-21-cdoprof-v1-roadmap.md](docs/superpowers/plans/2026-05-21-cdoprof-v1-roadmap.md)) — **Phase 1 + Pillar A + Phase 2 (A+B+C) + Phase 3 (Plan A + Plan B + Plan C)** реализованы и слиты (Plan C — PR #215). **Wave 1** — **Plan 1 (модульный гейтинг + время)** слит (PR #218), **Plan 2 (аутентификация перед экзаменом №816)** слит (PR #219). **Wave 2 (регуляторные выгрузки)** — **под-цели A (ФРДО #225) + B (ОТ-реестр #222/#223) + C (ЕИСОТ «лица на тестирование» #226) слиты**. **V1.1 антивирус-гейт полностью закрыт** (#216-217, #224). **Phase 5 Plan 5A (notification foundation)** слита в `main` (PR #228): `MailerService` (NoopMailer + SmtpMailer), `email_templates`/`email_deliveries` (миграция 0047), `NotificationDispatcher`, `EnrollmentEmailListener`, admin endpoints, `documents.revoked` event. **Phase 5 Plan 5B (recertification foundation)** реализована на ветке `feat/2026-06-05-phase-5-plan-b-recertification`: per-program validity `recertificationPeriodMonths` + штамповка `valid_until` при выдаче документа + таблица `recertification_drafts` (migration **0048**) + `RecertificationService` (scan→draft→approve/reject, письмо `recertification_due` слушателю/заказчику) + admin endpoints (`recertification.read/write`); планировщик/cron → отдельный план **5B-2**, frontend-очередь «Нужна переаттестация» → план **5C**. **Phase 5 Plan 5B-2 (recertification scheduler)** реализована на ветке `feat/2026-06-06-phase-5-plan-b2-recertification-scheduler`: ночной `@nestjs/schedule` cron (UTC, dormant за `RECERTIFICATION_SCAN_ENABLED=false`) + `pg_try_advisory_xact_lock` + cross-tenant скан (`MvpTenantRunner`); каденс 90/30/7 (`recertification_due`) и 14/7/1 (`course_deadline`) с send-once dedup (`email_deliveries.dedup_key`, migration **0049**); письмо `document_revoked`; `RecertificationScanner` вынесен из request-scoped сервиса. **Phase 5 Plan 5C (frontend-очередь «Нужна переаттестация»)** реализована на ветке `feat/2026-06-07-phase-5c-recertification-queue`: обогащение `GET /recertification-drafts` (`learnerName`/`courseTitle`/`learnerSnils?` через mvp-state резолверы, без миграции) + feature-модуль `features/recertification/` + страница `/admin/recertification` + навигация; объём «только список» (reject + «Проверить сейчас»; approve намеренно не выведен). Стабильный quality gate `pnpm -s ci:check` (Cyrillic-path fallback на isolated backend runs — см. CLAUDE.md Gotchas); миграции до **0049**.

### Current Goal

**Phase 0 — деплой-фундамент пилота** завершён → **PR #235** (ветка `feat/2026-06-08-phase-0-pilot-launch-foundation`): right-sized под соло-владельца — один сервер + Docker Compose + Caddy (авто-HTTPS) + сборка на сервере; 8 артефактов (`apps/frontend/Dockerfile`, `infra/docker-compose.prod.yml`, `infra/Caddyfile`, `infra/.env.production.example`, `.github/workflows/deploy.yml`, `infra/backup.sh`, рунбуки) + harden `scripts/check-env.ts`. **Production auth readiness (A+B)** **слита в `main` (#237)**: A) `EmailMagicLinkEmailSender` — доставка magic-link по SMTP; B) `SeedCredentialHygiene` — прод-хук, обезвреживающий утёкший seed-пароль `Password123!`. Активация ops пилота: `NOTIFICATIONS_EMAIL_ENABLED=true` + реальный `SMTP_*`. Рунбук `infra/bootstrap-admin.md` синхронизирован с этим (§5.115).

### Last Completed Task

**Clear-vs-keep контракт для форм редактирования (закрыт follow-up §5.154)** (2026-06-29, §5.157, PR #294) — `null`/`[]`/`''` очищают поле, опущенный ключ оставляет как было; backend нормализует `null → undefined` (типы сущностей не расширяются, `JSON.stringify`-персистенция роняет ключ, publish-гейт по-прежнему блокирует очищенный черновик); commission `description` оказался чисто фронтовым; clear-vs-keep маппинг вынесен в чистые юнит-тестируемые билдеры. mvp.service 106 / dto-validation 128 / frontend payloads 5 + api.contract 7 зелёные, monorepo typecheck 8/8, ESLint чисто; **без миграций/прав, без `contracts:generate`**. **Предыдущее:** **Закрыт follow-up §5.154 — кросс-потоковый дубль-сертификат (group-order vs авто-выдача)** (2026-06-29, §5.155): `issueGroupOrder` переиспользует уже авто-выданное действующее удостоверение вместо чеканки второго регистрационного номера (+2 TDD-теста, documents 227/227). _Ранее:_ **Закрыт отложенный follow-up §5.152 — bulk-import reuse за порогом 10k** (2026-06-29, §5.153) — snapshot существующих учётков для reuse-детекции строился из одной страницы `listLearners(page_size: 10_000)`; `MvpService.list()` режет `slice(0, 10_000)`, поэтому у тенанта с >10k слушателей совпадение за границей терялось → строка импорта классифицировалась `create`, а `createLearnerExtended` **не проверяет уникальность** email/СНИЛС → импорт **молча создавал дубликат**. Фикс (целевой lookup, без cap'а): новый `MvpService.findLearnersByEmailOrSnils(tenantId, emails, snilsValues)` сканирует все учётки тенанта и возвращает только совпавшие по email/СНИЛС строк импорта; память ограничена размером импорта (для Postgres-бэкенда `readSnapshot` грузит учётки без LIMIT → `state.learners` всегда полон). Классификация эквивалентна прежней (включая `identity_conflict`). TDD +1 регресс (10 001 учёток, цель за старой границей → `reused`, не `created`; RED подтверждён); learners-bulk-import **39/39**, backend typecheck 8/8, ESLint по 3 файлам clean; **без миграций/прав**, не закоммичено. **Предыдущее:** **Фикс латентной коллизии нумерации документов при сбросе периода** (2026-06-27, §5.147) — `DocumentsService.reserveNumber` при `resetPeriod:'year'|'month'` после смены периода сбрасывал счётчик, но форматировал номер **без токена периода** → `#000001` нового года совпадал с прошлогодним → `ConflictException`, проглатываемый fire-and-forget `EnrollmentDocumentIssuanceListener` → регулируемый документ **молча не выпускался**. Фикс (выбор владельца «год в номере»): токен `{period}` → `CERT-2026-000001` (глобально уникален, счётчик реально сбрасывается) + validate-before-commit (проваленная резервация больше не прожигает счётчик). TDD +3 теста (`vi.setSystemTime` clock-seam); documents.service 57 + смежные 43 зелёные, typecheck 8/8, ESLint clean; **без миграции/прав**, не закоммичено. **Предыдущее:** **Завершение редизайна UI + scope-rename `@cdoprof/*`→`@trudskill/*`** (2026-06-24, §5.143) — аудит ~93 маршрутов + миграция 14 легаси-экранов на дизайн-систему (**0 хардкод-цветов** во фронтенде) + переименование 8 пакетов / 962 ссылки в 125 build-файлах + `pnpm install`; гейты зелёные (typecheck 8/8, lint 8/8, contracts 7/7, frontend 643/643, backend smoke). Без миграций/прав, не закоммичено. **Предыдущее:** **UI-редизайн + ребрендинг trudskill (frontend-only)** (2026-06-24, §5.142) — токены индиго+коралл (WCAG AA) + Inter + focus-ring; 5 экранов ученика/входа/тестов + профиль; пользовательский ребрендинг `CDOProf` → `trudskill`. **Предыдущее:** **Phase 8 — provider-agnostic webinar seam (dormant, multi-provider + per-tenant)** (2026-06-20, ветка `feat/2026-06-20-phase-8-webinar-provider-seam`, §5.134): открывает Phase 8 «Вебинары» на уровне _шва_ поверх ранней CRUD-заглушки в `communication` (таблицы с 0007). 18 задач TDD subagent-driven + backend-ревью (SHIP) + финальное холистическое ревью (SHIP). **Поправка владельца:** мульти-провайдер + выбор per-tenant («своё» = самохостинг Jitsi, не движок с нуля). Отличие от платёжного эталона: **реестр стратегий `WEBINAR_PROVIDER_REGISTRY` + `WebinarProviderResolver.forTenant`** вместо одиночного токена; env `WEBINARS_ENABLED` лишь включает подсистему, провайдер из настроек тенанта, **prod-guard `fake` в резолвере**. Seam `WebinarProvider`+Noop/Fake/Jitsi(каркас); **миграция 0055** (права `webinars.read/write/attend/configure` + таблица `webinar_provider_settings` несекретного конфига + индекс `provider_session_id`, без изменений таблиц 0007); fail-soft `create`; unguarded webhook (тенант из строки вебинара) → durable посещаемость (НЕ влияет на завершение); хардбенинг контроллера. Frontend: `/admin/webinars` + `/admin/webinars/settings` + `/learner/webinars`; снесён legacy `/webinars`. **Ревью поймало:** порядок routeMeta (settings ПЕРЕД webinars иначе наследует `webinars.read`) → фикс+регресс-тест; мёртвые learner-плитки → переадресованы. Backend вебинары **37 тестов** (12 файлов), frontend **16**, typecheck **8/8**, ESLint clean, миграции до **0055**. **Остаток Phase 8 (активация):** реальные адаптеры за реестром (Jitsi createRoom+JWT+HMAC, опц. Pruffme/Zoom/BBB) + `rawBody` в `main.ts` + `WEBINARS_ENABLED=true`; отложено: посещаемость→вебинар-часы, per-tenant секреты, записи/.ics/напоминания. **Предыдущее: Phase 7 — provider-agnostic payment seam (dormant foundation)** (2026-06-20, ветка `feat/2026-06-20-phase-7-payment-provider-seam`, §5.133, **слит PR #262**): новый модуль `modules/payments/`: seam `PaymentProvider`+`Noop`/`Fake`+токен; **миграция 0054** (схема `payments`, integer-копейки, права `payments.read/write/self_purchase`); order/payment домен с line items; ручная отметка безнала (`mark-paid`) + dormant онлайн-pay; идемпотентный fail-soft fulfillment; unguarded webhook. **CRITICAL (финальное ревью):** fulfillment без `MvpRequestPersistenceInterceptor` → ноль зачислений → singleton `MvpEnrollmentService` через `MvpTenantRunner.runWithTenantStateAndSave`. Backend payments **46 тестов**, frontend **16**, typecheck **8/8**. **Предыдущее:** **Phase 6 — КЭП-подпись файлов выгрузок (provider-agnostic seam, dormant)** (2026-06-17, ветка `feat/2026-06-17-export-kep-signature-seam`, §5.132, PR #260): вторая половина гибридной модели (НЭП-документы + **КЭП-выгрузки**); зеркало document-seam (§5.129), но **detached `.p7s`**. 9 задач TDD: `ExportSignatureProvider`+`Noop`/`Fake`+токен; env `EXPORT_SIGN_*` (dormant) + prod-guard для fake; **fail-soft** `signExportArtifact` (хранит `.p7s` соседним файлом, сбой не откатывает выгрузку); 3 поля подписи на 5 `*Batch`/5 `*ExportOutcome` (без миграции); фабрика в `MvpModule`; инъекция в 5 экспортёров (ФРДО/ОТ/ЕИСОТ/Ростехнадзор/НМО) + `GET …/exports/:id/signature` (`regulatory.export.read`); frontend бейдж «Подписано КЭП». Subagent-driven: 2 волны по 4 параллельных субагента (интеграция + download-endpoint), inline-эталон ФРДО. Вскрыт пред-существующий красный `env.test.ts` (PR #258/ЕСИА не обновил фикстуру `ESIA_STATE_SECRET`) → follow-up `fix/2026-06-17-env-test-esia-state-secret`. Прогоны: объединённый backend **31 файл/114 тестов**, frontend gov-export 12, монорепо typecheck **8/8**, ESLint clean. **Остаток Phase 6:** реальный КриптоПро-адаптер за `EXPORT_SIGNATURE_PROVIDER`+`DOCUMENT_SIGNATURE_PROVIDER`, юр-оформление, ops (`EXPORT_SIGN_ENABLED=true`/`cryptopro`). **Предыдущее:** **Phase 6 — проброс `signatureStatus` end-to-end + prod-guarded fake staging-signer** (2026-06-17, ветка `feat/2026-06-17-document-signature-status-passthrough`, §5.131, **PR #259 слит**): `LearnerDocumentDto.signatureStatus`+бейдж в кабинете слушателя, `PublicVerifyResult.signatureStatus?: 'signed'` (сужено), `FakeDocumentSignatureProvider` (запрещён в `NODE_ENV=production`); финальное ревью SHIP; backend 65/65, frontend 9/9, без миграции/новых прав. **Ранее:** **Phase 6 — provider-agnostic e-signature seam (НЭП, dormant)** (2026-06-15, ветка `feat/2026-06-15-phase-6-esign-provider-seam`, §5.129): полный цикл — проработка ЭП 3 параллельными research-агентами (код/право РФ/провайдеры) → решения владельца (гибрид НЭП+КЭП; provider-agnostic по AV-паттерну) → план 9 задач → subagent-driven (two-stage review per task) + финальное холистическое ревью + fix-цикл. Seam зеркалит `AntivirusScanner`: интерфейс `DocumentSignatureProvider` + `NoopDocumentSignatureProvider` + токен `DOCUMENT_SIGNATURE_PROVIDER`; env `ESIGN_ENABLED`/`ESIGN_PROVIDER`/`ESIGN_SIGNER_NAME` (dormant, кастомный boolean-парс); 6 полей подписи на `GeneratedDocumentEntity` (**без миграции** — jsonb-снимок); опц. 6-я инъекция в `DocumentsService` (back-compat) + `applySignature` (**fail-soft** — сбой провайдера не откатывает финализацию) из `finalizeDocument` + публичный `signDocument` (гарды archived/revoked/!isFinal); фабрика в `DocumentsModule` (Noop даже при `ESIGN_PROVIDER=cryptopro` + warn); миграция **0053** право `documents.sign` (admin/methodist); endpoint `POST /documents/:id/sign`; frontend `LearnerDocument.signatureStatus?` (тип-готовность). Холистическое ревью поймало **IMPORTANT** (подпись отозванного документа — `revokeDocument` оставляет `isFinal=true`) → гард `revoked` + тест; minor — doc-комментарий активации для КриптоПро. Изолированные прогоны: backend-кластер **209/209** (documents.service 52, http.integration 15, env.esign 3, migration 0053 2, + все pre-existing), frontend learner-documents 6/6, typecheck 8/8, ESLint clean. Миграции до **0053**. Ожидает PR. **Остаток Phase 6 (follow-up):** реальный КриптоПро-адаптер (CSP+КриптоАРМ SDK), КЭП-подпись выгрузок, юр-оформление (оферта/Положение об ЭДО), проброс `signatureStatus` через `LearnerDocumentDto`+UI-бейдж, ops (сертификат УЦ ФНС + `ESIGN_ENABLED=true`). **Предыдущее:** **Phase 9 Plan A — SCORM 1.2 импорт + плеер** (2026-06-12, ветка `feat/2026-06-12-phase-9-plan-a-scorm`, §5.119): полный цикл brainstorming → spec (решения D1–D10) → план 17 задач → subagent-driven (two-stage review на каждую пачку) + fix-циклы. Backend: migration **0052** (`learning.scorm_packages`/`scorm_attempts` + `materials.scorm_package_id` + `'scorm'` в `materials_type_chk`; **новых прав нет** — пакеты `materials.write`, launch `materials.read`, commit `progress.recalculate`); чистые модули `parseScormManifest` (fast-xml-parser, отказ SCORM 2004), `scorm-zip-guards` (zip-bomb/traversal/encoded/NUL лимиты + MIME-map), `scorm-content-token` (HMAC base64url); `ScormService` (upload-intent с `maxBytes`, register, **синхронный process: AV-гейт → adm-zip → гард ВСЕХ entries до декомпрессии → putObject per entry**, idempotent, delete с 409-in-use + cleanup; launch с access-chain + единственный attempt + токен-launchUrl; commit cmi → завершение materialProgress); `ScormController` (8 authed) + **unguarded `ScormContentController`** (`:token/*rest`, HMAC-токен в пути, кросс-tenant изоляция по payload, stream+nosniff); files `getReadableFile`/`listObjectKeys`. Frontend: `features/scorm/` (api + чистый cmi-mapping), `/scorm` реестр пакетов (загрузка/обработка/удаление), scorm-опция в форме материала, **`ScormPlayer`** (scorm-again в `window.API`, same-origin iframe, commit на LMSCommit/LMSFinish, resume, ownership-checked cleanup) в курс-вьюере, next.config rewrite, e2e. Девиация: adm-zip вместо unzipper (тестируемость; лимиты сохранены). Холистические уловы review: zip-bomb-порядок гардов, encoded-traversal, Content-Type на presigned PUT (Windows .zip), 2 гонки `window.API` в плеере, prod secret-guard. Изолированные прогоны: frontend **537/537**, backend scorm **69** + mvp.http **78** + dto **125** + files **28** + migrations **50** + env **10**, typecheck 8/8, ESLint clean. Миграции до **0052**. PR не создан на момент записи — см. Current Task. **Предыдущее: Phase 4 Plan B — прокторинг** (2026-06-11, ветка `feat/2026-06-11-phase-4-plan-b-proctoring`, §5.118): полный цикл brainstorming → spec → план 15 задач → subagent-driven (two-stage review на каждую) → финальное холистическое ревью + fix-цикл + верификация фиксов. Backend: коллекция `proctoringRecordings`, `resolveProctoringRequirement` (override ?? флаг), lifecycle start(consent)/chunk-intent/complete/active(nextSequence), гейт + линковка attemptId, админ list/detail с `chunkIssues`, DTO, 7 эндпоинтов (+`PATCH /enrollments/:id/proctoring-override` под `learners.write`), retention-трио в `proctoring/`. Frontend: `features/proctoring/` (recorder state-machine без браузер-API в тестах, retry-once-then-skip с intent-кэшем, resume), `detectStartGate` (дедуп 3 регексов test-player), consent-панель, REC, resume-баннер, админ-плеер со склейкой Blob, навигация, e2e. Изолированные прогоны: backend-кластер **248/248** (10 файлов), миграции **48/48**, frontend таргет **72/72**, typecheck 8/8, ESLint clean; полный фронтенд 504/510 — 6 падений = известный environmental dynamic-import класс (изолированно зелёные, baseline идентичен). **Предыдущее:** **Phase 4 Plan A — идентификация личности (селфи + паспорт)** (2026-06-11, ветка `feat/2026-06-10-phase-4-plan-a-identity-verification`, §5.117): миграция 0050 + коллекция `identityVerifications` + lifecycle (start/upload-intent/submit/review) + гейт `assertIdentityVerificationGate` (412, без коллизии с Wave 1) + files-слой (`UploadIntentOptions`, `deleteObject`/`deleteFile`) + dormant retention-cron 90 дней + 7 эндпоинтов + frontend (`/learner/identity`, `/admin/identity-verifications`, интерстициал, e2e). 13 задач subagent-driven + two-stage review; **финальное холистическое ревью поймало CRITICAL** (read-only `MvpTenantRunner` терял штампы cron → `runWithTenantStateAndSave`) **и IMPORTANT** (заражённый файл брикал админ-ревью → graceful degradation `selfieFileError`/`passportFileError`). Изолированные прогоны зелёные (backend 214/214 + кластеры, frontend 39+, миграции 42/42, typecheck 8/8). **PR #240.** **Предыдущее:** **Production auth readiness (A+B)** (2026-06-08, ветка `feat/2026-06-08-production-auth-readiness`, **PR #237**, §5.114): A) `EmailMagicLinkEmailSender` доставляет magic-link через `MailerService`/SMTP (был log-only); B) `SeedCredentialHygiene` — прод-`OnApplicationBootstrap`-хук, ротирует утёкший seed-хеш `d845591…` → `disabled:<hex>` (прицельно, идемпотентно, non-prod no-op); парольный вход оставлен по решению владельца. 4 TDD-задачи subagent-driven + review (поймал/исправил SQL-дупликацию в гигиене → `RETURNING id` через `db.query`); backend `tsc` чист, IAM-регрессия 14 + новые наборы 8. **Слит (#237, 2026-06-08)**; рунбук-синхронизация §5.115 → **#238**. **Предыдущее (Phase 0): pilot launch foundation** (2026-06-08, ветка `feat/2026-06-08-phase-0-pilot-launch-foundation`, **слит #235**): 8 артефактов деплоя (single-VPS Docker+Caddy, build-on-server CD) + harden `check-env` + фикс 4 Next-15 route-param багов; brainstorm→spec→plan→8 задач subagent-driven + review (поймал маскировку `ignoreBuildErrors`, добавил tenant-id build-arg). 🚨 вскрыл 2 находки (magic-link log-only; seed `Password123!`) → закрыты auth-веткой (#237). **Предыдущее:** **Фикс pre-existing e2e `aggregateReviewerQueue` snapshot-shape** (2026-06-07, §5.113, ветка `fix/2026-06-07-reviewer-queue-e2e-snapshot` от main): закрыт known-failure (§5.110/§5.112) — `attemptAnswers: []`+`questions: []` в snapshot-литерал теста `admin-assessment-surface.e2e` + расширен локальный cast-тип; **backend untouched**; таргет-тест зелёный, ESLint clean. **NB для следующего агента:** PR #233 (Phase 5C) слит в `main` БЕЗ этого фикса (внешний `git reset` отбросил коммит до сквоша) → восстановлено cherry-pick'ом (`23e4743`), ожидает PR. **Предыдущее:** **Phase 5 Plan 5C — frontend-очередь «Нужна переаттестация»** (2026-06-07, ветка `feat/2026-06-07-phase-5c-recertification-queue`, §5.112, слит #233): backend-обогащение списка (`resolveLearnerDisplay` + `listDrafts`→`RecertificationDraftView` с `learnerName`/`courseTitle`, без миграции) + feature-модуль `features/recertification/` + страница `/admin/recertification` + навигация + e2e; объём «только список» (reject + «Проверить сейчас»; approve намеренно не выведен). 9 задач TDD subagent-driven (кластеры backend/frontend-core/screen) + two-stage review + holistic opus-review (READY TO MERGE, 0 Critical/Important); backend reminder-recipients 8 + recert-service 8 + mvp.http.integration 42, frontend format 10 + api.contract 5 + e2e 4; `pnpm typecheck` 8/8, ESLint clean. Review-фиксы: `resolveCourseTitleByVersion` тесты, `formatRemaining` NaN-guard, `reject` nullable-тип. Ожидает PR. **Предыдущее: Phase 5 Plan 5B-2 — recertification scheduler + reminder cadence** (2026-06-07, ветка `feat/2026-06-06-phase-5-plan-b2-recertification-scheduler`, §5.111): ночной cron + advisory-lock + cross-tenant скан (`MvpTenantRunner`) + каденс 90/30/7 & 14/7/1 (send-once `dedup_key`, migration 0049) + `document_revoked` listener + `RecertificationScanner`-рефактор; 15 задач TDD subagent-driven + two-stage review; full `tsc` чисто, таргет+регрессии зелёные; review поймал DI-провайдер (Task 10), UTC-cron (Task 12), DRY (Task 13). Ожидает PR. **Предыдущее: Визуальная дизайн-система (читаемость в приоритете)** (2026-06-06, ветка `feat/2026-06-06-visual-design-system` от свежего `origin/main`, §5.110): токены navy + читаемый синий `#1e40af` + холодные нейтрали (золото убрано по фидбэку владельца «плохо читаема»; **контраст всех пар измерен ≥ WCAG AA** — тело 18:1/14.6:1, минимум 5.37:1), шрифты Golos Text/PT Serif через `next/font` (раньше шрифт не грузился вовсе), герой «Следующий шаг» (`next-step-card.tsx`, виден на `/learner`), новый паттерн `.ui-callout`, чистка хардкод-цветов (вкл. баг тёмной темы в `tz-links` + theme-blind `calendar`). Всё через токены `packages/ui/src/tokens` + `styles/foundation.ts` → раскатано по ~40 экранам автоматически. UI-тесты 9/9, learner-home 26/26, typecheck ui+frontend + ESLint чисто. Ожидает PR. **Предыдущее (roadmap): Phase 5 Plan 5B — recertification foundation (validity stamping + recertification_drafts + scan + approve/reject endpoints)** (2026-06-05, ветка `feat/2026-06-05-phase-5-plan-b-recertification`; subagent-driven-development по [плану](docs/superpowers/plans/2026-06-05-phase-5-plan-b-recertification-cycle.md)). Все 9 задач: migration **0048** (`course_versions.recertification_period_months` + `generated_documents.valid_until` + `learning.recertification_drafts` + права `recertification.read/write`); чистый `addMonths/addDays` util (month-end clamp); writable `recertificationPeriodMonths` на program-meta; штамповка `valid_until = completed_at + период` (producer-resolved: `MvpService` → payload → issuance-listener → task → completeTask → document); `RecertificationDraftsRepository` (singleton: in-memory + postgres, идемпотентность по `(tenant,learner,sourceDoc)`); `RecertificationService` (pure `scanForRecertification(asOf, docs, 90)` через `DocumentsTenantRunner` → idempotent draft → письмо `recertification_due` слушателю+заказчику; `approveDraft` → reuse `createBulkEnrollments`; `rejectDraft`); закрыт 5A-gap пустого `{{courseTitle}}` (резолв названия курса на producer); admin endpoints `GET /recertification-drafts`, `POST /recertification/scan`, `POST /recertification-drafts/:id/{approve,reject}` + permission-boundary в `mvp.http.integration.test.ts`. Тесты: date-math 4, drafts-repo 5, recert-service 9, documents.service 46, issuance-listener 11, email-notifications 13, mvp.http.integration 42, dto-validation 104 (всего 234 таргет); `pnpm typecheck` 8/8; ESLint clean. Финальный code-review поймал и исправил 2 edge-bug'а (approve idem-key per-group; try/catch вокруг dispatch в scan). Handoff §5.106. **Предыдущее:** Phase 5 Plan 5A — notification foundation (PR #228, §5.105); Wave 2 Plan C — ЕИСОТ (#226, §5.104); Wave 2 Plan A — ФРДО (#225, §5.103).

### Current Task

**Стабилизация — латентный аудит, 7 фиксов** (2026-06-29, §5.152) — 2 HIGH (архивный сертификат проходил как `valid` по QR → `not_found`; bulk-import `dateOfBirth` был недостижим по HTTP из-за `forbidNonWhitelisted`) + MED-HIGH (дубль письма при повторе адреса в одной рассылке) + 3 MED (выдача набора документов прерывалась на первом плохом шаблоне → per-entry try/catch; `finishAttempt` воскрешал просроченную попытку в `finished`; дубль `rowNumber` схлопывал outcome-строки) + frontend data-loss (автосейв терял ответ при навигации/сабмите в окне дебаунса → `flushDraft`). Все с тестами; полный `pnpm test:backend`/`test:frontend` зелёные, typecheck 8/8, ESLint чисто; **без миграций/прав/изменения конвертов**, не закоммичено. Открыт спавн-таск: bulk-import reuse-cap 10k. **Предыдущее:** **Редизайн доведён до конца + scope-rename выполнен** (§5.143) — все легаси-экраны мигрированы на `@trudskill/ui`, пакеты переименованы, гейты зелёные; работа на ветке `chore/2026-06-23-phase-11-stabilization-security`, **не закоммичена** (большой scope-rename-дифф + визуальные правки в рабочем дереве). Следующему агенту: оформить коммит(ы) + PR; владельцу — переименовать GitHub-репозиторий. **Предыдущее:** **UI-редизайн платформы + ребрендинг trudskill** (§5.142) — пройдены ключевые экраны учащегося + вход + настройки; работа на ветке `chore/2026-06-23-phase-11-stabilization-security`, не закоммичена. Скриншот-инструмент превью в этом окружении таймаутит на захвате картинки → визуальная проверка велась через DOM-снимок + инспектор вычисленных стилей; экран входа удалось проверить полностью вживую (не за авторизацией). **Предыдущее:** **Deploy-readiness аудит — образы не бутятся «из коробки»** (§5.136, ветка `fix/2026-06-21-deploy-readiness-image-packaging-and-prod-env`): владелец выбрал вектор «готовность к деплою» (код-роадмап V1 завершён на уровне швов). Систематический проход по deploy-артефактам нашёл и закрыл **3 блокера подъёма пилота**: (1) CRITICAL — runtime-образ backend копировал только `dist/` без каталога миграций → boot `Migrations directory not found` → crash-loop → стек не поднимается (фикс: COPY migrations + Dockerfile-страж); (2) CRITICAL — `infra/.env.production.example` давал невалидный prod-env даже после CHANGE_ME (эмпирически **6** провалов boot-схемы: пустые VAPID-строки, unset `DEPLOYMENT_PROFILE`/`SECRETS_PROVIDER`/`MVP`+`DOCUMENTS_PERSISTENCE_DRIVER`/`ESIA_STATE_SECRET`) — фиксы в примере + **решение владельца** разрешить `SECRETS_PROVIDER=env` в проде (vault/kms тут тоже env-mirrored; все weak-secret-гарды сохранены) + страж парсит пример против boot-схемы; (3) IMPORTANT — frontend Dockerfile (Next standalone) не копировал `public/` → `/sw.js`+`/icons/*` 404 → PWA-слой ломается (фикс: COPY public + страж). Backend env+deploy 41/41, frontend 1/1, typecheck backend+frontend exit 0, ESLint clean; новых миграций нет (0055). **Ожидает PR.** **Предыдущее: Issue 4 — свежая БД накатывает всю цепочку миграций из коробки** (§5.135, ветка `fix/2026-06-20-migration-chain-fresh-bootstrap`, **PR #265 слит**): закрыты оставшиеся пункты 1–2 §13 Issue 4 через edit-in-place (не consolidated-baseline — безопасно, БД нигде не развёрнута). Полная цепочка `0001→latest` (56 файлов) теперь применяется на свежий Postgres; постоянный full-chain testcontainers-тест (`migration-bootstrap.full-chain.test.ts`: apply-all + все схемы + идемпотентность) и инструмент итерации, и страж регрессий. Эмпирически найдено и исправлено **6** поломок (0003 re-drop, 0004 композитные uniques esign, 0015 неквалифицированные `COMMENT ON INDEX`, 0016 осиротевшие payload-комментарии, 0032 `template_type`→`document_type`+`category_code`, 0036 `mvp`→`learning`). Главная первопричина — коллизия `create table if not exists` (0013 повторяет уже созданные 0002/0005/0009 таблицы = no-op). **NB:** локальную dev-БД пересоздать (checksum'ы правленых файлов изменились). full-chain 3/3 + migration-suite 7/7 + typecheck 8/8 + ESLint clean. Новых миграций нет (0055). **Ожидает PR.** **Предыдущее: Phase 8 — webinar seam (dormant, multi-provider + per-tenant)** **слит #264** на ветке `feat/2026-06-20-phase-8-webinar-provider-seam` (§5.134; спек+план `docs/superpowers/{specs,plans}/2026-06-20-phase-8-webinar-provider-seam*.md`, 18 TDD-задач). Все задачи + 2 review-fix (порядок routeMeta для settings-роута, мёртвые learner-плитки) + backend-ревью (SHIP) + финальное холистическое ревью (SHIP) зелёные: backend вебинары **37 тестов** (12 файлов), frontend **16**, typecheck **8/8**, ESLint clean, миграции до **0055**. **Ожидает PR.** Следующий агент: открыть PR (squash) → закрыть ветку (`finishing-a-development-branch`). **Предыдущее: Phase 7 — payment seam** (ветка `feat/2026-06-20-phase-7-payment-provider-seam`, §5.133, **слит PR #262**). **Предыдущее: Phase 6 — КЭП-подпись выгрузок** (ветка `feat/2026-06-17-export-kep-signature-seam`, §5.132, **PR #260 слит**), follow-up `env.test` (**PR #261 слит**). **Предыдущее: Пагинация реестровых экспортёров — завершение ФРДО (5-й из 5)** (§5.128): коммит §5.127 (`b81b647`) озаглавлен «all 5», но по факту вошли только **4** сервиса — правка `frdo-registry.service.ts` осталась незакоммиченной + у ФРДО не было >1000-regression. Этот шаг закрывает 5-й: `collectAllPages` через `offset`/`limit` (байт-в-байт паттерн слитого NMO-твина) + >1000-regression в `frdo-registry.service.test.ts` (1500 сертификатов → `exported === 1500`, `listIssuedDocuments` вызван дважды). `frdo-registry.service.test.ts` 4/4, typecheck 8/8, ESLint clean. Миграций нет. Ожидает коммит/PR. **Предыдущее (§5.127):** общий хелпер `collectAllPages` + пагинация 4 сервисов (ОТ/ЕИСОТ/Ростехнадзор/НМО); реально обрезались 3 на `listEnrollments`, NMO/ФРДО уже отдавали всё; +`registry-pagination.test.ts` (4) +>1000-regression на ЕИСОТ (1500) и NMO (1500). **Предыдущее: Phase 6 реестры Ростехнадзор + Минздрав-НМО** готовы на ветке `feat/2026-06-14-phase-6-rostechnadzor-nmo-registries` (§5.126; спек `docs/superpowers/specs/2026-06-14-phase-6-rostechnadzor-nmo-registries-design.md` + план `docs/superpowers/plans/2026-06-14-phase-6-rostechnadzor-nmo-registries.md`, 20 TDD-задач). Два durable-экспортёра (`rostechnadzor-registry/` + `nmo-registry/`) по паттерну Wave 2: без миграции, без нового права, XLSX-only, PROVISIONAL-`COLUMNS`; партиал-саксесс; durable batches+records; 4 endpoint'а каждый (`POST/GET exports`, `GET exports/:id`, `GET exports/:id/file`) под `regulatory.export.read/write`; frontend-секции в `gov-export`. Изолированные прогоны зелёные: backend 123 + frontend 17, typecheck 8/8, ESLint clean. Ожидает PR. **Предыдущее: Phase 0 readiness плейбук** (`docs/2026-06-14-phase-0-readiness-spec`, §5.125, docs-only, 7 owner-facing документов в `docs/phase-0/`). **Предыдущее: Phase 10 Track A — Excel-конструктор отчётов** на ветке `feat/2026-06-13-phase-10a-excel-report-builder` (§5.121; spec `docs/superpowers/specs/2026-06-13-phase-10a-excel-report-builder-design.md` + plan `docs/superpowers/plans/2026-06-13-phase-10a-excel-report-builder.md`, 10 задач TDD). Read-model **без миграции/нового права** (по образцу analytics-dashboard, переиспользует `enrollments.read`/`write`): декларативный реестр сущностей + чистый движок `buildReport` + обобщённый exceljs-writer + 6 endpoints `reports/builder/{entities,preview,export,templates,templates/:id}` + новая MVP-state коллекция `reportTemplates`; страница `/admin/reports/builder` (выбор сущности/полей/фильтров → превью → скачивание XLSX → сохранение/загрузка/удаление шаблонов). v1 = две сущности **ученики + назначения** (`documents` отложена — D-A5); XLSX отдаётся base64-в-конверте без S3 (D-A3). Изолированные прогоны зелёные: backend 33 + http-integration 85, frontend 14, contracts 7, typecheck 8/8, ESLint clean. **Слит #245.** **Track B (WCAG) слит (#246); Track C (PWA + web-push) реализован → PR #248** (этот мёрдж `main` → phase-10c; §5.123). **Phase 9 полностью слита (#242 + #243).**

### Next Task

**Коммит ветки редизайна + scope-rename** (большой дифф — желательно отдельными коммитами: визуальная миграция vs механический rename) + переименование GitHub-репозитория владельцем; затем вернуться к запуску (активация вебинаров/платежей, ops/NFR). Редизайн всех экранов и переименование пакетов завершены (§5.143). **Предыдущее:** **Phase 8 открыт на уровне кода-шва** (webinar-seam dormant, §5.134) — остаётся **активация вебинаров**: реальные адаптеры за реестром `WEBINAR_PROVIDER_REGISTRY` (приоритет — самохостинг Jitsi: createRoom + JWT + HMAC-webhook; опц. Pruffme/Zoom/BBB) + `rawBody:true` в `main.ts` + `WEBINARS_ENABLED=true` + per-tenant выбор провайдера в `/admin/webinars/settings`. **Отдельными spec'ами поверх готового шва:** посещаемость→вебинар-часы/завершение (кросс-модуль `communication`→`mvp`), per-tenant секреты провайдера (secret-ref/шифрование), записи/.ics/напоминания. **Весь код-роадмап V1 (Phase 1–10) реализован на уровне швов.** **Phase 7 открыт на уровне кода-шва** (payment-seam dormant, §5.133, **слит #262**) — остаётся **активация платежей**: реальный ЮKassa-адаптер за `PAYMENT_PROVIDER` + `rawBody:true` в `main.ts` + `PAYMENTS_ENABLED=true`/`yookassa` (+ договор ЮKassa/ОФД); отдельными spec'ами: чек 54-ФЗ, счёт/акт PDF (B2B), checkout-лендинг, рефанды. **Phase 6 закрыта на уровне кода-шва** — реестры (§5.126) + **НЭП-документы-seam (§5.129)** + **КЭП-выгрузки-seam (§5.132)** реализованы (обе половины гибридной модели); из Phase 6 остаётся только **активация ЭП**: реальный КриптоПро-адаптер за обоими токенами (`DOCUMENT_SIGNATURE_PROVIDER` в `DocumentsModule` + `EXPORT_SIGNATURE_PROVIDER` в `MvpModule`; CSP+КриптоАРМ SDK), юр-оформление (оферта/Положение об ЭДО, согласие 152-ФЗ, распорядительный акт об автоподписи 63-ФЗ ст.4), проброс `signatureStatus` через `LearnerDocumentDto`+UI-бейдж, ops (сертификат КЭП юрлица от УЦ ФНС + `ESIGN_ENABLED=true`/`ESIGN_PROVIDER=cryptopro`). Весь код-роадмап V1 (Phase 1–6, 9, 10) реализован. Дальше — **deploy-execution** (владелец: сервер + DNS + SMTP, прогон `infra/server-setup.md` + `infra/bootstrap-admin.md`). Ops-активация: retention-cron'ы `IDENTITY_IMAGE_RETENTION_ENABLED=true` (90 дней) + `PROCTORING_VIDEO_RETENTION_ENABLED=true` (365 дней); SCORM-env `SCORM_CONTENT_TOKEN_SECRET`/`SCORM_PACKAGE_MAX_BYTES`/`SCORM_CONTENT_TOKEN_TTL_SECONDS`. **Отложено по решению владельца:** ЕСИА, live-оператор видео-ИД, ЭП/НЭП-провайдер (Phase 6), Pruffme/ЮKassa (Phase 7/8), clamav, управляемые сервисы. **Реестры PROVISIONAL** — сверить `COLUMNS` 5 экспортёров (ФРДО/ОТ/ЕИСОТ/Ростехнадзор/НМО) с эталонами регуляторов перед боевой подачей. (Молчаливое обрезание источника на 1000 строк устранено — §5.127.)

### Do Not Touch

- Историю миграций в `apps/backend/migrations/*` (без отдельной задачи миграционного изменения).
- Сгенерированные контракты в `packages/api-contracts/src/generated/*` (менять только через генерацию).

### Important Decisions

- Репозиторий: единый **операционный** конспект между агентами — блок `README` + передача сессии в `LMS_AGENT_HANDOFF.md`. Продуктовый канон — `SDOPROF_TZ_FINAL.md`; роли и порядок чтения при фразе «продолжай по ТЗ» — [docs/DOCUMENTATION_MAP.md](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol).
- Изменения вносятся малыми итерациями с обязательной фиксацией тестового статуса и рисков.
- Следующий высокий приоритет: безопасность и устойчивость IAM + контроль доступа по ролям.

### Known Risks

- Риск рассинхронизации документации и кода при отсутствии регулярного обновления README после каждой итерации.
- Возможные регрессии в auth/session/permission flows при несистемном изменении backend и frontend.
- Наличие широкого backlog по enterprise-operational задачам; требуется приоритизация по критичности (security/reliability first).
- ✅ **Полное приложение бутится; логин работает** (исправлено 2026-06-06, §5.108): DI-«deadlock» был из-за того, что `tsx`/esbuild не эмитит `emitDecoratorMetadata` → type-based DI без `@Inject` виснет. Все инъекции переведены на явный `@Inject`, `CommunicationModule` импортирует `IamModule`, добавлен regression-guard. `pnpm dev:web` → `Nest application successfully started`, логин end-to-end OK.
- ⚠️ **Остаток (Issue 4):** цепочка миграций `0003`/`0004`/… не накатывается на свежую БД из коробки (паттерн «FK на `(tenant_id,id)` до UNIQUE») — нужен consolidated baseline. `DB_MIGRATIONS_ENABLED` (`z.coerce.boolean`) — **исправлен**. Dev-БД развёрнута обходным путём (FK-safe IAM/audit-миграции + запись всех checksum'ов). Детали — [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) §13 Issue 3 (resolved) + Issue 4, §5.108.

### Last Updated By

Claude (Opus 4.8) — §5.157: clear-vs-keep контракт для форм редактирования (закрыт follow-up §5.154) — brainstorming→spec→plan→TDD в изолированном worktree от `d6dd915`; backend нормализует `null→undefined` (типы сущностей не тронуты), frontend всегда шлёт поля пред-заполненных форм; чистые билдеры `buildProgramMetaPatch`/`buildCommissionInfoPayload`; PR #294. **Предыдущее:** Claude (Opus 4.8) — §5.155: закрыт follow-up §5.154 — кросс-потоковый дубль-сертификат в групповом приказе (`issueGroupOrder` переиспользует авто-выданное удостоверение, +2 TDD-теста). Ранее — аудит-проход «максимум багов» §5.154: 6 параллельных subagent-разведчиков (iam/auth · mvp bulk · documents/esign · assessment · communication/org · frontend), 8 подтверждённых багов (2 CRITICAL + 3 HIGH + 3 MEDIUM) перепроверены чтением кода и закрыты по TDD. **Предыдущее:** стабилизационный латентный аудит §5.152: 4 параллельных subagent-аудитора (assessment / documents-issuance / bulk-import+worker / progress-notifications), каждая находка перепроверена чтением кода лично; 7 фиксов с тестами. **Предыдущее:** subagent-driven execution (этой сессии, продолжение): **Аудит-хвост d–g закрыт** (план `docs/superpowers/plans/2026-06-28-audit-tail-dg-dedup-atomicity.md`): **d** license-expiry dedupKey теперь включает `validUntil` → продлённая лицензия снова напоминает; **e** ОБА request-persistence interceptor'а персистят domain-state только на успехе хендлера (throw → чистый rollback; аудит пишется в БД независимо; opus-ревью подтвердило «validate-first» — никто не полагался на save-on-error); **f** `completeTask` идемпотентен на redelivery (проверка `completed` до `startTask`); **g** `issueGroupOrder` до-выпускает недостающие сертификаты на ретрае (`ensureOrderCertificates` на обоих путях; seam e+g = выпуск приказа полностью retry-safe). Все TDD; полный `pnpm test:backend` **250 файлов / 1885 тестов / 0 падений**, typecheck 8/8, ESLint clean. **Без миграций (0057), без прав, без изменения API.** Поведенческое изменение (e): упавшая HTTP-мутация больше не персистит частичный state. Ветка `fix/2026-06-27-audit-tail-progress-dedup-notify` (PR #284 расширен до полного хвоста a–g), §5.150, коммиты `d4b7472`,`09afed9`,`8cd9a02`,`7acf6a9`. **Предыдущее:** subagent-driven execution: **Аудит-хвост — закрыты 3 подтверждённых MEDIUM-бага** (план `docs/superpowers/plans/2026-06-27-audit-tail-progress-dedup-notify.md`; implementer + spec-review + code-quality-review per task + финальное холистическое ревью): **1c** прогресс модуля/курса переведён с «посещённых» на count-based по полному набору обязательных материалов / модулей закреплённой версии (`countBasedProgress`-хелпер; precedence pinned>published>progress) — ревью поймало, что опциональные материалы блокировали 100% и что все published-версии раздували знаменатель v1-слушателю; **1a** durable-дедуп авто-выпуска сертификата по `state.tasks` (поверх 24ч TTL-кэша) — ревью поймало баг с `cancelled`-задачей; **1b** per-recipient дедуп рассылки (`listByDedupKey`) + `try/catch` на отправку → throw в середине цикла больше не «застревает» получателей. Все TDD (RED→GREEN на каждый, до/после на cancelled и pinned-v1); целевые 7 сьютов **193 теста**, **полный `pnpm test:backend` 246 файлов / 1876 тестов / 0 падений**, typecheck 8/8, ESLint clean. **Без миграций (0057), без новых прав, без изменения API-конвертов.** Поведенческое изменение: 100% курса/модуля теперь требует прохождения всех обязательных материалов закреплённой версии. Ветка `fix/2026-06-27-audit-tail-progress-dedup-notify`, §5.149, коммиты `3fcf874`,`e9c72c2`,`0b2316f`,`098a9ec`,`2e54fe9`,`1a94d7e`,`0ea7697`. **Предыдущее:** goal-режим, систематический прогон всех сьют по модулям + root-валидация: **Полный зелёный backend из корня** — typecheck 8/8, frontend 646, contracts 7, worker/realtime/ui/shared-types/test-utils зелёные; backend по модулям зелёный, затем **полный `pnpm test:backend` 244 файла / 1853 теста / 0 падений** в один процесс. Диагностика «Cyrillic-краша» вскрыла **реальный DI-баг**: `MvpController` получил 2 новых конструкторных аргумента (`LearnerPdfCardService`/`LearnersBulkImportService`), ручной тест-модуль `mvp.domains.http.integration` их не предоставлял → `NestFactory` бросал `UnknownDependenciesException` → дефолтный `abortOnError:true` звал `process.abort()` → жёсткий kill vitest-воркера (`ERR_IPC_CHANNEL_CLOSED`), который и принимали за «проблему пути». Фиксы: 2 провайдера `Scope.REQUEST` + `abortOnError:false`; **15** Nest-бутящих boot-хуков `}, 30_000)`→`120_000` (явный таймаут перекрывал глобальный `hookTimeout:120000` и не выдерживал CPU-конкуренции полного сьюта); `migrations.0055` `process.cwd()`→`__dirname`-путь. Изменены **только тест-файлы (16) + CLAUDE.md Gotchas**; продакшн-код/миграции/права не тронуты; §5.148, коммиты `cec6ca9`+`026e4d4`. **Предыдущее:** TDD bugfix (этой сессии): **Documents — коллизия нумерации после сброса периода** — systematic-debugging (сверка report'а с кодом: throw глотается issuance-listener'ом; public-verify ищет по `qr_token`, а не `reservedNumber` → period-scoping безопасен) → AskUserQuestion (владелец выбрал «год в номере») → RED 3 теста (`vi.setSystemTime` как clock-seam, без новых сигнатур) → GREEN (токен `{period}` + period-aware `defaultNumberingPattern` + validate-before-commit) → смежные сьюты 43 + typecheck 8/8 + ESLint clean; §5.147, без миграции (0057)/прав, не закоммичено. **NB:** worktree был без `node_modules` → `pnpm install` в worktree для изолированных прогонов. **Предыдущее:** redesign-completion + scope-rename execution: **Завершение редизайна UI + переименование `@cdoprof/*`→`@trudskill/*`** — аудит единообразия (4 параллельных Explore-агента) → миграция 14 экранов на существующие `.ui-*`-классы 5 параллельными агентами (общий гейт типов/тестов в конце) → переименование 8 пакетов / 962 ссылки / 125 файлов через скриптовый строковый replace + `pnpm install` + автофикс 1 `import/order`. Гейты: typecheck 8/8, lint 8/8, contracts 7/7, frontend 643/643, backend integration-smoke зелёный; **0 хардкод-цветов** во фронтенде; §5.143, без миграций (0057), не закоммичено. **Предыдущее:** frontend-design execution + inline browser-verification: **UI-редизайн + ребрендинг trudskill** — поэтапно (анализ → дизайн-токены → ребрендинг → экраны по одному с паузами на подтверждение владельца). Токены индиго `#3B4FE4`/коралл `#FF7A45` в [`packages/ui/src/tokens/index.ts`](packages/ui/src/tokens/index.ts) (+`--ui-on-accent` для AA-текста на коралле; success/warning затемнены до AA), Inter в [`app/layout.tsx`](apps/frontend/app/layout.tsx), стили в [`packages/ui/src/styles/*`](packages/ui/src/styles). Экраны: домашний ученика, каталог (`useLearnerHomeData` ради названий+прогресса), просмотрщик курса (визуальный прогресс + sticky TOC), вход (бренд-оболочка), тесты+результат (`.ui-option` карточки, таймер-эскалация, баннер pass/fail), карточка профиля. Ребрендинг user-facing текстов; `@trudskill/*` не тронуты. typecheck 8/8, ESLint clean, UI smoke 3/3, CSS-доставка подтверждена в браузере. §5.142, без миграций (0057), не закоммичено. **Предыдущее:** subagent-driven execution + inline verification (этой сессии): **Phase 7 — активация онлайн-платежей multi-provider + per-tenant** — рефактор шва на реестр + per-tenant резолвер (зеркало Phase 8) + миграция 0056 (`payments.payment_provider_settings` + право `payments.configure`) + per-tenant settings repo/service + provider-specific webhook `/payments/webhook/:providerCode` + **4 реальных credential-gated адаптера** (ЮKassa: ре-фетч статуса + IPv4-allowlist; Т-Касса: token SHA-256 + ACK 'OK'; CloudPayments: HMAC-SHA256 + ACK {code:0}; Robokassa: md5 redirect/ResultURL + ACK OK{InvId}) + env-обновление (убран `PAYMENTS_PROVIDER`, добавлены опц. cred-vars, `rawBody:true`) + frontend `/admin/payments/settings`. Part A зелёная: backend payments 85 (17 файлов), typecheck 8/8, ESLint clean (backend+frontend payments), frontend payments 12 (2 файла); §5.139, миграции до 0056. Follow-ups (отложены): provider-фильтр в `findOrderByProviderPaymentId`, сверка суммы в webhook, верификация контрактов 4 эквайеров перед go-live. **Предыдущее:** goal-режим, inline verification: **Полный зелёный прогон всех сьют + sharpen DI-guard** — typecheck/lint/frontend(634)/contracts(7)/backend (батчами по модулям) зелёные. Закрыта краснота §13 Issue 5: `di-explicit-injection.test.ts` переписан на сканирование конструкторов только `@Injectable()`/`@Controller()`-классов → убраны 4 ложных срабатывания **и вскрыты 2 реальных латентных DI-deadlock'а** (`ExportsController`/`SyncLogsController` инъектировали по типу без `@Inject`) → фикс `@Inject`. + перф `payments.http.integration` hookTimeout 30→120с. Единственное локальное исключение — `mvp.domains.http.integration` (tinypool+Cyrillic env-краш, в CI зелёный). §5.138, коммит `19b5a6f`, без миграций (0055). **Предыдущее:** inline TDD execution: **Deploy-readiness аудит** — после слияния PR #265 весь код-роадмап V1 на уровне швов; владелец выбрал вектор «готовность к деплою». Систематический проход по deploy-артефактам + эмпирическая сверка `infra/.env.production.example` с реальной boot-схемой (`backendEnvSchema`, не с мягким `check-env`). Закрыты 3 блокера: backend-Dockerfile без каталога миграций (crash-loop), 6 провалов prod-env-схемы (+решение владельца разрешить `SECRETS_PROVIDER=env` в проде — vault/kms env-mirrored, weak-secret-гарды сохранены), frontend-Dockerfile без `public/` (PWA 404). 3 file-content стража (2 Dockerfile + 1 prod-env-пример). RED→GREEN на схему-изменение; backend env+deploy 41/41, frontend 1/1, typecheck backend+frontend exit 0, ESLint clean; §5.136. **Предыдущее:** subagent-driven execution (этой сессии): **Phase 8 — provider-agnostic webinar seam (dormant, multi-provider + per-tenant)** — brainstorming (поправка владельца: мульти-провайдер + per-tenant, «своё» = самохостинг Jitsi) → spec → план 18 задач → имплементер пачками + backend-ревью (SHIP) + финальное холистическое ревью (SHIP). Seam `WebinarProvider`+Noop/Fake/Jitsi(каркас) + **реестр стратегий + per-tenant `WebinarProviderResolver`** (отличие от одиночного токена платежей; prod-guard `fake` в резолвере) + env `WEBINARS_ENABLED` (мастер-выключатель) + миграция 0055 (права `webinars.read/write/attend/configure` + таблица `webinar_provider_settings` несекретного конфига + индекс) + per-tenant настройки (repo/service) + fail-soft `create` + unguarded webhook (тенант из строки вебинара) + хардбенинг контроллера + frontend (`/admin/webinars`, `/admin/webinars/settings`, `/learner/webinars`, снос legacy `/webinars`). **Ревью поймало:** порядок routeMeta (settings ПЕРЕД webinars иначе наследует `webinars.read`) → фикс+регресс; мёртвые learner-плитки на `/webinars` → переадресованы. Девиация `ui-form-row`→`ui-inline` (реальный класс). Backend вебинары 37 (12 файлов), frontend 16, typecheck 8/8, ESLint clean; §5.134, миграции до 0055. **Предыдущее:** subagent-driven execution: **Phase 7 — provider-agnostic payment seam (dormant foundation)** — brainstorming → spec → план 15 задач → имплементер+spec/quality-review per task + финальное холистическое ревью; модуль `payments/` + seam + миграция 0054 + order/payment домен + mark-paid + idem fail-soft fulfillment + unguarded webhook + frontend; **CRITICAL** (пустой request-scoped `MVP_STATE` → ноль зачислений) → singleton `MvpEnrollmentService` через `runWithTenantStateAndSave`; backend 46, frontend 16; §5.133, **слит #262**. **Предыдущее:** subagent-driven execution: **Phase 6 — проброс `signatureStatus` end-to-end + prod-guarded fake staging-signer** — writing-plans → 5 задач TDD (implementer + spec-review + code-quality-review per task) + финальное холистическое ревью SHIP; DTO-проброс + frontend-бейдж + публичная QR-проверка + fake staging-провайдер (запрет в проде); Задача 4 уже покрыта (субагент поймал ошибочную посылку плана); code-review-улов (shared-тип вместо inline-литерала; plain-`<span>` вместо StatusChip); решение «staging разрешён» задокументировано+протестировано; backend 65/65, frontend 9/9, typecheck 8/8, ESLint clean; §5.131, без миграции/новых прав. **Предыдущее:** subagent-driven execution: **ЕСИА (Госуслуги) — вход + идентификация через provider-agnostic OAuth seam (dormant)** — brainstorming → spec → план 15 задач → имплементеры пачками (env+seam+mock+oidc+state; типы; mvp-методы; EsiaService; контроллер+проводка+http; frontend) + two-stage review per группу + финальное холистическое ревью, поймавшее **2 CRITICAL** (TenantGuard отклонял ЕСИА-маршруты до контроллера; identity-callback зависел от отсутствующего `context.userId`) → fix-редизайн (state-driven callback + SPA-POST identity + guard exemption) + повторное ревью READY TO MERGE + юнит-тест границы безопасности контроллера; §5.130, миграции до 0053. **Предыдущее:** subagent-driven execution: **Phase 6 — provider-agnostic e-signature seam (НЭП)** — проработка ЭП 3 research-агентами → решение владельца (гибрид НЭП+КЭП, AV-паттерн) → план 9 задач → имплементер+spec-review+quality-review per task + финальное холистическое ревью; seam (`DocumentSignatureProvider`+Noop+токен) + env-флаги dormant + поля подписи (без миграции) + fail-soft подпись при finalize + ручной `signDocument` + фабрика + миграция 0053 (`documents.sign`) + endpoint + frontend-тип; холистическое ревью поймало IMPORTANT (подпись отозванного документа) → гард+тест; backend-кластер 209/209, frontend 6/6, typecheck 8/8, ESLint clean; §5.129, миграции до 0053. **Предыдущее:** TDD-фикс: **Пагинация 5 реестровых экспортёров** — общий хелпер `collectAllPages` (terminate по `total` ИЛИ короткой странице — устойчив к замоканным стабам без `total`) + перевод всех 5 сервисов на полную пагинацию (`page`/`page_size` для `listEnrollments`, `offset`/`limit` для `listIssuedDocuments`); RED→GREEN на оба архетипа источника; 6 suite 31/31, typecheck 8/8, ESLint clean; §5.127, миграций нет. **Предыдущее:** subagent-driven execution: **Phase 6 — реестры Ростехнадзор + Минздрав-НМО** — два durable XLSX-экспортёра по паттерну Wave 2 (без миграции/нового права, PROVISIONAL `COLUMNS`), оба модуля + frontend через two-stage review + холистическое ревью; +2 теста-паритета на сервис (cross-tenant + dedup `failed`); backend 123 + frontend 17 зелёные, typecheck 8/8, ESLint clean; §5.126, ветка `feat/2026-06-14-phase-6-rostechnadzor-nmo-registries`. **Предыдущее:** executing-plans: **Phase 0 readiness плейбук** — некодовый owner-facing артефакт `docs/phase-0/` (7 документов), все 8 задач плана закрыты; поправка после сверки с записью владельца 2026-06-08 (бренд/домен/лицензия/клиенты уже есть → фокус НЭП+деплой+пилот); §5.125. **Предыдущее:** subagent-driven execution: **Phase 9 Plan A — SCORM 1.2 import + player** (загрузка zip + парсер манифеста + синхронная распаковка adm-zip с zip-bomb/traversal-гардами + раздача контента unguarded-роутом по HMAC-токену в пути (same-origin для iframe) + cmi-прогресс через scorm-again `window.API` → завершение materialProgress; 17 задач TDD, two-stage review per пачку; уловы: zip-bomb-порядок гардов, encoded-traversal/NUL, presigned PUT Content-Type на Windows .zip, 2 гонки `window.API` в плеере, prod secret-guard; девиация adm-zip↔unzipper задокументирована; миграция 0052; §5.119). **Предыдущее:** subagent-driven execution: **Phase 4 Plan B — proctoring** (MediaRecorder 30-сек чанки → presigned PUT; 5-й гейт 412 `proctoring_required`; согласие 152-ФЗ; resume после F5; админ-плеер со склейкой чанков; per-student override UI; dormant 365-дневный retention-cron lock 528_493 write-runner; 15 задач TDD, two-stage review per task; финальное холистическое ревью поймало CRITICAL C1 — мёртвый ретрай чанка + фантомный чанк ломал сборку видео (intent-кэш + per-chunk деградация), I1 — F5-обход записи (resume-баннер), I2 — override без UI (select на странице группы); миграция 0051; §5.118). **Предыдущее:** subagent-driven execution: **Phase 4 Plan A — identity verification** (селфи+паспорт → ручная сверка → гейт итогового экзамена; 13 задач TDD, two-stage review per task, финальное холистическое ревью поймало CRITICAL read-only `MvpTenantRunner` → `runWithTenantStateAndSave` + IMPORTANT graceful degradation на 423/409; миграция 0050; dormant retention-cron 528_492; §5.117). **Предыдущее:** subagent-driven execution: **Phase 0 pilot-launch-foundation** (8 артефактов деплоя, single-VPS Docker+Caddy, build-on-server CD, PR #235) + **Production auth readiness** (`EmailMagicLinkEmailSender` + `SeedCredentialHygiene`; 4 TDD-задачи, review поймал/исправил SQL-дупликацию в гигиене); brainstorm→spec→plan→implement обе; §5.114. **Предыдущее:** subagent-driven execution (Phase 5 Plan 5B-2 — recertification scheduler: @nestjs/schedule UTC-cron + pg_try_advisory_xact_lock + cross-tenant MvpTenantRunner + 90/30/7 & 14/7/1 каденс с send-once dedup_key (migration 0049) + document_revoked listener + RecertificationScanner-рефактор; 15 задач TDD, two-stage review поймал/исправил DI-провайдер, UTC-cron, DRY; full tsc чисто, регрессии зелёные; §5.111). **Предыдущее:** frontend-design execution (визуальная дизайн-система: navy+синий токены, Golos Text/PT Serif шрифты, герой «Следующий шаг», `.ui-callout`; убрано золото и пересобрана палитра под читаемость по фидбэку владельца — контраст измерен ≥ WCAG AA; чистка хардкод-цветов; временная витрина `/design-preview` создана и удалена; spawn_task на чужой pre-existing баг `aggregateReviewerQueue`; §5.110, ветка `feat/2026-06-06-visual-design-system`). **Предыдущее:** subagent-driven execution (Phase 5 Plan 5B — recertification foundation: migration 0048 + valid_until стамповка (producer→listener→task→document) + RecertificationDraftsRepository + RecertificationService scan/approve/reject + recertification_due email + courseTitle-fix + admin endpoints + permission-boundary tests; все 9 задач; review поймал и исправил баг чтения документов через DocumentsTenantRunner; backend таргет-набор зелёный 232 теста; pnpm typecheck 8/8, ESLint clean)

### Last Updated At

2026-06-29 (§5.157 — clear-vs-keep контракт для форм редактирования: `null`/`[]`/`''` очищают, опущенный ключ оставляет; backend нормализует `null→undefined`, frontend всегда шлёт поля; mvp.service 106 / dto-validation 128 / frontend payloads 5 зелёные, monorepo typecheck 8/8; без миграций/прав, без `contracts:generate`; PR #294). **Предыдущее:** 2026-06-29 (§5.155 — закрыт follow-up §5.154: кросс-потоковый дубль-сертификат (group-order vs авто-выдача), фикс по TDD, documents 227/227; §5.154 — аудит-проход «максимум багов»: 8 подтверждённых багов (2 CRITICAL + 3 HIGH + 3 MEDIUM) закрыты по TDD, затронутые наборы зелёные, typecheck 8/8, без миграций/прав; §5.153 — закрыт отложенный follow-up §5.152: bulk-import reuse-детекция за порогом 10k слушателей; целевой `MvpService.findLearnersByEmailOrSnils` вместо `listLearners(page_size: 10_000)` → совпадение за границей больше не теряется и не плодит молчаливый дубль; TDD +1 регресс, learners-bulk-import 39/39, typecheck 8/8, ESLint чисто; без миграций/прав; не закоммичено). **Предыдущее:** 2026-06-29 (Стабилизация — латентный аудит, 7 фиксов: 2 HIGH + MED-HIGH + 3 MED + frontend data-loss; полный backend/frontend зелёный, typecheck 8/8, ESLint чисто; §5.152; без миграций/прав; не закоммичено). **Предыдущее:** 2026-06-28 (Аудит-хвост follow-up — закрыт лог-счётчик: `NotificationDispatcher.dispatch` возвращает `{sent,skipped,failed}`, cron-сканеры считают фактические отправки, а не размер списка → нет завышения при ретрае; коммит `d0d2182`; полный `pnpm test:backend` 250/1887 зелёный, typecheck 8/8; **весь аудит-хвост a–g + follow-up закрыт, открытых пунктов нет**; PR #284; без миграций/прав). **Предыдущее:** 2026-06-28 (Аудит-хвост §5.150 — закрыты последние 4 бага d–g: **d** license-reminder dedup по `validUntil`, **e** оба persistence-interceptor'а сохраняют state только на успехе хендлера (rollback при throw), **f** `completeTask` идемпотентен на redelivery, **g** `issueGroupOrder` до-выпускает недостающие сертификаты; план `2026-06-28-audit-tail-dg-dedup-atomicity.md`; полный `pnpm test:backend` 250 файлов / 1885 тестов / 0 падений, typecheck 8/8; ветка `fix/2026-06-27-audit-tail-progress-dedup-notify`, PR #284 (хвост a–g целиком), коммиты `d4b7472`…`7acf6a9`; **без миграций** — 0057; **без новых прав**; поведенческое изменение: упавшая мутация не персистит частичный state). **Предыдущее:** 2026-06-28 (Аудит-хвост §5.149 — закрыты 3 MEDIUM-бага: **1c** прогресс по полному набору обязательных материалов/модулей закреплённой версии вместо «посещённых» (count-based), **1a** durable-дедуп сертификата поверх 24ч TTL, **1b** per-recipient дедуп рассылки + throw-safety; план `2026-06-27-audit-tail-progress-dedup-notify.md`; полный `pnpm test:backend` 246 файлов / 1876 тестов / 0 падений, typecheck 8/8; ветка `fix/2026-06-27-audit-tail-progress-dedup-notify`, коммиты `3fcf874`…`0ea7697`; **без миграций** — последняя 0057; **без новых прав**; поведенческое изменение: 100% требует всех обязательных материалов). **Предыдущее:** 2026-06-27 (Полный `pnpm test:backend` доведён до зелёного из корня — устранён давний «Cyrillic tinypool-краш»: реальная причина — DI-баг `mvp.domains.http.integration` (2 недостающих провайдера `MvpController` + `abortOnError:false`) под маской `process.abort()`, плюс 15 boot-хуков `}, 30_000)`→`120_000` и cwd-зависимый путь в `migrations.0055`; **полный сьют 244 файла / 1853 теста, 0 падений** в один процесс; изменены только тест-файлы + CLAUDE.md Gotchas; ветка `chore/2026-06-27-phase-5c-2-quality-followups`, §5.148, коммиты `cec6ca9`+`026e4d4`; **без миграций** — последняя 0057; **без новых прав**). **Предыдущее:** 2026-06-27 (Фикс латентной коллизии нумерации документов при year/month-сбросе: токен `{period}` (`CERT-2026-000001`, выбор владельца «год в номере») + validate-before-commit; §5.147; **без миграции** — последняя 0057; **без новых прав**; не закоммичено). **Предыдущее:** 2026-06-24 (Завершение редизайна UI + scope-rename `@cdoprof/*`→`@trudskill/*`: миграция 14 экранов на дизайн-систему + переименование 8 пакетов/962 ссылки/125 файлов; ветка `chore/2026-06-23-phase-11-stabilization-security`, §5.143; **без миграций** — последняя 0057; **без новых прав**; не закоммичено). **Предыдущее:** 2026-06-24 (UI-редизайн + ребрендинг `CDOProf` → `trudskill`, frontend-only; дизайн-токены индиго+коралл WCAG AA + Inter + focus-ring, 5 экранов учащегося/входа/тестов + карточка профиля; ветка `chore/2026-06-23-phase-11-stabilization-security`, §5.142; **без миграций** — последняя 0057; **без новых прав**; не закоммичено). **Предыдущее:** 2026-06-23 (Phase 7 — активация онлайн-платежей multi-provider + per-tenant: реестр `PAYMENT_PROVIDER_REGISTRY` + `PaymentProviderResolver.forTenant`; 4 реальных адаптера (ЮKassa/Т-Касса/CloudPayments/Robokassa) credential-gated; миграция **0056** `payments.payment_provider_settings` + право `payments.configure`; webhook `/payments/webhook/:providerCode`; frontend `/admin/payments/settings`; ветка `feat/2026-06-22-phase-7-payments-multi-provider`, §5.139; backend payments 85 / typecheck 8/8 / ESLint clean / frontend payments 12; последняя миграция 0056). **Предыдущее:** 2026-06-22 (Полный зелёный прогон всех сьют + sharpen DI-guard: `di-explicit-injection.test.ts` сканирует только `@Injectable()`/`@Controller()`-классы → 4 ложных срабатывания убраны + вскрыты/исправлены 2 реальных DI-deadlock'а в `integrations.controller.ts` (`ExportsController`/`SyncLogsController` без `@Inject`); + `payments.http.integration` hookTimeout 30→120с; ветка `fix/2026-06-21-payments-module-iam-boot`, §5.138, коммит `19b5a6f`; **новых миграций нет** — последняя 0055). **Предыдущее:** 2026-06-21 (Deploy-readiness аудит — закрыты 3 блокера подъёма пилота: backend-образ без каталога миграций, невалидный `infra/.env.production.example` (6 провалов boot-схемы + разрешён `SECRETS_PROVIDER=env` в проде по решению владельца), frontend-образ без `public/` (PWA `/sw.js`+иконки); 3 file-content стража Dockerfile'ов/prod-env; ветка `fix/2026-06-21-deploy-readiness-image-packaging-and-prod-env`, §5.136; **новых миграций нет** — последняя 0055). **Предыдущее:** 2026-06-20 (Issue 4 — свежая БД накатывает всю цепочку миграций из коробки, edit-in-place 6 фиксов + full-chain testcontainers-тест; ветка `fix/2026-06-20-migration-chain-fresh-bootstrap`, §5.135, **PR #265 слит**; **новых миграций нет** — последняя 0055; раннер/checksum не тронуты). **Предыдущее:** 2026-06-20 (Phase 8 — provider-agnostic webinar seam, dormant multi-provider + per-tenant; ветка `feat/2026-06-20-phase-8-webinar-provider-seam`, §5.134, **слит #264**; **миграция 0055** — права `webinars.read/write/attend/configure` + таблица `communication.webinar_provider_settings` + индекс `provider_session_id`, последняя 0055). **Предыдущее:** 2026-06-20 (Phase 7 — provider-agnostic payment seam, dormant; ветка `feat/2026-06-20-phase-7-payment-provider-seam`, §5.133, **слит #262**; **миграция 0054** — схема `payments`, права `payments.read/write/self_purchase`). **Предыдущее:** 2026-06-17 (Phase 6 — проброс `signatureStatus` end-to-end + prod-guarded fake staging-signer; ветка `feat/2026-06-17-document-signature-status-passthrough`, §5.131; **без миграции** — последняя 0053; **без новых прав**). **Предыдущее:** 2026-06-16 (ЕСИА (Госуслуги) — вход + идентификация, provider-agnostic OAuth seam, dormant; ветка `feat/2026-06-16-esia-oauth-login-identity`, §5.130; **без миграции** — последняя 0053; **без новых прав**). **Предыдущее:** 2026-06-15 (Phase 6 — provider-agnostic e-signature seam (НЭП), ветка `feat/2026-06-15-phase-6-esign-provider-seam`, §5.129; миграция **0053** `documents.sign`, последняя 0053). **Предыдущее:** 2026-06-15 (Завершение пагинации ФРДО — 5-й экспортёр, незакоммиченный в §5.127, + его >1000-regression, §5.128; миграций нет, последняя 0052). **Предыдущее:** 2026-06-15 (Пагинация 4 реестровых экспортёров — устранено молчаливое обрезание на 1000 строк, §5.127; миграций нет, последняя 0052). **Предыдущее:** 2026-06-15 (Phase 6 реестры Ростехнадзор + Минздрав-НМО, ветка `feat/2026-06-14-phase-6-rostechnadzor-nmo-registries`, §5.126; миграций нет, последняя 0052). **Предыдущее:** 2026-06-14 (Phase 0 readiness плейбук, docs-only, ветка `docs/2026-06-14-phase-0-readiness-spec`, §5.125). **Предыдущее:** 2026-06-12 (Phase 9 Plan A — SCORM 1.2 импорт + плеер, ветка `feat/2026-06-12-phase-9-plan-a-scorm`, §5.119; миграции до 0052). **Предыдущее:** 2026-06-11, вечер (Phase 4 Plan B — прокторинг, слит #241, §5.118; Plan A #240 слит утром). **Предыдущее:** 2026-06-11 (Phase 4 Plan A — идентификация личности, PR #240, §5.117). **Предыдущее:** 2026-06-08 (Phase 0 деплой-фундамент слит #235; Production auth readiness A+B → PR #237, §5.114). **Предыдущее:** 2026-06-07 (фикс e2e `aggregateReviewerQueue` §5.113 слит #234; Phase 5 Plan 5C §5.112 слит #233; Plan 5B-2 §5.111; визуальная дизайн-система §5.110 PR #231; Phase 5 Plan 5B PR #229; Plan 5A #228, Wave 2 A/B/C #222-226)

## 3. Current Project Status

### Что уже сделано

- Сформирована многосервисная архитектура (`apps/frontend`, `apps/backend`, `apps/worker`, `apps/realtime`).
- Добавлены ключевые backend-модули: IAM, документы, e-sign, коммуникации, интеграции, аудит, tenant/workspace.
- Добавлены frontend-страницы и feature-слои для auth, navigation, communication, integrations, MVP-сценариев.
- Есть инфраструктурные SQL-миграции, включая IAM/security и operational индексы.
- Подготовлены базовые runbook/observability/testing документы.

### Какие задачи выполнены (на уровне репозитория)

- Базовый foundation монорепозитория и окружения.
- Контрактный слой API и shared types packages.
- Набор unit/integration/e2e тестов по ключевым доменам (по структуре репозитория).

### Какие задачи в работе

- Поддержание синхронности README ↔ `LMS_AGENT_HANDOFF.md` после каждой инженерной сессии ([протокол](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)).

### Что делать следующим шагом

1. Открыть [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) §14 Recommended Next Steps и §20 Final Status.
2. Сверить с [docs/TZ_MVP_TRACEABILITY.md](docs/TZ_MVP_TRACEABILITY.md) и при необходимости с §41 [SDOPROF_TZ_FINAL.md](SDOPROF_TZ_FINAL.md).
3. Запустить `pnpm -s ci:check` перед и после значимых изменений; обновить этот README и handoff.

## 4. Iteration Log (текущая итерация)

### Измененные файлы

- `README.md`, `docs/DOCUMENTATION_MAP.md`, `LMS_AGENT_HANDOFF.md`, `SDOPROF_TZ_FINAL.md`, `AGENTS.md`, `.cursor/rules/lms-multi-agent-handoff.mdc` (протокол многоагентной передачи, Cursor rule `alwaysApply`, устранение противоречий README vs handoff).

### Принятые решения в итерации

- Один смысл «где остановились»: детали в `LMS_AGENT_HANDOFF.md`, краткое резюме в `README` §2; фраза «продолжай по ТЗ» → порядок в [DOCUMENTATION_MAP.md](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol).

### Проверки в итерации

- Сверка содержимого README, handoff и карты документации на логическую согласованность (без обязательного полного `ci:check` только для правок markdown).

## 5. Backlog (приоритезированный)

Укрупнённый продуктовый backlog и BL — только в [SDOPROF_TZ_FINAL.md](SDOPROF_TZ_FINAL.md) §41 и матрице [docs/TZ_MVP_TRACEABILITY.md](docs/TZ_MVP_TRACEABILITY.md). Ниже — **репозиторные** приоритеты, если не задан иной порядок:

1. **P0 Security/Auth:** см. [docs/security-remediation-roadmap.md](docs/security-remediation-roadmap.md) и handoff §10.
2. **P0 Reliability:** health/readiness/metrics, [docs/operations-runbook.md](docs/operations-runbook.md).
3. **P1 UX Learning Flow:** состояния загрузки/ошибок в learning flow.
4. **P1 Reporting:** сценарии KPI/экспорта (см. BL-008 в трассировке).
5. **P2 Integrations:** idempotency/observability webhooks.

## 6. Test Status

### Что уже проходило (по доступным артефактам репозитория)

- В репозитории присутствуют unit/integration/e2e тесты для backend/frontend/packages.
- Общий запуск проверок описан в quick start и `docs/run-tests.md`.

### Статус текущей итерации

- Запущен consolidated regression для IAM/role-access + permission-boundary HTTP integration:
  - `pnpm exec vitest run apps/frontend/src/e2e/lms-role-flows.e2e.test.ts apps/frontend/src/e2e/role-access.e2e.test.ts apps/frontend/src/e2e/auth-routing.e2e.test.ts apps/backend/src/modules/iam/permission.guard.test.ts apps/backend/src/modules/iam/auth.security.test.ts apps/backend/src/modules/workspace/workspace.http.integration.test.ts apps/backend/src/modules/documents/documents.http.integration.test.ts`
  - Результат: success, 7 files / 23 tests passed.

## 7. Known Issues / Open Errors

- **V1.1 AV-гейт ставится с `ANTIVIRUS_ENABLED=false`** — `NoopAntivirusScanner` помечает файлы `clean` (dev/пилот). Реальная защита требует поднятого clamd + `ANTIVIRUS_ENABLED=true` (ops, spec §9). Файлы, загруженные до V1.1, остаются `pending` и сканируются лениво при первом скачивании. Задачи 7-13 V1.1 (проактивный скан при submit, `antivirusStatus` в чтении сабмишена + очереди ревью, HTTP-гейт boundary, гейтинг кнопки скачивания у ревьюера, статус у слушателя) — **выполнены** (handoff §5.102, ветка `feat/2026-06-02-v1.1-antivirus-tasks-7-13`). Единственный остаток — ops: поднять clamd и включить флаг.
- Для полной картины по тестам и командам см. таблицу в [LMS_AGENT_HANDOFF.md](LMS_AGENT_HANDOFF.md) §12; здесь держите только краткое резюме после крупных прогонов.
- Регулярно синхронизировать `AI Agent State` с handoff ([протокол](docs/DOCUMENTATION_MAP.md#agent-handoff-protocol)).

## 8. Quick Start

1. Скопировать env: `cp .env.example .env`.
2. Поднять сервисы: `docker compose -f infra/docker-compose.yml up -d --build`.
3. Запустить проверки: `pnpm test`.

Backend при старте применяет SQL миграции (`apps/backend/migrations`) и baseline seed IAM при `DB_MIGRATIONS_ENABLED=true`.

## 9. Operational Endpoints

- Backend: `/api/v1/health/live`, `/api/v1/health/ready`, `/api/v1/health/startup`, `/api/v1/metrics`.
- Realtime: `/health`, `/ready`.
