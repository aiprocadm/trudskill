apiVersion: v1
kind: Service
metadata:
  name: {{ include "cdoprof.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "cdoprof.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
