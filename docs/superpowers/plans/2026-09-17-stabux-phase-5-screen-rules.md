# Фаза 5 — единые правила экранов (ТЗ «Стабилизация, UX и развитие», задачи 5.1–5.7)

Дата: 2026-09-17. Основание: очередь выполнения, позиция 7. Предшественники — фазы 2, 3, 4 и 10
(§5.469–§5.482), закрытые по одной задаче на PR без отдельного плана; для фазы 5 план нужен:
семь правил трогают десятки экранов, и порядок важен.

## Как читать ТЗ фазы

Каждое правило — сначала **компонент или утилита в `@trudskill/ui`** (правило живёт один раз),
затем запись в `docs/ui/patterns.md`, затем раскатка по экранам. Сторож на каждое правило —
как в фазах 2–4: по исходникам, с подсаженными поломками. Задача = один PR ≤ 30 файлов,
зелёный `pnpm ci:check` (код выхода из файла, документы отформатированы до запуска).

Порядок задач — порядок ТЗ, с одной оговоркой: 5.6 (единый шаблон списка) собирает остатки
5.1 и 5.5 (таблицы со своими колонками «Действия», панели массовых действий), поэтому идёт
после них.

## Задача 1 — 5.1 (Э1) Один набор действий в строке таблицы

**Файлы:** `packages/ui/src/components/overflow-menu/` (новый), `packages/ui/src/components/table/row-actions.ts` (новый), `packages/ui/src/components/table/index.tsx`, `packages/ui/src/styles/foundation.ts`, `packages/ui/src/styles/tables.ts`, четыре экрана с действием «Открыть …», `apps/frontend/src/e2e/one-action-set-per-row.e2e.test.ts` (новый), `docs/ui/patterns.md` (новый).

**Задачи:**

- [x] `OverflowMenu` — меню «…» на `<details>`, опасные пункты внизу за разделителем, красным; тест.
- [x] `splitRowActions` — чистое правило: одно основное действие в строке, остальное в меню; единственное неопасное — сразу; опасное основным быть не может; тест.
- [x] `DataTable` рисует действия через правило — все 18 экранов с `rowActions` получают меню автоматически.
- [x] Экраны с «Открыть …» помечают его `primary` (документы/задачи, заявки на проверку личности, слушатели, группы).
- [x] Сторож: таблица по правилу; опасные внизу; ≤1 `primary` на экран; очередь своих колонок «Действия» (12 мест в 5 файлах) сверяется на равенство — тает в 5.6.
- [x] `docs/ui/patterns.md` — запись Э1.

**Приёмка:** на «Арендаторах платформы» в строке — только «…»; в меню «Сменить тариф», «Войти от имени», ниже за чертой — «Приостановить центр» / «Перевести в архив» красным. На «Группах» — «Открыть группу» в строке.

## Задача 2 — 5.2 (Э2) Действие соответствует состоянию объекта

**Файлы:** карточка курса (`courses-screens.tsx`: «Опубликовать» → «Снять с публикации» / «Создать новую версию»), «Мои тесты» («Продолжить» при 0 попыток), «Документы» у руководителя (пустая колонка «Действия» и «Создать шаблон» без права), сторож.

**Задачи:**

- [x] Ревизия трёх примеров ТЗ: карточка курса — кнопка не смотрела на `status`, сервер публиковал опубликованное молча; «Мои тесты» — кнопка верная («Продолжить» только у начатой попытки), лгала подпись «0 из 1»; «Документы» — пустая колонка «Действия» (правило `DataTable`) и «Создать шаблон» / «Создать первый шаблон» без права.
- [x] Правило: чистая функция «состояние → действие» (`courseHeaderAction`, `attemptsCaption`); `DataTable` рисует колонку действий только при наличии действий; создание шаблона — за правом `documents.write`.
- [x] Сервер — источник правды: `publishCourse` → `409 course_not_draft`, `archiveCourse` → `409 course_already_archived`; тест сервиса.
- [x] Сторож `action-matches-state.e2e.test.ts` + запись Э2 в `patterns.md`.
- [ ] Не сделано осознанно: «Снять с публикации» — ручки нет, а что значит снятие с публикации для идущих групп, ТЗ не определяет; вопрос владельцу (журнал 437).

**Приёмка:** критерий ТЗ 5.2 дословно: у опубликованного курса главная кнопка — «Создать новую версию»; при начатой попытке — «Продолжить» с честной подписью, при исчерпанных — кнопки нет; у руководителя без права — ни колонки «Действия», ни «Создать шаблон».

## Задача 3 — 5.3 (Э3) Подтверждение необратимых действий

**Файлы:** `useConfirmDialog` в пакете (уже есть), экраны: «Приостановить центр», «Перевести в архив», «Отозвать лицензию», «Архивировать слушателя», «Закрыть группу», «Завершить тест»; сторож `irreversible-needs-typing` расширяется.

**Задачи:**

- [x] Проверены шесть действий ТЗ: три шли с одного нажатия без диалога («Приостановить центр», «Перевести в архив», «Закрыть группу» — одиночно и цепочкой), «Завершить тест» — тоже; «Отозвать лицензию» и «Обезличивание» — эталоны с вводом; «Архивировать слушателей» — диалог с числом и последствием (добавлен опасный тон).
- [x] Запросы диалогов — чистые функции с тестами (`statusChangeRequest`, `closeGroupRequest`, `finishTestRequest`); ввод названия/кода — `requireTyping` пакета (`CMP-005`), не копия механики: архив центра — код, закрытие группы — название.
- [x] Сторож `irreversible-confirms-object.e2e.test.ts`: шесть действий на месте; каждый опасный диалог на экранах называет объект и последствие; автосдача теста — без диалога.
- [x] Запись Э3 в `docs/ui/patterns.md`; у архива центра появилось своё имя действия («Перевести центр в архив» вместо «Перевести в «В архиве»»).

## Задача 4 — 5.4 (Э4) Опасное действие — не главная кнопка экрана

**Файлы:** карточка группы («Закрыть группу» — главная оранжевая кнопка), `PageHeader` (`primaryAction` не может быть `danger`), сторож `primary-action-budget` расширяется.

**Задачи:**

- [x] Тип: `primaryAction?: SafePageAction` (`PageAction & { danger?: false }`) — `danger: true` и вычисленный `danger: boolean` не компилируются (проверено двумя подсаженными поломками через `tsc`). Опасное вторичное — красным, в меню «Ещё» внизу; порядок считает компонент.
- [x] Карточка группы: главная кнопка — «Зачислить слушателя» (форма переехала в панель), «Закрыть группу» — вторичное красное. Попутно закрыт дрейф от Э2: обе формы карточки показывались без прав `enrollments.write` / `groups.write` (сверено по живой базе).
- [x] Сплошной обход 219 файлов экранов (8 агентов, каждая находка проверена на опровержение): три нарушения, все в пути закрытия группы; четвёртое (аннулирование) — попутно.
- [x] Сторож `danger-is-not-primary.e2e.test.ts` на ДВА признака: подпись (узкий словарь) и намерение (`tone: 'danger'` в подтверждении Э3). Словаря мало: «Запустить цепочку» по подписи конструктивна. 12 подсаженных поломок.
- [x] Запись Э4 в `docs/ui/patterns.md`.

## Задача 5 — 5.5 (Э5) Панель массовых действий — липкая

**Файлы:** `BulkActionBar` в пакете (`CMP-011`), стили (липкая к низу), «Слушатели» и остальные списки с выделением, сторож `selection-has-bulk-action` расширяется.

**Задачи:**

- [x] `BulkActionBar` прилипает к низу ОКНА (`position: fixed`, отступ слева на `--ui-shell-nav`) и сам оставляет под себя место распоркой; счётчик и порядок «опасное последним» считает компонент. Прежнее `position: sticky; bottom: 0` не работало ни разу — у последнего элемента страницы прилипать не к чему (журнал 448).
- [x] Ширина колонки меню — одна переменная `--ui-shell-nav` на три состояния оболочки (260 / 64 / 0 px) вместо трёх чисел (журнал 450).
- [x] «Слушатели»: «Добавить в группу» (право `enrollments.write`, ключ идемпотентности, итог поимённо) + «Выгрузить выбранных»; «Группы»: «Выгрузить выбранные». Выгрузка — общая `lib/export/csv.ts` (BOM + `;` + CRLF, открывается в Excel).
- [x] Сторож `bulk-bar-is-sticky.e2e.test.ts` (6 проверок, 9 подсаженных поломок) + запись Э5 в `patterns.md`.
- [ ] Не сделано осознанно: «Назначить курс» пачкой — ручки массового назначения курса нет, отдельная работа; действие появится вместе с ней.

## Задача 6 — 5.6 (Э6) Один шаблон списка для всех списков

**Файлы:** `ListPage` (`packages/ui/src/composition/list-page.tsx`), «Группы», «Курсы», «Пользователи» и очередь `list-page-ratchet`; таблицы со своими колонками «Действия» (из очереди сторожа 5.1); `Pagination` при одной странице.

**Задачи:**

- [x] Порядок блоков шаблона задаёт КАРКАС, а не экран: слоты `savedViews`, `filters`, `secondaryFilters`, `columnPicker`, `bulkBar`; 12 экранов переведены, «Слушатели» — эталон (журнал 458).
- [x] «Группы» получили поиск и отбор по статусу — оба НА СЕРВЕРЕ (журнал 452); «Пользователи» — колонку «Роль», ради которой `GET /users` отдаёт коды ролей строк страницы одним запросом (453).
- [x] Пагинация не показывается, когда страница одна (455).
- [x] Сторож `one-list-template.e2e.test.ts` (7 проверок, 10 подсаженных поломок) + запись Э6 в `patterns.md`.
- [ ] Очередь своих колонок «Действия» (5.1) обнулена НЕ полностью: 12 → 7. Переведены кнопочные (эксплуатация 3, оплата 1, учебные пакеты 1). У оставшихся семи в ячейке не кнопка, а ПОЛЕ — выбор файла ответа ведомства (5) и форма проверки работы (2); пунктом меню им быть нельзя, место — в панели, и оба экрана разбираются в 5.7. Причина записана у каждой записи храповика (журнал 454).
- [ ] Не сделано осознанно: на странице сделок панель отбора занята формой создания сделки — в очередь 5.7 (журнал 456).

## Задача 7 — 5.7 (Э7) Разбить «простыни»

**Файлы:** «Арендаторы платформы», «Документы», «Настройки» tenant_admin, карточка курса; `DetailLayout`/вкладки из пакета (`TPL-002`: один уровень вкладок).

**Задачи:**

- [x] Каждая «простыня» режется на вкладки ОДНОГО уровня: «Арендаторы платформы» (журнал 460), «Документы» (459), «Настройки» (461), карточка курса (462). Компонент `PageTabs` + `TabPanel` заведён в пакете — своего не было вовсе (463).
- [x] Формы создания — в боковой панели: «Новый арендатор» переехал из середины ленты в панель и стал первичным действием шапки, с предупреждением о несохранённом (`CMP-005`).
- [x] Активная вкладка отражается в адресе (`?tab=`), незнакомая возвращает к первой, отбор при переключении сохраняется. Маршруты не тронуты (Р2): старые ссылки с якорем работают и переводятся в `?tab=`.
- [x] «Настройки» — честные вкладки: оглавление разделено на «Настроить здесь» (кнопки) и «Открыть отдельный раздел» (ссылки).
- [x] Сторож `sheets-are-split.e2e.test.ts` (8 проверок, 11 подсаженных поломок) + запись Э7 в `patterns.md`; `settings-template` переведён с якорей на вкладки без ослабления инварианта.
- [ ] Не сделано осознанно: «Эксплуатация» и «Интеграции» продолжают рисовать свои полосы вкладок вручную — их нет в списке 5.7, перевод молча расширил бы задачу (журнал 463).

**Приёмка фазы:** чек-лист ТЗ, пункты 8 («подтверждение необратимого действия с названием объекта») и 6 («нет горизонтальной прокрутки при 1280 px») — по коду; остальное — на стенде за владельцем.
