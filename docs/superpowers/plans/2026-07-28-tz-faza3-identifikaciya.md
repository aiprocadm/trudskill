# Фаза 3 «Идентификация» — план реализации

> **Для агентов:** реализовывать по одной задаче за под-PR (`superpowers:executing-plans`). Фаза стартует только после **апрува владельца** (ТЗ §13, §15).

**Источник требований:** [`TZ_TRUDSKILL_ARENDNAYA_SDO.md`](../../../TZ_TRUDSKILL_ARENDNAYA_SDO.md) §5 (ЭПИК C) + ФТ-E3 из §7. Статус — [`docs/TZ_ARENDNAYA_SDO_STATUS.md`](../../TZ_ARENDNAYA_SDO_STATUS.md).

**Цель фазы:** учебный центр включает нужный уровень идентификации галочками в настройках, слушатель без подтверждённой личности не попадает на итоговый тест, а в «личном деле» лежит доказательная цепочка, которую можно отдать проверяющему одним PDF.

**Критерий приёмки эпика (ТЗ §5):** тенант включает «Уровень 2 + код на экзамен»; слушатель без подтверждённой личности не допускается к итоговому тесту; в личном деле видна полная доказательная цепочка; выгрузка в реестр не собирается, пока у слушателя нет валидного СНИЛС.

---

## Открытый вопрос №4 — СМС-код на экзамен и провайдер (нужно решение владельца)

**Что показала разведка кода (2026-07-28):**

- **Кирпичи есть, политики нет.** Три гейта уже стоят перед стартом итогового теста (`mvp.service.ts:3377–3381`): `assertPreExamAuthGate` (одноразовый код, `0044`), `assertIdentityVerificationGate` (селфи+паспорт, `0050`), `assertProctoringGate` (`0051`). Но включаются они **флагами на связке группа-курс**, а не уровнем политики тенанта. Требование ФТ-C1 — собрать их в настраиваемую политику 0–3, а не написать заново.
- **Одноразовый код на экзамен уже доставляется по email** (Фаза 0 Task 4, §5.171). СМС — это ВТОРОЙ канал доставки того же токена, а не новый механизм: `PreExamToken` и гейт не меняются.
- **esign legal-log существует** (`esign.service.ts:748`, `legalLogEntries`) — ПЭП-уровень (C1.1) расширяет типы событий, а не заводит новый журнал.
- **Согласие одно на всё:** в `IdentityVerification` есть единственный `consentAt` (`mvp.types.ts:494`). ФТ-C3.2 требует РАЗДЕЛИТЬ согласие на обработку ПДн и отдельное согласие на фото — это юридически чувствительная зона, граничащая с биометрией.
- **Очередь модерации и экран слушателя есть** (`features/identity-verification/`) — C1.2 доводит UX (повторная подача после отклонения), а не строит с нуля.
- **Проверка СНИЛС есть, pre-export проверки нет:** валидация контрольной суммы работает (`learners-bulk-import.service.ts`), но выгрузка в реестр (`0045`/`0046`) собирается и без СНИЛС — ФТ-C4.1 добавляет проверку полноты ДО формирования файла со списком «у кого чего не хватает».

**Рекомендация:** ответ на вопрос №4 нужен **только к Task 5** (СМС-канал). Остальные задачи фазы от него не зависят — ровно как вопрос №1 в Фазе 2 касался единственной задачи.

- **Апрув плана можно давать, не отвечая на №4** — фаза стартует, ответ нужен позже.
- **Рекомендуемый ответ по существу: СМС в MVP не нужен.** Уровень 3 уже закрывается одноразовым кодом по email + прокторингом. СМС добавляет ежемесячный счёт и договор с оператором ради канала, который в дистанционном обучении по ОТ не является обязательным. Разумнее: сделать **шов `SmsProvider`** (как `VideoProvider` и `WebinarProvider`) со спящим `noop`, а конкретного оператора подключить в Фазе 4, когда появится биллинг аренды и станет ясно, кто платит за сообщения.
- Если ответ будет «СМС нужен сразу» — меняется только Task 5: вместо `noop` пишется адаптер оператора. Шов и гейт не переделываются.

**Другие открытые вопросы фазы не блокируют:** №6 (ЕСИА) — по ТЗ это исследование Фазы 6, здесь только интерфейсная готовность (метод `'esia'` уже заложен).

---

## Архитектурные решения (зафиксировать на апруве)

1. **Политика — данные, а не код.** Уровень хранится в настройках тенанта и переопределяется на курсе/направлении; гейты читают вычисленную политику, а не свои флаги. Нынешние флаги на группе-курсе остаются как переопределение — ломать идущие группы нельзя.
2. **Уровень 0 всегда включён и не выключается.** Логин с обязательной сменой пароля — это не опция; галочка, которой можно снять базовую защиту, опаснее её отсутствия.
3. **Гейты не переписываются, а переключаются на политику.** Три существующих гейта остаются на месте (они уже покрыты тестами и стоят в правильной точке), меняется только источник решения «включено ли».
4. **ПЭП расширяет esign legal-log, а не заводит второй журнал.** Второй источник правды о подписанных действиях сделал бы «личное дело» недоказуемым.
5. **Согласие на фото — отдельная сущность от согласия на ПДн.** Объединять их нельзя: отзыв согласия на фото не должен отзывать согласие на обработку данных, и наоборот.
6. **Личное дело собирается из существующих источников** (идентификация, попытки, документы, legal-log) и не заводит своей таблицы: копия доказательств разошлась бы с оригиналом.
7. **Миграции только аддитивные** (ТЗ §13); URL и контракты `packages/api-contracts` не ломаем. Следующий свободный номер — **`0066`**.

---

## Порядок выполнения (каждый Task — отдельный под-PR)

| #   | Задача                                               | ФТ   | Зависит от ответа №4 |
| --- | ---------------------------------------------------- | ---- | -------------------- |
| 1   | Политика идентификации: хранение и вычисление        | C1   | нет                  |
| 2   | Гейты переключаются на политику                      | C1   | нет                  |
| 3   | ПЭП: соглашение и подписанные действия               | C1.1 | нет                  |
| 4   | Уровень 2: доведение UX модерации и повторной подачи | C1.2 | нет                  |
| 5   | Шов `SmsProvider` и второй канал доставки кода       | C1.3 | **да**               |
| 6   | Раздельные согласия на ПДн и на фото                 | C3.2 | нет                  |
| 7   | Настраиваемый срок хранения снимков                  | C3.1 | нет                  |
| 8   | СНИЛС: проверка полноты до выгрузки в реестр         | C4.1 | нет                  |
| 9   | «Личное дело» слушателя + экспорт PDF                | C2   | нет                  |
| 10  | Сквозной сценарий «экзамен → протокол → реестр»      | E3   | нет                  |

---

## Task 1 — ФТ-C1: политика идентификации (хранение и вычисление)

**Files:** Create: `apps/backend/migrations/0066_learning_identity_policy.sql`; `apps/backend/src/modules/mvp/identity/identity-policy.ts` (+тест) — чистое вычисление действующего уровня. Modify: `apps/backend/src/modules/mvp/mvp.types.ts` (тип политики), `mvp.dto.ts`, `mvp.controller.ts` (чтение/запись политики тенанта).

**Tasks:**

- [x] Миграция `0066`: таблица `learning.identity_policies` (`tenant_id`, `scope` check `tenant|direction|course`, `scope_id` null для тенанта, `level` check `0..3`, `require_photo_before_exam` boolean, `created_at`, `updated_at`) + уникальность по (`tenant_id`, `scope`, `scope_id`); право `identity.configure` с раздачей `platform_admin`/`tenant_admin`.
- [x] Чистая функция `resolveIdentityPolicy(tenantPolicy, directionPolicy, coursePolicy)` — побеждает самый узкий заданный уровень; ничего не задано → уровень 0. Уровень 0 **нельзя** отключить: функция никогда не возвращает «ничего не требуется».
- [x] Эндпоинты `GET/PUT /identity-policies` под `identity.configure`; чтение действующей политики для курса — отдельным методом сервиса.
- [x] Тесты: переопределение курсом сильнее направления и тенанта; отсутствие записей = уровень 0; мусорный уровень откатывается к 0; чужой тенант не видит политику.

**Acceptance:** тенант задаёт уровень в настройках, курс может ужесточить его; уровень 0 нельзя выключить ни одним сочетанием настроек; `test:isolation` не деградировал.

**Выполнено 2026-07-28 (§5.202).** Deviations: (1) **Уникальность через `coalesce(scope_id, '')`**, а не обычный UNIQUE: в SQL `NULL <> NULL`, поэтому две тенантские политики (у обеих `scope_id IS NULL`) прошли бы мимо обычного ограничения — и уровень допуска к экзамену выбирался бы «как повезёт». (2) **CHECK на согласованность области:** у `scope='tenant'` объект обязан быть пустым, у остальных — заполненным; «политика курса без курса» это настройка-призрак. (3) **Мусорный уровень опускается до 0, а НЕ поднимается до 3.** Поднять значило бы молча запретить экзамен всей группе из-за опечатки в данных; опустить — вернуться к базовому логину, что видно и исправимо. (4) **Побеждает самая узкая запись, это НЕ максимум уровней:** если центр держит уровень 2, а на ознакомительном курсе поставил 0, применяется 0 — иначе настройка курса не имела бы смысла. (5) Право `identity.configure` выдано только `platform_admin`/`tenant_admin`: ослабление политики = допуск к экзамену без подтверждения личности, это не решение методиста (проверяется тестом миграции). (6) Ограничения проверены на живой базе: дубль области, курс без объекта и уровень 9 отвергаются, повторный прогон идемпотентен.

## Task 2 — ФТ-C1: гейты переключаются на политику

**Files:** Modify: `apps/backend/src/modules/mvp/mvp.service.ts` (`assertPreExamAuthGate`, `assertIdentityVerificationGate`, `assertProctoringGate` читают политику), тесты рядом.

**Tasks:**

- [x] Гейты берут решение «включено ли» из вычисленной политики, а не только из флагов группы-курса. Существующие флаги остаются как ужесточение: снятие политики не должно ослаблять уже настроенную группу.
- [x] Уровень 2 → требуется подтверждённая личность; уровень 3 → плюс одноразовый код. **`require_photo_before_exam` НЕ применяется** — см. deviations.
- [x] Промежуточные тесты модулей идентификацией не гейтятся (текущее поведение сохраняется — иначе учёба встанет).
- [x] Тест на обход: старт итогового теста запросом мимо интерфейса при уровне 2 без подтверждения — отказ.

**Acceptance:** включение уровня 2 в настройках закрывает итоговый тест для неподтверждённых слушателей без единой правки на группе; выключение политики не открывает то, что явно ужесточено на группе.

**Выполнено 2026-07-28 (§5.203), кроме одного пункта.** Deviations: (1) **`require_photo_before_exam` пока НЕ применяется гейтом.** Флаг хранится и возвращается политикой (Task 1), но «фото непосредственно перед экзаменом» требует захвата снимка на конкретную попытку — такого потока в системе нет. Придумывать семантику «свежести» из головы хуже, чем честно отложить: применение переезжает в **Task 4**, где и делается UX подачи фото. Отмечено здесь, в handoff и в PR. (2) **Требование включается «либо политикой, либо флагом», а не заменой.** Флаги на связке группа-курс остались с прежних фаз на уже идущих группах: включение политики не должно их ослаблять, а выключение — открывать то, что центр явно ужесточил. Тест на это есть. (3) **Политика разрешается в контроллере и передаётся внутрь.** `startAttempt` синхронный, а политика лежит в отдельной таблице `0066`; делать метод async значило бы тронуть все его вызовы. Не передана политика → работают только прежние флаги (обратная совместимость для внутренних вызовов и тестов). (4) **Прокторинг не трогали:** по ТЗ он остаётся выборочным «для выбранных экзаменов», а не включается уровнем. (5) Попутно: HTTP-интеграционный тест поднимает `MvpController` своим модулем и упал на новой зависимости — ровно грабля из CLAUDE.md; провайдер добавлен в тестовый модуль.

## Task 3 — ФТ-C1.1: ПЭП — соглашение и подписанные действия

**Files:** Create: `apps/backend/src/modules/esign/simple-signature.service.ts` (+тест); фронт — экран принятия соглашения при первом входе. Modify: `apps/backend/src/modules/esign/esign.service.ts` (новые типы событий legal-log), миграция `0067` (текст соглашения per tenant + факт принятия).

**Tasks:**

- [x] Текст «Соглашения об электронном взаимодействии» редактируется тенантом; хранится с версией и хэшем — принятая версия должна быть доказуема спустя годы.
- [x] Принятие фиксирует кто/когда/IP/user-agent/**хэш текста** (не ссылку на текст: текст могут отредактировать позже).
- [x] После принятия действия «Ознакомлен», ответы на тесты и заявления пишутся в существующий esign legal-log новыми типами событий.
- [x] Тесты: без принятия соглашения ПЭП-действия не подписываются; повторное принятие той же версии идемпотентно; изменение текста требует нового принятия.

**Acceptance:** в личном деле видно, какую именно версию соглашения подписал слушатель и когда; правка текста задним числом не подменяет уже подписанное.

**Выполнено 2026-07-28 (§5.204), кроме экрана первого входа.** **НАХОДКА, важная для Task 9:** таблица `esign.legal_log_entries` существует с миграции `0004` и защищена триггером от изменений (append-only), но `EsignService` пишет записи **ТОЛЬКО В ПАМЯТЬ** (`InMemoryEsignState.legalLogEntries`) — в базу не попадает ничего. То есть журнал, на котором держится юридическая значимость, не переживает перезапуск приложения. ПЭП-события пишутся напрямую в существующую таблицу через `LegalLogWriter` (второго журнала не заводим, как требует план); **приведение остальных событий `EsignService` к durable-записи — отдельная задача, не входила в Task 3**. Deviations: (1) **Новая версия соглашения создаётся только при реальном изменении текста:** хэш считается по нормализованному тексту (CRLF→LF, хвостовые пробелы, крайние пустые строки), иначе копирование текста из Word породило бы «новую версию» и заставило всех слушателей принимать заново при неизменном содержании. (2) **Повторное принятие идемпотентно и НЕ сдвигает момент подписи** — юридически значим первый момент; в Postgres это `on conflict do update set user_id = excluded.user_id` (no-op), в БД проверено. (3) **`signAction` возвращает `false`, а не бросает**, если соглашение не принято: молча «подписать» без принятого соглашения — подделка доказательства, но и блокировать действие должен вызывающий код, а не журнал. (4) **Экран принятия при первом входе НЕ сделан** — клиент API и его контрактные тесты есть, но вклинивание экрана в поток входа трогает оболочку приложения; переносится в Task 4 вместе с UX идентификации.

## Task 4 — ФТ-C1.2: уровень 2 — доведение UX модерации

**Files:** Modify: `apps/frontend/src/features/identity-verification/` (статусы у слушателя, повторная подача), `apps/backend/src/modules/mvp/mvp.service.ts` (переподача после отклонения).

**Tasks:**

- [x] Слушатель видит понятный статус: на проверке / отклонено с причиной / подтверждено.
- [x] Повторная подача после отклонения: прежняя запись сохраняется (доказательная база), создаётся новая попытка.
- [x] Очередь модератора показывает срок ожидания — заявка, висящая три дня, это заблокированный экзамен.
- [x] Тесты: отклонённая заявка не блокирует новую; подтверждённая заявка не переоткрывается.

**Acceptance:** слушатель после отклонения понимает причину и подаёт заново без обращения в поддержку.

**Выполнено 2026-07-28 (§5.205).** Как и в Фазе 2 с SCORM, разведка изменила объём: **повторная подача уже работала**. `startIdentityVerification` блокируется только на `approved` и `pending`, отклонённая запись новой подаче не мешает и сохраняется целиком (включая причину отклонения) — экран слушателя причину уже показывал. Поэтому задача свелась не к переписыванию, а к ЗАКРЕПЛЕНИЮ поведения тестами плюс закрытию настоящих пробелов. Deviations: (1) **Срок ожидания в очереди модератора** — показываем «сколько ждёт», а не дату подачи: заявка, висящая три дня, это заблокированный экзамен, и дата требует счёта в уме. Заявки старше суток помечаются. Расхождение часов сервера и клиента даёт «только что», а не отрицательный срок — иначе модератор решит, что данные битые. (2) **Экран принятия соглашения (переехал из Task 3)** встроен в `ProtectedPage` ВНУТРИ `AppShell`: навигация и выход остаются доступны — запертый без выхода пользователь худший вариант, чем непринятое соглашение. Пока статус не загружен или запрос упал, содержимое НЕ прячем: блокировать работу из-за сбоя проверки хуже, чем пропустить. (3) **`require_photo_before_exam` (переехал из Task 2) реализован ЧАСТИЧНО** — требует подтверждения именно методом `selfie_passport` (ЕСИА недостаточно). **Открытый вопрос владельцу:** ТЗ говорит «фото непосредственно перед экзаменом», но не определяет «непосредственно» — минуты, часы, дни? Захват снимка на конкретную попытку и правило свежести требуют решения; вынесено как **открытый вопрос №8**.

## Task 5 — ФТ-C1.3: шов `SmsProvider` и второй канал доставки кода (**зависит от ответа №4**)

**Files:** Create: `apps/backend/src/infrastructure/sms-provider/sms.provider.ts` (+`noop`, тесты) по образцу `video-provider/`; настройки per tenant.

**Tasks:**

- [x] Интерфейс `SmsProvider.send(to, text): Promise<boolean | null>`; `noop` по умолчанию, `null` = провайдер спит.
- [x] Доставка одноразового кода вторым каналом: **токен и гейт не меняются**, меняется только транспорт.
- [x] **Ветка A (рекомендуемая):** только шов + `noop`; конкретный оператор — в Фазе 4 вместе с биллингом.
- [x] **Ветка B:** адаптер конкретного оператора; секреты — из env, в БД только несекретная конфигурация (правило шва).
- [x] Тесты: спящий провайдер не ломает выдачу кода — email-канал продолжает работать.

**Acceptance:** выключенный СМС-канал не влияет на существующую доставку по email; включённый доставляет тот же токен.

**Выполнено 2026-07-29 (§5.206). Пошли по ветке A**, как и рекомендовал план: шов + `noop`/`fake`, конкретный оператор — отдельно (открытый вопрос №4 остаётся открытым, но ФАЗУ БОЛЬШЕ НЕ БЛОКИРУЕТ: весь код написан против шва).

**Разведка изменила формулировку задачи.** «СМС-кода на экзамен» в системе нет и не было: одноразовый доступ — это ССЫЛКА (`/exam-auth/<32-байтный токен>`, таблица `assessment.pre_exam_tokens`, миграция 0044), которую доставляет письмо. Поэтому вторым каналом уходит та же ссылка, а не выдуманный цифровой код: изобретать второй тип секрета ради красивого слова «код» значило бы завести вторую поверхность атаки на ровном месте.

**Главная находка — у получателя не было телефона.** Колонка `learning.learners.phone` есть с миграции 0002, но в доменном типе `Learner`, в DTO и в `learnerRecipient` телефона не было вовсе — то есть отправлять СМС было буквально некуда. Добавлено минимально: поле в типе (JSONB-снапшот, миграция не нужна), в create/update и в получателя.

**Сделано:** `infrastructure/sms-provider/` (`sms.provider.ts` — интерфейс + `SMS_PROVIDER_REGISTRY` + `NoopSmsProvider`; `fake-sms.provider.ts`; `phone.util.ts` — нормализация к E.164), `modules/communication/sms/` (репозиторий настроек postgres + in-memory, сервис настроек, `SmsChannelService`), миграция **0068** `communication.sms_provider_settings` + право `sms.configure`, врезка второго канала в `ExamIdentityEmailListener`, регистрация фабриками в `CommunicationModule`.

**Deviations:**

1. **Порядок в коде — СМС перед письмом.** Не приоритет: `SmsChannelService.send` не бросает и возвращает `false`, если канал спит. Порядок выбран так, чтобы падение email-диспетчера (оно ловится ниже по коду) не отменяло уже отправленную СМС.
2. **`normalizePhone` считает 10 цифр без кода страны российским номером.** Это ДОПУЩЕНИЕ, а не факт; оправдано тем, что тенанты — российские учебные центры, а альтернатива — не отправить ничего. Записи с «+8…» не переписываются в «+7…»: это другая страна.
3. **Слушатель без email не получает НИ письма, ни СМС.** Весь контакт разрешается через `learnerRecipient`, который требует email. Так было и до второго канала; менять контракт получателя — отдельная задача, затрагивающая все рассылки.
4. **Эндпойнта настройки СМС нет** — право `sms.configure` заведено, интерфейса нет. Ровно так же поступили с `video.configure` (0063): право сеется заранее, UI приходит отдельно. Включение канала пока — запись в таблицу.
5. **Адаптера реального оператора нет** (ветка A). Реестр знает `noop` и `fake`; `fake` в production принудительно опускается до `noop` — молча «доставить» СМС, которой не было, хуже, чем не отправить: слушатель будет ждать сообщение, которого нет.

## Task 6 — ФТ-C3.2: раздельные согласия на ПДн и на фото

**Files:** Migration `0068`; Modify: `mvp.types.ts` (замена одного `consentAt` на два факта), `identity-verification` фронт и бэк.

**Tasks:**

- [x] Два независимых согласия: на обработку ПДн и **отдельно** на фото/изображение (зона, граничащая с биометрией). Тексты редактируются тенантом, факт фиксируется ПЭП-контуром (Task 3).
- [x] Отзыв одного согласия не отзывает другое.
- [x] Существующий `consentAt` мигрируется как согласие на ПДн; согласие на фото у старых записей считается данным ТОЛЬКО если есть загруженное фото (иначе мы бы задним числом «получили» согласие, которого не было).
- [x] Тесты: без согласия на фото загрузка селфи не принимается; отзыв согласия на фото не ломает уже принятое решение модератора.

**Acceptance:** слушатель может дать согласие на обработку данных, но отказаться от фото — и тогда уровень 2 для него недоступен явно, а не молча.

**Выполнено 2026-07-29 (§5.207).** Миграция получила номер **0069**: `0068` заняла Task 5 (СМС). Форма скопирована с ПЭП-контура `0067` — версионируемый текст + факт с хэшем ИМЕННО ТОГО текста, который человек видел; ссылка на «согласие тенанта» через год не доказывает ничего.

**Сделано:** таблицы `learning.consent_documents` / `learning.consent_facts` (0069) с переносом существующих согласий; модуль `modules/mvp/consents/` (чистое `resolveConsentState`, репозиторий postgres + in-memory, `ConsentService`, `ConsentController` на `/consents`); право `consent.configure`; проверка согласия на фото врезана в загрузку файла и в подачу заявки; на фронте — модуль `features/consents/` и ДВЕ галочки на экране слушателя вместо одной, с текстами центра и явным объяснением последствия отказа.

**Deviations:**

1. **Ключ факта — слушатель, а не пользователь** (в `0067` было наоборот). Согласие живёт в контуре подтверждения личности, который весь построен вокруг `learnerId`, и у слушателя может не быть учётной записи вовсе. Побочный выигрыш: перенос старых согласий из `identity_verifications` становится прямым, без догадок про iam-линк.
2. **`consentAt` НЕ заменён на два поля, а дополнен `photoConsentAt`.** План говорил «замена одного `consentAt` на два факта», но `consentAt` уже читают экран модератора, ЕСИА-ветка и выгрузки; ломать его ради симметрии значило бы чинить полсистемы в задаче про согласия. Источник правды — таблица фактов, поля в записи — снимок на момент подачи.
3. **Изменение текста согласия НЕ снимает уже данное согласие**, а поднимает флаг `renewalRecommended`. Иначе правка опечатки в тексте мгновенно отозвала бы согласия у всех слушателей центра и остановила бы приём документов.
4. **Согласие на фото закрывает весь путь «селфи + паспорт», а не только загрузку селфи.** Интент загрузки не различает, какой файл грузится, и разделять его ради формулировки значило бы городить второй эндпойнт; при этом без согласия на фото уровень 2 недоступен целиком, так что запрет честен.
5. **Проверка передаётся в `MvpService` функцией-гейтом, а не зависимостью конструктора** — как `EffectiveIdentityPolicy` в Task 1. `MvpService` создаётся вручную в 75 тестовых файлах; новая обязательная зависимость превратила бы задачу в массовую правку тестов. Гейт срабатывает ПОСЛЕ проверки владения записью, иначе по коду ошибки можно было бы прощупывать чужие заявки.
6. **Экрана редактирования текстов согласий нет** — есть эндпойнт и право `consent.configure`. Ровно как с `video.configure` (0063) и `sms.configure` (0068): право заводится заранее, UI приходит отдельно.

**Найденная тестом ошибка:** «последним» согласием при сортировке только по времени мог оказаться ОТОЗВАННЫЙ факт — отзыв и повторное согласие ложатся в одну миллисекунду. Обе реализации репозитория теперь ставят действующий факт выше отозванного.

## Task 7 — ФТ-C3.1: настраиваемый срок хранения снимков

**Files:** Modify: `apps/backend/src/modules/mvp/identity/identity-image-retention.ts` (срок из настроек тенанта), тесты рядом.

**Tasks:**

- [x] Срок хранения снимков — настройка тенанта; не задан → нынешние 90 дней (поведение по умолчанию не меняется).
- [x] Решение модератора хранится бессрочно — так уже спроектировано, **сохранить и покрыть тестом**, чтобы следующая правка не снесла доказательство.
- [x] Тесты: срок 30 дней чистит раньше, срок не задан — как раньше; мусорное значение откатывается к умолчанию.

**Acceptance:** тенант с повышенными требованиями ставит 30 дней и снимки чистятся раньше; решение модератора остаётся.

**Выполнено 2026-07-29 (§5.209).** Точка расширения уже была готова: `selectIdentityImagesToPurge` принимала `retentionDays` параметром, но ни один вызывающий его не передавал.

**Где хранится настройка:** `payload.identitySettings` реквизитов тенанта (JSONB) — как настройки нумераторов и картинки документов. Миграция не нужна. `updateRequisites` сливает payload, поэтому соседние настройки не затираются.

**Границы и умолчание.** Не задано → прежние 90 дней: обновление не имеет права молча изменить срок удаления персональных данных. Допустимо 1…1095 дней. Ноль запрещён — снимок удалялся бы сразу после решения модератора, и оспорить решение стало бы нечем. Верхняя граница есть потому, что бессрочное хранение паспортов противоречит принципу минимизации, ради которого очистка и существует.

**Мусор откатывается к умолчанию МОЛЧА** (строка, ноль, дробь, отрицательное, запредельное, `NaN`, не-объект). Альтернатива — уронить крон, то есть перестать удалять ПДн у ВСЕХ тенантов из-за опечатки у одного. По той же причине нечитаемые реквизиты не останавливают очистку: сбой чтения настроек не должен оставить паспорта в хранилище навсегда (тест есть).

**Решение модератора переживает удаление снимков** — закреплено отдельным тестом, как и требовал план.

**API:** `GET /tenant/identity-settings` (отдаёт действующий срок, флаг «работает умолчание» и границы) и `PUT /tenant/identity-settings` под правом `identity.configure`. Отдельная ручка, а не правка сырого `payload` через `PUT /tenant/requisites`: значение проверяется, и менять его может только тот, кому доверена настройка идентификации. `null` возвращает умолчание — центр должен уметь отказаться от своей настройки.

**Deviations:** (1) экрана настройки нет — только API, как у текста ПЭП, политики идентификации и текстов согласий; пачка экранов настроек по-прежнему в остатке; (2) срок хранения записей прокторинга (`PROCTORING_*`) остался глобальным — задача про снимки идентификации, а не про видео; зеркальная правка для прокторинга не делалась, чтобы не расширять объём молча.

## Task 8 — ФТ-C4.1: СНИЛС — проверка полноты до выгрузки

**Files:** Create: `apps/backend/src/modules/mvp/registry/export-readiness.ts` (+тест). Modify: экспортные сервисы `0045`/`0046`, фронт `features/gov-export/`.

**Tasks:**

- [x] Перед формированием выгрузки — проверка полноты: у кого нет СНИЛС, у кого он не проходит контрольную сумму, у кого нет других обязательных полей реестра.
- [x] Результат — понятный список «у кого чего не хватает», а не отказ без объяснения.
- [x] Выгрузка не собирается, пока список не пуст (критерий приёмки эпика).
- [x] Тесты: один слушатель без СНИЛС блокирует выгрузку и назван поимённо; невалидная контрольная сумма ловится.

**Acceptance:** методист видит поимённый список проблем до формирования файла, а не ошибку реестра после отправки.

**Выполнено 2026-07-29 (§5.210).** Разведка изменила объём в обе стороны: выгрузок оказалось **пять**, а не две (ОТ/ЕИСОТ, ФРДО, ЕИСОТ-тестирование, Ростехнадзор, НМО), и у каждой УЖЕ был `*-preflight.ts`. Но СНИЛС в четырёх из пяти был **опционален**: пустое поле давало пустую ячейку, файл уходил в реестр, и человек в нём фактически не опознавался (задокументировано как «Known deviation #2» в плане ФРДО).

**Явная смена принципа — зафиксировано осознанно.** В проекте действует принцип «частичного успеха» (взять хорошие строки, показать плохие). Для ИМПОРТА он верен. Для ОТПРАВКИ В ГОСРЕЕСТР — нет: неполный файл означает, что пропущенные люди в реестре просто не появятся, центр видит «выгрузка сформирована», а слушатель узнаёт об этом через год. ТЗ требует блокировать, и это правильнее.

**Ключевое различение, которое поймал существующий тест.** Первая версия гейта блокировала выгрузку при ЛЮБОЙ ошибке — и тест «не сдавший экзамен исключается из файла» справедливо упал. Группа, где часть людей ещё не сдала, — норма, запрещать из-за них выгрузку остальных абсурдно. Поэтому блокируют **только пробелы в данных строки** (preflight), а исключение кандидата из выгрузки (не сдал, битая связь сущности) работает как раньше.

**Сделано:** общий `registry-readiness.ts` (`buildReadinessReport` — группировка ошибок по ЛЮДЯМ с дедупликацией и сортировкой по ФИО), СНИЛС обязателен в четырёх preflight (в ОТ уже был), гейт `blocked` во всех пяти сервисах, поле `readiness` в пяти исходах, общий компонент `ReadinessNotice` вместо плоского списка ошибок на всех пяти блоках экрана `/gov-export`.

**Deviations и открытый вопрос:**

1. **Дата рождения НЕ сделана блокирующей.** Колонка `learners.date_of_birth` добавлена миграцией 0046 **без заполнения**, а форматы выгрузок помечены в коде как PROVISIONAL (не сверены с эталонными шаблонами). Требовать её сейчас значило бы остановить вообще все выгрузки ради непроверенного требования. **Открытый вопрос №12 владельцу:** считать ли дату рождения обязательной, и если да — как заполнять исторические карточки.
2. **Отдельного «сухого» эндпоинта проверки нет.** Список показывается при нажатии «Сформировать»: файл при этом не создаётся и в реестр ничего не уходит, то есть критерий приёмки («методист видит поимённый список ДО формирования файла, а не ошибку реестра после отправки») выполнен. Отдельная ручка `POST /<registry>/exports/readiness` — удобство, а не требование; вынесена в остаток.
3. **Валидатор СНИЛС в `shared-types` не вынесен.** Правило репозитория — выносить после ≥3 правок дрейфа; эта задача правок фронтового зеркала не потребовала (проверка целиком на бэкенде), поэтому счётчик не сдвинулся.

## Task 9 — ФТ-C2: «личное дело» слушателя и экспорт PDF

**Files:** Create: `apps/backend/src/modules/mvp/identity/learner-dossier.service.ts` (+тест), шаблон PDF через `@trudskill/docx-render` (движок из Фазы 1); фронт — вкладка в карточке слушателя.

**Tasks:**

- [x] Сводка: статус идентификации (когда и кем подтверждена), экзаменационные сессии (дата, IP, устройство, длительность), выданные документы, подписанные ПЭП действия.
- [x] Данные собираются из существующих источников — **своей таблицы не заводим**: копия доказательств разошлась бы с оригиналом.
- [x] Экспорт в PDF тем же конвейером, что документы Фазы 1 (Gotenberg) — второй способ рендера в проекте не нужен.
- [x] ПДн в деле: СНИЛС расшифровывается только для этого экспорта и только по праву `learners.read`; сам файл проходит обычный files-цикл.
- [x] Тесты: дело чужого слушателя недоступно; пустое дело не падает; в PDF попадают все четыре раздела.

**Acceptance:** проверяющему отдаётся один PDF с полной доказательной цепочкой по слушателю.

**Выполнено 2026-07-30 (§5.213 доказательная цепочка + §5.214 PDF).** Задача делалась двумя частями: сначала сборка четырёх разделов и эндпойнт, затем PDF — в проекте не было HTML→PDF вовсе, и добавление второго конвертера это своя работа со своими рисками.

**Главная находка:** читателя `esign.legal_log_entries` не существовало (писатель появился в Task 3) — написан `LegalLogReader`. Ещё три пробела (нефильтрованный `AuditService.list`, отсутствие фильтра по `learnerId`, отсутствие истории `consent_facts`) обойдены выборкой из состояния: дело собирается по одному слушателю.

**Deviations:** (1) IP и устройство экзаменационной сессии в деле НЕ показываются — в `TestAttempt` их нет вовсе, они лежат только в аудите, а `AuditService.list` тянет весь тенант без фильтров; добавление фильтрованного чтения аудита вынесено в остаток. (2) Вкладка «Личное дело» на фронте не сделана — эндпойнты готовы, фронтовая работа вынесена отдельно.

## Task 10 — ФТ-E3: сквозной сценарий «экзамен → протокол → реестр»

**Files:** Modify: `apps/backend/src/modules/mvp/mvp.service.ts` (валидации до старта), `documents` (уже умеет закрывать группу — Фаза 1 Task 7), фронт — одна кнопка в карточке группы.

**Tasks:**

- [x] Валидации ДО старта экзамена (ФТ-E3.2): комиссия ≥ 3 человек, у членов актуальные данные, у слушателей — валидный СНИЛС (переиспользует Task 8).
- [x] Цепочка одной кнопкой: результаты → протокол + удостоверения (`closeGroup` из Фазы 1) → журнал выдачи → выгрузка в реестр.
- [x] Номера записей реестра возвращаются в документы — иначе удостоверение и реестр расходятся.
- [x] Тесты: комиссия из двух человек не пускает к старту; цепочка не запускается, если у кого-то нет СНИЛС.

**Acceptance:** сценарий «назначить экзамен → сдать → протокол и удостоверения → реестр» проходится без ручных переходов между экранами.

**Выполнено 2026-07-30 (§5.215 проверки + §5.217 «одна кнопка»).**

**Deviations:** (1) **выгрузка в реестр в кнопку не включена** — она уходит во внешнюю госсистему, отменить отправку нельзя, и связывать её с выпуском документов в одну неразрывную операцию опасно; у неё своя проверка полноты (§5.210). (2) **Возврат номеров записей реестра в документы не сделан** — остаток. (3) Кнопка размещена в `MvpModule`, а не в `DocumentsModule`: обратное направление даёт цикл, вешающий приложение при старте (§5.215, §5.216).

---

## Завершение фазы (ТЗ §13/§15 + handoff-протокол)

- [ ] Критерий приёмки эпика прогнан вживую: тенант включает «Уровень 2 + код на экзамен», неподтверждённый слушатель не допускается к итоговому тесту, личное дело выгружается PDF, выгрузка в реестр блокируется без СНИЛС.
- [ ] `pnpm ci:check` (смотреть **код возврата**, а не строку «Tests passed»), `test:isolation`, `test:security`, `test:migrations` — зелёные.
- [ ] README §2, handoff §5.2xx, статус-трекер (ЭПИК C → ✅, ФТ-E3 обновить), CLAUDE.md (указатель миграций `0066`+).
