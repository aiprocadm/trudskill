# Роль куратора: закрытие МГ-J1 — Фаза 2, срез 8.10 — план

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax for tracking. План утверждён по поручению владельца (правила автономии ТЗ перехода).

**Goal:** Довести МГ-J1 («роль `curator` в меню и домашний маршрут») до ✅ в пределах Фазы 2: остатки по разведке — строка куратора в стороже домашних маршрутов, роль в запасном списке ролей режима без базы, куратор в реестре маршрутов `docs/ia/routes.md`; зафиксировать, что остальное принадлежит другим позициям (коды прав `reports.*`/`calendar.*`/`import.run` — с ручками по фазам, РМ21; панель `/curator` — позиция 11, МГ-L4.1).

**Architecture:** Кода роли и меню уже достаточно (§5.555 миграция 0099, §5.556 права `tasks.*`, §5.557 чертёж меню ≤7, словарь, домашний маршрут `/workspace`, снимки). Срез — только сверка и три хвоста; поведение не меняется.

**Spec:** `TZ_TRUDSKILL_CDOPROF_MIGRATION.md` МГ-J1.1–J1.2, §3 таблица ролей; приёмка «≤7 пунктов, ни одного 403 по меню, нет доступа к настройкам/шаблонам».

## Решения (журнал РМ трекера)

- **РМ71.** МГ-J1 считается закрытым по коду в Фазе 2: роль, права, меню ≤7, русское имя, домашний маршрут, снимки и реестр маршрутов есть; новые коды прав из J1.2 появляются вместе с ручками (РМ21 — по своим позициям), панель `/curator` — позиция 11 (МГ-L4.1). До неё дом куратора — оперативная панель (`workspace.read` роли выдан).

## Task 1

**Files:** Modify `features/navigation/role-home.test.ts` (+ куратор в проверке «дом открывается своим правом»), `modules/iam/services/iam.service.ts` (запасной список ролей: `curator`), `docs/ia/routes.md` (куратор обучения на маршрутах, которые роль открывает по правам 0099/0103: книга выдачи, шаблоны документов, курсы, зачисление списком, группы, слушатели, оперативная панель, госвыгрузки, отчёты, компании).

- [x] Патч → сторожа `src/e2e`, `features/navigation`, бэкенд `iam` → commit.

## Task 2: документация — [x] сделано (§5.582) — handoff §5.582, трекер (РМ71, МГ-J1 ✅ с остатками), README, CLAUDE. `pnpm ci:check`, PR, слияние.
