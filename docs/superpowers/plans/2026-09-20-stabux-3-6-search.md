# 3.6 — Поиск ищет данные, а не названия разделов

**ТЗ:** «Стабилизация, UX и развитие», задача 3.6 (Н6). **Дата:** 2026-09-20.
**Ветка:** `feat/2026-09-20-3-6-search`. **Очередь:** позиция 5, остаток фазы 3.

## Почему эта задача ждала

ТЗ прямо запрещало делать её до 13.4: «поиск по данным легче всего превращается в утечку между
центрами». 13.4 (сторож изоляции арендаторов) закрыт в позиции 12, PR #764 — запрет снят.

## Что было

`Ctrl+K` искал только по названиям пунктов меню. Человек вводит «серт» — получает «Ничего не
найдено», хотя в системе есть и «Удостоверения», и «Документы». Он ищет не раздел, он ищет СВОЁ:
фамилию слушателя, номер документа, название группы.

## Главное в этой задаче — не находить лишнего

Строка поиска стоит в шапке у всех, и она соблазняет ввести фамилию знакомого. Поэтому область
поиска считается **по правам, на сервере, до обращения к данным**. Скрыть строку на экране
недостаточно: данные к тому моменту уже ушли из системы.

## Задачи

### Task 1 — словарь синонимов и область поиска

**Files:** `apps/backend/src/modules/mvp/search/search-synonyms.ts`

**Tasks:**

- [x] `SEARCH_SYNONYMS` — отдельным файлом (прямое требование ТЗ: пополнять без правки логики)
- [x] `entitiesHintedBy` — сравнение по началу слова, чтобы окончания не плодили строк
- [x] `SEARCH_PERMISSION` — право на каждый тип
- [x] `allowedEntities`, `searchScope` — намёк СУЖАЕТ область, но не расширяет
- [x] `SEARCH_EMPTY_HINT`, `searchEmptyResult` — тексты по пункту 4

**Acceptance:** намёк на недоступный тип не даёт к нему доступа; намёк на недоступное не обнуляет
поиск; человек без прав не находит ничего.

### Task 2 — поиск по данным

**Files:** `apps/backend/src/modules/mvp/search/global-search.ts`, `mvp.service.ts`,
`mvp.controller.ts`

**Tasks:**

- [x] `search()` — пять типов, по 5 строк на тип, запрос от двух символов
- [x] СНИЛС ищется только от четырёх цифр и показывается частично (17.2)
- [x] каждая находка ведёт на карточку объекта, а не в раздел
- [x] ручка `GET /search` с ограничением частоты, зовёт службу с правами человека

**Acceptance:** по фамилии находится слушатель, по номеру — документ, по названию — группа и курс;
выдача ограничена; ручка зовётся с правами.

### Task 3 — строка поиска на экране

**Files:** `apps/frontend/src/features/search/data-search.ts`,
`apps/frontend/src/widgets/shell/command-palette.tsx`

**Tasks:**

- [x] запрос к серверу с задержкой 250 мс, короткий запрос не отправляется вовсе
- [x] подсказка в пустой строке и внятный ответ при пустом результате
- [x] находки сгруппированы русскими названиями типов, ни одного кода на экране

**Acceptance:** пункты меню остаются, но данные идут отдельной частью; текст пустого результата
называет сам запрос.

### Task 4 — сторож

**Files:** `apps/backend/src/modules/mvp/search/global-search.test.ts`,
`apps/backend/src/common/guards/permission-surface.isolation.test.ts`

**Tasks:**

- [x] 20 проверок: находки, синонимы, права, персональные данные, тексты, подключение ручки
- [x] 12 подсаженных поломок — все пойманы
- [x] ручка внесена в реестр «без права» с объяснением, чем защищена вместо права

**Acceptance:** `pnpm ci:check` — exit 0.

## Отклонения

**Права у самой ручки нет — намеренно.** Строка поиска есть у всех; право на ручку означало бы,
что у части людей поиск молча ничего не находит, и они решат, что он сломан. Права решают здесь
не «пустить ли к ручке», а ЧТО она найдёт. Записано в реестре `WITHOUT_PERMISSION` с причиной.

**Документы читаются страницей в 200 записей, а не целиком.** Поиск зовут на каждое нажатие
клавиши; читать всю таблицу документов на каждый запрос дороже, чем не найти документ,
выписанный тысячу выдач назад.
