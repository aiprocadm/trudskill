# Замеры нагрузки против §12 ТЗ

**Дата прогона:** 2026-08-09. **Стенд:** 8 ядер, 47 ГБ, база общая с другими проектами.
**Отдельный тенант:** `load-pilot` (`t_a79d1f5df60e`), 500 слушателей, залитых **обычным
массовым импортом** — тем же кодом, что в бою.

Оснастка: `infra/load/seed-load-tenant.sh` (подготовка) + `infra/load/k6-pilot.js` (прогон).
Прогон повторяем: те же две команды дают те же условия.

## Что требует §12

| Требование                                        | Значение |
| ------------------------------------------------- | -------- |
| §12.1 активных слушателей на тенант               | 300–500  |
| §12.1 параллельных экзаменационных сессий         | 20–50    |
| §12.1 **p95 API списков**                         | < 300 мс |
| §12.4 пакет документов на группу из 25 слушателей | ≤ 2 мин  |

## Что показали замеры

### Один запрос списка (без конкуренции)

| Тенант             | До правок | После правок |
| ------------------ | --------- | ------------ |
| демо (2 слушателя) | 0,056 с   | 0,021 с      |
| нагрузочный (500)  | 0,67 с    | 0,060 с      |

### Под нагрузкой (p95 по спискам)

| Одновременных сессий | Исходно     | 1-й заход | 2-й заход | 3-й заход      | Требование |
| -------------------- | ----------- | --------- | --------- | -------------- | ---------- |
| 50 (верх диапазона)  | **30,25 с** | 2,32 с    | 0,79 с    | **0,378 с**    | 0,3 с      |
| 20 (низ диапазона)   | не мерилось | 0,485 с   | 0,152 с   | **0,042 с** ✅ | 0,3 с      |

Пропускная способность: 1,4 → 21 → 45 → **59 запросов в секунду** (в 42 раза). Ошибок нет
ни в одном из шести прогонов (последние: 0 из 10 720 и 0 из 6370).

**Вывод: требование §12.1 ВЫПОЛНЕНО на нижней границе диапазона (20 сессий) с семикратным
запасом и пока НЕ выполнено на верхней (50 сессий) — 0,378 с против 0,3 с.** Разрыв
сократился со стократного до 1,26-кратного.

## Что нашли и починили по факту замеров

**Главное: обычное ЧТЕНИЕ списка переписывало всё состояние центра.**
Состояние сохранялось в конце каждого запроса — включая тот, который ничего не менял.
При 500 слушателях это «удалить всё и вставить заново» на каждый показ страницы.
Причём вставка шла **по одной строке**: больше тысячи обращений к базе на один клик.

Две точечные правки (обе — в `postgres-mvp-persistence.backend.ts`):

1. **Не сохранять, если ничего не менялось.** При загрузке снимается отпечаток состояния,
   в конце запроса он сравнивается. Совпал — писать нечего. Отпечаток считается по
   открытым значениям в памяти: шифрование ПДн даёт каждый раз разный шифртекст, сравнивать
   его бессмысленно.
2. **Вставка пачками по 500 строк** вместо построчной.

Результат первого захода: одиночный запрос ускорился в 11 раз, p95 под нагрузкой — в 13 раз.

### Второй заход (2026-08-09): чтение одним запросом вместо полусотни

Замер показал следующее по величине: загрузка состояния шла **циклом по коллекциям** — на
каждую свой `select`. Коллекций около пятидесяти, то есть полсотни обращений к базе на
КАЖДЫЙ запрос пользователя, включая простой показ списка. При этом всё состояние центра с
500 слушателями читается **одним** запросом за 2,8 мс.

Правка: один `select` на весь тенант с группировкой по коллекциям в памяти. Результат:
одиночный запрос 0,060 → 0,035 с, p95 при 50 сессиях 2,32 → 0,79 с, пропускная способность
21 → 45 запросов в секунду.

Попутно проверено, а не предположено: расшифровка ПДн (AES) стоит микросекунды на запись и
главным расходом не является.

### Третий заход (2026-08-09): ленивая раскладка коллекций

Замер второго захода показал: запрос к базе стоит 3 мс, подсчёт отпечатка состояния 7,4 мс
(дважды за запрос), остальное — раскладка 1504 записей по полусотне коллекций с расшифровкой
ПДн всех слушателей. При этом **обычный запрос списка трогает одну-две коллекции из
полусотни**: списку слушателей не нужны ни история статусов зачислений (500 записей), ни
кэши идемпотентности.

Правка: сырые строки кладутся в состояние как есть, а раскладываются по первому обращению —
и только те, к которым обратились. Отпечаток снимается по коллекциям в момент раскладки,
поэтому сравнивается только тронутое. Записываются в базу только изменившиеся коллекции.
**Ни один сервис не переписан** — они как читали `state.learners`, так и читают.

Результат: одиночный запрос 0,035 → 0,020 с, p95 при 50 сессиях 0,79 → 0,378 с, при
20 сессиях 0,152 → 0,042 с, пропускная способность 45 → 59 запросов в секунду.

Попутно сохранено важное свойство: старые строки с незашифрованными ПДн по-прежнему
перешифровываются при ближайшем сохранении. В памяти они выглядят как расшифрованные и по
отпечатку сошли бы за «не менялись» — коллекция помечается к записи явно. Это поймал
существующий тест, а не ревью.

### Где узкое место сейчас (замер 2026-08-09, после третьего захода)

Гипотезу проверили инструментом, а не рассуждением: под нагрузкой в 50 сессий процесс
бэкенда занимает **94% одного ядра** (мгновенный замер `top`, два подряд: 94,1% и 94,5%).
При этом на машине восемь ядер.

**Вывод: упираемся не в базу и не в код, а в то, что бэкенд — ОДИН процесс.** Node
выполняет запросы по очереди в одном потоке; 59 запросов в секунду × ~16 мс работы над
каждым и есть те самые 94% ядра. Остальные семь ядер простаивают.

Отсюда следующий шаг — не оптимизация кода, а **запуск нескольких экземпляров бэкенда за
обратным прокси**. Это стало безопасным именно сейчас: Фаза 6 Task 9 закрыла миграции
блокировкой уровня сеанса, а все планировщики берут свои блокировки — то есть второй
экземпляр не накатит миграции повторно и не задвоит ночные задачи.

Осторожно с двумя замерами, которые легко принять за правду:

- `ps -o pcpu` даёт **среднее за всю жизнь процесса**, а не текущую загрузку: на том же
  прогоне он показал 37% вместо 94%. Мгновенную загрузку берём вторым замером `top`.
- Токен доступа живёт 15 минут. Прогон с протухшим токеном показал «p95 = 9 мс и 86
  запросов в секунду» — и это были 100% отказов, отбитых на входе. Всегда проверять
  `checks` и `http_req_failed`, прежде чем радоваться числам.

## Что осталось и почему это отдельная задача

Остаток объясняется тем, что **каждый запрос по-прежнему загружает состояние центра
целиком**: демо-тенант (2 слушателя) — 21 мс, нагрузочный (500) — 60 мс на одном и том же
коде. Разница и есть цена чтения всего состояния плюс расшифровки ПДн всех пятисот
слушателей. Под 50 одновременными сессиями эта цена умножается.

До требования на верхней границе осталось около 20% (0,336–0,378 с против 0,3 с;
воспроизводится стабильно в трёх прогонах).

Порядок дальнейших шагов теперь определён замером, а не догадкой:

1. **Несколько экземпляров бэкенда за прокси** — самый дешёвый шаг: кода не трогает вовсе,
   а свободных ядер семь из восьми. Требует правки `infra/docker-compose.prod.yml` и
   `infra/Caddyfile` плюс проверки, что метрики и отметки планировщиков собираются со всех
   экземпляров.
2. **Постраничное чтение списков из базы** — если после (1) не хватит: сейчас показ первых
   50 слушателей всё ещё поднимает в память все 500. Это переписывание сервисов, поэтому
   браться за него стоит только по цифрам, оставшимся после (1).

Важная оговорка про условия замера: стенд делит машину с пятью другими проектами, средняя
загрузка процессора на старте прогона была около 4 из 8. На выделенной машине числа будут
лучше, но проверять это надо замером, а не рассуждением.

**Предложение по объёму** (для следующей задачи, не сделано здесь):

- читать по требованию только те коллекции, которые запросу действительно нужны;
- расшифровывать ПДн лениво — сейчас расшифровываются все 500 слушателей даже для списка,
  где ПДн не показываются;
- постранично читать списки из базы вместо среза массива в памяти.

## §12.4: пакет документов на 25 человек — оснастка готова, замер ждёт курса

**Числа до сих пор нет, и это сказано прямо.** Замер требует не данных, а полностью
настроенного курса, которого на стенде нет: в демо-центре есть программы, группа и комиссия,
но **нет набора документов курса** (`courseDocumentSets`) — значит выпускать нечего.

Что должно быть готово, чтобы замер вообще запустился (это требования продукта, а не
скрипта):

1. Программа с опубликованной версией и паспортом.
2. Набор документов курса с шаблонами и правилами нумерации.
3. Комиссия и действующая лицензия центра.
4. Группа из 25 слушателей с завершёнными зачислениями.

Оснастка написана: `infra/load/measure-document-package.sh`. Она меряет то, что чувствует
человек, — не «ручка ответила», а **время до момента, когда все задачи документов группы
завершились**. Перелёт за 2 минуты даёт ненулевой код возврата: замер должен краснеть, а не
сообщать «ну почти».

```bash
BASE_URL=http://127.0.0.1:3011/api/v1 TENANT_ID=<тенант> GROUP_ID=<группа> \
  TOKEN=<jwt> infra/load/measure-document-package.sh
```

Если подготовки не хватает, скрипт не молчит и не врёт нулём: он печатает код отказа и
отсылает к перечню выше.

## Как повторить

```bash
# 1. Подготовить тенант и данные
PLATFORM_PASSWORD='<пароль платформенного администратора>' LEARNERS=500 \
  infra/load/seed-load-tenant.sh

# 2. Получить токен администратора нагрузочного тенанта и прогнать профиль
k6 run -e BASE_URL=http://127.0.0.1:3011/api/v1 -e TENANT_ID=<id> \
       -e TOKEN=<jwt> -e VUS=50 -e DURATION=2m infra/load/k6-pilot.js
```

Порог `p95 < 300 мс` зашит в профиль как условие провала: прогон, который «почти уложился»,
краснеет и возвращает ненулевой код.

---

## 2026-09-23 — объём CDOPROF в текущей модели хранения (ТЗ перехода, §15.1 МГ-A3.1)

**Зачем.** ТЗ перехода с CDOPROF называет JSON-снимок на тенант главным техническим блокером и
требует p95 ≤ 500 мс при 25 000 групп / 14 000 слушателей. Этот прогон превращает оценку в число —
базу «до» для Фазы 1 (нормализованное хранение). Ожидание ТЗ: подтверждение блокера.

**Условия.** Та же машина (8 ядер, 47 ГБ, база общая). **Отдельная база** `trudskill_perf` в
`test-postgres` (миграции при старте создали `tenant_demo`), бэкенд из рабочей копии на `:3091`,
`MVP_PERSISTENCE_DRIVER=postgres`, `LMS_READ_MODEL=legacy`. Данные — `pnpm perf:synth`:
1 622 контрагента, 427 курсов, 25 000 групп (+ 25 000 связей группа→курс), 14 000 слушателей
**без ПДн**, 30 000 зачислений → **96 049 строк, 25 МБ** снимка, заливка 7 с. Профиль —
`infra/load/k6-cdoprof-volume.js`, порог p95 < 500 мс.

### Один запрос (curl, три подряд, секунды)

| Ручка              | 1-й  | 2-й  | 3-й  |
| ------------------ | ---- | ---- | ---- |
| `/groups`          | 1,70 | 1,09 | 1,11 |
| `/groups?page=400` | 1,06 | 1,08 | 1,26 |
| `/learners`        | 1,17 | 0,97 | 0,93 |
| `/learners?q=Ива`  | 1,17 | 0,87 | 0,90 |
| `/search?q=2024-0` | 1,48 | 1,36 | 1,24 |

### k6, 60 секунд

| Ручка       | VUS=1: p50 / p95 / max   | VUS=10: p50 / p95 / max  | Бюджет |
| ----------- | ------------------------ | ------------------------ | ------ |
| `/groups`   | 1,11 / **1,68** / 2,04 с | 11,7 / **14,0** / 14,3 с | 0,5 с  |
| `/learners` | 1,03 / **1,61** / 1,99 с | 11,1 / **12,3** / 12,8 с | 0,5 с  |
| `/search`   | 1,13 / **1,38** / 1,41 с | 14,0 / **14,5** / 14,5 с | 0,5 с  |

Ошибок 0 в обоих прогонах; пороги k6 — красные в обоих. Память процесса после прогонов — 121 МБ.

### Где время

- `select collection, data from learning.mvp_runtime_documents where tenant_id = …` с передачей
  25 МБ клиенту — **0,37–0,39 с в самом Postgres** (`psql`, вывод в `/dev/null`); подсчёт строк
  без передачи — 12–36 мс. То есть узкое место не индекс, а объём: снимок читается целиком.
- Остальные ~0,7 с на запрос — разбор 25 МБ JSON, отпечаток `JSON.stringify` тронутых коллекций
  (25 000 групп / 14 000 слушателей) и фильтр с пагинацией в памяти (`MvpService.list`).
- При 10 пользователях запросы одного тенанта стоят в очереди (`runExclusive`): 10 × ~1,2 с ≈ 12 с.

### Сравнение с 2026-08-09 (500 слушателей)

| Показатель                | 500 слушателей  | 25 000 групп / 14 000 слушателей |
| ------------------------- | --------------- | -------------------------------- |
| один запрос списка        | 0,06 с          | 1,0–1,7 с                        |
| p95 списков под нагрузкой | 0,042 с (20 VU) | 12–14 с (10 VU)                  |

### Вывод

**Блокер подтверждён.** На объёме CDOPROF текущая модель не укладывается в бюджет даже для
одного пользователя (в 3 раза), а при десяти — в 25–30 раз. Оптимизировать снимок дальше
бессмысленно: одна лишь передача снимка из базы съедает три четверти бюджета. Единственный путь —
Фаза 1 ТЗ: нормализованные таблицы, SQL-пагинация и поиск, чтение только нужных строк
(МГ-A1.1–A2.1). База `trudskill_perf` оставлена для замера «после».

**Оговорки.** Синтетика без ПДн: живые слушатели зашифрованы, и расшифровка 14 000 строк при
раскладке добавит времени — реальные числа будут ХУЖЕ. Документов (60 000 по ТЗ) и календаря
в наборе нет: их модели появятся в Фазах 3 и 5.

### Как повторить

```bash
docker exec test-postgres psql -U testuser -d postgres -c "create database trudskill_perf"
# бэкенд с DATABASE_URL на trudskill_perf, BACKEND_PORT=3091, MVP_PERSISTENCE_DRIVER=postgres
DATABASE_URL=… PERF_SYNTH_TENANT_ID=tenant_demo pnpm perf:synth
TOKEN=$(curl -s -X POST http://127.0.0.1:3091/api/v1/auth/login -H 'content-type: application/json' \
  -H 'x-tenant-id: tenant_demo' -d '{"login":"tenant_admin","password":"…"}' | python3 -c 'import json,sys; print(json.load(sys.stdin)["data"]["accessToken"])')
k6 run -e TOKEN=$TOKEN -e VUS=1  -e DURATION=60s infra/load/k6-cdoprof-volume.js
k6 run -e TOKEN=$TOKEN -e VUS=10 -e DURATION=60s infra/load/k6-cdoprof-volume.js
```

## 2026-09-23 — «после», Фаза 1 срез 1b: группы и контрагенты читаются из таблиц (МГ-A3.1, частично)

**Что изменилось.** Тот же стенд `:3091` и та же база `trudskill_perf`, миграции 0099–0110
применены при старте, бэкфилл `pnpm backfill:normalized` (95 620 строк за 260 с; 2 отказа — дубли
пары «группа + слушатель» в синтетике, названы поимённо в отчёте сверки), флаг
`LMS_NORMALIZED_COLLECTIONS=groups,counterparties`. `/groups` идёт из `learning.groups` без замка
арендатора и без загрузки снимка; `/learners` и `/search` — по-прежнему из снимка (срезы 2 и 6.x).

### Один запрос (curl, три подряд, секунды)

| Ручка              | «до» (1-й / 2-й / 3-й) | «после» (1-й / 2-й / 3-й) |
| ------------------ | ---------------------- | ------------------------- |
| `/groups`          | 1,70 / 1,09 / 1,11     | **0,10 / 0,07 / 0,04**    |
| `/groups?page=400` | 1,06 / 1,08 / 1,26     | **0,05 / 0,06 / 0,05**    |
| `/learners`        | 1,17 / 0,97 / 0,93     | 1,67 / 1,78 / 1,52        |
| `/learners?q=Ива`  | 1,17 / 0,87 / 0,90     | 1,50 / 1,84 / 2,35        |
| `/search?q=2024-0` | 1,48 / 1,36 / 1,24     | 1,86 / 1,30 / 0,91        |

### k6, 60 секунд

| Ручка       | VUS=1 «до» p50 / p95 | VUS=1 «после» p50 / p95 | VUS=10 «до» p50 / p95 | VUS=10 «после» p50 / p95 | Бюджет |
| ----------- | -------------------- | ----------------------- | --------------------- | ------------------------ | ------ |
| `/groups`   | 1,11 / **1,68** с    | 0,05 / **0,23** с       | 11,7 / **14,0** с     | 0,64 / **0,96** с        | 0,5 с  |
| `/learners` | 1,03 / **1,61** с    | 0,79 / **0,98** с       | 11,1 / **12,3** с     | 9,1 / **12,2** с         | 0,5 с  |
| `/search`   | 1,13 / **1,38** с    | 0,97 / **1,17** с       | 14,0 / **14,5** с     | 8,8 / **10,0** с         | 0,5 с  |

Ошибок 0; память процесса после прогонов — 128 МБ. У `/groups` при VUS=1 один выброс max 45,6 с в
самом начале прогона (первый запрос после бэкфилла; в curl-серии и дальше по прогону такого нет) —
повторить в срезе 2, прежде чем делать выводы.

### Вывод

- **`/groups` в бюджете при одном пользователе (p95 0,23 с против 1,68 с — в 7 раз) и в 15 раз
  быстрее при десяти (0,96 с против 14,0 с).** До 0,5 с при десяти пользователях не хватило потому,
  что в том же прогоне `/learners` и `/search` продолжают читать снимок целиком: они держат
  процессор и базу (10 × 25 МБ). Цель МГ-A3.1 по `/groups` закрывается вместе со срезом 2
  (слушатели) — замер повторить тем же профилем.
- `/learners` и `/search` без изменений в пределах разброса: их очередь — срезы 2 и 6.x.

### Как повторить

```bash
# стенд как в разделе выше, плюс в окружении бэкенда:
LMS_NORMALIZED_COLLECTIONS=groups,counterparties
DATABASE_URL=… pnpm backfill:normalized --batch 1000   # снимок → таблицы, отчёт сверки в stdout
k6 run -e TOKEN=$TOKEN -e VUS=1  -e DURATION=60s infra/load/k6-cdoprof-volume.js
k6 run -e TOKEN=$TOKEN -e VUS=10 -e DURATION=60s infra/load/k6-cdoprof-volume.js
```

## 2026-09-23 — «после», Фаза 1 срез 2b: слушатели тоже из таблиц (МГ-A3.1, `/groups` и `/learners`)

**Что изменилось.** Тот же стенд и база, флаг `LMS_NORMALIZED_COLLECTIONS=groups,counterparties,learners`
(бэкфилл из среза 1b — 14 000 слушателей уже в `learning.learners`). `/learners` идёт из таблицы: ФИО
через триграммы 0109, поиск `?q=Ива` — по ФИО. `/search` — по-прежнему из снимка (срез 6.x).

### Один запрос (curl, три подряд, секунды)

| Ручка              | «до» (§5.554)      | после 1b           | **после 2b**           |
| ------------------ | ------------------ | ------------------ | ---------------------- |
| `/groups`          | 1,70 / 1,09 / 1,11 | 0,10 / 0,07 / 0,04 | **0,05 / 0,04 / 0,04** |
| `/learners`        | 1,17 / 0,97 / 0,93 | 1,67 / 1,78 / 1,52 | **0,05 / 0,03 / 0,04** |
| `/learners?q=Ива`  | 1,17 / 0,87 / 0,90 | 1,50 / 1,84 / 2,35 | **0,09 / 0,08 / 0,08** |
| `/search?q=2024-0` | 1,48 / 1,36 / 1,24 | 1,86 / 1,30 / 0,91 | 0,93 / 0,72 / 0,67     |

### k6, 60 секунд

| Ручка       | VUS=1 «до» p95 | VUS=1 после 2b p95 | VUS=10 «до» p95 | VUS=10 после 2b p95 | Бюджет |
| ----------- | -------------- | ------------------ | --------------- | ------------------- | ------ |
| `/groups`   | 1,68 с         | **0,17 с**         | 14,0 с          | **0,67 с**          | 0,5 с  |
| `/learners` | 1,61 с         | **0,07 с**         | 12,3 с          | **0,59 с**          | 0,5 с  |
| `/search`   | 1,38 с         | 0,72 с             | 14,5 с          | 3,7 с               | 0,5 с  |

Запросов за 60 с при VUS=10: **1 077** (было 126 «до», 126 после 1b — тогда очередь держали `/learners`).
Ошибок 0; память процесса 122 МБ. Выброса 45 с из прогона 1b не повторилось.

### Вывод

- **`/groups` и `/learners` при одном пользователе — в бюджете с запасом в 3–7 раз**; при десяти —
  0,59–0,67 с против 0,5 с: остаток держит `/search`, который на каждый шестой запрос читает 25 МБ
  снимка под замком центра и грузит процессор. Ручки из таблиц замка не берут, но делят с ним базу и
  ядра. Цель МГ-A3.1 по спискам закроется, когда глобальный поиск уйдёт с снимка (срез 6.x) — либо
  раньше, если `/search` переписать на SQL-репозитории уже переведённых коллекций (кандидат в срез 3–4).
- В 20 раз быстрее «до» при десяти пользователях и в 8,5 раз больше запросов в минуту.

### Как повторить

```bash
LMS_NORMALIZED_COLLECTIONS=groups,counterparties,learners   # в окружении бэкенда, после бэкфилла
k6 run -e TOKEN=$TOKEN -e VUS=1  -e DURATION=60s infra/load/k6-cdoprof-volume.js
k6 run -e TOKEN=$TOKEN -e VUS=10 -e DURATION=60s infra/load/k6-cdoprof-volume.js
```

## 2026-09-23 — «после», Фаза 1 срез 3c: зачисления тоже из таблиц (МГ-A3.1: `/groups`, `/learners`, `/enrollments`)

**Что изменилось.** Тот же стенд и база; при старте применена 0111, повторён бэкфилл (95 620 строк за
194 с — колонка привязки заполнена), флаг `LMS_NORMALIZED_COLLECTIONS=groups,counterparties,learners,enrollments`.
В профиль k6 добавлен `/enrollments` (`''`, `?page=2`, `?page=500`, `?status=active`). `/search` — снимок.

### k6, 60 секунд

| Ручка          | VUS=1 «до» p95 | VUS=1 после 3c p95 | VUS=10 «до» p95 | VUS=10 после 3c p95 | Бюджет |
| -------------- | -------------- | ------------------ | --------------- | ------------------- | ------ |
| `/groups`      | 1,68 с         | 0,13 с             | 14,0 с          | 0,61 с              | 0,5 с  |
| `/learners`    | 1,61 с         | 0,06 с             | 12,3 с          | 0,52 с              | 0,5 с  |
| `/enrollments` | —              | **0,05 с**         | —               | **0,48 с**          | 0,5 с  |
| `/search`      | 1,38 с         | 0,62 с             | 14,5 с          | 4,1 с               | 0,5 с  |

Запросов за 60 с при VUS=10: **1 610** (126 «до», 1 077 после 2b). Ошибок 0; память 121 МБ.
Один запрос: `/groups` 0,02–0,05 с, `/learners` 0,02–0,03 с, `/learners?q=Ива` 0,06–0,08 с.

**Повторяющееся наблюдение (журнал 615):** первый `/groups` в прогоне VUS=1 после бэкфилла снова дал
выброс (max 25,5 с; в 1b — 45,6 с), остальные запросы прогона — десятки миллисекунд. Кандидаты:
первый план после массовой записи без `ANALYZE`, прогрев триграммного индекса. Проверить отдельно
(`ANALYZE` после бэкфилла в самом скрипте), не считать за поведение ручки.

### Вывод

- **`/enrollments` — в бюджете и при одном, и при десяти пользователях (0,05 / 0,48 с).** `/learners`
  на границе (0,52 с), `/groups` чуть выше (0,61 с): в том же прогоне `/search` читает снимок целиком
  под замком центра и грузит процессор и базу. Списки ускорены в 20–25 раз против «до».
- Цель МГ-A3.1 по всем спискам закроется, когда `/search` уйдёт со снимка (отложен, журнал 613):
  на репозиториях переведённых коллекций плюс курсы из снимка выигрыша нет, нужен перевод курсов
  (Фаза 2) или отдельный поисковый индекс.

### Как повторить

```bash
LMS_NORMALIZED_COLLECTIONS=groups,counterparties,learners,enrollments
DATABASE_URL=… pnpm backfill:normalized --batch 1000   # после 0111 — обязательно
k6 run -e TOKEN=$TOKEN -e VUS=10 -e DURATION=60s infra/load/k6-cdoprof-volume.js
```

## 2026-09-24 — «после», Фаза 1 срез 5b: все семь коллекций под флагом (`/exam-results`, `/documents` добавлены в сценарий)

Стенд тот же (`trudskill_perf`, :3091), бэкфилл повторён после проекций 4a/5a, флаг
`LMS_NORMALIZED_COLLECTIONS=groups,counterparties,learners,enrollments,groupCourses,examResults,generatedDocuments`.
Ручки `/exam-results` и `/documents` читаются из таблиц без замка центра и без загрузки снимков
(у документов — без девяти запросов снимка домена документов).

### k6, 60 секунд

| Ручка           | p95, 1 VU | max, 1 VU | p95, 10 VU | max, 10 VU | Бюджет |
| --------------- | --------- | --------- | ---------- | ---------- | ------ |
| `/groups`       | 0,12 с    | 0,14 с    | 0,62 с     | 0,83 с     | 0,5 с  |
| `/learners`     | 0,06 с    | 0,07 с    | 0,48 с     | 0,66 с     | 0,5 с  |
| `/enrollments`  | 0,05 с    | 0,08 с    | 0,49 с     | 0,85 с     | 0,5 с  |
| `/exam-results` | 0,01 с    | 0,03 с    | 0,33 с     | 0,46 с     | 0,5 с  |
| `/documents`    | 0,01 с    | 0,02 с    | 0,34 с     | 0,59 с     | 0,5 с  |
| `/search`       | 0,54 с    | 0,63 с    | 2,48 с     | 4,78 с     | 0,5 с  |

Запросов за 60 с при VUS=10: **2510** (1 610 после 3c, 126 «до»). Ошибок 0.00 %; память 121 МБ.

```
  /groups   p50 37 мс · p95 125 мс · max 145 мс
  /learners p50 24 мс · p95 56 мс · max 67 мс
  /enrollments p50 28 мс · p95 54 мс · max 80 мс
  /exam-results p50 7 мс · p95 9 мс · max 30 мс
  /documents p50 5 мс · p95 8 мс · max 20 мс
  /search   p50 471 мс · p95 541 мс · max 633 мс
  ошибок    0.00 %
```

```
  /groups   p50 373 мс · p95 617 мс · max 833 мс
  /learners p50 107 мс · p95 477 мс · max 659 мс
  /enrollments p50 91 мс · p95 486 мс · max 851 мс
  /exam-results p50 16 мс · p95 334 мс · max 463 мс
  /documents p50 11 мс · p95 337 мс · max 592 мс
  /search   p50 816 мс · p95 2483 мс · max 4782 мс
  ошибок    0.00 %
```

### Вывод

- `/documents` и `/exam-results` — в бюджете при десяти пользователях; сценарий стал тяжелее на два запроса за итерацию, поэтому числа списков не сравнимы напрямую с 3c по количеству запросов.
- Остаток на `/search` (снимок целиком под замком центра) — журнал 613; выброс первого запроса после бэкфилла — журнал 615.

### Как повторить

```bash
LMS_NORMALIZED_COLLECTIONS=groups,counterparties,learners,enrollments,groupCourses,examResults,generatedDocuments
DATABASE_URL=… pnpm backfill:normalized --batch 1000
k6 run -e TOKEN=$TOKEN -e VUS=10 -e DURATION=60s infra/load/k6-cdoprof-volume.js
```
